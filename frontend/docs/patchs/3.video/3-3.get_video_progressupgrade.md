# UPGRADE PROPOSAL — Phase 3-3 (Video Progress)

**Date**: 2026-01-16
**Author**: Amazing Korean Frontend Agent
**Scope**: Video Progress Logic & UX Expansion

## 1. Overview
현재 구현된 `GET /videos/{id}/progress`는 **비디오 상세 화면(Detail View)**에 진입했을 때 단일 비디오의 상태만 확인합니다.
하지만 학습 플랫폼의 완성도를 높이기 위해서는 **목록 화면(List View)에서의 직관적인 정보 제공**과 **학습 동기 부여(Gamification)** 요소가 추가되어야 합니다.

## 2. Proposed Features

### 🚀 Priority A: List View Integration (목록 화면 진도율)
- **Problem**: 현재 사용자는 영상을 클릭해서 들어가기 전까지는 내가 이 영상을 봤는지, 얼마나 봤는지 알 수 없음.
- **Solution**:
    - 비디오 목록(`/videos`) 조회 시 썸네일 하단에 **진행률 바(Progress Bar)** 노출.
    - 완료된 영상(`is_completed: true`)의 썸네일 구석에 **초록색 체크(✅) 배지** 표시.
- **Tech Impact**:
    - **N+1 문제 방지**: 목록에 영상이 20개일 때 API를 20번 호출하면 안 됨.
    - **Back-end Request**: `GET /videos` 응답 DTO에 `user_progress: { rate: number, completed: bool }` 필드를 포함하거나, `GET /videos/progress/batch` (Bulk Fetch) 엔드포인트 필요.

### 🎨 Priority B: Completion Celebration (완료 경험 강화)
- **Problem**: 영상을 끝까지 봐도 아무런 시각적 보상이 없어 성취감이 낮음.
- **Solution**:
    - `is_completed` 상태가 `false` -> `true`로 바뀌는 순간(Phase 3-4 POST 응답 시점), 화면에 **폭죽(Confetti) 애니메이션** 효과 실행.
    - "학습 완료!" 토스트 메시지를 단순 텍스트가 아닌, 캐릭터가 칭찬하는 형태의 모달이나 화려한 UI로 업그레이드.

### ⚙️ Priority C: Smart Resume (조건부 이어보기)
- **Current Status**: 영상이 짧아(10분 미만) 이어보기 기능을 제외함.
- **Upgrade**:
    - 추후 **20분 이상의 긴 강의(Long-form Lesson)**가 추가될 경우를 대비해 **"조건부 이어보기"** 로직 설계.
    - Logic: `if (duration > 600 && progress > 10%) { showResumeToast(); }`
    - 즉, 짧은 영상은 무시하되 긴 영상에만 선택적으로 기능을 활성화하는 유연한 UX 도입.

### 📊 Priority D: My Learning Dashboard (학습 현황판)
- **Goal**: 사용자가 자신의 학습 이력을 한눈에 볼 수 있는 대시보드.
- **Feature**:
    - **"최근 시청한 강의"**: `last_watched_at` 기준으로 정렬된 별도 섹션 제공.
    - **"학습 연속일(Streak)"**: `video_log` 데이터를 분석해 "3일 연속 학습 중🔥" 배지 표시.

## 3. Implementation Roadmap (Suggestion)

| Phase | Feature | Complexity | Dependency |
|---|---|---|---|
| **Phase 3.5** | **Completion Confetti** | Low | frontend-only |
| **Phase 3.6** | **List View Progress** | High | Backend API 수정 필요 (Join Query) |
| **Phase 4.0** | **My Learning Dashboard** | Medium | `/users/me/stats` API 필요 |

## 4. Conclusion
현재의 Phase 3-3, 3-4 작업은 "데이터를 쌓는 기초 공사"입니다.
**Phase 3.6 (List View Integration)** 이야말로 사용자가 "Amazing Korean은 편리하다"라고 느끼게 할 핵심 기능이므로, 백엔드 팀과 협의하여 우선적으로 도입하는 것을 권장합니다.