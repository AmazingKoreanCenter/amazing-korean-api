# PATCH REQUEST — FRONTEND Phase 3-1 Get Video List

**ROLE**:
- 당신은 **Amazing Korean API의 프론트엔드 전담 AI 에이전트**입니다.
- **Tech Stack**: React (Vite), TypeScript, Tailwind CSS, Shadcn/ui, TanStack Query, Zustand, React Hook Form, Zod.
- **AGENTS.md** 및 **AMK_API_MASTER.md (Section 6)** 의 프론트엔드 규칙을 엄격히 준수합니다.

**OBJECTIVE**:
- **Vimeo** 영상 목록을 조회하는 페이지(`/videos`)를 구현하고, 홈 화면에 진입 버튼을 만드시오.
- 페이지네이션(Pagination)을 포함하며, 그리드 레이아웃을 구성합니다.

**MCP ACTIONS (필수 수행)**:
작업을 시작하기 전에 다음 **도구(Tools)**를 사용하여 컨텍스트를 직접 확보하십시오.
1.  **Read Category Types (`types.ts`)**: `src/category/video/types.ts`가 없다면 아래 **DATA SPEC**을 그대로 사용하여 생성하고, 있다면 내용을 읽어오십시오.
    - *Rule*: 제공된 스펙은 이미 검증된 **절대 기준(ReadOnly)**입니다. 절대 필드명을 바꾸거나 수정하지 마십시오.
2.  **Focus on Category**: `src/category/video` 폴더와 `src/app/routes.tsx`, `src/category/home` 내에서만 작업하십시오.
3.  **Check Dependencies**: UI 구현에 필요한 `aspect-ratio`, `badge`, `pagination` 등이 설치되어 있는지 확인하고 명령어를 최상단에 작성하십시오.

**DATA SPEC (Confirmed SSOT)**:
- **Target**: `src/category/video/types.ts`
- **Warning**: 아래 코드를 그대로 사용하십시오. (수정 금지)

```typescript
export interface VideoSummary {
  video_id: number;
  title: string;
  description?: string;
  thumbnail_url: string;
  video_url_vimeo: string;
  view_count: number;
  duration: string;
  created_at: string;
  uploader_name: string;
}

export interface VideoListRes {
  items: VideoSummary[];
  meta: {
    total: number;
    page: number;
    per_page: number;
    total_pages: number;
  };
}

export interface VideoListReq {
  page?: number;
  per_page?: number;
  sort?: string;
  lang?: string;
}
```

**IMPLEMENTATION STEPS (Strict 5-Step Process)**:
1.  **Step 1. Read & Import Types (`types.ts`)**:
    - `src/category/video/types.ts`를 위 **DATA SPEC** 내용으로 작성하십시오.
    - 다른 파일에서는 이 파일을 Import 하여 사용하십시오.
2.  **Step 2. API & Hooks (`api.ts` -> `hooks/`)**:
    - **API**: `src/category/video/video_api.ts` 작성. `src/api/client.ts`의 Interceptor를 신뢰하여 `accessToken` 인자를 받지 마십시오.
    - **Hooks**: `src/category/video/hook/use_video_list.ts` 작성. 에러 발생 시(`onError`) `toast.error`를 띄우십시오. (Silent Failure 금지)
    - *Note*: Auth 상태가 `stop`(비로그인)이어도 조회 가능하므로, 훅 내부에서 불필요한 가드 로직을 넣지 마십시오.
3.  **Step 3. UI Component & Page**:
    - **Components**: `src/category/video/components/video_card.tsx` (Vimeo 썸네일/정보 표시).
    - **Page**: `src/category/video/page/video_list_page.tsx` (그리드 리스트 + 페이지네이션).
    - **Routing**: 상세 이동 시 `<a href>` 대신 **`<Link to={`/videos/${video.video_id}`}>`**를 반드시 사용하십시오.
4.  **Step 4. Navigation (Entry Point)**:
    - `src/app/routes.tsx`에 `/videos` 경로 등록.
    - `src/category/home/page/home_page.tsx`에 "영상 학습하기" 진입 버튼(`<Link>`) 추가.
5.  **Step 5. Dependency Clean-up**:
    - 불필요한 Import 제거 및 린트 정리.

**PATCH RULES (Iron Rules)**:
1.  **🚫 No Type Hallucinations**: 위 **DATA SPEC**에 정의된 필드명(`video_url_vimeo` 등)만 사용하십시오.
2.  **🔗 DTO 1:1 Mapping**: API 요청 파라미터는 `VideoListReq`와 일치해야 합니다.
3.  **🚦 No Silent Failures**: 400/422 에러 시 반드시 사용자에게 피드백(Toast)을 주십시오.
4.  **⚡ Performance Routing**: 무조건 `<Link>` 컴포넌트를 사용하십시오.
5.  **📦 Dependency Check**: 설치가 필요한 Shadcn 컴포넌트 명령어를 최상단에 명시하십시오.
6.  **Full File Replacement**: 수정되는 파일은 전체 코드를 출력하십시오.

**OUTPUT FORMAT**:

**PREREQUISITES (Dependencies)**:
```bash
npx shadcn@latest add ...
```

// FILE: src/category/video/types.ts
// FILE: src/category/video/video_api.ts
// FILE: src/category/video/hook/use_video_list.ts
// FILE: src/category/video/components/video_card.tsx
// FILE: src/category/video/page/video_list_page.tsx
// FILE: src/app/routes.tsx
// FILE: src/category/home/page/home_page.tsx