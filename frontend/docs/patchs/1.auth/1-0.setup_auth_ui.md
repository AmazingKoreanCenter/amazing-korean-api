# PATCH REQUEST — FRONTEND Phase 1-0 Setup Auth Store & Global UI

**ROLE**:
- Frontend Developer (React, Zustand, Shadcn/ui).

**OBJECTIVE**:
- Phase 1(Auth) 구현을 위한 **상태 관리 스토어**와 **전역 UI 설정**을 수행하시오.
- **설치 작업 금지**: 필요한 라이브러리(`zustand`)와 Shadcn 컴포넌트(`form`, `sonner` 등)는 이미 설치되어 있습니다. 코드 구현에만 집중하십시오.

**TASKS**:
1.  **Auth Store (`src/hooks/use_auth_store.ts`)**:
    - `zustand`의 `create`와 `persist` 미들웨어를 사용.
    - **State**:
        - `user`: User info object (nullable).
        - `accessToken`: string (nullable).
        - `isLoggedIn`: boolean.
    - **Actions**:
        - `login(data: LoginRes | SignupRes)`: 토큰과 유저 정보를 저장하고 `isLoggedIn=true` 설정.
        - `logout()`: 모든 상태 초기화 (localStorage 포함).
    - **Types**: `src/category/auth/types.ts`와 `src/category/user/types.ts`에서 DTO 타입을 import해서 사용.

2.  **Global Toaster (`src/App.tsx` or Layout)**:
    - `src/components/ui/sonner.tsx`의 `<Toaster />` 컴포넌트를 앱 최상단에 배치.

**IMPLEMENTATION STEPS**:
1.  **Store**: `src/hooks/use_auth_store.ts` 작성. (DTO 타입 참조 필수)
2.  **App**: `src/App.tsx`에 `<Toaster />` 추가.

**FILE PATCHES**:
// src/hooks/use_auth_store.ts
// src/App.tsx

**PATCH RULES**:
1.  **Strict Typing**: `any` 타입을 금지하고, 만들어둔 DTO(`LoginRes` 등)를 적극 활용하시오.
2.  **Persist**: 새로고침 해도 로그인이 풀리지 않도록 `persist` 옵션을 올바르게 설정하시오. (Storage Key: "auth-storage")