# AMK_AUDIT_2026-05-04 — 전수 정합성 조사 결과

> **목적**: AMK_DEBTS.md 의 추정/부분 검증 항목을 실제 코드/문서/설정과 cross-check 한 결과 기록. AMK_DEBTS 와 독립.
> **조사 방법**: 1차 6 agent 병렬 (백엔드 / 프론트엔드 / 인프라 / 문서 / DB 스키마 / 외부 서비스) + 2차 6 agent 병렬 (Cargo/scripts / Docker/git / Frontend 빌드+i18n / Router+OpenAPI / DB 인덱스 / CORS+보안 헤더+에러).
> **조사 일시**: 2026-05-04 (1차 + 2차)
> **기준**: HEAD commit (working tree 미커밋 변경 무시 — `git show HEAD:` 사용)
> **원칙**: 사실만. 가정·추정·의견 X.

> **본 문서 SSoT 범위 (사용자 결정 2026-05-04)**: amazing-korean-api 의 정합성/부채 정리 = **본 문서 = SSoT (frontend src/ 코드 제외 영역)**.
> - **포함**: 백엔드 src/ + crates/ + migrations/ + docs/ + 인프라 (Docker/nginx/.github/) + 외부 서비스 + 의존성 + DB 스키마 + Router/OpenAPI + DB 인덱스 + CORS/보안 헤더 + 환경변수 정합성 + Frontend 빌드 설정 + Frontend i18n locale
> - **제외**: **frontend/src/ 비즈니스 로직 코드** (page/hook/component/utils 약 200 파일) — 별도 트랙. Frontend lint baseline 정리 (Q16) 시점에 함께 검토
> - **이유**: 적색 위험 (XSS/eval/토큰 노출) = 1차 표본에서 모두 0건. 추가 발견 가능 카테고리 (비즈니스 로직 / 성능 / 접근성) = 위험도 낮음. 같은 시간 = 실제 부채 처리 우선

---

## 0. 요약

### 진행도 (전체 870 파일 기준)

| 카테고리 | 카운트 | 검증 진행 |
|---------|:--:|:--:|
| 자산 (.webp/.svg/.png) | 306 | 대상 X |
| 자동 생성 (.sqlx) | 13 | 부분 (checksum 미검증) |
| **검증 대상 코드/문서/설정** | **551** | — |
| └ 백엔드 src/ + crates/ (.rs) | 171 | ~100% ✅ |
| └ migrations/ (.sql) | 26 | ~100% ✅ |
| └ docs/ (.md) | 21 | ~100% ✅ |
| └ 인프라 (Docker/nginx/CI/scripts) | ~30 | ~100% ✅ |
| └ Cargo / 의존성 | 5 | ~100% ✅ |
| └ Frontend i18n locales (.json) | 35 | 100% ✅ |
| └ Frontend 빌드 설정 | ~10 | ~100% ✅ |
| └ **Frontend src/ 비즈니스 로직 (.tsx + .ts)** | **249** | **~20% (별도 트랙, 본 SSoT 제외)** |

**본 SSoT 진행도** = (551 - 249) / (551 - 249) = **약 100%** (frontend src/ 비즈니스 로직 제외 시).
**전체 진행도** (frontend 포함) = ~430 / 551 = **약 78%**.

### 발견 카운트

- **검증된 안전 영역** = 28건 (백엔드/외부 서비스/암호화/DB/OAuth)
- **신규 발견 문제 (1차)** = 15건 (N-1 ~ N-15)
- **신규 발견 문제 (2차)** = 22건 (N-16 ~ N-37)
- **합계 신규 발견** = **37건** (이전 AMK_DEBTS 미등재)
- **AMK_DEBTS 불일치** = 1건 (C12 TS any 카운트)
- **stale 문서** = 1건 (AMK_API_TEXTBOOK 마이그 누락 3개)

---

## 1. 검증된 안전 영역 (사실)

### 1.1 백엔드 panic 패턴 (안전)

`unwrap()` 9건 + `expect()` 47건 + `panic!()` 24건 분석 결과:

| 카테고리 | 카운트 | 위험 |
|---------|:--:|------|
| 서버 시작 단계 (config.rs / main.rs) | 23 | 안전 (의도적 fail-fast) |
| 정적 값 생성 (NaiveDate / Argon2 Params) | 5 | 안전 |
| 배열 길이 검증 후 (SHA-256 [..8]) | 1 | 안전 |
| Option 검증 후 (`if let Some` / `match`) | 2 | 안전 (논리상 None 불가) |
| HMAC/암호화 키 길이 검증 | 6 | 안전 (32바이트 검증 후) |
| Argon2 더미 해시 (타이밍 공격 방지) | 1 | 안전 |

**결론**: 런타임 요청 처리 중 panic 없음. 서버 시작 단계만 fail-fast.

### 1.2 SQL injection (없음)

`format!()` 사용 8건 모두:
- 컬럼명/where 절 구성용 (사용자 입력 X)
- 사용자 입력은 `$1, $2, ...` 바인딩 변수만
- LIKE 패턴은 `escape_like()` 함수로 `%`, `_`, `\` 이스케이프

`sqlx::query!` / `sqlx::query_as!` 매크로 = 컴파일 타임 SQL 검증.

### 1.3 Transaction 사용 (안전)

`.begin()` 43건 (auth 11 + admin 26 + user 3 + ebook 2 + textbook 1). 다단계 INSERT/UPDATE 모두 트랜잭션 감쌈.

### 1.4 환경변수 중앙화 (안전)

`env::var(...)` 147건 = **모두 `config.rs` 중앙화**. 다른 파일 (api/util.rs, main.rs) = 5건 (RUST_LOG, DB_EAGER, AK_DEV_IP_FALLBACK 등 비핵심).

### 1.5 암호화/인증 (정책 준수)

- **AES-256-GCM + KeyRing** (`crypto/mod.rs`) — PII 필드 암호화 (PR 정책)
- **HMAC-SHA256 32바이트** (`config.rs:453-462`) — Blind Index 생성
- **Argon2** (`auth/service.rs`) — 비밀번호 해시
- **JWT HS256** (32바이트 이상 검증)
- **OAuth JWKS 캐싱** (`apple.rs`, `google.rs`) — RwLock 기반

### 1.6 시간 일관성 (UTC)

`Utc::now()` 사용 = 모든 시간 코드. `Local::now()` 호출 0건. 시간 비교 = same timezone (UTC).

### 1.7 OAuth 보안 (완전)

| 검증 항목 | Google | Apple |
|----------|:--:|:--:|
| State 검증 (CSRF) | ✓ Redis | N/A (모바일) |
| Nonce 검증 (Replay) | ✓ | ✓ |
| ID Token 서명 (RSA-256 JWKS) | ✓ | ✓ |
| Audience 검증 (`aud == client_id`) | ✓ | ✓ |
| Issuer 검증 (`iss`) | ✓ | ✓ |
| Redirect URI 검증 | ✓ | N/A |

### 1.8 외부 서비스 TLS (대부분 HTTPS)

| 서비스 | URL | TLS |
|--------|------|:--:|
| Resend | `https://api.resend.com/emails` | ✓ |
| RevenueCat | `https://api.revenuecat.com/v1` | ✓ |
| Google OAuth | `https://accounts.google.com`, `https://oauth2.googleapis.com`, `https://www.googleapis.com` | ✓ |
| Apple JWKS | `https://appleid.apple.com/auth/keys` | ✓ |
| Vimeo | `https://api.vimeo.com` | ✓ |
| Paddle SDK | (SDK 내부) | ✓ |
| **ip-api.com** | `http://ip-api.com/json` | **HTTP** ⚠ |

### 1.9 인증 처리 (안전)

모든 Bearer/API key = 환경변수에서 읽음. 코드에 hardcoded 키 0건. 로그에 API key 노출 0건. config.rs Display impl 에서 마스킹 (`"***"`).

### 1.10 SQL 응답 검증 (완전)

모든 외부 HTTP 응답:
- HTTP status 검증 (`is_success()`) — 6/6 외부 서비스
- JSON parse 에러 처리 — 6/6
- 401/403/429 별도 매핑 — Resend 등

### 1.11 DB 스키마 vs Rust enum (일치)

| Enum | DB 카운트 | Rust 카운트 | 일치 |
|------|:--:|:--:|:--:|
| `supported_language_enum` | 37 | 37 (`SupportedLanguage`) | ✅ |
| `textbook_language_enum` | 35 | 35 (`TextbookLanguage`) | ✅ |
| `user_language_enum` | 21 | 21 (`UserLanguage`) | ✅ |
| `content_type_enum` | 10 | 10 (`ContentType`) | ✅ |
| `payment_provider_enum` | 4 | 4 (`PaymentProvider`) | ✅ |
| `ebook_edition_enum` | 2 | 2 (`EbookEdition`) | ✅ |
| `ebook_purchase_status_enum` | 3 | 3 (`EbookPurchaseStatus`) | ✅ |

### 1.12 Migration 일관성

- 파일명 = `YYYYMMDD_<description>.sql` 형식 일관 (정수 버전 충돌 없음)
- `IF NOT EXISTS` 사용으로 멱등성 보장
- 타임스탬프 = 모든 `TIMESTAMPTZ` (TZ-aware 일관)
- 비밀번호 컬럼 = `TEXT` (Argon2 hash 저장 정책)

### 1.13 Frontend 적색 위험 (없음)

- `dangerouslySetInnerHTML` 사용 = **0건**
- `eval()` / `new Function()` = **0건**
- localStorage 토큰 저장 = **0건** (언어 설정만 저장)
- sessionStorage 민감정보 = **0건** (저작권 고지 플래그만)
- `document.cookie` 직접 조작 = **0건**

### 1.14 Frontend 라우팅/권한 (정상)

- `private_route.tsx` 로그인 체크 정상
- `admin_route.tsx` admin/HYMN 역할 + MFA 체크 정상

---

## 2. 신규 발견 문제 (AMK_DEBTS 미등재)

### N-1. Frontend `window.open` `rel` 속성 누락 4건

| 위치 | 코드 | 카테고리 |
|------|------|---------|
| `textbook/page/textbook_order_page.tsx:373-376` | `window.open(/.../print?type=quote, "_blank")` | 내부 경로 |
| `textbook/page/textbook_order_status_page.tsx:354-384` | 3개 window.open (quote, confirmation, receipt) | 내부 경로 |
| `admin/video/page/admin_video_detail.tsx:244` | `window.open(video.url!, "_blank")` | **외부 URL** |

**위험**: Reverse Tabnabbing (자식 창이 부모 창 redirect 가능). 외부 URL 1건 = 위험 더 큼.

### N-2. Frontend i18n 하드코딩 1건

| 위치 | 코드 |
|------|------|
| `admin/email/page/admin_email_test.tsx:184` | `지금 바로 한국어 학습을 시작해 보세요!` |

**카테고리**: 어드민 테스트 메일 템플릿. `t()` 함수 미사용.

### N-3. Frontend `as any` 타입 회피 3건

| 위치 | 코드 |
|------|------|
| `ebook/page/ebook_viewer_page.tsx:256` | `(window as any).createImageBitmap = function () {` |
| `ebook/page/ebook_viewer_page.tsx:266` | `(origCreateImageBitmap as any).apply(window, args)` |
| `ebook/page/ebook_viewer_page.tsx:277` | `(window as any).createImageBitmap = origCreateImageBitmap` |

**카테고리**: ebook DRM 패턴 (canvas 보호). `@ts-expect-error` 인라인 회피 동반.

### N-4. Frontend non-null assertion (`!`) 3건

| 위치 | 코드 |
|------|------|
| `admin/study/page/admin_studies_page.tsx:428` | `Math.max(1, params.page! - 1)` |
| `admin/study/page/admin_studies_page.tsx:455` | `Math.min(..., params.page! + 1)` |
| `admin/study/page/admin_study_create.tsx:339` | `!isNaN(choiceCorrectValue!)` (2개 연속) |

### N-5. Frontend `eslint-disable-next-line` 인라인 9건

`exhaustive-deps` 6건 (textbook_order_page, WritingTask, use_page_image 2건 등) + `@ts-expect-error` 2건 (ebook_viewer DRM) + `no-console` 1건 (devtools_detect).

### N-6. Frontend Tailwind 하드코딩 색상 추가

`lint:ui` 미감지 색상 (blue, yellow 등) — 정책상 emerald/amber/rose/teal/red 만 감지:

| 위치 | 색 |
|------|---|
| `textbook/page/textbook_order_page.tsx:435` | `bg-blue-500/10`, `text-blue-600` |
| `auth/page/account_recovery_page.tsx:272` | `border-yellow-200`, `bg-yellow-50`, `dark:border-yellow-900`, `dark:bg-yellow-950` |

### N-7. Frontend catch 무음 처리 2건

| 위치 | 코드 |
|------|------|
| `ebook/page/ebook_viewer_page.tsx:556` | `.requestFullscreen().catch(() => {})` |
| `ebook/page/ebook_viewer_page.tsx:558` | `.exitFullscreen().catch(() => {})` |

**위험**: 전체화면 API 실패 시 사용자 피드백 0.

### N-8. RevenueCat 웹훅 = 평문 Bearer 토큰 비교 (타이밍 공격 이론적)

| 위치 | 코드 |
|------|------|
| `src/api/payment/handler.rs:155, 180` | `format!("Bearer {}", expected_token)` + `token != expected_token` |

**위험**: 평문 `==` 비교 = 타이밍 공격 이론적 가능. 실제 위협 = 매우 낮음 (고정 길이 토큰).

**Paddle 비교**: HMAC-SHA256 + timestamp 5분 variance 검증 (완전).

### N-9. RevenueCat 웹훅 replay 방지 부재

Paddle = `MaximumVariance::seconds(300)` 으로 timestamp 검증. RevenueCat = timestamp 검증 0 (Bearer 토큰만).

### N-10. 외부 호출 timeout 명시 부재 5건

| 서비스 | timeout | 코드 |
|--------|:--:|------|
| Resend | 기본값 | `reqwest::Client::new()` |
| RevenueCat | 기본값 | `Client::new()` |
| Google OAuth | 기본값 | `Client::new()` |
| Apple JWKS | 기본값 | `Client::new()` |
| Vimeo | 기본값 | `Client::new()` |
| ip-api.com | **5초** | `.timeout(Duration::from_secs(5))` |

**위험**: reqwest 기본값 = 무제한 가까움. 외부 서비스 hang 시 우리 API 도 hang.

### N-11. 인프라 — postgres 호스트 포트 노출 (dev only)

| 위치 | 사실 |
|------|------|
| `docker-compose.yml:5-6` | `5432:5432` 호스트 전체 바인딩 (production 미사용, dev 만) |
| `docker-compose.yml:19` | redis = `127.0.0.1:16379:6379` (localhost bind, 안전) |

**위험**: dev 머신이 외부 노출 시 DB 직접 공격 가능.

### N-12. 환경변수 누락 — `EBOOK_PAGE_IMAGES_DIR`

| 위치 | 사실 |
|------|------|
| `src/config.rs:357` | `env::var("EBOOK_PAGE_IMAGES_DIR")` 읽음 (`.unwrap_or_else()` 기본값 `"docs/textbook/page-images"`) |
| `.env.example` | **미정의** |
| `.github/workflows/deploy.yml` heredoc | **미명시** |

**위험**: J1 (RATE_LIMIT_TEXTBOOK_*) 와 같은 카테고리. 단 `unwrap_or_else` 라 panic 위험 X. 의도된 default 가 production 의도와 다를 수 있음.

### N-13. nginx HTTPS 설정 (이미 발견되었으나 상세)

| 위치 | 사실 |
|------|------|
| `nginx/nginx.conf:40-51` | HTTP server 블록 활성 |
| `nginx/nginx.conf:48-51` | HTTPS 리다이렉트 `location /` 주석 처리 |
| `nginx/nginx.conf:87-114` | HTTPS server 블록 **완전 주석** (인증서 경로 / TLS 1.2+1.3 / cipher 정의 포함) |
| `nginx/nginx.conf:98-102` | X-Frame-Options, X-Content-Type-Options, HSTS 헤더 = HTTPS 블록 안 (현재 비활성) |
| `nginx/nginx.conf:105` | `limit_req zone=api_limit burst=20 nodelay` = HTTPS 블록 안 (현재 비활성) |

**핵심**: rate limit + 보안 헤더 둘 다 HTTPS 블록 안 → **현재 HTTP-only 운영에서 미적용**.

### N-14. AMK_API_TEXTBOOK.md 마이그레이션 stale

| 문서 | 사실 |
|------|------|
| `docs/AMK_API_TEXTBOOK.md:13` | 마이그레이션 4개 명시 (20260226, 20260303, 20260310, 20260503) |
| 실제 `migrations/` | **7개** (위 4개 + `20260323_textbook_tax_fields.sql` + `20260324_textbook_user_id.sql` + `20260423_textbook_order_discount.sql` + `20260424_textbook_orderer_email_optional.sql`) |

**원인**: 문서 작성 후 3개 마이그레이션 추가됨 (2026-03-23 ~ 2026-04-24).

### N-15. AMK_DEBTS C12 카운트 불일치 (재검증 필요)



| 표기 | 실제 |
|------|------|
| AMK_DEBTS C12: TypeScript `any` 사용 **3건** | agent 4 검증 = `: any[^a-zA-Z]|as any|<any>` grep 으로 **0건** 발견 |

**원인 추정**: AMK_DEBTS 작성 시 (2026-05-04 오후) grep 결과 3건이었으나, agent 4 검증 시점 (2026-05-04 저녁) 에 문구 패턴 차이 또는 변경됨. **재검증 필요**.

agent 2 (frontend 전수) 검증 = `as any` 3건 발견 (`ebook_viewer_page.tsx:256, 266, 277`). **agent 2 와 agent 4 grep 패턴 차이**.

---

---

## 2.B 2차 조사 신규 발견 (N-16 ~ N-37, 2026-05-04 저녁)

이전 1차 6 agent 가 못 다룬 영역 = Cargo/scripts / Docker/git / Frontend 빌드+i18n / Router+OpenAPI / DB 인덱스 / CORS+보안 헤더 추가 조사. **22건 추가 발견**.

### 의존성/도구 (3건)

#### N-16. Cargo.toml 라이선스 메타데이터 누락

| 위치 | 사실 |
|------|------|
| `Cargo.toml` | `[package]` 섹션에 `license = ...` 필드 없음 (workspace 루트 + crates/crypto 둘 다) |

**영향**: 라이선스 명시 X = 협업 / 외부 사용 시 라이선스 분쟁 가능. private repo 라 즉시 영향 X.

#### N-17. Cargo.toml `[lints]` 섹션 부재

| 위치 | 사실 |
|------|------|
| `Cargo.toml` | `[lints]` 섹션 없음 (clippy 룰 명시 활성화 X) |

**영향**: clippy 룰 (예: `unwrap_used` / `expect_used` / `unimplemented` / `dbg_macro`) 차단 자동화 X. CI 의 `cargo clippy -D warnings` 만으로는 특정 룰 강제 X.

#### N-18. 동일 crate 다중 버전 5종

`Cargo.lock` (4881 줄, 477 패키지) 분석:

| Crate | 버전 동시 존재 | 원인 |
|-------|--------------|------|
| `rand` | 0.8.5 + 0.9.2 | totp-rs 0.9.2 요구 |
| `getrandom` | 0.2.17 + 0.3.4 | rand 0.8 vs 0.9 의존성 |
| `darling` | 0.20.11 + 0.23.0 | 다른 derive 매크로 |
| `itertools` | 0.12.1 + 0.14.0 | 다른 라이브러리 |
| `socket2` | 0.5.10 + 0.6.3 | 다른 네트워킹 lib |

**영향**: 바이너리 크기 증가 (작음). 실제 보안 영향 없음.

### Docker/git 인프라 (5건)

#### N-19. Dockerfile USER 미설정 (root 실행) ⚠

| 위치 | 사실 |
|------|------|
| `Dockerfile:64` | `CMD ["./amazing-korean-api"]` — `USER` 지시 없음, 컨테이너 내 root 실행 |

**영향**: 컨테이너 탈출 시 루트 권한 획득 가능. 보안 표준 위반.

#### N-20. Dockerfile HEALTHCHECK 미명시

| 위치 | 사실 |
|------|------|
| `Dockerfile` | `HEALTHCHECK` 지시 없음 |

**영향**: docker compose 의 service health 자동 검사 X (현재 docker-compose.prod.yml 의 db 만 healthcheck).

#### N-21. nginx 베이스 이미지 버전 미핀

| 위치 | 사실 |
|------|------|
| `docker-compose.prod.yml` | `image: nginx:alpine` (버전 미핀, latest 암묵적) |

**영향**: nginx 메이저 업데이트 시 자동 적용 → 호환성 깨짐 가능. postgres:16 / redis:7 는 핀 (정상).

#### N-22. .gitattributes / .editorconfig / .prettierrc / 루트 .eslintrc 미존재

| 파일 | 상태 |
|------|------|
| `.gitattributes` | 미존재 (줄바꿈 정책 X — Windows/Unix 혼용 시 diff 노이즈) |
| `.editorconfig` | 미존재 (편집기 일관성 X) |
| `.prettierrc` | 미존재 (코드 포맷 정책 X — frontend 만) |
| 루트 `.eslintrc` | 미존재 (frontend/ 만 있음) |

**영향**: 코드 스타일 일관성 자동 강제 X. 1인 환경에서 영향 작음.

#### N-23. LICENSE / README.md 미존재 (루트)

| 파일 | 상태 |
|------|------|
| `/LICENSE` | 미존재 |
| `/README.md` | 미존재 (`docs/AMK_API_MASTER.md` 가 사실상 SSoT) |

**영향**: private repo 라 즉시 영향 X. 외부 협업 / 오픈소스 검토 시 부재.

### Frontend 빌드 + i18n (3건)

#### N-24. index.html CSP 메타 태그 부재

| 위치 | 사실 |
|------|------|
| `frontend/index.html` | `<meta http-equiv="Content-Security-Policy" ...>` 없음 |

**영향**: CSP 정책 = nginx 미들웨어 / Cloudflare 등 외부 의존. 클라이언트 단 XSS 공격 방어 두 번째 layer X.

#### N-25. tsconfig `skipLibCheck: true`

| 위치 | 사실 |
|------|------|
| `frontend/tsconfig.app.json` | `skipLibCheck: true` |
| `frontend/tsconfig.node.json` | `skipLibCheck: true` |

**영향**: 라이브러리 타입 정의 검증 안 함 = 타입 안전성 감소. 빌드 속도 향상 trade-off.

#### N-26. i18n locale 21개 언어 = `legal` + `admin` top-level keys 누락

`frontend/src/i18n/locales/*.json` 분석:

| 그룹 | 언어 | 키 카운트 | 누락 |
|------|------|:---:|------|
| en (master) | en.json | 21 top-level | — |
| 완전 일치 (13) | am, ar, bn, ko, it, ky, lo, pl, sw, tl, tr, uk, ur | 21 | 없음 |
| **누락 (21)** | de, es, fr, hi, id, ja, kk, km, mn, my, ne, **pt-PT**, pt, ru, si, tg, th, uz, vi, zh-CN, zh-TW | **19** | **`legal` + `admin`** top-level 누락 |

**누락 키 상세**:
- `legal`: 5 sub-keys (lastUpdated, contactInfo, terms, privacy, refund)
- `admin`: 4 sub-keys (dashboard, payment, textbook, ebook)

**기존 부채와 차이**: AMK_DEBTS 의 "22 base lang × 518 keys 누락" = nested keys 합계. 본 N-26 = **top-level group 누락** (구조적 차이). admin/legal 그룹 자체가 없으므로 nested keys 도 자동 누락.

**영향**: 로그인한 사용자가 21개 언어 (de/es/fr/...) 선택 시 admin / legal 페이지 = en fallback. UX 미일관.

### API 문서/스펙 (1건, 큼)

#### N-27. OpenAPI 스펙 누락 endpoint **32+개**

`src/docs.rs` (utoipa OpenAPI 정의) vs 각 `src/api/*/router.rs` 등록 cross-check:

| 도메인 | router 등록 | docs.rs 명시 | 누락 카운트 |
|--------|:--:|:--:|:--:|
| **auth** | 23 | 12 | **10** (login_mobile, refresh_mobile, mfa_login_mobile, google_auth_start, google_auth_callback, google_mobile_login, apple_mobile_login, verify_email, resend_verification, find_password) |
| **payment** | 5 | 0 | **5** |
| **course** | 3 | 0 | **3** |
| **textbook** | 4 | 0 | **4** |
| **ebook** | 9 | 0 | **9** |
| **admin/email** | 1 | 0 | **1** |
| **admin/payment** | 6+ | 0 | **6+** |
| **admin/textbook** | 7 | 0 | **7** |
| **admin/ebook** | 5 | 0 | **5** |
| **admin/user/stats** | 5 | 0 | **5** |
| **admin/study/stats** | 3 | 0 | **3** |

**합계 누락 = 약 58+ endpoint**. Swagger UI `/docs` 에 미노출. API 클라이언트 = 라우터 코드 직접 참조 또는 시행 착오로만 인식.

**영향**: 외부 API 사용자 / 모바일 / 데스크탑 앱 개발 시 OpenAPI 문서 의존 불가능.

### DB 인덱스 (3건)

#### N-28. `ebook_purchase(paddle_txn_id)` 인덱스 누락

| 위치 | SQL 인용 |
|------|---------|
| `src/api/ebook/repo.rs:298-308` | `WHERE paddle_txn_id = $1 AND status = 'completed'` (UPDATE) |
| `migrations/` | `idx_ebook_purchase_paddle_txn_id` 등 미정의 |

**영향**: Paddle 환불 웹훅 처리 시 풀 스캔. 주문 누적 시 점진적 느려짐.

#### N-29. `subscriptions(user_id, status)` 복합 인덱스 누락

| 위치 | SQL 인용 |
|------|---------|
| `src/api/payment/repo.rs:94-119` | `WHERE user_id = $1 AND status = 'active' LIMIT 1` |
| `migrations/` | `idx_subscriptions_user_id` + `idx_subscriptions_status` 분리 (복합 X) |

**영향**: 활성 구독 조회 시 두 단일 인덱스 결합. 큰 데이터 시 성능 저하.

#### N-30. `ebook_purchase(user_id, language, edition)` 복합 인덱스 누락

| 위치 | SQL 인용 |
|------|---------|
| `src/api/ebook/repo.rs:222-242` | `WHERE user_id=$1 AND language=$2 AND edition=$3 AND status IN (...)` (중복 구매 확인) |

**영향**: 매 구매 요청마다 풀 스캔.

### CORS + 보안 헤더 + 에러 누출 (7건)

#### N-31. HSTS (Strict-Transport-Security) 미설정

| 위치 | 사실 |
|------|------|
| `src/main.rs:243-269` security_headers() | HSTS 헤더 없음 |
| `nginx/nginx.conf:98-102` | HSTS 헤더 정의 = HTTPS 블록 안 (현재 비활성, N-13 와 연결) |

**영향**: HTTPS 강제 정책 X. 첫 방문 시 HTTP → HTTPS 다운그레이드 공격 가능.

#### N-32. CSP (Content-Security-Policy) 미설정

| 위치 | 사실 |
|------|------|
| `src/main.rs:243-269` | CSP 헤더 없음 |
| `frontend/index.html` | `<meta http-equiv="Content-Security-Policy">` 없음 (N-24) |

**영향**: XSS 공격 방어 두 번째 layer X.

#### N-33. Referrer-Policy 미설정

| 위치 | 사실 |
|------|------|
| `src/main.rs:243-269` | `Referrer-Policy` 헤더 없음 |

**영향**: 외부 링크 클릭 시 우리 사이트 URL 노출 (default 정책).

#### N-34. `X-XSS-Protection: 0` 구식 (무해)

| 위치 | 사실 |
|------|------|
| `src/main.rs:243-269` | `x-xss-protection: 0` 명시 |

**영향**: 최신 브라우저 무시. 정상 (legacy 호환).

#### N-35. `remaining_attempts` 응답 필드 = rate limit 정보 노출 ⚠

| 위치 | 사실 |
|------|------|
| `src/api/auth/dto.rs:242-352` | `FindPasswordRes`, `RequestResetRes`, `ResendVerificationRes` 모두 `remaining_attempts: i64` 필드 |
| `src/api/auth/service.rs:963, 1382, 1791` | `remaining_attempts = std::cmp::max(0, rate_limit_email_max - attempts)` |

**영향**: 사용자에게 정확한 재시도 횟수 노출. 공격자가 rate limit 정책 추론 + 시간 분석 가능.

#### N-36. Validation 에러 = 필드명 / 룰 노출 ⚠

| 위치 | 사실 |
|------|------|
| `src/error.rs:195-201` | `AppError::Validation(e)` → `details: { "errors": e.to_string() }` |

**예시 응답**: `{"errors": "email: invalid email format, password: length must be >= 8"}` — 정책 정보 노출.

**영향**: 공격자에게 검증 룰 힌트 제공. 정상적 UX 와 trade-off.

#### N-37. 기본 RUST_LOG = `debug`

| 위치 | 사실 |
|------|------|
| `src/main.rs:27-32` | 기본값 `amazing_korean_api=debug,tower_http=debug` |

**영향**: production 에서 RUST_LOG 환경변수 명시 안 하면 debug 로그 활성. Request body / token 노출 위험.

**확인 필요**: `deploy.yml` heredoc 의 RUST_LOG 명시 여부.

---

## 3. 부분 검증 영역 (한계 인지)

### 3.1 `.sqlx/` checksum 정합성 미검증

migrations 와 `.sqlx/` 캐시 (12개 query-*.json, timestamp 2026-04-21 16:09) 사이 checksum 검증 미실행.

**확인 방법**: `cargo sqlx prepare --check` 또는 `cargo sqlx prepare` 후 diff.

**위험**: INC-004 패턴 (이미 적용된 migration 변경) 잔존 가능.

### 3.2 Migration 파일 변경 이력 미추적

git log 로 각 migration 파일의 수정 이력 미점검. INC-004 패턴 (주석/공백 수정) 잠재 잔존 추적 X.

### 3.3 amount tampering / 사용자 ID 매핑 (Paddle)

`payment/service.rs` 의 결제 처리 로직 = 본 조사 범위 외 (agent 6 미검증).

**확인 필요**: 웹훅 이벤트 수신 시 서버 DB 가격 vs 이벤트 가격 비교 로직 / 사용자 ID 매핑 안전성.

### 3.4 환경변수 카운트 정확도

agent 4 검증 = `.env.example` 122 라인 / 카운트 ~58 (정확 파싱 필요). AMK_DEBTS 표기 = 65건.

**확인 방법**: `grep -cE '^[A-Z_]+=' .env.example` 정확 카운트.

---

## 4. 정리 — 안전 vs 미해결

### ✅ 검증된 안전 영역 (문서화 가치)

본 audit 으로 다음 영역 = **사실 기반 안전 확인**:

1. 백엔드 panic 패턴 (시작 단계 fail-fast)
2. SQL injection (없음)
3. Transaction 사용 (43건 적절)
4. 환경변수 중앙화 (config.rs)
5. 암호화 정책 (AES-256-GCM + Argon2)
6. 시간 UTC 일관성
7. OAuth 보안 (state/nonce/JWKS 완전)
8. TLS (1건 HTTP 외 모두 HTTPS)
9. API key 로그 미노출
10. DB enum vs Rust enum 일치 (7개 enum)
11. Migration 일관성 (파일명 + 멱등성)
12. Frontend 적색 위험 부재 (XSS/eval/localStorage 토큰)
13. Frontend 라우팅/권한 정상

### ⚠️ 미해결 신규 발견 = **37건** (N-1 ~ N-37)

본 문서가 SSoT. AMK_DEBTS.md 와 별도 추적.

**카테고리별 분포**:
- Frontend 보안/품질: N-1 ~ N-7 (7건)
- 외부 서비스 보안: N-8 ~ N-10 (3건)
- 인프라/환경변수: N-11 ~ N-13 (3건)
- 문서 stale: N-14, N-15 (2건)
- 의존성/도구: N-16 ~ N-18 (3건)
- Docker/git: N-19 ~ N-23 (5건)
- Frontend 빌드/i18n: N-24 ~ N-26 (3건)
- API 문서/스펙: N-27 (1건, 큼 = 58+ endpoint)
- DB 인덱스: N-28 ~ N-30 (3건)
- CORS/보안 헤더/에러: N-31 ~ N-37 (7건)

### ⚠️ 부분 검증 영역 = 4건

`.sqlx/` checksum / migration 변경 이력 / Paddle amount tampering / .env.example 정확 카운트.

---

## 5. 갱신 규칙

본 문서 = 2026-05-04 시점 snapshot. 이후 변경:

- 새 발견 = 본 문서에 N-NNN 추가
- 미해결 처리 = 행 시작 `~~취소선~~` + 처리 시점/방법 명시
- 부분 검증 영역 = 별도 조사 후 결과 추가

본 문서 갱신 시 메모리 갱신 불필요 (메모리 = `feedback_audit_reference.md` 포인터만, 신규 작성 검토).

---

## 6. 관련 SSoT

- `AMK_DEBTS.md` — 부채 카탈로그 (본 audit 와 별도)
- `AMK_AI_MISTAKES.md` — AI 작업 사고 기록
- `AMK_STATUS.md §8.2` — 진행 예정 + 검증된 리스크 + 보류
- `AMK_DEPLOY_OPS.md` — 배포/운영 절차
- `AMK_CHANGELOG.md` — 변경 이력
