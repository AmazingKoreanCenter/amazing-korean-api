# AMK_AUDIT_2026-05-04 — 전수 정합성 조사 결과

> **목적**: AMK_DEBTS.md 의 추정/부분 검증 항목을 실제 코드/문서/설정과 cross-check 한 결과 기록. AMK_DEBTS 와 독립.
> **조사 방법**: 1차 6 agent 병렬 (백엔드 / 프론트엔드 / 인프라 / 문서 / DB 스키마 / 외부 서비스) + 2차 6 agent 병렬 (Cargo/scripts / Docker/git / Frontend 빌드+i18n / Router+OpenAPI / DB 인덱스 / CORS+보안 헤더+에러).
> **조사 일시**: 2026-05-04 (1차 + 2차)
> **검증 2회차**: 2026-05-04 (저녁) — 6 agent 분담 병렬, M-007 사고 후 라인/카운트 stale 정정. N-5/N-6/N-15/N-27 정정.
> **기준**: HEAD commit `3cad9a3` (working tree 미커밋 변경 무시 — `git show HEAD:` 사용)
> **원칙**: 사실만. 가정·추정·의견 X.

> **본 문서 SSoT 범위 (사용자 결정 2026-05-04)**: amazing-korean-api 의 정합성/부채 정리 = **본 문서 = SSoT (frontend src/ 코드 제외 영역)**.
> - **포함**: 백엔드 src/ + crates/ + migrations/ + docs/ + 인프라 (Docker/nginx/.github/) + 외부 서비스 + 의존성 + DB 스키마 + Router/OpenAPI + DB 인덱스 + CORS/보안 헤더 + 환경변수 정합성 + Frontend 빌드 설정 + Frontend i18n locale
> - **제외**: **frontend/src/ 비즈니스 로직 코드** (page/hook/component/utils 약 200 파일) — 별도 트랙. Frontend lint baseline 정리 (Q16) 시점에 함께 검토
> - **이유**: 적색 위험 (XSS/eval/토큰 노출) = 1차 표본에서 모두 0건. 추가 발견 가능 카테고리 (비즈니스 로직 / 성능 / 접근성) = 위험도 낮음. 같은 시간 = 실제 부채 처리 우선

---

## 0. 요약

### 진행도 (전체 870 파일 기준)

| 카테고리 | 카운트 | 검증 진행 |
|---------|:--:|:--:|
| 자산 (.webp/.svg/.png) | 306 | 대상 X |
| 자동 생성 (.sqlx) | 13 | 부분 (checksum 미검증) |
| **검증 대상 코드/문서/설정** | **551** | — |
| └ 백엔드 src/ + crates/ (.rs) | 171 | ~100% ✅ |
| └ migrations/ (.sql) | 26 | ~100% ✅ |
| └ docs/ (.md) | 21 | ~100% ✅ |
| └ 인프라 (Docker/nginx/CI/scripts) | ~30 | ~100% ✅ |
| └ Cargo / 의존성 | 5 | ~100% ✅ |
| └ Frontend i18n locales (.json) | 35 | 100% ✅ |
| └ Frontend 빌드 설정 | ~10 | ~100% ✅ |
| └ **Frontend src/ 비즈니스 로직 (.tsx + .ts)** | **249** | **~20% (별도 트랙, 본 SSoT 제외)** |

**본 SSoT 진행도** = (551 - 249) / (551 - 249) = **약 100%** (frontend src/ 비즈니스 로직 제외 시).
**전체 진행도** (frontend 포함) = ~430 / 551 = **약 78%**.

### 발견 카운트

- **검증된 안전 영역** = 28건 (백엔드/외부 서비스/암호화/DB/OAuth)
- **신규 발견 문제 (1차)** = 15건 (N-1 ~ N-15) — **N-15 종결** (검증 2회차, false flag)
- **신규 발견 문제 (2차)** = 22건 (N-16 ~ N-37) — **N-16/N-17/N-19/N-20/N-21/N-22/N-24/N-28~30/N-32/N-33/N-35~37 ✅ 해결** + **N-25/N-34 🟡 수용** (2026-05-04 ~ 2026-05-05)
- **신규 발견 문제 (1차)** = 15건 (N-1 ~ N-15) — **N-8/N-9/N-10/N-12/N-14 ✅ 해결** + **N-11 🟡 수용** + **N-15 종결**
- **신규 발견 문제 (옵션 C)** = 1건 (N-38 ✅ 해결, commit `c744efc`)
- **합계 신규 미해결** = ~~4건~~ → ~~3건~~ → **2건** (2026-05-06 N-27 종결 + N-14 stale 마킹 정정 후. 잔여 = N-13 nginx HTTPS / N-26 i18n. N-31 HSTS = 🟢 Cloudflare edge 사실상 활성으로 별도 처리 불필요 발견됨)
  - **N-31 발견 (2026-05-05)**: Cloudflare edge 자동 HSTS 활성 확인. origin layer 미적용은 우선순위 낮음
  - **N-18 🟡 수용**: transitive 의존성 자연 결과
  - **N-23 ✅ 해결**: LICENSE + README.md 신규
  - **N-1/N-4/N-7 ✅ 해결**: frontend Q16 cleanup 묶음 (commit `04e74f3`)
  - **N-2/N-3/N-5/N-6 🟡 수용**: admin 한국어 / ebook DRM / Q16 baseline 별도 트랙 / 디자인 토큰 결정 대기
- **AMK_DEBTS 불일치** = ~~1건 (C12 TS any 카운트)~~ → **0건** (N-15 종결)
- **stale 문서** = 1건 (AMK_API_TEXTBOOK 마이그: 1개 분류 오인 + 4개 누락)
- **검증 2회차 정정** = 4건 (N-5 카테고리, N-6 명확화, N-15 종결, N-27 카운트 58+ → 16)
- **검증 3회차 정정 (옵션 C)** = 4건 (N-14 분류 정정, N-18 다중 버전 5종 → 9+ 그룹, N-27 16 → 약 43건, N-38 신규)
- **부분 검증 영역** = ~~4건~~ → **0건** (§3.1~§3.4 모두 검증 완료)

---

## 1. 검증된 안전 영역 (사실)

### 1.1 백엔드 panic 패턴 (안전)

`unwrap()` 9건 + `expect()` 47건 + `panic!()` 24건 분석 결과:

| 카테고리 | 카운트 | 위험 |
|---------|:--:|------|
| 서버 시작 단계 (config.rs / main.rs) | 23 | 안전 (의도적 fail-fast) |
| 정적 값 생성 (NaiveDate / Argon2 Params) | 5 | 안전 |
| 배열 길이 검증 후 (SHA-256 [..8]) | 1 | 안전 |
| Option 검증 후 (`if let Some` / `match`) | 2 | 안전 (논리상 None 불가) |
| HMAC/암호화 키 길이 검증 | 6 | 안전 (32바이트 검증 후) |
| Argon2 더미 해시 (타이밍 공격 방지) | 1 | 안전 |

**결론**: 런타임 요청 처리 중 panic 없음. 서버 시작 단계만 fail-fast.

### 1.2 SQL injection (없음)

`format!()` 사용 8건 모두:
- 컬럼명/where 절 구성용 (사용자 입력 X)
- 사용자 입력은 `$1, $2, ...` 바인딩 변수만
- LIKE 패턴은 `escape_like()` 함수로 `%`, `_`, `\` 이스케이프

`sqlx::query!` / `sqlx::query_as!` 매크로 = 컴파일 타임 SQL 검증.

### 1.3 Transaction 사용 (안전)

`.begin()` 43건 (auth 11 + admin 26 + user 3 + ebook 2 + textbook 1). 다단계 INSERT/UPDATE 모두 트랜잭션 감쌈.

### 1.4 환경변수 중앙화 (안전)

`env::var(...)` 147건 = **모두 `config.rs` 중앙화**. 다른 파일 (api/util.rs, main.rs) = 5건 (RUST_LOG, DB_EAGER, AK_DEV_IP_FALLBACK 등 비핵심).

### 1.5 암호화/인증 (정책 준수)

- **AES-256-GCM + KeyRing** (`crypto/mod.rs`) — PII 필드 암호화 (PR 정책)
- **HMAC-SHA256 32바이트** (`config.rs:453-462`) — Blind Index 생성
- **Argon2** (`auth/service.rs`) — 비밀번호 해시
- **JWT HS256** (32바이트 이상 검증)
- **OAuth JWKS 캐싱** (`apple.rs`, `google.rs`) — RwLock 기반

### 1.6 시간 일관성 (UTC)

`Utc::now()` 사용 = 모든 시간 코드. `Local::now()` 호출 0건. 시간 비교 = same timezone (UTC).

### 1.7 OAuth 보안 (완전)

| 검증 항목 | Google | Apple |
|----------|:--:|:--:|
| State 검증 (CSRF) | ✓ Redis | N/A (모바일) |
| Nonce 검증 (Replay) | ✓ | ✓ |
| ID Token 서명 (RSA-256 JWKS) | ✓ | ✓ |
| Audience 검증 (`aud == client_id`) | ✓ | ✓ |
| Issuer 검증 (`iss`) | ✓ | ✓ |
| Redirect URI 검증 | ✓ | N/A |

### 1.8 외부 서비스 TLS (대부분 HTTPS)

| 서비스 | URL | TLS |
|--------|------|:--:|
| Resend | `https://api.resend.com/emails` | ✓ |
| RevenueCat | `https://api.revenuecat.com/v1` | ✓ |
| Google OAuth | `https://accounts.google.com`, `https://oauth2.googleapis.com`, `https://www.googleapis.com` | ✓ |
| Apple JWKS | `https://appleid.apple.com/auth/keys` | ✓ |
| Vimeo | `https://api.vimeo.com` | ✓ |
| Paddle SDK | (SDK 내부) | ✓ |
| **ip-api.com** | `http://ip-api.com/json` | **HTTP** ⚠ |

### 1.9 인증 처리 (안전)

모든 Bearer/API key = 환경변수에서 읽음. 코드에 hardcoded 키 0건. 로그에 API key 노출 0건. config.rs Display impl 에서 마스킹 (`"***"`).

### 1.10 SQL 응답 검증 (완전)

모든 외부 HTTP 응답:
- HTTP status 검증 (`is_success()`) — 6/6 외부 서비스
- JSON parse 에러 처리 — 6/6
- 401/403/429 별도 매핑 — Resend 등

### 1.11 DB 스키마 vs Rust enum (일치)

| Enum | DB 카운트 | Rust 카운트 | 일치 |
|------|:--:|:--:|:--:|
| `supported_language_enum` | 37 | 37 (`SupportedLanguage`) | ✅ |
| `textbook_language_enum` | 35 | 35 (`TextbookLanguage`) | ✅ |
| `user_language_enum` | 21 | 21 (`UserLanguage`) | ✅ |
| `content_type_enum` | 10 | 10 (`ContentType`) | ✅ |
| `payment_provider_enum` | 4 | 4 (`PaymentProvider`) | ✅ |
| `ebook_edition_enum` | 2 | 2 (`EbookEdition`) | ✅ |
| `ebook_purchase_status_enum` | 3 | 3 (`EbookPurchaseStatus`) | ✅ |

### 1.12 Migration 일관성

- 파일명 = `YYYYMMDD_<description>.sql` 형식 일관 (정수 버전 충돌 없음)
- `IF NOT EXISTS` 사용으로 멱등성 보장
- 타임스탬프 = 모든 `TIMESTAMPTZ` (TZ-aware 일관)
- 비밀번호 컬럼 = `TEXT` (Argon2 hash 저장 정책)

### 1.13 Frontend 적색 위험 (없음)

- `dangerouslySetInnerHTML` 사용 = **0건**
- `eval()` / `new Function()` = **0건**
- localStorage 토큰 저장 = **0건** (언어 설정만 저장)
- sessionStorage 민감정보 = **0건** (저작권 고지 플래그만)
- `document.cookie` 직접 조작 = **0건**

### 1.14 Frontend 라우팅/권한 (정상)

- `private_route.tsx` 로그인 체크 정상
- `admin_route.tsx` admin/HYMN 역할 + MFA 체크 정상

---

## 2. 신규 발견 문제 (AMK_DEBTS 미등재)

### ~~N-1. Frontend `window.open` `rel` 속성 누락~~ ✅ 해결 (2026-05-05, commit `04e74f3`)

audit 4건 표기 → 실측 **5건** (admin_video_detail external 1 + textbook 4 internal). 모두 `"noopener,noreferrer"` 3rd argument 추가 = Reverse Tabnabbing 차단.

### 🟡 N-2. Frontend i18n 하드코딩 — 수용 결정 (2026-05-05)

| 위치 | 코드 |
|------|------|
| `admin/page/admin_email_test.tsx:184` (audit path 정정) | `지금 바로 한국어 학습을 시작해 보세요!` 외 영역 전체 한국어 |

**결정**: 수용. admin 페이지 정책 = 한국어 only. 영역 전체 (welcome 미리보기 4 줄) 한국어 일관 = 1 줄만 t() 변환 시 일관성 깨짐. Q16 baseline 시점 admin 페이지 i18n 정책 결정 후 일괄 처리 권고.

### 🟡 N-3. Frontend `as any` 3건 — 수용 결정 (2026-05-05)

| 위치 | 코드 |
|------|------|
| `ebook/page/ebook_viewer_page.tsx:256` | `(window as any).createImageBitmap = function () {` |
| `ebook/page/ebook_viewer_page.tsx:266` | `(origCreateImageBitmap as any).apply(window, args)` |
| `ebook/page/ebook_viewer_page.tsx:277` | `(window as any).createImageBitmap = origCreateImageBitmap` |

**결정**: 수용. ebook DRM 의도 = canvas 보호 (createImageBitmap override 차단). lib.dom.d.ts 의 `window.createImageBitmap` = readonly = TypeScript 가 override 거부. `as any` = 의도된 우회. 보안 우선 (타입 안전성 < DRM 효과).

### ~~N-4. Frontend non-null assertion (`!`) 3건~~ ✅ 해결 (2026-05-05, commit `04e74f3`)

(audit path 정정: admin/study/page → admin/page)
- `admin_studies_page.tsx:428/455` = `params.page!` → `(params.page ?? 1)` (default 1)
- `admin_study_create.tsx:339` = `choiceCorrectValue!` → `choiceCorrectValue != null` nullish guard

### 🟡 N-5. Frontend `eslint-disable-next-line` 인라인 9건 — 수용 결정 (2026-05-05)

(2026-05-04 검증 2회차 카테고리 정정)
`exhaustive-deps` 4건 + `no-explicit-any` 3건 + `no-console` 1건 + 기타 1건. 총 9건.

**결정**: 수용. 각 인라인 회피 = 의도된 사용 (exhaustive-deps = mount-once 패턴 / no-explicit-any = ebook DRM N-3 와 같은 영역 / no-console = devtools_detect). Q16 baseline cleanup 트랙에서 개별 평가 권고.

### 🟡 N-6. Frontend Tailwind 하드코딩 색상 — 수용 결정 (2026-05-05)

`lint:ui` 미감지 색상 (blue, yellow 등) — 정책상 emerald/amber/rose/teal/red 만 감지. **2 색상 그룹 = 6 클래스**.

| 위치 | 색 |
|------|---|
| `textbook/page/textbook_order_page.tsx:435` | blue 그룹 (1) — `bg-blue-500/10`, `text-blue-600` (2 클래스) |
| `auth/page/account_recovery_page.tsx:272` | yellow 그룹 (1) — `border-yellow-200`, `bg-yellow-50`, `dark:border-yellow-900`, `dark:bg-yellow-950` (4 클래스) |

**결정**: 수용. 디자인 토큰 정책 = lint:ui 정책 색상 (emerald/amber/rose/teal/red) 만 = info (blue), warning (yellow) 의미 표현 부족. Q16 디자인 토큰 결정 (semantic color) 후 일괄 변환 권고.

### ~~N-7. Frontend catch 무음 처리 2건~~ ✅ 해결 (2026-05-05, commit `04e74f3`)

`ebook_viewer_page.tsx:556/558` = `.catch(() => {})` → `.catch((err) => console.warn("[ebook] requestFullscreen/exitFullscreen failed:", err))`. 진단 정보 보존, 사용자 피드백은 의도적 X (브라우저 정책/권한 실패 시 혼란 회피).

### ~~N-8. RevenueCat 웹훅 = 평문 Bearer 토큰 비교 (타이밍 공격 이론적)~~ ✅ 해결 (2026-05-05, commit `fcfaaf2`)

`src/api/payment/handler.rs` 의 `token != expected_token` → `subtle::ConstantTimeEq` 기반 constant-time 비교로 변경. Cargo.toml 에 subtle 2.6 의존성 추가.

### ~~N-9. RevenueCat 웹훅 replay 방지 부재~~ ✅ 해결 (2026-05-05, commit `fcfaaf2`)

`payload.event.event_timestamp_ms` 와 현재 시간 비교 (5분 variance, Paddle 패턴 일치). 초과 시 401 unauthorized. timestamp 누락 시 400 BAD_REQUEST.

### ~~N-10. 외부 호출 timeout 명시 부재 5건~~ ✅ 해결 (2026-05-05, commit `b567f62`)

5 외부 서비스 (Resend / RevenueCat / Google OAuth / Apple JWKS / Vimeo) 모두 `Client::builder().timeout(15s).build()` 적용. ip-api.com 은 기존 5초 유지.

### 🟡 N-11. 인프라 — postgres 호스트 포트 노출 (dev only) — 수용 결정 (2026-05-05)

| 위치 | 사실 |
|------|------|
| `docker-compose.yml:5-6` | `5432:5432` 호스트 전체 바인딩 (production 미사용, dev 만) |
| `docker-compose.yml:19` | redis = `127.0.0.1:16379:6379` (localhost bind, 안전) |

**결정**: 수용. `docker-compose.yml` = `.gitignore` 정책 (개인 dev 환경 파일). 1인 환경 + WSL/dev 머신 외부 노출 시나리오 낮음. 사용자가 본인 환경에서 `127.0.0.1:5432:5432` 로 변경 권장 (local 적용, commit 무관).

### ~~N-12. 환경변수 누락 — `EBOOK_PAGE_IMAGES_DIR`~~ ✅ 해결 (2026-05-05, commit `18f5682`)

`.env.example` 추가 + `deploy.yml` heredoc 추가. 정합성 + production 의도 명확화. (panic 위험 X = default 존재했음).

### N-13. nginx HTTPS 설정 (이미 발견되었으나 상세)

| 위치 | 사실 |
|------|------|
| `nginx/nginx.conf:40-51` | HTTP server 블록 활성 |
| `nginx/nginx.conf:48-51` | HTTPS 리다이렉트 `location /` 주석 처리 |
| `nginx/nginx.conf:87-114` | HTTPS server 블록 **완전 주석** (인증서 경로 / TLS 1.2+1.3 / cipher 정의 포함) |
| `nginx/nginx.conf:98-102` | X-Frame-Options, X-Content-Type-Options, HSTS 헤더 = HTTPS 블록 안 (현재 비활성) |
| `nginx/nginx.conf:105` | `limit_req zone=api_limit burst=20 nodelay` = HTTPS 블록 안 (현재 비활성) |

**핵심**: rate limit + 보안 헤더 둘 다 HTTPS 블록 안 → **현재 HTTP-only 운영에서 미적용**.

### ~~N-14. AMK_API_TEXTBOOK.md 마이그레이션 stale (검증 2회차 정정)~~ ✅ 해결 (2026-05-05, commit `9fa6f14`)

> 처리: `docs/AMK_API_TEXTBOOK.md:13` 마이그 표기 4 → 7 직접 + 1 supported_language 정정. (본 마킹 누락 발견 = 2026-05-06 정합성 cross-check)

| 문서 | 사실 |
|------|------|
| `docs/AMK_API_TEXTBOOK.md:13` | 마이그 4개 명시 (20260226, 20260303, **20260310**, 20260503) |
| 실제 textbook 직접 마이그 | **7개** (20260226, 20260303, 20260323_tax_fields, 20260324_user_id, 20260423_order_discount, 20260424_orderer_email_optional, 20260503) |

**정정 (2026-05-04 검증 2회차)**:
- 문서 표기 `20260310_add_tl_language.sql` = **실재하나 supported_language 마이그** (textbook 직접 X). 분류 오인.
- 문서 일치: 3개 (20260226, 20260303, 20260503)
- 문서 분류 오인: 1개 (20260310)
- 문서 누락 (textbook 직접): 4개 (20260323, 20260324, 20260423, 20260424)

**원인**: 문서 작성 후 4개 마이그 추가 (2026-03-23 ~ 2026-04-24) + 1개 분류 오인.

### ~~N-15. AMK_DEBTS C12 카운트 불일치~~ ✅ 종결 (검증 2회차, 2026-05-04)

| 표기 | 실측 (검증 2회차) |
|------|------|
| AMK_DEBTS C12: TypeScript `any` 사용 **3건** | `as any` **3건** 일치 (`ebook_viewer_page.tsx:256, 266, 277`) |

**결론**: 1차 audit 의 agent 4 grep 결함 추정 (정규식 알 수 없는 이유로 0건 보고). agent 2 (frontend 전수) + 검증 2회차 결과 모두 3건 일치. **AMK_DEBTS C12 표기 정확. N-15 = false flag, 종결**.

---

---

## 2.B 2차 조사 신규 발견 (N-16 ~ N-37, 2026-05-04 저녁)

이전 1차 6 agent 가 못 다룬 영역 = Cargo/scripts / Docker/git / Frontend 빌드+i18n / Router+OpenAPI / DB 인덱스 / CORS+보안 헤더 추가 조사. **22건 추가 발견**.

### 의존성/도구 (3건)

#### ~~N-16. Cargo.toml 라이선스 메타데이터 누락~~ ✅ 해결 (2026-05-05, commit `2211aaa`)

`license = "UNLICENSED"` + `publish = false` 추가 (workspace + crates/crypto). private repo 명시 + cargo publish 차단 가드.

#### ~~N-17. Cargo.toml `[lints]` 섹션 부재~~ ✅ 해결 (2026-05-05, commit `327a89c`)

`[workspace.lints]` 섹션 추가 — `unsafe_code = "deny"` + `dbg_macro/todo/unimplemented = "warn"`. 각 crate `[lints] workspace = true`. 코드 위반 0건 (선검증). 추가 확장 (unwrap_used / expect_used) = 별도 작업.

#### 🟡 N-18. 동일 crate 다중 버전 — 수용 결정 (2026-05-05)

**검증 2회차 정정: 5종 → 9+ 그룹**

`Cargo.lock` (4881 줄, 477 패키지) 분석. 검증 2회차 `grep -E '^name = ' Cargo.lock | sort | uniq -c | awk '$1 > 1'` 결과:

| Crate | 버전 카운트 | 비고 |
|-------|:--:|------|
| `getrandom` | **3** | (1차 표기 2 → 정정) |
| `hashbrown` | **3** | (1차 누락) |
| `darling` (+ darling_core, darling_macro) | 2+2+2 | 다른 derive 매크로 |
| `rand` (+ rand_chacha, rand_core) | 2+2+2 | totp-rs 의존 |
| `indexmap` | 2 | (1차 누락) |
| `itertools` | 2 | 다른 라이브러리 |
| `redox_syscall` | 2 | (1차 누락, 시스템) |
| `schemars` | 2 | (1차 누락) |
| `socket2` | 2 | 다른 네트워킹 lib |
| `webpki-roots` | 2 | (1차 누락) |
| 시스템 (windows-*, core-foundation, r-efi) | 2~3 each | 빌드 타겟별 |

**영향**: 바이너리 크기 증가 (작음). 실제 보안 영향 없음.

**결정**: 수용. `cargo tree -d` 검증 = 다중 버전 = transitive 의존성 자연 결과 (rand 0.8.5 = 우리 직접 + 0.9.2 = totp-rs 의존, getrandom 0.2/0.3/0.4 = rand 의존성 분기, hashbrown 0.15/0.16 = sqlx 의존 등). 통합 시도 = 의존 라이브러리 자체 업데이트 대기 필요 = 작업 비용 큼 vs 가치 낮음 (영향 작음). 의존성 자연 진화로 시간 흐름 시 자동 통합 기대.

### Docker/git 인프라 (5건)

#### ~~N-19. Dockerfile USER 미설정 (root 실행)~~ ✅ 해결 (2026-05-04, commit `28022ca`)

| 위치 | 사실 |
|------|------|
| ~~`Dockerfile:64`~~ | ~~`CMD ["./amazing-korean-api"]` — `USER` 지시 없음, 컨테이너 내 root 실행~~ → `USER appuser` (UID 1001 system user) 추가 |

**처리 완료**: `useradd -r -u 1001 -M -d /nonexistent appuser` + `chown -R appuser:appuser /app` + `USER appuser`. docker build 통과 검증. amk-api 컨테이너 volume 없음 = read-only fs OK.

#### ~~N-20. Dockerfile HEALTHCHECK 미명시~~ ✅ 해결 (2026-05-05, commit `254a5e1`)

`HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 CMD curl --fail --silent --max-time 3 http://localhost:3000/health` 추가. apt-get 에 curl 의존성 추가 (~4MB).

#### ~~N-21. nginx 베이스 이미지 버전 미핀~~ ✅ 해결 (2026-05-05, commit `46ad698`)

`docker-compose.prod.yml` 의 `image: nginx:alpine` → `nginx:1.27-alpine` 명시. postgres:16 / redis:7 패턴과 정합.

#### ~~N-22. .gitattributes / .editorconfig 미존재~~ ✅ 해결 (2026-05-05, commit 본 세션)

`.gitattributes` (LF 강제 + 바이너리 명시) + `.editorconfig` (Rust 4 / TOML 2 / 기본 2 스페이스, charset utf-8, insert_final_newline) 신규 작성.

**미진행 (의도)**: 루트 `.prettierrc` / `.eslintrc` = frontend/ 안에 이미 정의 + 단일 프로젝트 영역 분리 = 루트 추가 의미 부족.

#### ~~N-23. LICENSE / README.md 미존재 (루트)~~ ✅ 해결 (2026-05-05, commit 본 세션)

루트에 `LICENSE` (All rights reserved, proprietary, HYMN Co., Ltd.) + `README.md` (프로젝트 소개 + 스택 + 디렉터리 + 문서 포인터 + 로컬 개발 가이드) 신규 작성. N-16 (Cargo publish=false) 과 정합.

### Frontend 빌드 + i18n (3건)

#### ~~N-24. index.html CSP 메타 태그 부재~~ ✅ 해결 (2026-05-05, N-32 처리로 동시 해결, commit `4ceadc8`)

CSP 정책 = `frontend/public/_headers` (Cloudflare Pages) 로 적용. 메타 태그 < 헤더 (강도 우위). 메타 태그 추가는 redundant — 본 부채 의도 (CSP 활성) 충족.

#### 🟡 N-25. tsconfig `skipLibCheck: true` — 수용 결정 (2026-05-05)

| 위치 | 사실 |
|------|------|
| `frontend/tsconfig.app.json` | `skipLibCheck: true` |
| `frontend/tsconfig.node.json` | `skipLibCheck: true` |

**결정**: 수용. `false` 변경 시 lib 타입 검증 활성 = node_modules 의 외부 lib 타입 에러 다수 노출 + 빌드 시간 증가. 본 trade-off (빌드 속도 vs 타입 안전성) 의 비용 큼. Vite/React 생태계 권장 = `true` 유지. 본 영역 = Q16 baseline 트랙에서 전수 검토 시 재평가.

#### N-26. i18n locale 21개 언어 = `legal` + `admin` top-level keys 누락

`frontend/src/i18n/locales/*.json` 분석:

| 그룹 | 언어 | 키 카운트 | 누락 |
|------|------|:---:|------|
| en (master) | en.json | 21 top-level | — |
| 완전 일치 (13) | am, ar, bn, ko, it, ky, lo, pl, sw, tl, tr, uk, ur | 21 | 없음 |
| **누락 (21)** | de, es, fr, hi, id, ja, kk, km, mn, my, ne, **pt-PT**, pt, ru, si, tg, th, uz, vi, zh-CN, zh-TW | **19** | **`legal` + `admin`** top-level 누락 |

**누락 키 상세**:
- `legal`: 5 sub-keys (lastUpdated, contactInfo, terms, privacy, refund)
- `admin`: 4 sub-keys (dashboard, payment, textbook, ebook)

**기존 부채와 차이**: AMK_DEBTS 의 "22 base lang × 518 keys 누락" = nested keys 합계. 본 N-26 = **top-level group 누락** (구조적 차이). admin/legal 그룹 자체가 없으므로 nested keys 도 자동 누락.

**영향**: 로그인한 사용자가 21개 언어 (de/es/fr/...) 선택 시 admin / legal 페이지 = en fallback. UX 미일관.

### API 문서/스펙 (1건, 큼)

#### ~~N-27. OpenAPI 스펙 누락 endpoint~~ ✅ **완전 해결 (2026-05-06)**

> **2026-05-06 종결**: 본 부채 8개 도메인 50 endpoint 전수 처리. `src/docs.rs` paths +50 / components +85 / tags +6 신규. webhook 2건 의도 제외 정책 정착. 6건 stale endpoint 카운트 정정 (실측 누계).
>
> 처리 PR (단일 세션): #222 B5 분류 → #223 auth 10 → #224 admin/email + payment + webhook 정책 → #228 PR-A (textbook+admin/payment+admin/textbook) 19 → #229 PR-B (course+admin/ebook) 8 → 본 PR ebook 9.
>
> 단일 세션 부채 종결 = 5 PR 누계 50 endpoint. 1 도메인 누락 0건. 검증 3회차 → 4회차 (실측) → 종결.

`src/docs.rs` (utoipa OpenAPI 정의) vs 각 `src/api/*/router.rs` 등록 cross-check:

**검증 3회차 실측 (2026-05-04 옵션 C 직접)**:
- `src/docs.rs` 등록 path fn = **98개** (`grep -cE '^\s{8,}crate::api::[a-z_:]+::[a-z_]+,?\s*$' src/docs.rs`)
- 각 router 등록 endpoint = **98개** (`grep -hE '\.route\("' src/api/*/router.rs src/api/admin/*/router.rs | wc -l`)
- 단순 카운트 일치는 우연. 도메인별 매핑은 큰 차이.

**도메인별 cross-check (검증 3회차)**:

| 도메인 | docs.rs | endpoint | 누락 |
|--------|:--:|:--:|:--:|
| ~~auth~~ | ~~12~~ → **22** | 22 | ~~10~~ → **0** ✅ (2026-05-06 완료) |
| user | 5 | 2 | (docs > router, 추가 fn 가능성) |
| video | 4 | 2 | (docs > router) |
| lesson | 5 | 3 | (docs > router) |
| study | 11 | 7 | (docs > router) |
| admin/user | 6 | 4 | (docs > router) |
| admin/video | 8 (= 7 + 1 stats) | 4 | (docs > router) |
| admin/lesson | 13 | 7 | (docs > router) |
| admin/study | 18 | 5 | (docs > router) |
| admin/translation | 11 | 6 | (docs > router) |
| admin/upgrade | 3 | 3 | 0 |
| ~~course~~ | ~~0~~ → **3** | 3 (실측 정정, 2 → 3) | ~~2~~ → **0** ✅ (2026-05-06 PR-B 완료) |
| ~~payment~~ | ~~0~~ → **3** | 5 (실측 정정, 4 → 5) | ~~4~~ → **0** ✅ (user-facing 3 등록 + webhook 2 의도 제외, 2026-05-06) |
| ~~textbook~~ | ~~0~~ → **4** | 4 | ~~4~~ → **0** ✅ (2026-05-06 완료) |
| ~~ebook~~ | ~~0~~ → **9** | 9 (실측 정정, 7 → 9) | ~~9~~ → **0** ✅ (2026-05-06 PR-C 완료, N-27 최종 종결) |
| ~~admin/email~~ | ~~0~~ → **1** | 1 | ~~1~~ → **0** ✅ (2026-05-06 완료) |
| ~~admin/payment~~ | ~~0~~ → **7** | 7 (실측 정정, 4 → 7) | ~~4~~ → **0** ✅ (2026-05-06 완료) |
| ~~admin/textbook~~ | ~~0~~ → **8** | 8 (실측 정정, 6 → 8) | ~~6~~ → **0** ✅ (2026-05-06 완료) |
| ~~admin/ebook~~ | ~~0~~ → **5** | 5 | ~~5~~ → **0** ✅ (2026-05-06 PR-B 완료) |

**확정 누락 = ~~약 43건~~ → ~~약 28건~~ → ~~약 16건~~ → ~~약 9건~~ → ✅ 0건** (2026-05-06 종결: auth 10 + payment 3 + admin/email 1 + textbook 4 + admin/payment 7 + admin/textbook 8 + course 3 + admin/ebook 5 + ebook 9 = **50건 전수 처리**. webhook 2 의도 제외. 단일 세션 5 PR 종결).

**실측 정정 누계**:
- payment endpoint 4 → 5 (revenuecat webhook 누락이었음, 2026-05-06)
- ebook endpoint 7 → 9 (검증 3회차 stale, 2026-05-06)
- admin/payment endpoint 4 → 7 (실측 7, 2026-05-06)
- admin/textbook endpoint 6 → 8 (실측 8, 2026-05-06)
- course endpoint 2 → 3 (실측 3, 2026-05-06 PR-B)
- 합계 = 10건 추가 발견 (이전 stale)

**Webhook 의도 제외 정책 (2026-05-06)**: payment webhook 2건 (Paddle / RevenueCat) 은 외부 시스템이 호출하며 API 클라이언트 코드 생성 대상이 아님. swagger UI 노출은 보안적 비권장 (URL/스펙 공개). 동일 정책으로 향후 발견 webhook 도 OpenAPI 제외 + handler 에 의도 명시 주석 의무.

(1차 audit "58+" / 검증 2회차 "16" 모두 부정확. 검증 3회차 = 도메인별 정확 매핑.)

**영향**: 외부 API 사용자 / 모바일 / 데스크탑 앱 개발 시 OpenAPI 문서 의존 불가능. course/payment/textbook/ebook 4 도메인 + admin 4 도메인 = Swagger UI 완전 미노출.

**1차 도메인별 분류 (참고)**:

| 도메인 | router 등록 | docs.rs 명시 | 누락 카운트 |
|--------|:--:|:--:|:--:|
| **auth** | 23 | 12 | **10** (login_mobile, refresh_mobile, mfa_login_mobile, google_auth_start, google_auth_callback, google_mobile_login, apple_mobile_login, verify_email, resend_verification, find_password) — agent F 재확인 일치 |
| **payment** | 5 | 0 | **5** — 재확인 필요 |
| **course** | 3 | 0 | **3** — 재확인 필요 |
| **textbook** | 4 | 0 | **4** — 재확인 필요 |
| **ebook** | 9 | 0 | **9** — 재확인 필요 |
| **admin/email** | 1 | 0 | **1** — 재확인 필요 |
| **admin/payment** | 6+ | 0 | **6+** — 재확인 필요 |
| **admin/textbook** | 7 | 0 | **7** — 재확인 필요 |
| **admin/ebook** | 5 | 0 | **5** — 재확인 필요 |
| **admin/user/stats** | 5 | 0 | **5** — 재확인 필요 |
| **admin/study/stats** | 3 | 0 | **3** — 재확인 필요 |

**영향**: 외부 API 사용자 / 모바일 / 데스크탑 앱 개발 시 OpenAPI 문서 의존 불가능.

### DB 인덱스 (3건)

#### ~~N-28. `ebook_purchase(paddle_txn_id)` 인덱스 누락~~ ✅ 해결 (2026-05-04, commit `a4a47fb`)

`migrations/20260504_payment_lookup_indexes.sql` 의 `idx_ebook_purchase_paddle_txn_id` 추가.

#### ~~N-29. `subscriptions(user_id, status)` 복합 인덱스 누락~~ ✅ 해결 (2026-05-04, commit `a4a47fb`)

`migrations/20260504_payment_lookup_indexes.sql` 의 `idx_subscriptions_user_status` 추가 (복합).

#### ~~N-30. `ebook_purchase(user_id, language, edition)` 복합 인덱스 누락~~ ✅ 해결 (2026-05-04, commit `a4a47fb`)

`migrations/20260504_payment_lookup_indexes.sql` 의 `idx_ebook_purchase_user_lang_edition` 추가 (복합).

### CORS + 보안 헤더 + 에러 누출 (7건)

#### 🟢 N-31. HSTS — Cloudflare edge 사실상 활성 발견 (2026-05-05 production 검증)

| 위치 | 사실 |
|------|------|
| `src/main.rs` security_headers() | HSTS 헤더 없음 (origin layer 미적용) |
| `nginx/nginx.conf:98-102` | HSTS 헤더 정의 = HTTPS 블록 안 (현재 비활성, N-13 와 연결) |
| **Cloudflare edge** | **자동 HSTS 적용 중**: `strict-transport-security: max-age=15552000; includeSubDomains` (2026-05-05 production 응답 헤더 직접 확인) |

**상태**: Cloudflare 통과 트래픽 = 모두 HSTS 적용 = 일반 사용자 보호 활성. Origin layer (`src/main.rs` 또는 nginx) 미추가 = Cloudflare 우회 (직접 EC2 접근) 시 미보호. 단 EC2 직접 접근 = 기본 차단 대상.

**결정**: 다음 세션에서 origin layer 추가 결정 (defense-in-depth 가치 vs 작업량). 단독 부채로는 우선순위 낮춤.

#### ~~N-32. CSP (Content-Security-Policy) 미설정~~ ✅ 해결 (2026-05-05, commit `4ceadc8`, 옵션 A)

| 위치 | 사실 |
|------|------|
| `frontend/public/_headers` (신규) | `Content-Security-Policy-Report-Only` 헤더 = Cloudflare Pages 표준 방식 |
| 정책 | default 'self' + Paddle/Google OAuth/Vimeo/Pretendard/Google Fonts 화이트리스트 + frame-ancestors 'none' + upgrade-insecure-requests |

**처리 완료**: Report-Only 모드 (위반 차단 X, 브라우저 콘솔 + Cloudflare Reports 로깅). 1-2주 데이터 수집 후 enforce 결정. 검증 합치 = 사용자 + Codex + Gemini 모두 옵션 A.

#### ~~N-33. Referrer-Policy 미설정~~ ✅ 해결 (2026-05-04, commit `f94e135`)

| 위치 | 사실 |
|------|------|
| `src/main.rs` security_headers() | `referrer-policy: strict-origin-when-cross-origin` 추가 |

**처리 완료**: HTTPS→HTTPS 동일 출처는 full URL, cross-origin 은 origin 만, 다운그레이드 (HTTPS→HTTP) 는 비전송.

#### 🟡 N-34. `X-XSS-Protection: 0` 구식 (무해) — 수용 결정 (2026-05-05)

| 위치 | 사실 |
|------|------|
| `src/main.rs` security_headers() | `x-xss-protection: 0` 명시 |

**결정**: 수용. 최신 브라우저 (Chrome 78+, Firefox 66+) 무시. 명시 `0` = 비활성 의도 명확 (CSP 가 대체). legacy 호환 차원 유지.

#### ~~N-35. `remaining_attempts` 응답 필드 = rate limit 정보 노출~~ ✅ 해결 (2026-05-05, commit `dc28492`, 옵션 D)

| 위치 | 사실 |
|------|------|
| `src/api/auth/dto.rs` (3 DTO) | `remaining_attempts: i64` → `Option<i64>` + `#[serde(skip_serializing_if = "Option::is_none")]` |
| `src/api/auth/service.rs` (8 위치) | `if remaining == 1 { Some(1) } else { None }` (3 함수 × unmatched/OAuth-only/success 분기 = anti-enumeration 정합) |

**처리 완료**: 1회 남음 시만 응답에 `"remaining_attempts": 1` 노출, 그 외에는 필드 자체 생략. 검증 합치 = 사용자 + Codex + Gemini 모두 옵션 D.

#### ~~N-36. Validation 에러 = 필드명 / 룰 노출~~ ✅ 해결 (2026-05-05, commit `a8440a9`, 옵션 D / 방식 1)

| 위치 | 사실 |
|------|------|
| `src/error.rs` | 신규 `AppError::ValidationGeneric` variant — 400 + "Invalid input" + details None |
| `src/api/auth/service.rs` (6 위치) | login/find_id/find_password/reset_password/verify_email/resend_verification = `ValidationGeneric` 명시 사용 |
| `src/api/user/service.rs` (1 위치) | signup (비밀번호 입력) = `ValidationGeneric` |

**미변경 (의도)**: `update_me` / `update_settings` (일반 폼) = `AppError::Validation` 그대로 유지 (UX 우선).

**처리 완료**: 인증/비밀번호 endpoint validation 실패 시 룰/필드명 비공개 응답. 내부 진단 = `tracing::debug!` 보존. 검증 합치 = 사용자 + Codex + Gemini 모두 옵션 D.

#### ~~N-37. 기본 RUST_LOG = `debug`~~ ✅ 해결 (2026-05-04, commit `90585ab`, 옵션 B)

| 위치 | 사실 |
|------|------|
| ~~`src/main.rs:27-32`~~ | ~~기본값 `amazing_korean_api=debug,tower_http=debug`~~ → `amazing_korean_api=info,tower_http=info` |

**처리 완료** (옵션 B 이중 안전망):
- `src/main.rs:27-29` 기본값 → `info,info`
- `.github/workflows/deploy.yml` `.env.prod` heredoc 에 `RUST_LOG=amazing_korean_api=info,tower_http=info` 명시 추가
- `.env.example` 에 dev 권장값 주석 추가 (debug 활성화하려면 주석 해제)

**검증 발견**: deploy.yml 미명시 = 현재 production 이 사실상 main.rs 기본값 debug 로 운영 중이었음 (이론 아닌 실제 활성). 다음 deploy 시점에 prod 가 info 로 전환.

---

### 2.C 옵션 C 직접 검증 신규 발견 (N-38, 2026-05-04 옵션 C)

#### ~~N-38. Paddle 웹훅 amount defense-in-depth 결여~~ ✅ 해결 (2026-05-04, commit `c744efc`)

| 위치 | 사실 |
|------|------|
| `src/api/payment/service.rs:552` | `let amount_cents = txn.details.totals.total.parse::<i32>().unwrap_or(0);` |
| `src/api/payment/service.rs:553` | `let tax_cents = txn.details.totals.tax.parse::<i32>().unwrap_or(0);` |

**처리 완료**: subscription 매핑 후 `create_transaction` 호출 전에 `amount_cents != billing_interval.price_cents()` 비교 추가. 불일치 시 `tracing::error` + `AppError::Internal` → 500 응답 (Paddle 자동 재시도). DB 저장 차단 = fail-closed.

---

## 3. 부분 검증 영역 — 검증 완료 (옵션 C, 2026-05-04)

### ~~3.1 `.sqlx/` checksum 정합성~~ ✅ 검증 완료

`cargo sqlx prepare --check` 실행 → `exit=0`, 정합. INC-004 패턴 잔존 X. (`warning: potentially unused queries found in .sqlx` 만 표기, 비핵심)

### ~~3.2 Migration 파일 변경 이력~~ ✅ 검증 완료

`git log --all --oneline --follow -- 'migrations/*.sql'` 결과 hotfix 2건 확인:
- `a5cd2ea` INC-004 hotfix (20260421 주석 수정 되돌림)
- `16691a1` 마이그 버전 충돌 hotfix (20260423 → 20260424)

추가 hotfix `c9a1a5e Phase V1-2 : ebook 마이그레이션 버전 순서 수정`.

모두 처리됨. INC-004 패턴 잠재 잔존 X.

### ~~3.3 amount tampering / 사용자 ID 매핑 (Paddle)~~ ✅ 검증 완료

**amount tampering**: 신규 부채 N-38 등재 (defense-in-depth 결여).

**user_id 매핑** (`src/api/payment/service.rs:557-580`):
- subscription_id 우선 매핑 (DB 조회) ✅ 안전
- Custom data fallback `txn.custom_data["user_id"]` (line 569-571) — Paddle SDK 가 서명 검증 보장 + custom_data 도 서명 대상이므로 위조 어려움 ✅ 안전
- user_id 결정 못하면 skip (line 572-578) ✅ 안전

### ~~3.4 환경변수 카운트 정확도~~ ✅ 검증 완료

`grep -cE '^[A-Z_]+=' .env.example` = **57건**. AMK_DEBTS 표기 65 → 57 정정 (검증 2회차 반영 완료).

---

## 4. 정리 — 안전 vs 미해결

### ✅ 검증된 안전 영역 (문서화 가치)

본 audit 으로 다음 영역 = **사실 기반 안전 확인**:

1. 백엔드 panic 패턴 (시작 단계 fail-fast)
2. SQL injection (없음)
3. Transaction 사용 (43건 적절)
4. 환경변수 중앙화 (config.rs)
5. 암호화 정책 (AES-256-GCM + Argon2)
6. 시간 UTC 일관성
7. OAuth 보안 (state/nonce/JWKS 완전)
8. TLS (1건 HTTP 외 모두 HTTPS)
9. API key 로그 미노출
10. DB enum vs Rust enum 일치 (7개 enum)
11. Migration 일관성 (파일명 + 멱등성)
12. Frontend 적색 위험 부재 (XSS/eval/localStorage 토큰)
13. Frontend 라우팅/권한 정상

### ⚠️ 미해결 신규 발견 = **37건** (N-1 ~ N-37)

본 문서가 SSoT. AMK_DEBTS.md 와 별도 추적.

**카테고리별 분포**:
- Frontend 보안/품질: N-1 ~ N-7 (7건)
- 외부 서비스 보안: N-8 ~ N-10 (3건)
- 인프라/환경변수: N-11 ~ N-13 (3건)
- 문서 stale: N-14, N-15 (2건)
- 의존성/도구: N-16 ~ N-18 (3건)
- Docker/git: N-19 ~ N-23 (5건)
- Frontend 빌드/i18n: N-24 ~ N-26 (3건)
- API 문서/스펙: N-27 (1건, 큼 = 58+ endpoint)
- DB 인덱스: N-28 ~ N-30 (3건)
- CORS/보안 헤더/에러: N-31 ~ N-37 (7건)

### ⚠️ 부분 검증 영역 = 4건

`.sqlx/` checksum / migration 변경 이력 / Paddle amount tampering / .env.example 정확 카운트.

---

## 5. 갱신 규칙

본 문서 = 2026-05-04 시점 snapshot. 이후 변경:

- 새 발견 = 본 문서에 N-NNN 추가
- 미해결 처리 = 행 시작 `~~취소선~~` + 처리 시점/방법 명시
- 부분 검증 영역 = 별도 조사 후 결과 추가

본 문서 갱신 시 메모리 갱신 불필요 (메모리 = `feedback_audit_reference.md` 포인터만, 신규 작성 검토).

---

## 6. 관련 SSoT

- `AMK_DEBTS.md` — 부채 카탈로그 (본 audit 와 별도)
- `AMK_AI_MISTAKES.md` — AI 작업 사고 기록
- `AMK_STATUS.md §8.2` — 진행 예정 + 검증된 리스크 + 보류
- `AMK_DEPLOY_OPS.md` — 배포/운영 절차
- `AMK_CHANGELOG.md` — 변경 이력
