# 📝 Phase 6-17 Admin Update Study - Issue & Troubleshooting Log

## 1. 기술적 이슈 (Technical Issue)

### ⚠️ 이슈 1: Validator Custom Function 타입 불일치
* **현상:** `#[validate(custom(...))]` 매크로 사용 시 컴파일 에러 발생 (`expected &Option<String>, found &String`).
* **원인:** `validator` 크레이트는 `Option<T>` 필드에 대해 유효성 검사를 할 때, 값이 `None`이면 건너뛰고 **`Some`일 때만 내부 값을 언래핑하여(`&T`)** 검증 함수에 전달합니다. 하지만 구현된 함수는 `&Option<T>`를 받도록 정의되어 있었습니다.
* **해결:** 검증 함수의 인자 타입을 `&Option<String>`에서 `&String`으로 수정하여 매크로 동작 방식과 일치시켰습니다.

---

## 2. 성공적인 구현 포인트 (Success Points)

### ✅ 동적 쿼리 패턴의 정착 (Dynamic Query Mastery)
* **성과:** `admin_update_video`에서 겪었던 콤마(`,`) 처리나 파라미터 바인딩 순서 문제 없이, `QueryBuilder`를 사용하여 깔끔하게 부분 수정(Partial Update) 쿼리를 구현했습니다.

### ✅ Before/After 로그 완비
* **성과:** 업데이트 트랜잭션 안에서 `SELECT`(기존 데이터) -> `UPDATE`(변경) -> `INSERT`(로그) 순서를 정확히 지켜, 데이터 변경 이력을 완벽하게 추적할 수 있게 되었습니다.

---

## 3. 총평 (Conclusion)

Video 모듈에서 겪었던 시행착오들이 확실히 자양분이 되었습니다.
복잡한 로직을 "참조(Reference)"하는 것만으로도 구현 속도와 안정성이 크게 향상됨을 확인했습니다.