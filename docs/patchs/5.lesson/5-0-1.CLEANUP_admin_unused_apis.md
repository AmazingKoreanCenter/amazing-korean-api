# CLEANUP — Phase 5-0 Remove Unused Admin Video APIs

ROLE: Senior Rust Backend (Axum) AI Assistant

OBJECTIVE:
- Remove unused "Admin Video Caption" and "Admin Video Tag" APIs from the project.
- Ensure `cargo check` passes after removal (no dangling imports or dead code).

TARGETS (To be removed):
1. **Caption APIs**:
   - `crate::api::admin::video::caption::handler::admin_create_caption`
   - `crate::api::admin::video::caption::handler::admin_update_caption`
   - `crate::api::admin::video::caption::handler::admin_delete_caption`
2. **Tag APIs**:
   - `crate::api::admin::video::tag::handler::admin_add_tags`
   - `crate::api::admin::video::tag::handler::admin_remove_tags`

IMPLEMENTATION STEPS:
1.  **Docs (`src/docs.rs`)**:
    - Remove the 5 handler references from `paths(...)`.
    - Remove any related DTOs (e.g., `CreateCaptionReq`, `AddTagsReq`) from `components(schemas(...))` if they are no longer used elsewhere.
2.  **Router (`src/api/admin/video/router.rs` or `src/api/admin/router.rs`)**:
    - Remove the `.route(...)` lines corresponding to these endpoints.
3.  **Source Files (`src/api/admin/video/...`)**:
    - Delete the `caption` and `tag` modules if they are exclusively used for these APIs.
    - If `src/api/admin/video/caption/` and `src/api/admin/video/tag/` folders become empty or contain only unused code, **delete the files/folders** and remove their declaration (`mod caption;`, `mod tag;`) from the parent `mod.rs`.
4.  **Cleanup Imports**:
    - Remove unused `use` statements in `handler.rs`, `service.rs`, etc.

FILE PATCHES START
// src/docs.rs
// src/api/admin/video/router.rs (or equivalent)
// src/api/admin/video/mod.rs
// src/api/admin/video/caption/handler.rs (Delete or Clear)
// src/api/admin/video/tag/handler.rs (Delete or Clear)
FILE PATCHES END

# VERIFICATION
- Run `cargo check` to ensure no broken links.