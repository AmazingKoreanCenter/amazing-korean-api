# CLEANUP — Remove Admin Delete Video API

ROLE: Senior Rust Backend (Axum) AI Assistant

OBJECTIVE:
- Remove the unused `admin_delete_video` API endpoint and its related code.
- Ensure the project builds successfully (`cargo check`) after removal.

TARGET:
- Function: `crate::api::admin::video::handler::admin_delete_video`

IMPLEMENTATION STEPS:
1.  **Docs (`src/docs.rs`)**:
    - Remove `crate::api::admin::video::handler::admin_delete_video` from the `paths(...)` macro.
2.  **Router (`src/api/admin/video/router.rs`)**:
    - Remove the route registration for `DELETE /videos/{id}` (or similar) that points to `handler::admin_delete_video`.
3.  **Handler (`src/api/admin/video/handler.rs`)**:
    - Delete the `pub async fn admin_delete_video(...)` function block entirely.
4.  **Service/Repo (Optional)**:
    - If `Service::delete_video` or `Repo::delete_video` exists and is **ONLY** used by this handler, remove them as well.
    - If used by other parts, keep the Service/Repo methods.

FILE PATCHES START
// src/docs.rs
// src/api/admin/video/router.rs
// src/api/admin/video/handler.rs
// src/api/admin/video/service.rs (Check usage)
// src/api/admin/video/repo.rs (Check usage)
FILE PATCHES END

# VERIFICATION
- Run `cargo check` to ensure no broken references (e.g., router pointing to deleted handler).