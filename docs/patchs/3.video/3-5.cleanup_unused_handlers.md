# PATCH — Cleanup Unused Video Handlers

ROLE: Senior Rust Backend AI Assistant

OBJECTIVE:
- `src/api/video/handler.rs`에서 더 이상 사용하지 않는 레거시 핸들러 삭제.
- `src/api/video/router.rs`에서 해당 핸들러와 연결된 라우팅 설정 제거.
- 컴파일 에러가 발생하지 않도록 깔끔하게 정리.

TARGET FUNCTIONS:
1. `health()`: 초기 헬스 체크용 (현재 `src/api/health` 등으로 대체됨/불필요).
2. `list_video_captions()`: 자막 관련 기능은 현재 스펙에서 제외됨.

IMPLEMENTATION STEPS:
1.  **Handler (`src/api/video/handler.rs`)**:
    - `pub async fn health ...` 함수 전체 삭제.
    - `pub async fn list_video_captions ...` 함수 전체 삭제.
2.  **Router (`src/api/video/router.rs`)**:
    - `.route("/health", ...)` 라인 삭제.
    - `.route("/{id}/captions", ...)` (또는 유사한 경로) 라인 삭제.
    - `use` 문에서 `health`, `list_video_captions`가 import 되어 있다면 제거.

FILE PATCHES START
// src/api/video/handler.rs
// src/api/video/router.rs
FILE PATCHES END