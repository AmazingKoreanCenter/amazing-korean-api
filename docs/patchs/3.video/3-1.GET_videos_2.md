# PATCH — Phase 3-1 Fix: Video ID Type Mismatch

ROLE: Senior Rust Backend AI Assistant

OBJECTIVE:
- `GET /videos` 호출 시 발생하는 500 DB Error 해결.
- 원인: PostgreSQL의 `video_id`는 `INT4`인데, Rust DTO는 `i64`를 기대함.

ACTION:
- `src/api/video/repo.rs`의 `find_open_videos` 함수 내 SQL 쿼리 수정.
- `v.video_id`를 조회할 때 **`v.video_id::bigint`**로 캐스팅하여 Rust의 `i64` 타입과 일치시킬 것.

FILE PATCHES START
// src/api/video/repo.rs
FILE PATCHES END