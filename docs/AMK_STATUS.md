# AMK_STATUS — 작업 현황 & 로드맵

> 완료 항목, 진행 예정, Paddle 전환, 보안 전략, 콘텐츠 개선.
> 공통 규칙(인증, 에러, 페이징): [AMK_API_MASTER.md §3](./AMK_API_MASTER.md)
> DB 스키마: [AMK_SCHEMA_PATCHED.md](./AMK_SCHEMA_PATCHED.md)
> 코드 패턴: [AMK_CODE_PATTERNS.md](./AMK_CODE_PATTERNS.md)
> 교재/출판: `amazing-korean-books` 프로젝트 참조

---

## 작업 현황

### 8.1 완료 항목 ✅

| # | 항목 | 카테고리 | 내역 | 완료일 | 향후 확장 |
|:-:|------|---------|------|:------:|----------|
| 1 | Admin 통계 API | 기능 | users/logins/studies/videos 통계 엔드포인트 + 프론트 UI | 2026-01 | 시스템 모니터링 (DB/Redis) |
| 2 | RBAC + Admin 감사 로그 | 보안 | role_guard, IP Allowlist, actor_user_id 전달, AdminRoute, 에러 페이지 | 2026-02-02 | manager class 기반 접근, 세분화 권한 |
| 3 | 코드 일관성 + 네이밍 | 코드 품질 | 함수명/URL 통일, Stateless 패턴, Refresh Token 포맷 등 8건 | 2026-02-02 | — |
| 4 | 내부 DB 작업 | 인프라 | Redis 보안(인증+포트), 시청 시간 추적, Study 레이트리밋, Course 도메인, 수강권 정책 | 2026-02-02 | — |
| 5 | Google OAuth | 외부 API | Authorization Code Flow, 프론트엔드 소셜 로그인 | 2026-02-03 | Apple OAuth (비용 보류) |
| 6 | Login/Login_log 개선 | 기능 | UA 서버파싱(woothee), 세션 컬럼 활성화, JWT jti, Geo 기본값 | 2026-02-05 | — |
| 7 | DB 암호화 (Phase 1~3) | 보안 | AES-256-GCM + HMAC Blind Index, 키 로테이션, 55+ call sites 적용 | 2026-02-08 | AWS KMS envelope → HSM |
| 8 | 프로덕션 배포 + 하드닝 | 인프라 | 통합 마이그레이션, Redis 보안, 보안 헤더, Swagger/Health 숨김, 404 Fallback | 2026-02-10 | — |
| 9 | 이메일 시스템 (Resend) | 외부 API | 회원가입 인증, 계정 복구, Rate Limiting, 관리자 초대, 도메인 검증 | 2026-02-09 | — |
| 10 | 다국어 (i18n) | 기능 | 21개 언어, 번역 CRUD API 7개, `?lang=` fallback, Noto Sans 동적 로딩 (Google Translate 해지 — 2026-03-24) | 2026-02-14 | — |
| 11 | 세션 보안 + MFA | 보안 | 역할별 TTL, 토큰 재사용 탐지 (409 Conflict), TOTP MFA + 백업 코드 10개, 강제 설정 가드 | 2026-02-14 | 동시 세션 제한, step-up MFA |
| 12 | 결제 시스템 (Paddle) | 외부 API | Webhook 9종, 구독 취소, 수강권 자동 부여/회수, 관리자 9개 API, Pricing UI (Paddle.js) | 2026-02-16 | Paddle Live 전환 |
| 13 | Design System v2/v3 | UI | 공유 컴포넌트 6개 (PaginationBar, EmptyState, SkeletonGrid, ListStatsBar, StatCard, Card CVA), 다크모드 (next-themes, CSS 변수 60+ 토큰, 22개 로케일), UI/UX 가이드라인 문서화 | 2026-02-19 | — |
| 14 | Paddle KYB 서류 제출 | 결제 | 사업자등록증 + 주주명세서 (한/영) Paddle Dashboard 업로드 | 2026-02-19 | Live 전환 심사 대기 (2~4 영업일) |
| 15 | CEO 영문 이름 통일 | 관리 | i18n 18개 로케일 `Kyoung Ryun KIM`, noscript `KIM KYEONGRYUN` (사업자등록증 기준) | 2026-02-19 | — |
| 16 | 교재 주문 시스템 | 기능 | 비회원 주문, 계좌이체, 20개 언어 × 2종, ₩25,000/권, 최소 10권, DB 4 ENUM + 3 테이블, 백엔드 8 API, 프론트 5 페이지, 견적서/주문확인서 인쇄, 약관 동의 모달, 상태 머신 검증, 이메일 알림 (주문확인/상태변경), Rate Limiting, Advisory Lock, Soft Delete | 2026-03-03 | 카드/Paddle 결제 |
| 17 | 교재 HTML 재구축 시스템 | 콘텐츠 | JSON(11) + JS 컴포넌트(10) + CSS(9) → HTML → PDF 자동 생성 파이프라인, 120페이지, 원본 99.2% 일치, Puppeteer CSS 전수 감사 | 2026-03-02 | 20개 언어 자동 생성 토대 |
| 18 | 교재 번역 Wave 1 | 콘텐츠 | ja, zh_cn, id, th — 923항목 × 4언어 번역, translate_extract/merge 도구, PDF 생성 완료 | 2026-03-02 | Wave 2~5 (16개 언어) |
| 19 | QR 교재 랜딩 페이지 | 프론트 | `/book/:isbn` — 교재 QR 스캔 → 서비스 연결. 10개 언어 × 2종(학생/교사) ISBN 20개, 3섹션 구조 (Hero+CTA, 서비스안내+다른언어, 하단CTA), PageMeta 동적 SEO, changeLanguage 자동 전환, 국기 SVG 10개 | 2026-03-20 | — |
| 21 | Coming Soon + 에러 페이지 개선 | 프론트 | 영상/학습/레슨 → ComingSoonPage (HeroSection + Feature 미리보기 + E-book CTA), 에러 페이지 RootLayout 통합 (Header/Footer 유지), i18n `comingSoon.*` 13키 추가 | 2026-03-23 | 콘텐츠 오픈 시 원래 컴포넌트 복원 |
| 20 | 홈/소개 페이지 문구 & UI 개선 | 프론트 | 플레이스홀더 숫자(1,000+ 영상, 50+ 강사, 10,000+ 수강생) → 실제 차별점(20개 언어, 500+ 핵심 문장, TOPIK 연계)으로 교체, Feature 3번 "1:1 수업" → "교재로 정리하기" (미구현→구현 기능), Trust Indicators text-gradient, Feature/Value 카드 호버 강화, 아이콘 교체 (Layers/Languages/GraduationCap/BookMarked), 문서 검증 후 미구현 기능 5개 수정 | 2026-03-23 | — |
| 22 | 교재 카탈로그 리디자인 | 프론트 | 표지 이미지 기반 카탈로그 페이지(`/textbook`), 주문 폼 분리(`/textbook/order`), 헤더 "교재" 메뉴 추가, 카탈로그→주문 URL 파라미터 연동, 주문 항목 표지 썸네일 | 2026-03-23 | — |
| 23 | 교재 카탈로그 캐러셀 뷰 | 프론트 | 그리드 ↔ 캐러셀 뷰 토글 (localStorage, 기본 carousel), Swiper v12 Coverflow 캐러셀 (국가 씰 SVG + Thumbs 동기화), 학생/교사 탭, 언어 검색 필터, 50/50 레이아웃 (씰 리스트 + 선택 교재 상세), ISBN 뱃지, 반응형 (터치 스와이프), 22개 로케일 i18n | 2026-03-25 | — |
| 24 | 교재 상세 모달 | 프론트 | 교재 클릭 → 상세 모달 (겉표지/속표지/목차 갤러리 + 설명 + 주문 버튼), 그리드/캐러셀 양쪽 뷰에서 재사용, 이미지 fallback 처리, 22개 로케일 i18n | 2026-03-24 | 속표지/목차 이미지 추가 |
| 25 | 구매이력 + 비회원 주문 차단 | 기능 | `textbook.user_id` 컬럼 추가 (마이그레이션), 주문 생성 인증 필수화 (`AuthUser`), `GET /textbook/my` 내 주문 목록 API, 마이페이지 "구매이력" 버튼, 주문 폼 사용자 정보 자동 채움, `/textbook/order` PrivateRoute 이동 | 2026-03-24 | — |
| 26 | 견적서/주문확인서 사용자 공개 | 프론트 | 사용자용 견적서/주문확인서 인쇄 페이지 (`/textbook/order/:code/print?type=quote\|confirmation`), 주문 상태 페이지에 견적서/주문확인서 버튼, 주문 완료 화면에 견적서 버튼, `window.print()` + PDF 저장 안내, 22개 로케일 i18n | 2026-03-24 | — |
| 27 | E-book 구매 완료 안내 페이지 | 프론트 | 구매 완료 전용 페이지 (`/ebook/purchase-complete`), 구매코드 표시+복사, 에디션/가격/결제수단 요약, 계좌이체 입금안내, `location.state` 데이터 전달 (직접 접근 시 fallback), 22개 로케일 i18n | 2026-03-24 | — |
| 28 | E-book 샘플 미리보기 | 프론트 | 카탈로그 언어 카드에 "미리보기" 버튼, 모달 (표지/목차/샘플1/샘플2 이미지 갤러리 + 가격/페이지수 + 구매 버튼), 정적 이미지 방식 (`/ebook-previews/{edition}/{language}/`), 이미지 fallback, 22개 로케일 i18n | 2026-03-24 | 이미지 업로드 |
| 29 | E-book 이메일 알림 | 기능 | 구매 접수(pending) 확인 이메일 + 결제 완료(completed) 열람 안내 이메일, Resend 패턴 (fire-and-forget), 관리자 상태 변경 + Paddle webhook 양쪽 대응, 암호화 이메일 복호화(CryptoService) | 2026-03-24 | — |
| 30 | E-book 환불 정책 | 법적 | 환불 정책 제5조 E-book 섹션 추가 (pending 즉시취소, 미열람 7일 환불, 열람 후 불가, Paddle 별도), 카탈로그에 환불 정책 링크, 22개 로케일 i18n | 2026-03-24 | — |
| 31 | E-book 모바일 최적화 | 프론트 | 터치 스와이프 페이지 네비게이션 (50px 수평 임계값), 모바일(768px 미만) spread 자동 비활성화+토글 숨김, 모바일 UI 축소 (페이지/줌 텍스트 숨김, 하단 바 compact, 슬라이더 flex 확장) | 2026-03-24 | — |
| 32 | E-book Paddle 결제 연동 | 기능 | 카탈로그 결제 방식 선택 (계좌이체/카드결제), usePaddle 훅의 openEbookCheckout() 연동, checkout.completed 이벤트 → 완료 페이지, custom_data { type: "ebook", purchase_code } 전달, 22개 로케일 i18n | 2026-03-24 | — |
| 33 | 교재 ISBN 발급 상태 표시 | 기능 | 카탈로그 API에 isbn_ready 필드 추가 (9개 발급 완료), 카드/캐러셀/모달에 미발급 시 "약 1주 추가 소요" 안내, 주문 페이지 ISBN 미발급 언어 선택 시 알림, tl 누락 수정 | 2026-03-24 | — |
| 34 | Book 허브 + 라우트 재구성 | 프론트 | `/textbook/*`→`/book/textbook/*`, `/ebook/*`→`/book/ebook/*` 전면 이동, `/book` 허브 랜딩 (교재 소개 + i18n 기반 표지 + 샘플 5장 + CTA), 기존 경로 리다이렉트, 헤더 nav.book, E-book 카탈로그 Tabs 통일, 22개 로케일 i18n | 2026-03-25 | 샘플 이미지 220장 생성 |
| 35 | 교재 그리드/상세 모달 개선 | 프론트 | 그리드 카드 제목 통일 + ISBN 뱃지→모달 이동 + 버튼 "상세보기", 상세 모달 좌우 스와이프 갤러리 + ISBN 뱃지 우측 배치, 가격 우측 정렬 | 2026-03-25 | — |
| 36 | 교재 캐러셀 모바일 최적화 | 프론트 | 모바일: 상단 Coverflow 숨김 + 하단 Thumbs만 표시, 교재 상세 세로 쌓기 (표지 위 + 설명 아래) | 2026-03-25 | — |
| 37 | E-book 카탈로그 출판본 패턴 적용 | 프론트 | E-book 카탈로그 전면 리라이트 (출판본 패턴 통일): 그리드 CoverCard + 캐러셀 SealList + 상세 모달 (좌우 스와이프), HeroSection/Tabs/검색/뷰토글, SealList SealItem 인터페이스 일반화, 표지 이미지 공유, 22개 로케일 i18n 14키 추가 | 2026-03-25 | 구매 섹션 최종 디자인 결정 대기 |
| 38 | Book 허브 갤러리 + 라이트박스 + 가격 통일 + UI 개선 | 풀스택 | 허브 6슬라이드 갤러리(키워드 태그+스펙 카드), 상세 모달 3→6이미지 확장, blur 라이트박스(createPortal+Radix Dialog 호환), 카탈로그 도서/E-book 전환 탭, E-book 백엔드 가격 단일화(15,000 KRW/$9.99), Zod 유효성 검증 i18n(auth+주문), 주문 안내 이동, /book/ 링크 수정, Google Translation API 잔여물 정리, 캐러셀 씰 링 제거+크기 조정, 카드 이미지 구분선, 모달 학생/교사 표기 숨김+간격 조정, E-book "곧 출판 예정" warning 뱃지, 뱃지 크기 통일, 22개 로케일 i18n 50+키 | 2026-03-26 | — |
| 39 | 교재 주문 안내 카드 UI 개선 | 프론트 | 주문 안내 4열 그리드, 30부 할인 카드(BadgePercent), 카드별 색상(blue/emerald/violet/amber), "무료 배송" 문구, whitespace-pre-line | 2026-03-27 | 20개 locale 미반영 |
| 40 | E-book 뷰어 보안 강화 | 보안 | CORS `x-ebook-viewer`/`x-ebook-session` 허용, session_id 비교, Content-Disposition/Referrer-Policy/Cache-Control `no-store`, Rate Limit TOCTOU 수정(3곳), 마이크로도트 y분산, Heartbeat Canvas 클리어, print CSS 강화 | 2026-03-28 | Error Boundary 보류 |
| 41 | Gemini 코드 리뷰 반영 | 코드 품질 | 마이그레이션 문서 HHMMSS 예시 모순 수정, embla-carousel-react 미사용 패키지 제거, queryClient 불필요 의존성 제거, TiledPageCanvas Promise.allSettled 부분 렌더링 | 2026-03-28 | — |
| 42 | E-book 저작권 보호 고지 | 보안/법적 | 뷰어 최초 진입 시 저작권법 제104조의2 고지 모달 (ShieldCheck + sessionStorage), 22개 locale 번역 5키 | 2026-03-29 | — |
| 43 | 워터마크 진위확인 API | 보안 | `GET /admin/ebook/verify/{watermark_id}` — 관리자 전용, ebook_access_log + ebook_purchase JOIN 조회 | 2026-03-29 | — |
| 44 | 이미지 AES-256-GCM 암호화 저장 | 보안 | `encrypt_bytes`/`decrypt_bytes` + `EBOOK_IMAGES_ENCRYPTED` 피처 플래그, 페이지+타일 복호화 로직, 테스트 25개 | 2026-03-29 | CLI 암호화 도구 별도 |
| 45 | DevTools 감지 | 보안 | `devtools_detect.ts` 신규, 창 크기+console getter 2초 폴링, 3초 유예 블러, DevTools 닫으면 복원 | 2026-03-29 | — |
| 46 | 요청별 HMAC 서명 | 보안 | 세션별 32바이트 secret, Web Crypto HMAC-SHA256, ±30초 타임스탬프 윈도우, 상수시간 비교, 페이지/타일 요청 서명 검증 | 2026-03-29 | — |
| 47 | 홈/소개 페이지 리디자인 | 프론트 | Hero 한 줄 타이틀, 핵심가치 3카드, 기능 4카드(준비중 뱃지), 소개 차별점 3카드 상세, Stats 카드(2블록), "30,000" 삭제, 22개 locale 전면 교체, HeroSection 다국어 정렬 | 2026-03-30 | 다국어 반응형 디자인 규격 |
| 48 | 앱 로드맵 + 문서 정비 | 문서 | AMK_APP_ROADMAP.md 신규(Flutter+Tauri, 리스크 11건), STATUS 순서 재정렬, 6개 문서 참조 갱신, archive 4건 삭제, 메모리 정비 | 2026-03-30 | — |
| 49 | 디자인 시스템 v4 (V1-1~V1-8) | UI | 토큰 정리(dead code 삭제+container/motion-reduce), max-w-[1350px]→토큰, sections/→blocks/ 리네이밍, AuthLayout 추출(6 인증 페이지), CoverCard+FeatureGrid 블록, SectionContainer 확대(4파일), lazy loading 전수, 문서 동기화 | 2026-03-31 | — |
| 50 | 디자인 시스템 v4 (V1-5 DataTable) | UI | DataTable+useDataTable 블록 추출(`blocks/data_table.tsx`), 관리자 3페이지(users/lessons/videos) 적용, 검색+정렬+선택+페이지네이션 공통화, 각 ~200줄 감소 | 2026-03-31 | — |
| 51 | 디자인 시스템 v4 (V1-9~V1-10) | UI | 색상 토큰 교체(7파일): status badge→status-warning/success, neutral-900→surface-inverted, coming-soon 배지 토큰화, text-white→surface-inverted-foreground, 장식용 8건 의도적 유지, 문서 최종 동기화 | 2026-03-31 | — |
| 52 | 모바일 UX 79건 수정 | UI | `@media (pointer: coarse)` 터치 타겟 44px(45건), 고정 그리드 반응형(5건), 모달 뷰포트 제한(3건), 타이포그래피 가독성(7건), 패딩/간격 반응형(13건), Dialog 닫기·라이트박스·모달 네비 확대(4건), header 스크롤 잠금+햄버거 확대(2건). §04 Mobile Checklist 완료 | 2026-04-01 | — |
| 53 | 모바일 백엔드 API 5건 | 백엔드 | `POST /auth/google-mobile` (Google ID token 직접 검증), `POST /auth/apple-mobile` (Apple JWKS RS256, email 부재 처리), `POST /auth/mfa/login-mobile` (MFA JSON body), `POST /ebook/purchase/iap` (RevenueCat 영수증 검증, status=completed), `POST /payment/webhook/revenuecat` (Bearer 인증, 멱등성). OAuthUserInfo 일반화, `src/external/apple.rs`·`revenuecat.rs` 신규, DB enum 확장, 환경변수 5개 추가 | 2026-04-07 | 개발자 계정 등록 후 실 테스트 |
| 54 | 동시 세션 수 제한 | 보안 | 역할별 최대 세션 수 (HYMN:2, Admin:2, Manager:3, Learner:5). `enforce_session_limit()` — 유령 세션 정리(SMEMBERS+EXISTS) + SCARD 카운트 + 정책 분기. Learner: FIFO 자동 퇴장(가장 오래된 세션 강제 로그아웃). Admin/Manager/HYMN: 로그인 거부(403). `login()` + `create_oauth_session()` 2곳 적용. 환경변수 4개 | 2026-04-08 | — |
| 55 | 디자인 시스템 v4.2 보강 (전수 조사 19건) | UI/문서 | §00 Visual Theme & Atmosphere 신규, §01 Color Tokens HSL+Hex 병기 전면 재구성, §01 Shadow/Radius/Typography 정확값 명시, §03 CTA 패턴 3변형, §04 Responsive Behavior 신규, §05 Do/Don't 통합, §07-B Agent Prompt Guide 신규. 코드 수정 5건: index.css `--warning-foreground` WCAG AA, ebook_viewer 토큰 교체 15건, pagination_bar `rounded-md`, about_page CTA `hover:shadow-xl` | 2026-04-09 | — |
| 57 | 속도 개선 Phase S1+S2: 측정 인프라 + Quick Win 번들 분할 | 성능 | `frontend/perf-audit/` 신규 — Lighthouse 기반 자동 측정 (8페이지, Playwright Chromium 동적 탐색). `vite.config.ts` manualChunks — vendor 11종 분리. `routes.tsx` React.lazy — Admin 30+, Auth 보조 5, Legal 3, Textbook/Ebook 후속 5, Error 3, ComingSoon/FAQ 총 50+ 페이지 lazy 전환. **메인 번들 1,620KB → 271KB (-83.3%)**, home Performance 48→72(+24), TBT 2572→315ms(-88%). npm audit 3건 동시 패치(axios 1.15.0 + vite 7.3.2 + basic-ftp override). 코드 품질 13건 수정. 목표 Lighthouse 90+ (현재 48~78) | 2026-04-10 | Phase S3: faq/book-hub LCP 12~17s 조사 + Font preload + 이미지 최적화 + K6 백엔드 |
| 58 | 속도 개선 Phase S3: LCP 수정 + Font preload + 이미지 최적화 + K6 + 프로덕션 측정 | 성능 | **S3-1**: book-hub LCP 이미지 `loading="lazy"` → `eager` + `fetchPriority="high"` (초기 슬라이드만). **S3-2**: Pretendard CSS `preload` 힌트 추가, Noto Color Emoji 비동기 로딩 전환 (`media="print" onload`). **S3-3**: 로고 PNG 6000×4000 1.7MB → 1200×800 52KB(-97%), favicon 분리(32px+192px), 인증서 PNG→WebP(256KB→40KB, 100KB→28KB). **S3-4**: `k6/` 디렉터리 신규 — config.js(목표치), scenario_smoke.js(VU=1), scenario_load.js(10→100 VU ramp-up). K6 v0.56.0 설치. **S3-5**: 프로덕션 Lighthouse 베이스라인 — home Perf 52, about 34, faq 26(LCP 14.1s), book-hub 33(LCP 18.6s), login 35. 배포 후 재측정 필요 | 2026-04-12 | 배포 후 프로덕션 재측정으로 효과 확인 |
| 60 | 속도 개선 Phase S4 + React 19 StrictMode 전수 점검 | 성능/품질 | **S4-1**: `frontend/perf-audit/audit.mjs` `PERF_TARGET_URL` env 오버라이드 추가 + `userDataDir` mkdir 선생성 버그 수정. 프로덕션(https://amazingkorean.net) 8페이지 × 3회 Lighthouse → 중앙값 집계. S3-5 대비 전 페이지 Performance **+20~+35**, `about` TBT **13500→199ms (-98.5%)**, `faq` LCP **14.1→5.0s**, `book-hub` LCP **18.6→9.3s**, `home` TBT 810→209ms. S3 배포 효과 수치로 확인. **S4-2**: BP=82 원인 = Cloudflare `cdn-cgi/challenge-platform/scripts/jsd/main.js` 의 deprecated API 3종 (`SharedStorage`/`StorageType.persistent`/`Fledge`). CF 가 첫 페이지(home)에만 challenge JS 주입 → 3회 모두 home BP=82, 나머지 7페이지 BP=100. 측정 순서 아티팩트이자 우리 코드 아님 → 조치 불가/불필요. **S4-3**: about TBT 13.5s 는 S4-1 재측정에서 199ms 로 자동 해결 (S3 vendor 청크 분리 + 폰트 preload 효과). 별도 trace 조사 생략. **StrictMode Track A**: `useRef(false)`+`useEffect([])` 가드 4건 (`use_oauth_callback`/`use_paddle`/`use_language_sync`/`devtools_detect`) triage → 전부 `useMutation` observer 미사용, 무해. **StrictMode Track B**: `useEffect` 블록 내 `.mutate(` 호출 12개 후보 파일 전수 검토 → 모든 호출이 onClick/onSubmit/이벤트 콜백, `useEffect` 본문 동기 호출 **0건**. WritingTask 가 코드베이스 유일한 사례로 확정. 메모리(`project_perf_plan`/`project_strictmode_audit_plan`/`feedback_react_strictmode_mutation`) 동기화. S5(가칭)는 Critical CSS inline + 이미지 srcset 으로 Perf 90+ 도달 목표 | 2026-04-14 | S5 별도 스코프로 착수 |
| 162 | guide PR-1 — 마이그 + 시드 변환기 + seed_guide 로더 구현 ✅ (06-13 런타임 검증 완료 + Gemini 리뷰 5건 반영) | 콘텐츠 | **(06-13 갱신) 런타임 fresh DB 검증 전부 통과**: amk-pg에 `amk_guide_verify` 생성 → CI 동일 경로(psql lexicographic, G16 우회)로 **전체 마이그 체인** 적용(주의: `sqlx migrate run`은 fresh DB에서 G16으로 fail = 알려진 이슈, CI 경로가 정본) → 시딩 67\|16,375\|500 + zh_cn/id 각 11,797(찌꺼기 제거 정확히 2건: zh 18_002·id 24_234, 한글 토큰 복원 확인) → 검증 쿼리(placeholder 잔존 0·전부 approved·source_version 1·문장 오버레이 500 전부 section 정합·span 518+29=547·테마 10색/카테고리 4 분포 일치) → 멱등 재실행 불변 → **재실행 가드 거부 동작**(편집 흔적 1건 → 거부) → ko/en 참조본 거부(enum 게이트) → **CASCADE 전파**(문장 삭제→로그/상태 연쇄, 단원 삭제→블록 279·문장 10 정확 전파). **Gemini 리뷰 5건 반영**(마이그 미적용 상태라 파일 직접 수정): log/status FK `ON DELETE CASCADE`(HIGH 2) + FK 단독 인덱스 2 + `strip_suffix` 교체. **(원기록 06-12)** — #161 설계대로 구현. **마이그 2건**: `20260612`(content_type `guide_block` 단독 — 'guide' 값은 미사용이라 미추가, 설계 정정) + `20260613`(guide/guide_block/guide_sentence/guide_sentence_log/guide_sentence_status + enum 6종 + content_translations.source_version). **변환기** `scripts/build_guide_seed.py`(.txt 67 전 세그먼트 + HTML 병합메타 + sentences.json 테마 + guide_sections.json 카테고리 → guide_seed.json): **67 guide·16,375 block·500 sentence**, 문장 1~500 완전커버, 납품 참조본 교차검증 **en/ko 11,797 전부 키존재·텍스트차이 0**, 문장 KO↔HTML data-ans 드리프트 **0**, 표 구조정합 100%(span 547 부여)·텍스트차이 404=HTML(6/6 빌드)이 의미감사(6/11) 이전인 예상 stale. **실측 변칙 흡수 6**: LIST_ITEM 13번째 타입(스펙 12종 표본누락, enum 추가)/TITLE 부재 3단원/문장 61·71 PARAGRAPH 타이핑→정규화/파일명 드리프트 1(366~369). **로더** `src/bin/seed_guide.rs`: --input(구조)·--translations(meta.target_lang, {{koN}} 복원, 트레일링 `},` 결함규칙, status=approved, ko/en 거부) + **재실행 가드**(편집 흔적 시 거부, D-0) + 단일 tx fail-loud. Cargo [[bin]]+Dockerfile 3곳. 검증: SQLX_OFFLINE cargo check/clippy(-D)/fmt ✓. ~~🟡 런타임 DB 검증 미수행(06-12 환경 블로커)~~ → **06-13 해소(위 갱신)**. 시드 4종+산출물 gitignore(커밋 금지), prod 전달=scp(DEPLOY_OPS §12-A 런북 신설). | 2026-06-12 | PR #326 머지 → prod 시딩(§12-A) → PR-2(서빙+뷰어) |
| 161 | 온라인 콘텐츠 guide 도메인 설계 확정 + zh_cn/id 번역 납품본 검증 통과 | 콘텐츠 | **2026-06-12** — 해설집 번역(zh_cn+id, 맥미니 첫 생산물) 도착 계기로 온라인 콘텐츠 트랙 본격화. **조사 2회(워크플로 13 agent)**: ① 적재 경로 부재 확정(구 --translations 계약 deprecated·키 체계 불일치·DB 미결) ② 해설집 HTML 67단원 전수 분석(공통 스켈레톤: 문법 도입→문장 사이클[어휘→쓰기시도→정답+TTS→해설표]→복습 4종, 한 문장이 6곳 재사용=정규화 근거) + 학습 UX 연구 §7.5~7.7 매핑 + 기존 자산 평가 + 세그먼트↔HTML 대응(복잡표 병합 메타 .txt 부재 — HTML 1회 추출 필요). **사용자 결정 8건 확정**(D-0~D-7): DB=편집 SoT(books 핸드오프 종료)·기존 자산 전면 교체(네이밍 준수·간결유연)·어휘 출현단위·완전일치 채점·zh/id 먼저·인쇄 별개·브라우저 TTS·표 재조립=api. **스키마 설계 확정**: guide/guide_block/guide_sentence/guide_sentence_log/guide_sentence_status 5테이블 + enum 4 + content_translations 확장(content_type 2값+source_version) — "모든 것은 블록, 문장만 학습항목 승격" 원리. **납품본 4종 검증 통과**(`seeds/0~3_*.json`, gitignored): 4×11,797건, id·source_text·ko_preserve=v6 완전일치, {{koN}} 1,405 보존, 한글혼입 0, 결함 2건(트레일링 `},`)=로더 제거 규칙 처리. SoT=`docs/AMK_GUIDE_CONTENT_DESIGN.md`, 메모리=`project_guide_content_track`. | 2026-06-12 | PR-1(마이그+시딩)→PR-2(서빙+뷰어)→PR-3(admin 편집+stale 추적)→PR-4(학습로그+구 자산 정리). 공개 flip=사용자 트리거 |
| 160 | 조합-결함 심층 감사 (인증·세션 / 결제 웹훅) — **발견·기록만, 수정 미착수** | 보안 | **2026-06-10** — Workflow 멀티에이전트(87 agent) 경로 단위 조합-결함 사냥: 11 흐름 finder → 가설별 3-렌즈 적대적 검증(반증 우선) → 5필드 게이트. 25 가설 → 16 생존 → **확정 10건(HIGH 7·MEDIUM 3)**. **인증·세션 4**: A-1 OAuth/MFA `create_oauth_session`에 user_state 게이트 부재→**정지 계정 세션 재발급(ban 우회)**(HIGH·정적확정) / A-2 refresh 회전이 `ak:session` TTL 미갱신→access-TTL 후 **거짓401 좀비세션**(HIGH, finder가 incident "G4 API-dead"로 교차참조=확인필요) / A-3 reset/resend anti-enum 타이밍 오라클(M) / A-4 MFA pending 1회용×5회 rate-limit 충돌(M·가용성). **결제 6**: P-1 웹훅 멱등 read-then-write 갭+UNIQUE 부재→transaction 이중INSERT(HIGH) / P-2 구독 FSM 순서·동시성 가드 부재→취소구독 좀비부활(HIGH) / P-3 구독 환불이 수강권 미회수(HIGH) / P-4 RevenueCat 환불 arm stub→IAP e-book 영구열람(HIGH) / P-5 부분환불=전액환불 처리(HIGH) / P-6 admin 결제목록 행단위 hard decrypt→1행 손상에 페이지 500(M). **적대적 검증 반증 1**(pending 재사용=TOTP는 POST바디라 미성립) + **busywork 폐기 1**(orphan ak:refresh=TTL 자연만료·인증우회 아님). 양호 확인 8. 정적확정=A-1·A-4, 나머지 8=메커니즘 확정·영향규모 런타임 확인필요(prod `JWT_ACCESS_TTL_MIN`·Paddle/RevenueCat 환불이벤트 발송행태·IAP 라이브). SoT=`docs/AMK_AUDIT_2026-06-10_COMBINATION_DEFECTS.md`, 메모리=`audit_combination_defects_2026-06-10`. | 2026-06-10 | 수정 착수 여부·우선순위·모델 별도 결정 / 선행=A-2 incident G4 교차참조 확인 + 런타임 사실 확인 |
| 159 | 주간 Security Audit 실패 대응 (npm advisory 패치 + cargo-deny 신규 advisory ignore) | 보안 | **2026-06-09** — 매주 월 Security Audit(스케줄)이 main(`b48191d`) 양쪽 잡 실패. **코드 변경 아님 = 신규 advisory 게시로 인한 시한폭탄형**(기존 main에 새로 터짐). **npm**: react-router-dom 7.14.0(HIGH — turbo-stream RCE·open redirect·DoS) + vitest 3.2.4(CRITICAL — @vitest/ui, devDep·UI서버시만·prod무관) + ws·brace(moderate) → `npm audit fix`(비파괴) **7건 전부 → 0 vuln**(react-router-dom→7.17.0, vitest→3.2.6). 검증 `npm run build`✓ + `vitest run` **253/253**✓. **cargo-deny**: RUSTSEC-2026-0173 `proc-macro-error2` unmaintained(transitive validator 0.20→derive, **빌드타임 proc-macro·런타임 미포함·informational·업그레이드 경로 없음**=validator 0.20 최신) → `deny.toml` ignore(`paste` 2024-0436 동일클래스). 검증 `cargo deny check`→advisories/bans/licenses/sources **ok**. deny.toml 정책상 ignore=사용자 결정. | 2026-06-09 | 커밋·푸시 후 다음 주 Security Audit 녹색 확인 |
| 158 | E-book 36언어 웹 라이브 인프라 배선 (compose mount + 폰트 + TTL 교정) | 콘텐츠/배포 | **2026-06-04** — books 36언어 e-book 빌드 완료(트리거) → prod 라이브 인프라 배선. **정합 전수 검증**: books `dist/ebook_pages/`(711MB·webp 8,928·manifest 72=36 lang×2 edition) ↔ api `catalog_languages()` 36 **1:1 일치**, `es_es`/`pt_pt` 디렉터리 ↔ `to_code()` 일치, `page-{:03}.webp` 네이밍 일치, 평문 webp ↔ `ebook_images_encrypted=false` 기본값 일치, manifest `total_pages`/`toc` 구조 일치. **배선 변경**: ① `docker-compose.prod.yml` api 에 `volumes: ./ebook-pages:/data/ebook-pages:ro` + `EBOOK_PAGE_IMAGES_DIR: ${...:-/data/ebook-pages}`(기존 죽은 env 활성화) ② `deploy.yml` ebook-pages mkdir+chown(db-init 패턴) + 폰트 `NotoSans-Regular.ttf` scp(레포 `ebook-pages/` 커밋, 569KB) + `.env.prod` 경로 `docs/textbook/page-images`→`/data/ebook-pages` 교정. **드리프트 1건 교정**: `EBOOK_SESSION_TTL_SEC` compose `:-90` → `:-300`(config.rs·.env.example 과 일치, 모바일 표준). **폰트 갭 발견·해소**: books 산출물·레포·이미지 어디에도 `NotoSans-Regular.ttf` 없어 가시 풋터 워터마크가 silent 비활성(LSB+마이크로도트 포렌식은 생존)이던 것 → 레포 추가 + deploy 멱등 scp. 검증=`docker compose config`(volume bind/ro·env default `/data/ebook-pages`·TTL 300 렌더 확인) + 폰트 TTF 유효성. src/·frontend 무변경. **남은 1단계(사용자)**: books `dist/ebook_pages/` → EC2 `~/amazing-korean-api/ebook-pages/` rsync(`--delete` 금지) → 도착 즉시 catalog `available=true`(재배포 불요). SoT=`AMK_API_EBOOK.md "페이지 이미지 저장 위치 정책"`, 메모리=`project_ebook_webp_upload`. | 2026-06-04 | PR 머지·배포 후 사용자 rsync → 뷰어 end-to-end(실구매·워터마크) 검증 / RDS 이전 시 S3 전환(Q9) |
| 157 | 온라인 콘텐츠 세그먼트 포맷 스펙 (번역·DB 적재 계약) 🟡 DRAFT/잠정 | 콘텐츠 | **2026-06-04** — 온라인 콘텐츠 트랙 착수. 두 트랙(콘텐츠/e-book) 실측 감사 → 둘 다 완성도 높음(콘텐츠: study/4유형 채점·프론트 대부분 / e-book: 웹 production-ready, 실블로커=WebP 미업로드). 우선순위=병행·콘텐츠 풀스코프·e-book 웹먼저. **explanation 컨텍스트 통합 1차**: 프론트 `category/explanation/` 모듈+study 상세 pattern_guide 접이식(prod 실데이터 대조+build/lint ✓, 런타임검증 로컬DB stale로 보류). **핵심**: 콘텐츠 원본=`books/해설집/*.txt`(33파일) 세그먼트가 **SSoT**. 전수집계 **타입 12종**(TITLE·PARAGRAPH·PATTERN·SECTION·VOCAB·UI·NOTE·BLOCKQUOTE·OTHER·TABLE_HEADER·TABLE_CELL·PRACTICE)+**마커 6종**(`(empty)`/`(symbol_only)`/`(ko_learning)`류 불역/`(table_content_en)` 번역, 마커가 셀별 실내용쪽+번역여부 명시). 표 재조립 규칙(`Tn_Rr_Cc`→격자) 설계. **확정**: ①모든항목 번역(역할 포함) ②표 재조립 로직 필수 ③**콘텐츠 확정 시 DB 대폭/전면 재설계 가능→현 explanation/study+프론트 모듈 전부 잠정** ④UI/PRACTICE 라벨=메타 추후. SoT=`docs/AMK_CONTENT_SEGMENT_SPEC.md`, 메모리=`project_content_segment_spec`. **+학습 UX 증거기반 설계원칙(딥리서치 06-04, §7.5)**: 청킹·시도먼저·분산학습 ✅유지 / 4조정(의미경계 강제분할·새개념은 예제먼저+복습은 시도먼저·블록→인터리빙·"2~3분"근거없음). 흐름=예제도입→페이딩→문장회상→인터리빙→분산복습. 미답=선도앱 실제흐름·한국어특화UX·게이미피케이션(제품분석 별도). 메모리=`project_content_ux_research`. | 2026-06-04 | DB/적재=콘텐츠 확정 대기 / 미답 UX(Q1·Q3·Q5)=별도 제품분석 라운드 / 다음=e-book 세부 논의 |
| 156 | 관리자 세션 보안 모델 v2 (Phase 2) — 구현+회귀테스트+**prod 실측 검증 완료** ✅ (단일탭/evict/max1/1h TTL) | 운영/보안 | **2026-06-02** — #155(Phase 1, PR #317 prod 안착)로 동시세션 카운팅이 정확해지자 기존 **reject 정책이 관리자를 자기 세션에 락아웃**시키는 결함 노출(MFA 통과한 본인을 본인이 못 쓰는 옛 세션이 막음). git 추적: reject는 **사용자 미지시·근거문서 0의 미문서화 AI 기본값**(커밋 `0d85b19` 2026-04-08 Claude Opus 4.6). 부수 코드확정: "재오픈/몇시간뒤 로그아웃"의 진짜 원인 = 멀티탭 아니라 **`frontend/src/api/client.ts:42-89` refresh가 single-flight 아님**(동시 401→동시 `/auth/refresh`→reuse 감지→compromised→로그아웃, 단일탭만으로 발생; prod compromised **누적 24·발생률 1~2일당 1건·Phase 1 후 ~0** — 06-02 실측 정정, 종전 "24건/일"=`AI_MISTAKES M-014` 과장). **확정 설계(HYMN/Admin/Manager, Learner 현행유지)**: 최대1·evict·refresh TTL 1h sliding·동시1(advisory lock을 evict 경로에)·요청별 ak:session 즉시강퇴 + 프론트 single-flight refresh·관리자 단일탭(2번째 탭 차단). **§4.1 선결 확정**: `refresh_ttl_days_for_role` → `refresh_ttl_secs_for_role`(초 반환) 일반화 + 신규 `REFRESH_TTL_SECS_ADMIN`(기본 3600), Learner는 days×86400 유지. 결정 2건: reject 경로=유지(dormant)·ak:session 즉시강퇴=관리자 라우트 한정. **PR 분할(2회 prod 배포)**: **PR #1 = 프론트 single-flight refresh(`client.ts`, `refreshPromise` 게이트) — 구현·`npm build` 통과, 분리 선배포(저위험·BE무관)**. **PR #2 = BE 세션모델(config·evict·advisory lock·ak:session) + 프론트 단일탭 차단**. 설계·근거 SoT=`docs/AMK_ADMIN_SESSION_MODEL_V2.md`, 메모리=`project_admin_session_model_v2`. **PR #1 ✅ prod 라이브**(#318 main `ea9bf59`, Cloudflare Pages success). **PR #2 구현·검증 완료**: ①②③ config(`refresh_ttl_secs_for_role` 초·`REFRESH_TTL_SECS_ADMIN=3600`·`MAX_SESSIONS_*=1`·`is_session_evict_role` 전역 true, dead 필드 제거) / ④ 원자 evict(**Learner 경로 무변경**, HYMN/Admin/Manager만 `enforce_admission_in_tx` advisory lock 안 `evict_oldest_sessions_tx` in-tx UPDATE…RETURNING + commit 후 `cleanup_evicted_sessions_redis`) / ⑦ FE `useAdminSingleTab`(BroadcastChannel)+AdminLayout 차단. **🔎 구현 중 발견**: #5(ak:session 즉시강퇴)는 **audit 2.1(`[x] 2026-05-17`)로 이미 전역 구현** → 무코드(설계 §3.1 #5 stale 정정). 검증: cargo check/clippy(-D)/fmt + 통합 3/3(신규 evict 실DB) + npm build/eslint/lint:ui. env 동기화(docker-compose.prod.yml·.env.example·DEPLOY_OPS·API_MASTER §동시세션 표). **PR #2 ✅ 머지·deploy·prod 스모크**(#319 main `74634fd`·health 200·CF success). **PR #3 핵심 동작 회귀 테스트**(#320 main `18ed243`, 코드 무변경): BE `admin_over_limit_login_evicts_instead_of_rejecting`(login 전체 경로 evict≠403, config 무관) + FE single-flight(동시 401→refresh 1회) + FE `useAdminSingleTab`. **적대적 검증 2명**(테스트 validity / 백엔드 correctness=NONE). **🔑 정직**: 동시성(lock) 테스트는 argon2 staggering 으로 vacuous(lock 제거해도 통과) → 미채택·NOTE 기록 / **CI frontend 잡 vitest 미실행** → FE 테스트 로컬만(253/253) / 단일탭 mock-bound / 사용자 질문("테스트 다 했나")으로 **vitest 미실행 공백 발견·회복**. 통합 4/4 실DB. **🟢 prod 실측 검증 완료(06-02, 운영자 직접)**: ✅ 단일탭 차단(스샷) / ✅ evict·last-login-wins(`login 84`가 `83` evict·기존기기 로그아웃) / ✅ max=1(현재 admin active=1) / ✅ 1h sliding(`expire−begin=1h`) / ✅ env(`printenv` max1·3600) / ✅ compromised 깨끗(A-1). 관찰: 찰나 active=2→evict로 self-heal to 1. **정정 2(정직)**: "24건/일"=누적값 확대 과장(M-014) / active=2 원인 printenv 전 단정 오류(M-015). | 2026-06-02 | ✅ **구현+테스트+prod 실측 완료**. 잔여 = 동시성·idle만료 등 행동지표 비자동(SoT §7, 1인 관리자 실무 위험 0) |
| 155 | MFA 세션 limit 사건 — 즉시 조치 + Phase 1 영구 패치 머지·배포·prod검증 완료 ✅ | 운영/보안 | **2026-05-30** — 사용자(Admin) Google OAuth → MFA 6자리 입력 시 `AUTH_403_SESSION_LIMIT:2`+`MFA_TOKEN_EXPIRED` 반복. 진단: prod amk-redis 정상(3주 uptime, evicted=0, maxmemory_policy=noeviction, dbsize 15), api/EC2 정상(api 10일·EC2 123일 uptime), 서버 시계 0초 정합 → 코드/배포 변경 0건(마지막 배포 5/19 #316 doc-only) → **런타임 상태 누적이 원인**. 메커니즘 확정: `enforce_session_limit`(`service.rs:198`) 이 `ak:refresh:{hash}` 살아있으면 1c에서 ghost 판정 실패 → 활성 카운트 포함. 사건시점 `ak:refresh:*` **8개 누적** → active_count=8 ≫ max_sessions_admin=2 → 403 거부. **즉시 조치**: prod Redis `ak:refresh:*` 8개 수동 DEL → 정상 복구 검증(ak:session=1·ak:refresh=1·DB login_state active=1·TTL 정책치). **재발 위험 🔴 7일내 보장**(7일 refresh TTL=누적 동일 메커니즘). DB login 분포: compromised=24·logged_out=17·expired=6·active=1 → 매일 reuse detection 발동 패턴(클라이언트 다중탭/캐시 추정). **누적 정확 경로**(왜 자동 정리가 안 됐는지)는 미스터리·다음 세션 1단계 조사. **영구 패치 단일 SoT 신규**: `docs/AMK_INCIDENT_2026-05-30_MFA_REFRESH_REUSE.md`(원인/현상/결과/패치계획 분류). 패치 후보: A(카운팅 SoT를 DB로 통일, 권장1순위) / B(유령 cleanup 강화) / C(정기 background 정리). 1단계 메커니즘 규명 후 결정. SSoT=메모리 `project_mfa_session_limit_patch`. **(2026-05-30 후속) 영구 패치 구현 완료**: 코드 트레이싱 + 전수 정합 감사(워크플로) + prod 실측(DB user_1 active=0인데 SCARD=8 발산 = 순수 Redis↔DB 발산 확정) → 6축 통합 패치(A+C DB권위카운트+`pg_advisory_xact_lock` 원자 admission·B ghost강등·D 시간기반 reaper·E reuse fail-closed·F ban 무효화). cargo check/clippy/fmt + npm + 회귀테스트 2건(phantom 제외·reaper) 통과. 마이그 0(컬럼·인덱스 기존재). 설계·구현 SoT=`INCIDENT 문서 §6~§8`. **(완결) PR #317 squash 머지(main `fa1ae2e`)·Deploy to EC2 success·무중단(health 200)·KKRYOUN reset(divergence 0)·prod 스모크 PASS**(막혔던 Admin admit / reaper 유령3 ttl_reaped / `1=1=1` 정합 / 거짓403·panic 0). **후속 = #156 Phase 2.** | 2026-05-30 | ✅ 완결 → #156(관리자 세션 v2)로 이어짐 |
| 154 | 스키마 명명 트랙 — 2단계 ①②③ prod 안착 + AUD-1 안착 + KKRYOUN 충돌 근본해소 / 그룹 ④ 분석완료·R1 다음세션 | 스키마/부채 | **2026-05-19 — AUD-1 prod 안착 + KKRYOUN 충돌 근본해소 + 그룹 ④ 분석**: ① AUD-1 PR #315 머지·`Deploy to EC2` success·prod `/health`·리네임 테이블 스모크 200 = prod 안착. ② **KKRYOUN 충돌 근본원인 규명**: GitHub squash 머지(main `(#NNN)` 단일 SHA) + KKRYOUN 머지 후 미리셋 → 71커밋 분기 누적 → 매 PR CHANGELOG/STATUS 충돌 재발. **영구해소**: 머지 후 즉시 `git reset --hard origin/main`+force-push 규칙(`feedback_git_branching` 메모리) + `settings.local.json` 권한 2개(force-with-lease/reset 자동화) 정착. KKRYOUN=origin/main 분기 0 확정. ③ **그룹 ④ 정밀 분석**(AUDIT §11): 이중모델 실측(study_explain 0행·학습자 해설 0 / explanation_* 568·1317·ct 4362·소비처 0). **R3 확정=R3-a**(/explanations→/study-contents, 클라이언트 소비 전무 실측 → 계약파손 0). **R1(A2 순수리네임 / A3 통합후리네임 / A1 비권장) = 다음 세션 결정**(사용자 "내일"). 사고: M-013(손패치 DB 신뢰·fmt 누락 CI적발). **(이전: AUD-1 정정/검증패스/그룹①②③)** **2026-05-19 — AUD-1 ✅ 해결**: `rekey_encryption.rs:80 pk:"oauth_id"`→`"user_oauth_id"`(실제 PK, rekey SQL 직접 삽입 컬럼). 4 RekeyTarget pk 전수 live 실측=형제 버그 0 + 정정쿼리 유효 + fmt/`cargo check --locked` 0. AUDIT §6 AUD-1 마킹. **(이전: PR #314 CI 적발 결함 2건 정정)** **2026-05-19 — PR #314 CI 적발 결함 2건 정정**: 사용자 머지 시도 → CI backend/integration/Playwright FAIL. ③마이그 제약 RENAME 에 손패치 로컬에만 있던 중복 FK `user_export_data_user_id_fkey1`(clean/prod 부재) 포함 → fresh DB 마이그 실패 ④`cargo fmt --check` FAIL(M-008 재발). 정정=마이그 3개 **존재 가드 재작성**(DO+pg_constraint IF EXISTS/ALTER INDEX IF EXISTS, 환경독립)+`cargo fmt --all`. 진짜경로 재현(fresh DB CI동일 psql 루프=무에러·`_fkey1` skip·8테이블)+backend(fmt/check/clippy) 0. main #313 머지(STATUS/CHANGELOG 충돌만, 코드 0, HEAD 상위호환 채택). 사고=`AMK_AI_MISTAKES M-013`(스키마명 SoT=마이그 원본≠런타임 DB), 상세=`§10.4`. **(이전: 커밋 전 검증 패스)** **2026-05-19 — 커밋 전 검증 패스(사용자 요구, 추측 배제·실측)**: 결함 **2건 적발·정정** — ①마이그 3파일 버전 전부 `20260519` 중복(sqlx PK 충돌=CI/prod 마이그 실패 유발) → `20260519/20520/20521` 정정(README §1.3) ②`AMK_APP_ROADMAP.md:134` 구명 `subscriptions` 잔존 → 정정+전 docs 재sweep 0. 결정론 게이트: 코드 diff 45/45 대칭·DB FK/뷰/트리거 무결·AAD 6 불변·전역 grep 0·fresh DB `sqlx migrate run`(우리 3 버전검증 통과=수정 유효 / 실패=사전존재 G16 부채, 우리 무관·prod 무영향). 잔여=명명된 3(prod checksum 등록·런타임쿼리 통합테스트·prod 스모크)=기존 배포 게이트로 닫힘. 사고=`AMK_AI_MISTAKES M-012`, 상세=`AMK_SCHEMA_NAMING_AUDIT §10`. **(그룹 ③, 2026-05-19)**: **2단계 그룹 ③ payment→payment_* 완료(사용자 체크포인트 통과)**: 마이그 `20260521_rename_payment_tables.sql` — `subscriptions`→`payment_subscription`/`transactions`→`payment_transaction`/`webhook_events`→`payment_webhook_event` + 인덱스 7(`idx_subscriptions_user_status`=20260504 로컬 비-head라 `ALTER INDEX IF EXISTS`, prod 적용분 정상)·제약 6 RENAME. 코드 20곳: payment/repo.rs 9 + admin/payment/repo.rs 8 + tests/payment_integration.rs raw SQL 3. `unique_provider_subscription`/`unique_provider_event`=토큰 미포함 미변경. 라우트/JSON키(API_PAYMENT)=계약 스코프 외 미변경. 검증: 로컬 마이그 + 잔여 0 + `cargo check --all-targets` 0(**컴파일타임 매크로가 리네임 라이브DB 검증=고위험군 강검증**) + `sqlx prepare` .sqlx 누적 14 재생성 + payment SQL p1~p4 실DB 정합(**①×③ 교차 JOIN `users_course`×`payment_subscription` 포함**) + SCHEMA_PATCHED/MASTER 동기화. **잔여**: 3 마이그(`20260519`/`20260520`/`20260521` — 동일날짜 복수=README §1.3 연속날짜 정책 적용, 버전중복 결함 검증서 적발·정정) 로컬 검증용 수동적용만(prod/CI 미적용), .sqlx 14 + 코드 + 마이그 + 문서 = **커밋·PR 미실행**(그룹별 PR or 일괄 = 사용자 결정). 앱 통합테스트 env 패닉 이연(불변). **다음 = 커밋/PR 전략 결정 → 그룹 ④(explanation→study_contents, study_explain·/explanations 결정) 별도**. **(그룹 ②, 2026-05-19)**: 마이그 `20260520_rename_writing_practice_to_study.sql`(테이블 2 + 인덱스 1 + 제약 3 RENAME, 메타데이터). 코드: study/repo.rs SQL 식별자 9곳(7 `query!` 컴파일타임 매크로 + 2 QueryBuilder 런타임). 함수/핸들러/라우트명=기능명(테이블 아님) 스코프 외 미변경. 약어 객체(idx_wps_*/fk_wps_*)=토큰 미포함 미변경. 검증: 로컬 마이그 적용 + 잔여 0 + `cargo check --all-targets` 0(**7 매크로가 리네임 라이브DB 검증=강검증**) + `sqlx prepare` .sqlx 7건 재생성(커밋 대상) + repo SQL w1~w3 실DB 정합 + SCHEMA_PATCHED/LEARNING 동기화. **다음 ③ payment(8 웹훅 고위험) = 착수 전 사용자 체크포인트**. **(그룹 ①, 2026-05-19)**: 신규 마이그 `20260519_rename_user_tables_to_users.sql`(테이블 3 + 인덱스 7 + 제약 11, 전부 메타데이터 RENAME, 데이터 0). 코드 풀정합: `user_oauth/user_course` SQL 식별자 10곳(auth/payment/lesson/admin repo) + 주석 + `rekey_encryption.rs table:` 치환, **AAD 암호문 문자열 6곳 불변 유지**(`"user_oauth.oauth_email"` 등 = 복호화 키, 테이블명 아님), AUD-1 pk 오타는 별건 미변경. `user_export_data`=코드 0 → 마이그+문서만. §3.2.1 보강(user 도메인 토큰=USERS, USER_* 금지). 검증: 로컬 DB 마이그 클린 적용 + 토큰 잔여 0 + `cargo check --all-targets` 0(rekey bin 포함) + `sqlx prepare` 무변경(런타임 쿼리) + 리네임 repo SQL q1~q6 실DB 정합 + SCHEMA_PATCHED/MASTER 동기화. 앱 통합테스트는 사전존재 env 패닉(`config.rs:421` EBOOK_IMAGE_ENCRYPTION_KEY, 리네임 무관·explanation 선례)로 prod 재검 이연. **다음 ② writing_practice→study_writing_practice_***. 설계 SoT=`AMK_SCHEMA_NAMING_AUDIT.md §9`. **(1b/1c, 2026-05-19)**: 병렬 Explore 3 + DB/코드 실측으로 11 위반 테이블×사용처(file:line)·데이터 유무(local 실측) 전수매핑. 이중모델 갭 실측 확정(`study/repo.rs:522 find_task_explain` → 빈 `study_explain` → 학습자 해설 0). 부수발견 **AUD-1**(`rekey_encryption.rs:80` pk `"oauth_id"` 오타=실제 `user_oauth_id`, user_oauth 키 로테이션 시 실패·잠복, 별건)·**AUD-2**(`user_export_data` dead, 코드 0건)·**AUD-3**(`transactions.updated_at` 부재 기존제약). 난이도순 explanation(enum4+content_translations 4362행 INC-004급)>payment(8 웹훅 핸들러)>writing_practice(study 도메인)>user_*. **전용 감사 SoT 신규 = `docs/AMK_SCHEMA_NAMING_AUDIT.md`**(2단계 설계서 참조 대상). 다음=2단계 설계(통합 후 리네임). **(1a, 2026-05-18 `7716d03`)** 사용자 발견: 테이블명 중구난방, "발견했으니 안 하고 넘어가면 안 됨, 쌓이면 감당 불가"(부채 즉시 처리). **SSoT 확정 = `AMK_API_MASTER §3.2.1`**(테이블=`<도메인(단수)>_<의미>...` 도메인 접두사 필수, users만 예외, ADMIN_ 패턴). **1a 감사(49 테이블 전수)**: 🔴`explanation_unit`·`explanation_block`(explanation=도메인 아님)·`subscriptions`·`transactions`·`webhook_events`(payment 무접두사+복수) ·`writing_practice_seed`·`writing_practice_session` / 🟡`user_oauth`·`user_export_data`·`user_course`(user_ vs users_ 혼용) / 🟠`content_translations`(폴리모픽 i18n 경계). 메타부채: §3.2.1 규칙 존재하나 신규 마이그 강제 게이트 없어 드리프트. **핵심**: `explanation_*`(신·구조화·prod 적재 SoT, PR #298 568/1317) ↔ `study_explain`(레거시 평면 migrations/20260208:313·이 콘텐츠 빈·5파일 배선: study/repo.rs:533 학습`/explain`→빈 study_explain→학습자 해설0, admin/study/repo.rs 8곳, admin/translation/repo.rs:362/639) **이중모델 미통합**(설계근거 AMK_API_LEARNING §5.10 D1/D3). 어제 시드(20260518)=study/study_task/study_task_typing(67/500/500, study 테이블 맞음), explanation_*는 5/17 PR#298(어제 아님). **확정 결정(사용자)**: `explanation_unit/block`→**`study_contents`**(+block, 충돌 없음 실측. study_description=study.study_description 컬럼 충돌로 폐기). study_explain 흡수/폐기 + 학습/관리자 흐름 study_contents 배선 + content_type_enum(4362 content_translations 참조) 처리. video_/lesson_ 병렬테이블=YAGNI. enum 리네임=INC-004급(이미적용 enum 수정 시 sqlx checksum crash 선례) → 계획 마이그 필수. **사용자 작업순서**: ①전수조사+사용파악 ②정리+마이그 ③study 테이블 역할조정. **다음 세션 진입=1b**(위반 테이블 코드 사용처·데이터 유무 전수매핑→1c 문서화→2→3). 통합 후 리네임이 본질(리네임 선행 시 churn). **AI 자인**: SSoT Core(`AMK_API_MASTER §3.2.1`) 사용자 2회 지적까지 미확인 / 충돌 이름(study_description) 제시 / 의미정밀 과집착 vs 오너 가독성 1순위 놓침. SSoT=메모리 `project_schema_naming_cleanup` + `docs/AMK_SCHEMA_NAMING_AUDIT.md`. | 2026-05-19 | 2단계 설계(study_explain→study_contents 통합 후 리네임, INC-004급 신중) → 3 study 역할조정 |
| 153 | 교재 캐러셀 국기(seal) 14개 직접 복사 — 텍스트 폴백 해소 | 프론트/콘텐츠 | **2026-05-18** — 사용자 스크린샷으로 화면 확정(교재 카탈로그 캐러셀 `seal_list.tsx` `SealImage`, **book_landing 아님 — 내 앞 추측 화면 오판 정정**). 36 확장 부수효과 cover 와 동일 14언어: `/seals/{langcode}.svg` 22 보유 → 14 미존재로 `SealImage` onError 폴백(언어코드 텍스트 원, 기존 보유=깨짐 아님)으로 degrade. **seal=SVG 라 변환 불필요** → books `books/seals/seal_{LANG}.svg` 36 실재·규격 동일(`viewBox 0 0 204 204` 204px, prolog 일치) 확인 → 14개(am/ar/bn/es_es/fa/it/ky/lo/pl/pt_pt/sw/tr/uk/ur) `frontend/public/seals/{langcode}.svg` **직접 복사·리네임 완료**(books 핸드오프 불요 — cover 와 달리 PDF→WebP 변환 없음). public/seals 22→36, 전 카탈로그 언어 커버, 캐러셀 텍스트 폴백 해소, 코드 변경 0(폴백 기존). 14 무결성·SVG 유효·prolog 일관 검증. **후속 정정 (사용자 스크린샷 — ES-ES/PT-PT 만 텍스트 잔존)**: 최초 복사 시 snake `es_es.svg`/`pt_pt.svg` 명명 오류 — api `TextbookLanguage::EsEs/PtPt` 는 `#[serde(rename="es-ES"/"pt-PT")]` BCP47 직렬화(types.rs:252/256, 설계상)라 프론트가 `/seals/es-ES.svg` 요청 → 404 → 텍스트 폴백. `git mv` → `es-ES.svg`/`pt-PT.svg` 정정(serde 전수 audit: 하이픈/대문자=es-ES/pt-PT 둘뿐, zh_cn/zh_tw=snake 정상). 표지 핸드오프 §5.12 파일명 규칙도 `es-ES`/`pt-PT` 로 정정. **AI 사고**: 앞서 "직렬화 코드형 다름" 관찰했으나 seal 파일명에 미반영 = verify-before-assert 누락 자인. SSoT=`AMK_API_TEXTBOOK §5.12`. | 2026-05-18 | (cover WebP 28 = #152 books 핸드오프 잔존, seal 은 종결) |
| 152 | 교재 표지 깨짐 수정 — CoverCard 폴백(B) + books 표지 28 WebP(A 완료) | 프론트/콘텐츠 | **2026-05-18 — A 완료**: books 가 14언어×{student,teacher}=**28 WebP** 생성→`frontend/public/covers/` 수령. api 검증 PASS = 28 파일명 정확(12 snake + `es-ES`/`pt-PT` region) / 전부 VP8 805×1138 38~42KB(기존 22 동일 인코딩) / 기존 44 무변경 / **catalog 36×2=72 완전 매칭·누락 0**. 소스 입력 정정(books 회신): `*_COVER_{LANG}.pdf`→실제 `*_COVER_OUTER_{LANG}.html`(§5.12 반영). 표지 트랙 종결, 카탈로그 36 언어 실표지 노출. ──── (이력) #151 확장 부수효과: 카탈로그/ebook `CoverCard` 가 `/covers/{type}-{lang}.webp` 표시하는데 `public/covers/` 는 22언어×2=44 만 보유 → **14언어 표지 미존재로 깨진 이미지**(am/ar/bn/es_es/fa/it/ky/lo/pl/pt_pt/sw/tr/uk/ur). 정직 귀속: 내 36 확장이 표지 부재 드러낸 회귀. **AI 사고**: es_es/pt_pt "es/pt 별칭" 추정 → books 실측서 별도 표지 PDF 존재 확인·정정(verify-before-assert). 사용자 결정 C(A+B), D1=(a) books 파이프라인. **B 완료**: `cover_card.tsx` `useState`+`onError`→`ImageOff` 플레이스홀더(깨진 이미지 제거, 향후 언어 추가 재발 방어, i18n 키 무추가=Karpathy #2; book_hub 는 자체 폴백 기존 보유). npm build clean. **A 대기(books 핸드오프)**: 14언어×{student,teacher}=28 WebP, 규격=기존 22 실측 **VP8 805×1138 ~40KB 앞표지**, 파일명 `{type}-{langcode}.webp`(ES_ES→es_es), 소스 books `{student,teacher}-cover/*.pdf`(14 전부 존재), books `generate_cover_images.js` 사용, 반영처 `api/frontend/public/covers/`. 명세=`AMK_API_TEXTBOOK §5.12`. | 2026-05-18 | ✅ 종결 (cover+seal 전부 완료, 교재 이미지 트랙 닫음) |
| 151 | 교재 역본 36 반영 + 표시 숫자 단일소스화 (드리프트 제거) | 콘텐츠/프론트 | **2026-05-18 (커밋 `a1ec9be`)** — 사용자 보고: 발간분 미반영 + 역본 수 stale. 실측: books 가 **36개 언어 역본 전부 빌드**(student/teacher-inner·cover 각 36). api·프론트가 옛 숫자(api available 22·isbn_ready 10 / 프론트 "34"·"22"·"20" / books MASTER "22×2") 다층 stale. **AI 사고: 내가 처음 "22=정확"이라 단정한 건 api 플래그만 본 미검증 오류 → 사용자가 books/teacher-inner 36개 지적 → 실측 정정 (verify-before-assert, subagent 결과도 개념혼동 정정)**. 결정(사용자): ① 만든 것 전부 주문가능 ② 표시 36. **사실 정정(api)**: `catalog_languages()` 36 전부 `available=true`, `isbn_ready=true`=발간 검증 12(10+몽골어 2026-03-27+타지크어 2026-05-11, ISBN/PRINT/납본 로그). **본질 수정(현상 아닌)**: 숫자 36 locale 하드코딩→드리프트 재발 원인 → `lib/catalog.ts` 단일소스(LANG=36/EDITION=72) + i18n `defaultVariables(langCount/editionCount)` 한 곳 해석(PageMeta/slides/컴포넌트 무변경) + 36 locale json `{{langCount}}`/`{{editionCount}}` 치환(숫자값 매칭=현지숫자 ৩৪/۳۴/၃၄/३४ 무관, 500문장 보존, 413 인스턴스, 잔존 stale 0) + book_hub specLanguages 상수화. B-1 채택(A 현상땜질 거부, 사용자). **검증**: cargo check + npm build clean / 전 locale stale 언어수 0 / specLanguages {{count}} plural 36 locale 유지. ne/my 일부 seo·ebook=번역이 애초 언어수 미포함(Mac Mini 표현차, 범위 외). 문서: `AMK_API_TEXTBOOK §5.12` 정책 정정. **books `MASTER.md:52` "22×2=44 표지"=books측 stale(실 36×2), books 트랙**. | 2026-05-18 | prod 배포 후 카탈로그/마케팅 숫자 라이브 확인 / books MASTER 22→36 (books측) |
| 150 | 콘텐츠 시딩 1차 — HYMN 귀속계정 bin + study/task 숨김 시드 구현 | 콘텐츠 | **2026-05-18 (커밋 `eff6994`)** — #149 분석 후속 구현. 결정(사용자): HYMN=귀속전용(로그인불가) / 숨김 시딩(staged) / 기존 books 파일(20260504/05, 바이트동일·스키마 수동검증)+로컬 dry-run(books 재생성 비요구) / 번역=Mac Mini. **산출**: ① `src/bin/seed_hymn_account.rs` — raw SQL 불가(user_email/name/birthday=앱 KeyRing AES-256-GCM+blind index)→앱 `CryptoService`(AAD 일치) 암호화, `user_state=false`·`user_password=NULL`, 멱등(`user_auth='HYMN'` skip+`ON CONFLICT(user_email_idx)`), Dockerfile 3곳. ② `seeds/20260518_seed_textbook_studies.sql`(books 20260504 복사, **유일차이 study_state open→ready 67행**=숨김) + `_tasks.sql`(books 20260505 무변경, 500 typing). 번역 없음(ko 원문, Mac Mini 후속). **숨김 근거**: 서빙 전경로 `study_state='open'` 필터(repo.rs 169/216/290/442) → ready=비노출, UPSERT DO UPDATE SET=title/sub/desc만→flip 보존(footgun 없음). **검증**: cargo check/clippy/fmt + 로컬 전체체인 dry-run(현 마이그30→hymn→studies→tasks) 전부 PASS(HYMN state=f/pw=NULL·멱등 / study67 ready / task500 / FK·updated_by=HYMN / 재시딩 불변 / flip 보존). dry-run 이 로컬 .env 빈 crypto키 적발(prod 실키, 버그 아님). **prod 적용 완료 (2026-05-18, main `e9568e7` 배포 후 docker exec 1회)**: HYMN `user_id=8` state=false/pw=NULL / `study=67 ready=67 open=0` / `study_task=500 typing=500` / `study_task_typing=500` / task→study FK 500 OK / 라이브 `/studies` 노출 0(ready 차단=숨김 prod 강제 확인). 로컬 dry-run 과 완전 일치. SSoT=`AMK_API_LEARNING §5.11`. **트랙 1차 종료** — 남은 것=공개 flip(사용자 게이트)/Mac Mini 번역/choice·voice·writing·어휘/lesson·course 전부 별건·외부·미래. | 2026-05-18 | 공개 flip = 검증+Mac Mini 번역 후 사용자 트리거 |
| 149 | 광범위 온라인 콘텐츠 시딩 트랙 — 현황 분석 (구현=#150) | 콘텐츠 | **2026-05-18** — 보안 §4 종결 후 사용자 명시 차기 트랙 착수 전 현황 파악. **스코프 확정**: study/study_task 텍스트(video 제외=Phase 16 별건). **소스 실재 — 지난 "비어있음" 판단 정정**: books `scripts/guide-v2/seed_output/20260504_seed_textbook_studies.sql`+`20260505_..._tasks.sql`(448KB) 이미 생성. books `놀라운 한국어 500 해설집_완성/*.html`(76개)=사람용 렌더링 뷰(어휘·플래시카드·연습 포함), 실 구조 데이터 SSoT=`scripts/guide-v2/` 파이프라인. **번역=Mac Mini 정본**(books `_translations.sql` 무시, 사용자 확정 — 원래 books, 맥미니 도입 후 이관). 본문 ko=테이블 컬럼, 다국어=본문 시드 후 맥미니 후속(explanation 동형). **검증된 패턴**=explanation 계약(`books/docs/guide/explanation_seed_contract_from_api.md`)+`seed_explanation.rs` 멱등 로더. **다음 본작업 2건**: ① books SQL↔현 api 스키마(study_task_idx 20260418/writing 20260412 이후 drift) 정합 검증 ② 적재 방식(raw SQL vs `seed_study.rs` 멱등 로더), study 먼저. api 인프라(스키마·서빙·i18n 폴백)=study/task/course 완성, lesson i18n 폴백만 미확인(소규모). AMK_API_LEARNING §5.11 신설 + §5.9 번역 인프라 정정(books seed SQL→맥미니). AI 사고: Explore 에이전트 "explanation 메인 대기/시드 비어있음" 2 오류를 메모리·prod·books 실측으로 정정=subagent verify-before-assert. SSoT=`AMK_API_LEARNING §5.11`+메모리 `project_content_seeding_track`. | 2026-05-18 | 다음 세션: study 스키마 정합 검증 → 멱등 로더 |
| 148 | 보안 감사 2.3 Phase 2 — DB 컷오버 postgres → amk_app NOSUPERUSER | 보안 | **2026-05-18** — Phase 2 착수 전 prod 실측에서 **Phase 1 prod 미적용 발견**(`pg_roles` amk_app 0 rows). **근본 원인 = deploy.yml scp 갭 2건**: ① scp `source` 가 db-init 누락 → 호스트 미생성 ② scp source 추가 후 = Docker 가 bind 소스를 root 소유로 자동 생성 → scp untar Permission denied. **수정**: scp source 에 `db-init/10_least_priv_role.sql` 추가 + scp 이전 ssh 단계로 `mkdir -p + sudo chown ec2-user`(매 배포 멱등=재발 불가). **정직 정정**: 어제 "2.3 Phase1 prod 동작 확정" 은 과한 표현(로컬 dry-run 만, prod 전달 미검증)이었음 → SECURITY_AUDIT §0/§2.3 정정. **컷오버 절차**: scp 수정 배포 → P1(EC2 prod SQL stdin 적용, `amk_app\|f\|t` 검증) → P2(Secret 등록) → Stage D 4곳(compose:14 `DATABASE_URL` user postgres→amk_app + `${POSTGRES_PASSWORD}`→`${APP_DB_PASSWORD}` / deploy.yml `.env.prod` `APP_DB_PASSWORD` / DEPLOY_OPS §13 / SECURITY_AUDIT). URL-safe **hex** 비번(base64 의 `/+=` 가 URL 파서 깨뜨림 회피). **prod 라이브 검증**: `/health 200` + `/explanations?study_task_idx=` 200(DB SELECT 경유) + `pg_stat_activity` = 앱 `amk_app` 2연결 단독, `postgres` 1=진단 psql 자신(break-glass 잔존). superuser 폭발반경(COPY PROGRAM RCE/CREATE ROLE/타 DB/ALTER SYSTEM) 제거. **🔴/🟡 보안 §4 = 2.1~2.6 전부 종결.** AI 사고: 인프라 "완료" 표기 = prod end-to-end 미검증 시 과신, doc↔prod 실측이 갭 적발(verify-before-assert). SSoT=`AMK_API_SECURITY_AUDIT §2.3`+`DEPLOY_OPS §13`. | 2026-05-18 | 🟢 장기(ENABLE_DOCS guard/typed claims/nginx rate-limit/KMS) 별도 |
| 147 | cargo-deny 2.4 게이트 연쇄 종결 + 로컬 검증 SOP + prod 라이브 검증 | 보안 | **2026-05-17** — 2.4 게이트 가동 시 **기존 deny.toml 부채 4층 표면화·전부 해소**(첫 에러서 멈추는 도구 특성상 한 겹씩): ① UNLICENSED exceptions v2 unknown → `private.ignore=true`(`bc5bf33`) ② `CDLA-Permissive-2.0`/`NCSA` allow 추가(실 전이 의존·permissive·비배포·사유 인라인, `94a2765`) ③ 내부 path crate wildcard 오탐 → `allow-wildcard-paths=true` ④ `core2 0.4.0` yanked(image 전이·업글 경로 없음) → `yanked="warn"`(`e1e3ee3`). **본질 판정**(사용자 "현상 아닌 본질" 지적): ①~③=메커니즘 교정·명시 정책(게이트 본질 무손상), ④=회피불가 trade-off지만 실 신호 `RUSTSEC-2026-0105` advisories.ignore strict 별도 보존=문서화된 의도적 결정(yanked deny 유지 시 전 PR 영구 차단→게이트 무력화). **로컬 검증 SOP**: `cargo-deny 0.19.6` 설치 → 푸시 전 `cargo deny check` 4종 확정, blind CI 왕복 종료. **prod 라이브 검증**(main `0fab7b2` squash, EC2 deploy success): `/health 200` / CSP `default-src 'none'`(2.6, /docs 완화) / explanation `sent:300` 풀 응답+list 200 → **보안 §4(2.1·2.2·2.4·2.5·2.6+2.3 P1) prod 동작 확정**. 부산물: XFO 중복(앱DENY+nginx SAMEORIGIN)=무해, `AMK_OBSERVATIONS §3`. SSoT=`AMK_API_SECURITY_AUDIT §2.4`. | 2026-05-17 | 2.3 Phase 2 컷오버(사용자 승인 게이트) |
| 146 | 보안 감사 2.3 — DB superuser→amk_app (Phase 1) | 보안 | **2026-05-17** — 사실조사: 앱 상시 `postgres` superuser 접속·부팅 sqlx 마이그(DDL), superuser-only 연산 0건(불필요)·악용 이력 0(잠재 위험). 풀 role 분리 비채택(수익체감) → **단일 NOSUPERUSER 소유 role `amk_app`**. **Phase 1 완료(런타임 영향 0)**: `db-init/10_least_priv_role.sql`(멱등, env없음/빈값 가드, GRANT+ALTER DEFAULT PRIVILEGES+앱객체 OWNER→amk_app) + `docker-compose.prod.yml` db initdb 마운트+`APP_DB_PASSWORD` env. 앱은 계속 postgres 접속=동작 무변경. 로컬 amk-pg dry-run 검증(멱등 2회·owner 이전·NOSUPERUSER, **dry-run 이 REASSIGN OWNED 거부 + psql $$ 내 :'var' 미치환 2버그 사전 적발·정정**). `AMK_DEPLOY_OPS §13` 절차. **Phase 2(컷오버=DATABASE_URL user 교체, 4곳 동시 INC-001 클래스) = 사용자 명시 승인 게이트 대기**. SSoT=`AMK_API_SECURITY_AUDIT §2.3`. | 2026-05-17 | Phase 2 컷오버 (사용자 승인 게이트) |
| 145 | 보안 감사 2.4+2.6+2.5 일괄 (cargo-deny PR / CSP / admin IP XFF) | 보안 | **2026-05-17** — 🟡 3건 항목별 커밋. **2.4**(`20129f6`): `pr-check.yml` 에 cargo-deny job 신설(security-audit.yml 미러) → 신규 취약 의존성 머지 전 차단. **2.6**(`6b5275d`): app `security_headers` 미들웨어 CSP — `default-src 'none'; frame-ancestors 'none'` / `/docs`·`/api-docs` 만 완화. nginx-level 의도적 미적용(앱이 전 프록시 응답 커버). **2.5**(`8970a4d`): **감사 인용 오류 정정**(지목 header_utils=로그용, 실제 강제자 admin_ip_guard 가 XFF-first 취약) → `trusted_client_ip()` 분리, **CF-Connecting-IP 만 권위**(클라 위조 불가), 부재 시 fail-closed, 단위 7종. scope=ip_guard 만(header_utils 별도 후속). 운영주의: 활성화 시 CF 경유 필수. 현 ADMIN_IP_ALLOWLIST 빈값=런타임 영향 0. cargo check/clippy/fmt + lib(ip_guard 7·jwt 등) clean. **🔴+🟡 보안 §4 = 2.1·2.2·2.4·2.5·2.6 완료, 2.3(별도 트랙)만 잔여.** SSoT=`AMK_API_SECURITY_AUDIT.md`. | 2026-05-17 | 2.3 DB 최소권한 별도 신중 트랙 |
| 144 | 보안 감사 2.2 — JWT iss 강제 + reset 토큰 혼용 차단 | 보안 | **2026-05-17 (진행 중)** — 착수 전 reset 흐름 실측 → **감사 전제 stale 발견**: 활성 reset 토큰은 이미 불투명 `ak_reset_<uuid>` Redis(JWT 아님). 진짜 취약점 = `reset_password_with_token`(라우터 연결 활성) else 분기가 access JWT 를 reset_token 으로 수용 → **계정 탈취**(피해자 access token 으로 비번 재설정). `service::reset_password`(1095) = dead code. **완료 (2026-05-17)**: (a) `decode_token` `Validation::new(HS256)`+`set_issuer([amk])` (b) `reset_password_with_token` 레거시 JWT 폴백 제거 → opaque `ak_reset_` 전용 (c) dead `service::reset_password`(~100줄) 삭제. **라이브 검증**: jwt 단위 iss 거부 + lib 213 passed + clippy/fmt clean + 통합 `--ignored` service_integration 9/9(**access JWT→reset 401 = 계정 탈취 차단 실증**)·auth_extractor 6/6·admin_rbac 8/8(2.1 무회귀). SSoT = `AMK_API_SECURITY_AUDIT.md §2.2`. **다음**: 2.3(DB 최소권한)/2.4(cargo-deny PR)/2.5/2.6. | 2026-05-17 | 보안 §4 잔여 2.3~2.6 |
| 143 | 보안 감사 2.1 — access token 세션 폐기 검증 | 보안 | **2026-05-17** — `AMK_API_SECURITY_AUDIT.md` §2.1(최우선) 해결. 로그아웃/비번변경/강제퇴장 후에도 access token 이 만료(15분) 전까지 유효하던 갭 차단. **신규** `src/api/auth/session.rs::ensure_session_active` — JWT 디코드 직후 Redis `ak:session:{sid}` 존재 검증, `extractor.rs`(AuthUser/OptionalAuthUser)+`role_guard.rs` 연결. **정책 = fail-open + 관찰성**(사용자 결정, 감사 기본권고 fail-closed 와 의식적 상이): 단일 Redis SPOF 에서 fail-closed=전면 인증 마비, fail-open 노출은 access TTL 15분 상한=2.1 이전 베이스라인 이하로만 후퇴(악화 0). Redis 정상·키부재→401 / Redis 불가→SKIP+`tracing::warn!(target="security.session_revocation")`. 통합 테스트 계약 갱신(`common::seed_session`)+폐기 세션 401 회귀 테스트 신설. **로컬 라이브 검증**: admin_rbac 8/8·auth_extractor 6/6(시드→200/403, 폐기→401, 미인증→401), lib 212 passed, clippy/fmt clean. doc↔code 재검증으로 인용 정확성 확인 후 착수. **다음**: 2.2(JWT iss/aud)·2.3(DB 최소권한) 등 §4 잔여. | 2026-05-17 | 보안 §4 잔여(2.2~2.6) 순차 |
| 142 | 설명(해설) 콘텐츠 books→api 인계 — 스키마 아키텍처 결정 | 학습/설계 | **2026-05-17** — books가 해설집을 api-무관 중립 모델(568 Unit = pattern_guide 68 + sentence_explain 500, 1,317 block)로 정리해 인계. 계약 = `amazing-korean-books/docs/guide/explanation_handoff_to_api.md`. **정합성 확인**: study_explain 부적합·연결키(study_idx/study_task_idx) 존재 = 정확 / content_translations 조인은 구조 불일치 갭 발견. **결정 (D1~D4)**: study_explain 재사용 ❌ / lesson_item kind ❌(시기상조) / **전용 `explanation_unit`+`explanation_block` 신설 ✅** / **서버 저장+API 서빙 ✅** (정적 에셋 ❌ — 번역이 이미 content_translations DB 파이프라인, 콘텐츠만 정적화하면 소스 분리). **i18n 조인 = B 확정** (content_translations 무변경, 변환은 books 시드 생성기+로더 / A 기각: 식별 2방식 공존 부담, EAV 약점은 저심각도 별도 백로그). **스키마 적용**: `20260517_explanation_content_type_values.sql`(content_type +2, 단독) + `20260518_explanation_tables.sql`(3 enum + explanation_unit/explanation_block) + src/types.rs(ContentType +2, ExplanationUnitKind/Source/BlockType 신규). title/subtitle 평면화(㉠) / 연결키 논리 참조(FK 강제 안 함) / structured 골격=JSONB·번역텍스트=content_translations 분리. cargo check ✅. **점검**(커밋 5897cc8 전수 대조): 계획 대비 누락·이탈 0 + books 모델 필드 커버리지 확인. 정직 고지 5건(의도적 선택): 20260518 미래날짜=README관례 / title_en·ko 둘다 nullable(en 비강제, 사용자확인) / study_task_idx=books 파생 / enum 정적정합만 / i18n_key 미저장=B의도. **문서**: `AMK_API_LEARNING.md §5.10`. **books 핸드오프 전달 + 회신 라운드트립 완료**: `explanation_seed_contract_from_api.md`. books 회신(갭1+확인2) → api 회답: **갭1=(a) 채택** (concept_card `explanation_block_card_{i}_desc` + qword_card `explanation_block_qword_{i}_header` field_name 신설, structured 경계 동일 규칙 — **스키마 무변경**, enum/테이블 그대로) / **확인1 수용** (`{inherit:true}`=마커 유지·행 없음·직전 row 계승) / **확인2 수용** (산출 B=en 행만, ko=도메인 테이블 원본, 35언어=맥미니 후속). **books 착수 승인**. **books 시드 산출 완료 → api 독립 검증 PASS·채택**: `explanation_seed.json` (산출 A unit 568/block 1,317 + 산출 B en 4,362) 계약 전수 대조 (meta.self_check 비신뢰 재계산) — unit_idx UNIQUE/enum/av_307_313/lang-invariant 누출/PK 해소/study_task_idx/field_name 9종 전 항목 ✅. **계약 정정 1건(api 귀책)**: §2 inherit 문구 모호 → explanation 한정 상속(en은 정상 번역)으로 정정, books 시드 정상·재작업 불필요. **적재 로더 구현 완료**: `src/bin/seed_explanation.rs` (Cargo [[bin]] 신규) — 트랜잭션 멱등 적재(unit/block upsert→PK 해소→content_translations en/approved upsert) + **연결키 정합 검증 내장**(작업 #2 흡수). 마이그 20260518 정정(`updated_by_user_id` nullable+FK→users = lesson/study 컨벤션). cargo check/clippy/fmt ✅. **조회 API 구현 완료**: 신규 도메인 `src/api/explanation/` (dto→repo→service→handler→router, `/explanations` nest, 공개 읽기). `GET /explanations/{unit_idx}` + `GET /explanations?study_idx=&study_task_idx=` (lang 폴백 user→en→ko, inherit explanation 서버 계승, structured 골격+i18n 맵 모델). docs.rs paths/schemas/tags 등록 + openapi 회귀 7/7 통과(paths-router 정합 포함). cargo check/clippy/fmt ✅. **로컬 DB 런타임 검증 완료(부분)**: 우리 마이그만 직접 적용 → seed 실행 = unit 568/block 1317/translation 4362 멱등·전부 en·approved / repo SQL 실측(find_unit_by_idx·find_translations·find_units_by_link·갭1 card desc) ✓ / 연결키 566·500 미해소=정상(로컬 study 시드 없음). **한계**: 라이브 HTTP(service i18n/inherit/폴백)는 미검증 — 서버 부팅이 dev DB 사전 이력분기(20260419, 무관)에 막힘. 프로덕션/정상 마이그 환경 재검증 필요. **배포 INC (2026-05-17)**: PR #294/#295 가 미푸시 stale 머지(explanation 미포함) → KKRYOUN 9커밋 push + main 머지 충돌 해소(`a9812de`) → PR #296 머지. **PR #296 배포 build 실패**(`cargo build --release` exit 101 = `couldn't read src/bin/seed_explanation.rs`): Dockerfile dep-cache 더미가 `[[bin]]` 하드코딩(rekey_encryption만) → 신규 bin 더미 누락. **프로덕션 무영향**(deploy job skip → 구버전 유지). Dockerfile 2곳(더미 생성+touch) 수정 `1ae7e3a`. **재머지 필요**. **PR #297 배포 성공** (`d4f51e1`, Dockerfile fix 포함): build-and-push ✅ / deploy ✅ = 서버 부팅·헬스체크 통과 = **프로덕션 DB 마이그 20260422~20260518 정상 적용**(로컬 `20260419` 분기는 **로컬 한정** 확정). **프로덕션 HTTP 검증 ✅**: `/health` 200 / `GET /explanations` 400(study_idx 필요) / `/explanations/__nope__` 404 / `?study_idx=foo` 200 `{items:[]}` — 라우팅·handler·service·repo·스키마 실검증. **남은 1건(코드 아님)**: 콘텐츠 서빙(i18n·inherit·폴백)은 프로덕션 시드 데이터 부재로 미확인 → `seed_explanation` 프로덕션 실행(books explanation_seed.json + prod DB 접근, EC2 books 리포 없음 = 운영 단계) 후 확정. **books 후속 회답(2026-05-17)**: 시드 재생성 통지 프로토콜 동의(+구조변경 포함여부 명시 요청) / **번역 트랙 적재 계약 확정** = lang별 분리 `explanation_translations.{lang}.json`(en 5-튜플 동일) + `seed_explanation --translations` 신규 모드(구현은 맥미니 산출 도착 시, 계약만) + 튜플 멱등키 + status approved. books 단계④ 진행 가능. **프로덕션 시딩·라이브 검증 완료 ✅ (PR #298 배포 + EC2 `docker exec` 시딩)**: `적재 완료: unit=568 block=1317 translation=4362` 멱등 / 연결키 566·500 미해소=정상(prod study/study_task 매칭 시드 없음, 논리 참조 독립). **프로덕션 실데이터 HTTP 검증 전부 통과**: inherit 계승(sent:300 row1/2 explanation=직전 row 서버 계승) / i18n 맵 조립 / 링크 조회(study_task_idx) / 폴백(vi→en, ko→원본). **로컬 20260419 로 막혔던 service.rs 변환 로직 = 프로덕션 end-to-end 확정. 설명 콘텐츠 트랙 완결.** **남은 건 코드 외**: 프론트 렌더 연동(별도 트랙) / 맥미니 Phase C 35언어 도착 시 `seed_explanation --translations` 구현(계약 확정) / (선택) prod study/study_task 시드 시 연결키 재확인. | 2026-05-17 | 트랙 완결 — 프론트 연동·맥미니는 외부/별도 |
| 141 | RCE 선행 공격면 검증 (dirtyfrag 후속) | 보안 | **2026-05-17** — #140 dirtyfrag 의 전제(RCE 선행 후 root 탈취) 1차 방어선 실측 검증. **결과 = 이미 양호, 코드/구성 변경 0건.** Docker socket 마운트 0건 / privileged·cap_add·host network 0건 / db·redis·api 호스트 포트 노출 없음(nginx 80·443만). cargo-deny(RUSTSEC)+npm audit 는 `security-audit.yml` 에 **이미 통합** (주간 cron). EC2 배포 = SSH 키 인증(비번 미사용). `sudo sshd -T` 실측 = `passwordauthentication no` / `permitemptypasswords no` / `kbdinteractiveauthentication no` / `pubkeyauthentication yes` → **비번 무차별 공격면 0**. **비채택 hardening** (효익 0 수렴, 기록만): `PermitRootLogin no`(현재 갭 아님 — 비번 차단 + AL2023 root authorized_keys 강제커맨드로 직접 root SSH 이미 무력) / fail2ban(비번 off 라 무차별 표적 없음). **문서**: `docs/AMK_DEPLOY_OPS.md §11-3` 신설 (검증 표 + 비채택 사유 + 재검증 시점). | 2026-05-17 | sshd_config / compose / 워크플로 보안 변경 시 §11-3 표 재확인 |
| 140 | EC2 호스트 OS — dirtyfrag mitigation 적용 | 보안 | **2026-05-13** — V4bel/dirtyfrag (CVE-2026-43284 xfrm-ESP + CVE-2026-43500 RxRPC, Linux 커널 LPE 2 CVE 체인, embargo 2026-05-07 깨짐 / 메인라인 패치 2026-05-05·05-10 / Amazon Linux 2023 백포트 도착 전) 대응. **적용**: EC2 (`ip-172-31-33-214`, ec2-user) 에 `/etc/modprobe.d/dirtyfrag.conf` 생성 = `install esp4/esp6/rxrpc /bin/false` 3 줄 + `sudo rmmod esp4 esp6 rxrpc` + `echo 3 > /proc/sys/vm/drop_caches`. **검증**: `cat` = 3 줄 OK + `lsmod \| grep -E "(esp4\|esp6\|rxrpc)"` = 빈 결과 (취약 모듈 로드 없음). **앱 영향 0** = Rust/Axum HTTP API 는 IPsec(ESP)/RxRPC 미사용. **문서**: `docs/AMK_DEPLOY_OPS.md §11` 신설 (호스트 OS 보안 + 일반 커널 CVE 대응 SOP). **후속**: AL2023 보안 패치 도착 시 `sudo dnf update`, 블랙리스트는 그대로 두거나 제거 (모듈 미사용이라 무해). RCE 공격면 축소 (SSH 비번 로그인 / cargo audit CI / Docker socket 등) = 별도 트랙 → **#141 에서 검증 완료 (이미 양호)**. | 2026-05-13 | distro 패치 도착 후 mitigation 유지/제거 결정 |
| 139 | 부채 카탈로그 5필드 게이트 정착 + 카운트 재정리 (30 → 15) | 정책/메타 | **2026-05-12 (PR #291)** — 어제 18 PR busywork 분석 결과 후속. **5필드 게이트** = WHERE / WHAT / HOW MUCH / WHY / END. 부채 등재 시 모두 채워야. **카탈로그 재정리**: D1~D4 (A2 중복, -4) + F5 (외부 SSoT, -1) + G10/G12 (5필드 미충족 → `AMK_OBSERVATIONS.md`, -2) + I 1~8 (`AMK_AI_MISTAKES.md` SSoT, -8). 잔여 = A 3 + B 1 + E 11 = **15건** (모두 외부 트리거 / 수용). **12 해결 부채 cross-reference**: 8/12 = incident-based 실제 가치 (B3/B4/B7/G1/G2/G2-1/G16/J1-J3) / 4/12 = 메타 (C1/C2/C7/G15). **AI 자동 진입 차단 정책** 정착 = 카탈로그 첫 항목 자동 처리 금지. **신규 문서**: `docs/AMK_OBSERVATIONS.md` (관찰 영역, 부채 아님). **메모리**: `feedback_debts_reference.md` 5필드 게이트 정착 + `project_status.md` / `MEMORY.md` 갱신. **구조적 차단** = 룰 의존도 ↓ 카탈로그 자체 차단 ↑ (사용자 "이쁘게 꾸미기" 지적 후 근본 해결). | 2026-05-12 | 다음 세션 = 카탈로그 본 후 능동 작업 0건 인지, 사용자 명시 결정 또는 외부 트리거 필요 |
| 138 | T-G10-deep admin subset + G10 backend deeper error.rs = 19 신규 / 249 frontend + 212 lib | 품질 | **2026-05-11 후속¹⁷** — 본 세션 마지막 batched PR. **admin subset** = use_admin_email hook (1 mutation, 2 tests) — admin_api.ts 797 라인 전체는 too big for batch. **G10 backend deeper error.rs** = 17 tests = into_response 의 13 AppError variants (Internal/HealthInternal/BadRequest/Unprocessable/Unauthorized/Forbidden/NotFound/Conflict/TooManyRequests with Retry-After header/ServiceUnavailable/External/Sqlx/Jsonwebtoken) status code + error_code 검증 + CryptoError 3 분기 From 매핑 (DecryptionFailed/InvalidFormat/Internal → Internal) + ValidationGeneric anti-enumeration 검증. **검증**: frontend vitest = **249 passed** (이전 247 + 신규 2 admin) / 48 파일. backend lib = **212 passed** (이전 195 + 신규 17 error). 모든 신규 모듈 100% (admin/hook) all metrics. clippy / fmt clean. **본 세션 종결** = 18 PR / frontend 122→249 (+127) / lib 183→212 (+29) / integration 8→25 (+17) / §0 31→30. | 2026-05-11 | **세션 종결** — 다음 세션 admin_api 전체 cover / 외부 트리거 |
| 137 | T-G10-deep batch — user/video/lesson 도메인 api+1 hook 각각 = 29 신규 / 247 passed | 품질 | **2026-05-11 후속¹⁶** — T-G10-deep ebook/study/textbook (#281/#283/#284) 패턴 user/video/lesson 도메인 확장. **6 신규 파일**: user_api (5) + use_user_me (3 = success/404 retry-false/500 retry-then-fail) + video_api (6) + use_video_list (4 = success + 3-branch getErrorMessage) + lesson_api (7) + use_lesson_list (4 = 같은 패턴). **수정 패턴 학습**: 401 path 에서 axios 인터셉터의 /auth/refresh 자동 호출이 jsdom 환경과 충돌 → 404 사용 (같은 retry:false 분기). **검증**: vitest --coverage = **247 passed** (이전 218 + 신규 29) / 47 파일. 모든 신규 모듈 100% all metrics. npm build 16.82s clean / lint 0. | 2026-05-11 | T-G10-deep admin (선택) / G10 backend deeper subset 후속 |
| 136 | T-G10-page-cont batch — signup_page (7) + pricing_page (9) = 16 신규 / 218 passed | 품질 | **2026-05-11 후속¹⁵** — 본 세션 batched PR (2 페이지 함께). **signup_page.test.tsx 7 tests**: 렌더 / Google mutation / Google pending disabled / Collapsible 펼침 / requires_verification=true → /verify-email + state / requires_verification=false → /login / submit pending → "auth.signingUp" disabled. **pricing_page.test.tsx 9 tests**: skeleton loading / plan card 렌더 / not logged in → /login?redirect / logged in + no sub → openCheckout / active sub → toast.info / ?success=true → toast + clear params / cancel dialog → cancelAtPeriodEnd (immediately:false) / cancelImmediate (immediately:true) / promo code clear button. **검증**: vitest --coverage = **218 passed** (이전 202 + 신규 16) / 41 파일. signup_page 100% all / pricing_page 98.94/75/81.81/98.94 (모두 threshold 통과). npm build 18.60s / lint 0 (1 unused param 수정 후). | 2026-05-11 | T-G10-deep user/video/lesson/admin / G10 backend deeper subset 후속 |
| 135 | G10 backend deeper subset — `api::util::extract_client_ip` pure helper 10 tests / 195 passed | 품질 | **2026-05-11 후속¹⁴** — Track 6 (G10 backend deeper) 의 부분. 전체 service.rs (1d+) 대신 작은 pure helper 1건 cover. **신규 10 tests** in `src/api/util.rs::tests`: x-forwarded-for 첫 IP / 단일 IP / 공백 trim / 잘못된 IP → x-real-ip 폴백 / x-real-ip 만 / 둘 다 없음 + AK_DEV_IP_FALLBACK=true → 127.0.0.1 / false → 0.0.0.0 / "0" → 0.0.0.0 / IPv6 / 빈 첫 segment 폴백. **Rust 2024 unsafe set_var 처리**: `#[allow(unsafe_code)]` on test module + SAFETY 주석 (single-thread 가정). **검증**: cargo test --lib = **195 passed** (이전 185 + 신규 10). clippy --all-targets / fmt clean. **본 세션 종결 시점** 권고 = 14 PR 완료, marginal returns 감소 영역. | 2026-05-11 | 다음 세션 = G10 backend deeper 본격 (admin service.rs / paddle SDK mock) |
| 134 | C-doc-sync-cont — **자동 regression 검증 test** + 신규 2 endpoint 등록 + revenuecat policy 강화 = 1 신규 test / 185 passed | 문서/품질 | **2026-05-11 후속¹³** — C-doc-sync (#275) 자연 후속. 본 PR 머지 후 신규 endpoint 추가 시 docs.rs 등록 누락 즉시 fail. **신규 test**: `openapi_paths_match_router_handlers` (src/docs.rs). 동작 = (1) `src/api/**/router.rs` 파일 모두 재귀 수집 (CARGO_MANIFEST_DIR + fs::read_dir 재귀). (2) regex 로 handler 참조 추출. (3) docs.rs paths(...) handler 참조 추출. (4) Diff 후 `policy_excluded` (handle_webhook + handle_revenuecat_webhook) 제외하고 missing 검출 → 있으면 fail. **본 test 가 발견한 3 stale endpoint**: `admin_create_vimeo_upload_ticket` / `admin_get_lesson_progress_detail` / `handle_revenuecat_webhook`. 첫 2개 = docs.rs paths() 등록. 마지막 = webhook 정책 (handler.rs 주석 "swagger UI 노출 비권장") = `policy_excluded` 추가. **검증**: cargo test --lib openapi = 7 passed (기존 6 + 신규 1). cargo test --lib 185 passed (전 184 + 1). clippy / fmt clean. | 2026-05-11 | G10 backend deeper (1d+) |
| 133 | C-payment-ebook-txn — ebook transaction.completed + refund 분기 = 3 신규 / payment_integration 25 passed | 보안/품질 | **2026-05-11 후속¹²** — Track 4 자연 후속³. ebook 결제 분기 (subscription_id=null + custom_data.type="ebook") happy path 검증. **helper 변경**: `make_transaction_completed_event_json_ext` 신규 (custom_data override 가능). 기존 `make_transaction_completed_event_json` 은 wrapper 로 유지 (None 전달 = 기본 user_id custom_data). **신규 fixture**: `insert_test_ebook_purchase(st, user_id, unique)` = ebook_purchase 행 시드 (status=pending). **3 신규 tests**: (a) ebook txn happy path = purchase_code 매칭 → status=completed + paddle_txn_id 저장. (b) purchase_code 누락 = handler early return → status=pending 유지. (c) refund 환불 = 사전 ebook 결제 완료 + adjustment.created(refund+approved) → status=refunded. **검증**: `cargo test --test payment_integration -- --ignored` = **25 passed** (이전 22 + 신규 3). cargo test --lib 184 / clippy / fmt clean. **수정**: language 'en' → 'ja' (textbook_language_enum 에 'en' 미포함, 첫 deserialize 시 발견). | 2026-05-11 | C-doc-sync-cont / G10 backend deeper |
| 132 | G10-frontend T-G10-deep-study — study_api (11 함수) + 2 hooks = 21 신규 / 202 passed | 품질 | **2026-05-11 후속¹¹** — T-G10-deep-ebook (#283) 패턴 study 도메인 확장. **3 신규 파일**: (a) `study_api.test.ts` (13 tests = getStudyList params + sanitize / getStudyDetail / getStudyTask / submitAnswer POST / getTaskStatus / getTaskExplain / startWritingSession POST / finishWritingSession PATCH / listWritingSessions / getWritingStats / getWritingPracticeSeed / 5xx ApiError). (b) `hook/use_study_list.test.tsx` (4 tests = isSuccess / ApiError branch / Error branch / 비-Error fallback branch). (c) `hook/use_writing_stats.test.tsx` (4 tests = 같은 패턴). **getErrorMessage 3 분기 모두 cover** = vi.spyOn(studyApi, ...) 으로 mockRejectedValueOnce(new Error / 문자열). **검증**: vitest run --coverage = **202 passed** (이전 181 + 신규 21) / 39 파일. category/study = 100% all metrics. npm build 16.22s clean / lint 0. | 2026-05-11 | C-payment-ebook-txn / C-doc-sync-cont / G10 backend deeper |
| 131 | G10-frontend T-G10-deep-ebook — ebook_api (12 함수) + 2 hooks = 21 신규 / 181 passed | 품질 | **2026-05-11 후속¹⁰** — T-G10-deep (#281) 패턴 ebook 도메인 확장. **3 신규 파일**: (a) `ebook_api.test.ts` (15 tests = 6 user-facing GET/POST/DELETE + 2 binary fetchPageImage [no-HMAC + with-HMAC] + 2 fetchPageTile [같은] + heartbeat + 4 admin functions + 5xx ApiError). HMAC 헤더 검증 (X-Ebook-Signature 64 hex chars + X-Ebook-Timestamp). (b) `hook/use_ebook_catalog.test.tsx` (2 tests). (c) `hook/use_my_purchases.test.tsx` (4 tests = enabled false / success / error / pending paddle 분기). **Web Crypto API 사용 검증** = jsdom 환경에서 crypto.subtle.importKey + sign 정상 작동 확인. **검증**: vitest run --coverage = **181 passed** (이전 160 + 신규 21) / 36 파일. category/ebook = 100% all metrics. npm build 16.61s clean / lint 0. | 2026-05-11 | T-G10-deep-study / C-payment-ebook-txn / C-doc-sync-cont / G10 backend deeper |
| 130 | G10-frontend T-G10-page-cont — reset_password_page (Form + mutation + token guard) = 4 신규 / 160 passed | 품질 | **2026-05-11 후속⁹** — T-G10-page (#280) 자연 후속. **reset_password_page.test.tsx 4 tests**: (a) form 필드 + submit 버튼 렌더. (b) location.state.token=null → toast.error + navigate /login (useEffect path). (c) Password123 입력 + submit → mutation 호출 (reset_token + new_password) + onSuccess navigate /login. (d) isPending → 버튼 disabled + Loader 표시. **mock 전략**: react-router-dom (useNavigate / useLocation), react-i18next (t() identity), sonner (toast capture), `useResetPassword` 직접 mock (mutate fn + opts.onSuccess 즉시 호출 패턴). **vitest.config.ts coverage whitelist** 추가. **검증**: vitest run = **160 passed** (이전 156 + 신규 4) / 33 파일. category/auth/page = 97.79 Stmts / 92.3 Branch / 100 Funcs / 97.79 Lines (threshold 90/75/60/90 통과). npm build 16.76s clean / lint 0. | 2026-05-11 | T-G10-deep-ebook / T-G10-deep-study / C-payment-ebook-txn / C-doc-sync-cont / G10 backend deeper |
| 129 | G10-frontend T-G10-deep — textbook api + 2 hooks = 9 신규 / 156 passed | 품질 | **2026-05-11 후속⁸** — T-G10-page 자연 후속. **3 신규 파일**: (a) `textbook_api.test.ts` (5 tests = getTextbookCatalog / createTextbookOrder POST body / getTextbookOrderByCode / getMyTextbookOrders / 5xx ApiError). (b) `hook/use_catalog.test.tsx` (2 tests = isLoading→isSuccess / isError). (c) `hook/use_my_orders.test.tsx` (2 tests = isLoading→isSuccess / isError). **#276 패턴 재사용** = MSW + QueryClient wrapper. **vitest.config.ts coverage whitelist**: 3 textbook 모듈 추가 (textbook_api / use_catalog / use_my_orders). **검증**: vitest run --coverage = **156 passed** (이전 147 + 신규 9) / 32 파일. category/textbook = 100% (3 파일). 전체 = stmts 98.99 / branches 94.09 / funcs 93.75 / lines 98.99. npm build 17.08s clean / lint 0. | 2026-05-11 | 본 세션 4안 + 4 후속 모두 종결, 세션 정리 |
| 128 | G10-frontend T-G10-page — error pages 3건 smoke (NotFound/AccessDenied/Error) = 9 신규 / 147 passed | 품질 | **2026-05-11 후속⁷** — Track 3 자연 후속. **3 신규 page-level tests**: (a) `not_found_page.test.tsx` (3 tests = 404 badge + title / navigate(-1) / 홈으로 link). (b) `access_denied_page.test.tsx` (3 tests = 403 + title / navigate(-1) / 홈으로). (c) `error_page.test.tsx` (3 tests = Error badge + title / window.location.reload spy / 홈으로). **패턴 정착**: vi.mock("react-router-dom") 으로 useNavigate spy + vi.mock("react-i18next") 로 t() 인라인 매핑 + MemoryRouter wrapper. **vitest.config.ts coverage whitelist**: 3 error pages 추가. **검증**: vitest run --coverage = **147 passed** (이전 138 + 신규 9) / 29 파일. category/error/page = 100% (3 파일). 전체 = stmts 98.97 / branches 93.44 / funcs 93.10 / lines 98.97 (threshold 90/75/60/90 perFile 통과). npm build 17.14s clean / lint 0. **defer**: page-level page-level 후보 = pricing_page (348 라인 paddle 의존, 시드 ↑) / signup_page (521 라인 form 복잡) = 별도 트랙. | 2026-05-11 | T-G10-deep |
| 127 | C-payment-event T-Subset-Txn — transaction.completed + adjustment.created/updated + **버그 수정** = 5 신규 / payment_integration 22 passed | 보안/품질 | **2026-05-11 후속⁶** — Track 4 자연 후속². **2 신규 JSON helper**: `make_transaction_completed_event_json` (Transaction 22 필드 + 중첩 TransactionDetails 의 totals/adjusted_totals/line_items 등 nested 80+ 라인) + `make_adjustment_created_event_json` (Adjustment 13 필드 + AdjustmentTotals nested). **5 신규 tests**: (a) Transaction wire-format deserialize / (b) transaction.completed → payment_transaction INSERT (sub 시드 후 link) / (c) Adjustment wire-format deserialize / (d) adjustment refund+approved → transaction.status=Refunded / (e) adjustment credit action skip → status=Completed 유지. **🐛 실제 버그 발견 + 수정**: `PaymentRepo::update_transaction_status_by_provider_id` 가 존재하지 않는 `updated_at` 컬럼 SET → 환불 webhook 모두 fail 하는 런타임 path. 통합 테스트로 발견, SQL 에서 `updated_at = NOW()` 제거. **검증**: `cargo test --test payment_integration -- --ignored` = **22 passed** (이전 17 + 신규 5). cargo test --lib 184 passed / clippy --all-targets clean (1 too_many_arguments allow). | 2026-05-11 | T-G10-page / T-G10-deep |
| 126 | C-payment-event T-Subset-Cont — 나머지 6 Subscription variants 상태 전이 = 6 신규 / payment_integration 17 passed | 보안/품질 | **2026-05-11 후속⁵** — Track 4 자연 후속. 본 PR helper 일반화 + 6 variants 추가. **helper 변경**: `make_subscription_created_event_json` → `make_subscription_event_json(event_type, status, ...)` = event_type + status 파라미터화. items[0].status = "active" 하드코딩 (핸들러가 미사용). **신규 helper**: `run_create_then_variant(st, user_id, unique, variant_event_type, variant_status) -> SubscriptionRow` = create + variant 시퀀스 실행 후 row 반환 (DRY). **6 신규 tests**: subscription.activated/updated/canceled/paused/past_due/trialing 각 variant 가 SubscriptionStatus 를 Active/Active/Canceled/Paused/PastDue/Trialing 으로 전이. **검증**: `cargo test --test payment_integration -- --ignored` = **17 passed** (이전 11 + 신규 6). clippy --all-targets clean / fmt clean. **defer**: transaction.completed + adjustment.created/updated = T-Subset-Txn (다음 PR). | 2026-05-11 | T-Subset-Txn / T-G10-page / T-G10-deep |
| 125 | C-payment-event Subset — subscription.created happy path + DB INSERT + idempotency = 3 신규 tests / payment_integration 11 passed | 보안/품질 | **2026-05-11 후속⁴** — Track 4. **메모리 "별도 새 세션 권장" deferral 옵션 거부 + 사용자 push 으로 본 세션 강행**. 작업 전략 = wire-format JSON 생성 yak-shaving 회피 → process_webhook_event 직접 호출 (signature path 4 tests 가 wire format 검증 담당). **신규 helper**: `make_subscription_created_event_json` = minimal-valid Subscription Event JSON (event_id / occurred_at / event_type=subscription.created / data 의 Subscription 구조 27 필드 + 중첩 SubscriptionItem 의 Price 13 + Product 11 = 총 ~80 라인). `parse_event` = serde_json::from_value → paddle_rust_sdk::entities::Event. **3 신규 tests**: (a) `test_subscription_created_event_json_deserializes_via_paddle_sdk` = wire-format sanity (Paddle SDK deserialize 성공 + sub.id / customer_id / custom_data 검증). (b) `test_process_webhook_event_subscription_created_inserts_db_row` = User 시드 → Event 구성 → process_webhook_event 호출 → payment_subscription 행 INSERT verify. (c) `test_process_webhook_event_is_idempotent_for_same_event_id` = 동일 event_id 2회 호출 = subscription 행 1건 유지 (is_webhook_event_processed 멱등성 검증). **인프라**: `#![recursion_limit = "512"]` (nested json! macro), `.env.test` 신규 (gitignored, 32-byte HMAC + ENCRYPTION_KEY_V1 ephemeral). **검증**: `cargo test --test payment_integration -- --ignored` = **11 passed** (기존 8 + 신규 3). cargo test --lib 184 passed / clippy --all-targets clean / fmt clean. **defer 트랙 = 나머지 6 variants**: subscription.activated / updated / canceled / paused / past_due / trialing + transaction.completed + adjustment.created/updated. 본 PR 의 helper + 패턴 = 후속 트랙 재사용 가능 (price_id / user_id 만 변경). | 2026-05-11 | 나머지 6 variants (별도 PR 후보) / 세션 종결 정리 |
| 124 | G10-frontend 후속 — 컴포넌트 smoke 2 + hook 1 + api util 1 = 16 신규 tests / 138 passed | 품질 | **2026-05-11 후속³** — Track 3. **신규 4 파일**: (a) `frontend/src/components/blocks/feature_grid.test.tsx` (3 tests: title/desc/icon 렌더 / empty grid / 반응형 클래스). (b) `frontend/src/components/blocks/hero_section.test.tsx` (6 tests: marketing variant 기본 / badge+subtitle+children optional / size sm vs default py-section / variant=list 레이아웃 / className 전파). (c) `frontend/src/category/payment/payment_api.test.ts` (5 tests: getPaymentPlans happy / getSubscription / cancelSubscription POST body 검증 / 5xx ApiError / error envelope `{error:{message}}` 추출). (d) `frontend/src/category/payment/hook/use_payment_plans.test.tsx` (2 tests: isLoading→isSuccess + isError). **MSW 패턴 정착** = health_page.test 의 server.use + http.get 동일 패턴. **vitest.config.ts 갱신**: coverage whitelist 4 추가 (feature_grid / hero_section / payment_api / use_payment_plans). **검증**: vitest run --coverage = 138 passed (이전 122 + 신규 16) / 26 파일 / coverage thresholds 90/75/60/90 perFile 통과. category/payment = 100% / blocks/feature_grid = 100% / blocks/hero_section = 100%. npm run build 15.88s clean / lint 0 errors. **payment_api.ts 의 cancelSubscription Tier 2 + getSubscription 도 같이 cover** (perFile threshold 90% 만족용). | 2026-05-11 | C-payment-event 1일+ |
| 123 | C-doc-sync — N-27 종결 후 신규 admin GET/DELETE/stats 21 endpoint OpenAPI 등록 | 문서/품질 | **2026-05-11 후속²** — Track 2. N-27 (2026-05-06 종결) 후 신규 추가된 admin endpoint 21건 (path 기준 15 unique) 의 OpenAPI 누락 정합성 정리. **분류**: (a) admin/user logs 2 = `/admin/users/{user_id}/{admin,user}-logs`, (b) admin/video preview 1 = `/admin/videos/{video_id}/preview`, (c) admin GET detail 4 (paths 중복 = lesson/study/video/study-task 의 기존 PATCH path 와 같은 URL), (d) admin DELETE 2 (lesson item / bulk), (e) admin/video stats 3 + admin/study stats 3 + admin/user-login stats 5 = stats 11. **검증 인프라**: `src/docs.rs::tests::openapi_spec_includes_doc_sync_paths` 신규 test (13 unique path 표본). `openapi_spec_summary_sanity` 의 baseline 갱신 (paths 100→130 / schemas 130→160). **결과**: paths 121→136 (+15) / schemas 334→367 (+33 = 신규 stats DTO + admin GET/DELETE DTO 가 utoipa 의 auto-resolve 로 자동 포함). webhook 정책 (handle_webhook 제외) 유지. **검증**: cargo test --lib 184 passed (+1 신규 test) / clippy --all-targets clean / fmt clean. **메모리 정정**: "C-doc-sync N-27 ~43건" 은 stale — N-27 은 2026-05-06 종결, 본 작업은 그 후 신규 endpoint 의 hygiene maintenance | 2026-05-11 | G10-frontend 후속 / C-payment-event |
| 122 | G2-1 ✅ 해결 — e2e vite cold start 안정화 (login_flow dormant 해제) | 품질/CI | **2026-05-11 후속** — 옵션 a 채택 = vite preview + production build 전환. **3 파일 변경**: (1) `.github/workflows/e2e.yml` = `npm run dev` → `npm run build` step + `npm run preview -- --host 0.0.0.0 --port 5173`. CI runtime +20s 비용 vs cold-compile risk 원천 차단. (2) `frontend/vite.config.ts` = `preview.proxy` 추가 (server.proxy mirror). vite preview 는 `server.proxy` 미사용 = 별도 `preview` 키 명시 필요. (3) `frontend/e2e/login_flow.spec.ts` = `test.describe.skip` → `test.describe`, `test.setTimeout(120_000)` 제거 + `emailInput.waitFor({ timeout: 90_000 })` 제거 → default 회복, `waitUntil: "domcontentloaded"` → default `load`, `expect(page).toHaveURL` timeout override 제거. **검증**: `npm run build` 16.49s clean. **부채 변화**: G2-1 ✅ → G 3→2, 31 → **30** (어제 신규로 발견된 부채 다음 세션 즉시 해결). 로컬 dev 워크플로우 (`npm run dev`) 영향 0 = README 갱신 불필요. | 2026-05-11 | C-doc-sync 0.5일 / G10-frontend 후속 / C-payment-event 1일+ |
| 59 | 한글 자판 연습 Phase 1 — DB + 백엔드 + 관리자 CRUD + 세션 API + 프론트 타입/훅 + HangulKeyboard + 연습 페이지 + 통계 대시보드 + 관리자 프론트 writing 폼 + 자유 연습 시드 + 자유 연습 실연결 + **Playwright E2E** | 기능/풀스택 | 설계 플랜 P1~P10 **전부 완료 (100%)**. **P10-C**: Playwright E2E — `frontend/playwright.config.ts` + `frontend/e2e/{fixtures/auth.ts, writing_practice.spec.ts, README.md}` 신규. 플로우: `/studies/writing` → 초급 → 자모 → 세션 시작 응답 대기 → 시드 prompt `pressSequentially` → "결과 확인" → 결과 카드 stat 라벨 검증 → `/studies/writing/stats` + `GET /studies/writing/stats?days=1` 서버 값 전/후 비교(total_sessions +1 이상). `frontend/vite.config.ts` 의 proxy target 을 `VITE_PROXY_TARGET` env 로 override 가능하게 변경 (다른 프로젝트가 3000 점유 시 우회용). `package.json` `test:e2e` 스크립트 추가. `.gitignore` 에 `frontend/test-results/`, `frontend/playwright-report/` 추가. **WritingTask StrictMode 버그 수정**: 기존 `useStartWritingSession().mutate(..., {onSuccess})` + `useRef(false)` 가드 패턴이 React 19 StrictMode 이펙트 이중 호출 + TanStack Query mutation observer double-invocation 파괴와 맞물려 자유 연습 모드에서 sessionId 가 영구 null 로 남는 버그가 E2E 에서 재현됨. `startWritingSession` 직접 호출 + cancelled 플래그 cleanup 으로 대체. 검증: backend `BIND_ADDR=127.0.0.1:3100 EMAIL_PROVIDER=none` + frontend `VITE_PROXY_TARGET=http://127.0.0.1:3100 npm run dev` + `npm run test:e2e` → 1 passed (6.4s), `npm run build` 9.56s 클린. **이전 P1~P10-B 요약**: **P10-B**: 프론트 자유 연습 실연결 — `frontend/src/category/study/types.ts` seed 스키마 3종(Req/Item/Res) 추가. `study_api.ts`에 `getWritingPracticeSeed` 함수 추가 (비인증 GET /studies/writing/practice). `hook/use_writing_practice_seed.ts` 신규 (staleTime 10분). `WritingTask.tsx` props `taskId: number | null`로 변경, mutate 시 study_task_id=null 허용. `writing_practice_page.tsx` 완전 리라이트 — "준비 중" 카드 제거, `FreePracticeRunner` 서브 컴포넌트(시드 로드 → currentIndex 순차 → WritingTask 재마운트 key 전략 + 결과 카드 → 다음 문제 버튼 → 전체 완료 시 처음부터/다른 유형 선택 CTA). 세션 네비게이션 정책 A안(한 세션=1 아이템). 초급만 answer 전달(study_task_page와 동일 정책). i18n 키 교체: freePracticeComing* 3종 제거, freePracticeProgress/freePracticeLoadError/freePracticeEmpty/finishItem/finishAll/nextItem/allCompleted/restart/selectOther 9종 ko/en 추가. `npm run build` 클린 통과. **P10-A**: 자유 연습 시드 컨텐츠 + 백엔드 엔드포인트 — `migrations/20260413_writing_practice_seed.sql` 신규(`writing_practice_seed` 테이블 + UNIQUE/Index + INSERT 190개: 초급 jamo 40/syllable 30/word 30, 중급 word 30/sentence 30, 고급 sentence 20/paragraph 10). DTO `WritingPracticeSeedReq`/`Item`/`Res`, repo `list_writing_practice_seed`, service + handler `list_writing_practice_seed`, router `GET /studies/writing/practice` (비인증). OpenAPI(docs.rs) 등록. `cargo sqlx prepare` + `cargo check` 클린 통과. 로컬 DB COUNT 검증 완료. 다음 P10-B: 프론트 자유 연습 실연결 → P10-C: Playwright E2E. **P9**: `frontend/src/category/admin/study/types.ts` writing 필드 4개(writing_level/writing_practice_type/writing_hint/writing_keyboard_visible)를 `studyTaskCreateReqSchema`/`studyTaskUpdateReqSchema`/`studyTaskUpdateItemSchema`/`adminStudyTaskDetailResSchema`에 추가. `admin_study_create.tsx` 단일/벌크 Task 탭에 writing 드롭다운 + 조건부 writing 폼 박스 (level Select, practice_type Select, hint Input, keyboard_visible checkbox). `parseCSV`에 writing 컬럼 4개 헤더 파싱 + level/type enum 화이트리스트 + writing row 검증(prompt+answer+level+type 필수), validKinds에 writing 추가, CSV 포맷 가이드 업데이트. `onCSVTasksSubmit` 매핑에 writing 4필드 전달. `admin_study_detail.tsx` TaskDetailsContent에 writing 읽기 전용 카드 + badge variant 분기. `npm run build` 8.43s 클린 통과. **P8**: `writing_stats_page.tsx` 신규 (/studies/writing/stats) — `useWritingStats({days})` 훅 기반. 상단 기간 선택 Select (7/14/30/60/90/180/365일). Summary 카드 3개 (총 세션/평균 정확도/평균 CPM, Keyboard·Target·Gauge 아이콘). LevelBreakdownCard (레벨별 세션 progress bar + 정확도/CPM 라인, LEVEL_ORDER 상수로 초→중→고 정렬). WeakCharsCard (취약 글자 Top 10, destructive 카드 + 상대 bar width). DailyTrendCard (날짜 내림차순 테이블: 세션/정확도/CPM). totalSessions=0 시 empty CTA 카드. 라우트 `/studies/writing/stats` Private 등록 — React Router v6 static segment "stats"가 `:level` 파라미터보다 우선 매칭되어 순서 무관. level_select_page에 P7에서 임시 제거했던 stats 버튼 복원. i18n `study.writing.stats.*` 30+ 키 추가. `npm run build` 클린 통과 (9.78s). **P7**: 연습 페이지 + `study_task_page.tsx` writing 분기 — **P7**: 연습 페이지 + `study_task_page.tsx` writing 분기 — `component/writing/WritingPracticeInput.tsx` (컴포지션 이벤트 기반 textarea, 초급=글자별 실시간 피드백 green/red/gray, 오탈자 누적 추적, 다음 기대 자모 추출), `WritingResultPanel.tsx` (세션 결과 카드: accuracy/CPM/오답 글자 하이라이트), `WritingTask.tsx` (WritingPracticeInput + HangulKeyboard 합성, 태스크 마운트 시 `useStartWritingSession` 1회 호출 → session_id 상태 끌어올림). `study_task_page.tsx`: writing 분기 채움 — writingText/writingStats/writingSessionId/writingResult state, handleSubmit writing 분기에서 `finishWritingSession` + `submitAnswer({kind:"writing", session_id})` 순차 호출 후 결과 렌더링. 페이지 2개 신규: `writing_level_select_page.tsx` (/studies/writing, 3단 레벨 카드 + 통계 링크), `writing_practice_page.tsx` (/studies/writing/:level/:practiceType, 유형 선택 + P10 자유연습 준비중 카드 — 시드 전 빈 상태). 라우트 3개 Private 추가. i18n 30+ 키 추가 (레벨명/유형명/결과 라벨). `npm run build` 클린 통과. **P6**: `frontend/src/category/study/component/writing/` 신규 — `keyboard_layout.ts` (2벌식 KS X 5002 DUBEOLSIK_ROWS + KeyCap 타입 + findKeyForJamo + decomposeSyllable (U+AC00~U+D7A3 초/중/종성 분해)), `HangulKeyboardKey.tsx` (개별 키 버튼, 기본/Shift/영문 3단 라벨, primary/amber ring highlight), `HangulKeyboard.tsx` (Props: highlightKeys/onKeyPress/visible/onToggle/level/disabled/className. 초급=항상 표시, 중급/고급=toggle 존중, 숨김 시 "키보드 보기" 버튼만 노출). i18n `study.writing.showKeyboard`/`hideKeyboard` 키 추가. **P6**: `frontend/src/category/study/component/writing/` 신규 — `keyboard_layout.ts` (2벌식 KS X 5002 DUBEOLSIK_ROWS + KeyCap 타입 + findKeyForJamo + decomposeSyllable (U+AC00~U+D7A3 초/중/종성 분해)), `HangulKeyboardKey.tsx` (개별 키 버튼, 기본/Shift/영문 3단 라벨, primary/amber ring highlight), `HangulKeyboard.tsx` (Props: highlightKeys/onKeyPress/visible/onToggle/level/disabled/className. 초급=항상 표시, 중급/고급=toggle 존중, 숨김 시 "키보드 보기" 버튼만 노출). i18n `study.writing.showKeyboard`/`hideKeyboard` 키 추가. **이하 P1~P5**: **P1**: 마이그레이션 `20260412_writing_practice.sql` — `study_task_kind_enum`에 `writing` 추가, 신규 enum 2종(writing_level: beginner/intermediate/advanced, writing_practice_type: jamo/syllable/word/sentence/paragraph), 테이블 2개 (`study_task_writing` 서브테이블, `writing_practice_session` 통계), 인덱스 7개. **P2**: `WritingPayload` DTO + `TaskPayload::Writing` + `SubmitAnswerReq::Writing{text,session_id}` + find_task_detail/find_answer_key SQL에 LEFT JOIN 추가. 초급 레벨에만 answer 응답 포함 (클라이언트 실시간 피드백). **P3**: 관리자 단일/벌크 CRUD 확장 (question/answer 재사용, writing_level/writing_practice_type/writing_hint/writing_keyboard_visible 신규 필드), create_task_writing repo 함수, update match Writing 분기. **P4**: 세션 API 4개 엔드포인트 — `POST /studies/writing/sessions` (시작), `PATCH /studies/writing/sessions/{id}` (완료, 서버 사이드 accuracy/CPM 계산), `GET /studies/writing/sessions` (목록, level/finished_only 필터 + 페이지네이션), `GET /studies/writing/stats` (days=1~365, total/avg_accuracy/avg_cpm + 레벨별 + 일별 추이 + 취약 글자 Top 10, jsonb_array_elements LATERAL JOIN). UPDATE WHERE user_id 소유권 검증. **P5**: 프론트 타입/API/훅 — `types.ts`에 Zod 스키마 11종 + discriminated union에 writing variant 추가, `writingPayloadSchema` 추가. `study_api.ts`에 세션 4개 함수 + sanitizeParams 제네릭화. `hook/use_writing_session.ts` 3개 훅(start/finish/list), `hook/use_writing_stats.ts` 1개 훅. `studyTaskKindSchema` 확장으로 기존 `Record<StudyTaskKind, ...>` 매핑 3곳 writing 엔트리 추가(PenLine 아이콘, i18n `study.kindWriting` 키). npm run build 클린 통과 | 2026-04-14 | — |
| 56 | Figma Phase A 완료 + 상시 운영 도구로 전환 | UI/도구 | `frontend/figma-capture/` 신규 — Playwright 기반 레퍼런스 캡처 도구 일체. 1440×900 Retina viewport, Vite webServer 자동 기동, ko-KR 로캘. `document.fonts.ready` + 점진 스크롤 + img decoded 대기 + next-themes localStorage 주입으로 기존 34프레임의 3대 문제(한글/lazy/토큰) 근본 해결. 16 페이지 × Light/Dark = **32 PNG** 생성. textbook/ebook catalog API 모의 응답으로 백엔드 부재 시에도 카탈로그 렌더링 보장. **2026-04-10**: Figma Phase B/C 보류 결정 — Phase A 도구를 디자인 작업 상시 시각 레퍼런스 + 시각 회귀 감지 도구로 위치 재정의. 3계층 SSoT(AMK_DESIGN_SYSTEM.md + 32 PNG + 코드) 확정. 상세: `AMK_DESIGN_SYSTEM.md §08` | 2026-04-09 ~ 2026-04-10 | Phase B/C는 디자이너 영입/멀티 플랫폼 일관성 필요 시 재개 |
| 121 | 2026-05-11 세션 종결 — 3 PR / B5 트랙 완전 종결 + G2 안정화 트랙 발견 | 종합 | **2026-05-11 일일 종결**. KKRYOUN ↔ origin/main = 53a3296. **본 세션 PR #270~#272 (3 PR)**: #270 docs 일일 종결 (어제 commit 머지) + #271 B5 Tier 2 reqwest builder 6건 Result 전파 → **B5 트랙 완전 종결** (🟢 44 + 🟡 0 + 🔴 0) + #272 G2 e2e 시나리오 추가 (login_flow.spec dormant 마킹). **부채 변화**: B5 트랙 완전 종결 / G2 e2e 안정화 트랙 새 발견 (login page React.lazy chunk cold-compile 안정성 문제, 해제 조건 = vite preview webServer / dev warmup / chunk 사전 컴파일). **production**: 3 deploy success, /health 200 유지. **다음 세션 진입점**: G2 e2e 안정화 ⭐ 0.3-0.5일 (login_flow dormant 해제) / C-doc-sync ⭐ 0.5일 / C-payment-event ⭐⭐⭐ 1일+ / G10-frontend 추가 page-level. | 2026-05-11 | 다음 세션 |
| 120 | [3/4] G2 e2e 후속 — login_flow.spec.ts 추가 (1 spec → 2 spec) | 품질/CI | **2026-05-11** — 본 세션 권장 4단계 [3/4]. e2e CI 안정화 트랙 시나리오 확장. **신규 `frontend/e2e/login_flow.spec.ts`** = `/login` 페이지 진입 → `input[name=email]` + `input[name=password]` 입력 (fixtures TEST_EMAIL/PASSWORD) → `button[type=submit]` 클릭 → `useLogin` mutation onSuccess → `navigate("/about")` → `expect(page).toHaveURL(/\\/about$/, timeout: 10s)`. **E2E 수트** = writing_practice.spec.ts (P10-C, 2026-04-14) + login_flow.spec.ts (본 PR). **rate limit**: RATE_LIMIT_LOGIN_MAX=10 / WINDOW=900s. CI run = login_flow 1회 + writing_practice 1회 = 2회 (안전 마진 8). **검증**: build 17.59s / lint 0 / unit test 122 (영향 0). 실 e2e 검증 = 본 PR push 후 e2e.yml workflow run. | 2026-05-11 | #4 본 세션 종결 |
| 119 | [2/4] B5 Tier 2 reqwest builder 6건 Result 전파 → B5 트랙 완전 종결 | 보안/품질 | **2026-05-11** — 본 세션 권장 4단계 [2/4]. **6 파일 new() 시그니처 변경**: `src/external/{vimeo,revenuecat,ipgeo,apple,email,google}.rs` 모두 `pub fn new(...) -> AppResult<Self>` 로 변환. builder fail 시 `AppError::Internal` 전파. **Caller 9군 전파**: main.rs startup 3 = `.expect("...at startup")` (Tier 1 fail-fast) / auth/service.rs runtime 3 (build_google_client wrapper + 2 직접 호출) = `?` 전파 / admin/video/service.rs runtime 3 = `?` / tests/common + auth_oauth_integration + mod tests 4 = `.expect("...in test")`. **IpGeoClient::Default impl 제거** (호출처 0 + Result 변환 비호환). **검증**: cargo check --tests clean / cargo test --lib 183 / clippy --all-targets clean / fmt clean. **B5 트랙 종결**: 🟢 44 + 🟡 0 (~~auth:447 + reqwest 6~~ 모두 ✅) + 🔴 0 = 위험 expect 0건. | 2026-05-11 | #3 G2 e2e 후속 / #4 종결 |
| 118 | 2026-05-10 세션 완전 종결 — 15 PR / 125+ 신규 tests / 부채 31→30 | 종합 | **2026-05-10 일일 종결**. KKRYOUN ↔ origin/main = 561baba 정렬, production 5 deploy success / /health 200 유지. **본 세션 누계 PR #255~#269 (15 PR)**: G10-frontend Phase 1~9 (#255~#263 = 9 PR / frontend 0→117 tests / vitest+RTL+jsdom+msw 인프라 + 16 모듈 화이트리스트 + perFile threshold) + G10-deep-2 (#264 = lib 175→183 dto validators) + 권장 5단계 (#265~#269 = 5 PR / page-level + extractor / CI 캐시 / Dependabot + axios+ip-address / G2 e2e 첫 도입 / B5 expect Tier 3 변환). **부채 변화**: G2 ✅ 해결 (#268) + B5 auth:99 dummy hash 추가 cleanup (#269) → AMK_DEBTS §0 31→30. **production 영향 0** (모든 변경 = 기존 동작 호환, env 미설정 시 기본 path 유지). **다음 세션 진입점**: C-payment-event ⭐⭐⭐ 1일+ / C-doc-sync ⭐ 0.5일 / B5 Tier 2 잔여 ⭐ 0.2일 (external/* reqwest builder 3건) / G10-frontend 추가 page-level (auth/login_page / payment/pricing_page) / G2 e2e 안정화 후속 (required check 등재 / pr-check 통합). | 2026-05-10 | 다음 세션 |
| 117 | [5/5] B5 expect 분류 종결 + hot path 1건 변환 (dummy_password_hash) | 보안/품질 | **2026-05-10 (후속²¹)** — 사용자 권장 5단계 [5/5] **마지막**. 본 세션 완전 종결. **B5 분류 결과** (production code, mod tests 제외): Tier 1 startup fail-fast 의도 (config.rs ~36건 env parsing + main.rs 6건 init = ~42건, 변환 부적합 = 잘못된 설정 즉시 알림) / Tier 2 매우 낮음 (external/{vimeo,revenuecat,ipgeo}.rs reqwest Client::builder 3건 = default config 사실상 unreachable, 별도 PR) / **Tier 3 hot path 1건 = `auth/service.rs:123` `dummy_password_hash`** (모든 login attempt 의 anti-enumeration path). **변환**: OnceLock get_or_init 클로저 안의 `.expect("argon2 dummy hash should succeed")` 제거 → fallible match 패턴 (argon2 fail 시 AppError::Internal 전파). fn 시그니처 그대로 (`AppResult<PasswordHash<'static>>`) → 호출자 영향 0. 캐시 동작 동일 (첫 호출 1회 hash, 이후 hit). argon2 default config = fail 사실상 unreachable, 단 B5 정책상 hot path expect 제거 (panic 회피 = future-proof). **검증**: cargo test --lib 183 (변경 없음) / clippy / fmt clean. **G10 누계** = 415 / 424. **권장 5단계 모두 종결** ✅. **본 세션 누계 = PR #255~#269 (15 PR, 125+ 신규 tests, frontend 0→117, lib 175→183)**. | 2026-05-10 | (다음 세션) C-payment-event / C-doc-sync / B5 Tier 2 잔여 / G10-frontend 추가 page-level / G2 e2e 안정화 후속 |
| 116 | [4/5] G2 Playwright e2e CI 도입 — 별도 workflow + 첫 도입 안정화 트랙 | 품질/CI | **2026-05-10 (후속²⁰)** — 사용자 권장 5단계 [4/5]. **설계 결정 = 별도 workflow** (`.github/workflows/e2e.yml`). pr-check.yml 통합 X = 첫 도입 시 안정성 미검증 → 모든 PR 차단 위험 회피. branch protection required check 미등재 → fail 시 머지 가능 (안정화 트랙). **trigger**: push KKRYOUN + workflow_dispatch. **timeout** 20min. **service container** postgres:16 + redis:7-alpine (pr-check integration 패턴 재사용). **cache** Swatinem/rust-cache@v2 shared-key="backend" (dep 공유). **단계**: checkout → Rust toolchain → cargo cache → HMAC+ENC key 생성 → migrations 적용 (G16 lex order) → cargo build --bin amazing-korean-api (debug, ~1-2min) → backend 백그라운드 → /healthz 30회 polling → E2E 테스트 계정 생성 (POST /users) → setup-node + npm ci + playwright install chromium → vite dev 백그라운드 → vite polling → npm run test:e2e → fail 시 playwright-report + backend.log artifact (7일 retention). **시나리오** = `frontend/e2e/writing_practice.spec.ts` (P10-C, 1 spec, 자유 연습 1회 → stats total_sessions +1 검증). **검증**: yaml syntax OK. 실 동작 = 본 PR push 후 첫 run 관찰. **G10 누계** = 415 / 424 (코드 변경 0). 다음 [5/5] B5 expect. | 2026-05-10 | 안정화 후 (a) required check 등재 / (b) pr-check 통합 결정 / [5/5] B5 expect |
| 115 | [3/5] Dependabot auto-merge + axios/ip-address 부채 종결 | 품질/보안 | **2026-05-10 (후속¹⁹)** — 사용자 권장 5단계 [3/5]. 두 변경 묶음. **(1) Dependabot auto-merge workflow 신규** (`.github/workflows/dependabot-auto-merge.yml`): trigger=pull_request / actor 검증 = `dependabot[bot]` (다른 사람 PR 차단) / `dependabot/fetch-metadata@v2` 로 update-type 파싱 / patch+minor → `gh pr review --approve` + `gh pr merge --auto --squash` / major = 수동 (breaking changes 위험). branch protection 의 모든 required check + linear history 통과 시만 발동. **(2) axios + ip-address 부채 동시 종결**: npm audit fix 로 transitive resolve. axios 1.15.0→1.16.0 (direct, 13 prototype pollution advisory 종결) / ip-address 10.1.0→10.2.0 (puppeteer→@puppeteer/browsers→proxy-agent→socks-proxy-agent→socks 경로, Address6 XSS 종결). package.json range 그대로 (^1.15.0) / package-lock.json 만 업데이트. **0 vulnerabilities** 달성. **검증**: yaml 문법 OK / npm audit clean / test 122 / build 16.32s / lint 0. **G10 누계** = 415 / 424 (코드 변경 0, lockfile 만). 다음 [4/5] G2 e2e. | 2026-05-10 | [4/5] G2 e2e / [5/5] B5 expect |
| 114 | [2/5] CI 캐시 최적화 — pr-check.yml rust-cache 옵션 보강 (shared-key + save-if + cache-on-failure) | 품질/CI | **2026-05-10 (후속¹⁸)** — 사용자 권장 5단계 [2/5]. 인프라 측 작은 PR (실 코드 변경 0). **진단**: 기존 = `Swatinem/rust-cache@v2` default 옵션 (main 만 save → KKRYOUN 단일 브랜치 정책에서 cache miss 누적) + setup-node cache: npm 이미 OK. **변경**: pr-check.yml backend + integration job 양쪽에 옵션 명시. `shared-key: "backend"` (두 job 간 dep cache 재사용) + `save-if: KKRYOUN` (KKRYOUN push 마다 cache 누적) + `cache-on-failure: "true"` (compile fail 해도 dep cache 저장 → 재시도 hit). **예상 효과** (PR #265 기준): backend 54s → 30-40s / integration 2m47s → 1m30s 추정. frontend 1m16s 변경 없음 (npm cache 이미 적용). **검증**: yaml 문법 OK / cargo test --lib 183 변경 없음. **G10 누계** = 415 / 424 passed (본 PR 코드 변경 0). 다음 [3/5] Dependabot. | 2026-05-10 | [3/5] Dependabot / [4/5] G2 e2e / [5/5] B5 expect |
| 113 | [1/5] G10-frontend page-level (VideoListPage) + G10-deep-2 (extractor) 합치기 — 5 frontend + 5 backend(#[ignore]) | 품질/CI | **2026-05-10 (후속¹⁷)** — 사용자 권장 5단계 순서 진입 [1/5]. **frontend** = `video_list_page.test.tsx` 5 (loading SkeletonGrid / empty EmptyState / success VideoCard 2 + ListStatsBar / totalPages=1 PaginationBar 미렌더 / Next 클릭 page=2 재요청). MemoryRouter + QueryClientProvider + react-i18next + i18next + sonner vi.mock. **backend** = `tests/auth_extractor_integration.rs` 5 #[ignore] (유효 Bearer / 헤더 누락 / Bearer prefix 부재 / malformed / 다른 secret). admin_role_guard 패턴 재사용. **vitest.config.ts**: include 19→20 (video_list_page 추가, 측정 100/93.75/100/100). 잔여 video 단위 (video_card/use_video_list/video_api) = 별도 PR. **검증**: cargo test --lib 183 / clippy / fmt clean / npm run test 122 passed (117→122) / coverage All 98.72/92.48/91.30/98.72 (perFile 통과) / build 16.67s / lint 0. **G10 누계**: 293 backend (lib) + 122 frontend = **415 신규 / 424 passed**. 다음 [2/5] = CI 캐시 최적화. | 2026-05-10 | [2/5] CI 캐시 / [3/5] Dependabot / [4/5] G2 e2e / [5/5] B5 expect |
| 112 | G10-deep-2 = backend dto validators 8 신규 / lib 175 → 183 | 품질/CI | **2026-05-10 (후속¹⁶)** — G10-frontend 9 PR 마무리 후 **순차 #2 G10-deep-2 (backend, 0.3일) 진입**. **신규 8 lib tests**: (a) `src/api/auth/dto.rs` 6 = validate_birthday 5 (iso accept / 길이 != 10 reject / 잘못된 calendar reject / non-numeric reject / wrong separator reject) + default_true 1, (b) `src/api/video/dto.rs` 2 = default_page=1, default_per_page=20. **잔여 G10-deep-2 검토**: lib 175 의 거의 전 helpers 가 이미 cover (auth/jwt 7, password 6, refresh helpers 7, mask_email 5 etc.). 잔여 = dto validators (module-private fn, mod tests 부재) → 본 PR 으로 종결. **검증**: `cargo test --lib` = **183 passed** (175 + 8). clippy / fmt clean. **G10 누계**: **293 backend + 117 frontend = 410 신규 / 419 passed**. | 2026-05-10 | 순차 #3 C-payment-event (Paddle Subscription, 1일+) / B5 expect (0.5일) / C-doc-sync (0.5일) |
| 111 | G10-frontend Phase 9 — HealthPage page-level + 4 신규 / 117 누계 (19 모듈 화이트리스트) | 품질/CI | **2026-05-10 (후속¹⁵)** — G10-frontend 트랙 sub-step. 후속⁹ defer (b) 처리. **page-level 통합 패턴 첫 정착** (TanStack Query + msw). **HealthPage page-level smoke** (`src/category/health/page/health_page.test.tsx`, 4 tests): QueryClientProvider wrapper (retry: false). (1) loading 50ms 지연 → "Checking Server Status..." / (2) success {status,uptime_ms,version} → 12345 / v1.2.3 / ok badge / (3) 500 → "Server Offline" + offline badge / (4) refetch button → 두번째 응답 status=degraded → badge 갱신. **vitest.config.ts**: include 화이트리스트 16→19 (health_api + use_health + health_page). 측정 = health_api 100/100/100/100, use_health 100/100/100/100, health_page 98.21/94.44/100/98.21. **검증**: `npm run test` = **117 passed / 21 files / 13.50s** (113 → 117). coverage All 98.33/92.18/88.88/98.33 (perFile 통과). build 16.58s / lint 0. **G10 누계**: 285 backend + 117 frontend = **402 신규 / 411 passed**. **본 세션 G10-frontend Phase 1~9 = 9 PR (#255~#262 + 본 PR 예정)**. 후속 page-level = QueryClientProvider + msw 패턴 재사용. | 2026-05-10 | 다른 page (auth/login_page / video_list_page) 동일 패턴 / 순차 #2 G10-deep-2 |
| 110 | G10-frontend Phase 8 — msw + axios 인터셉터 통합 / 6 신규 / 113 누계 (16 모듈 화이트리스트) | 품질/CI | **2026-05-10 (후속¹⁴)** — G10-frontend 트랙 sub-step. 후속⁹ defer (d) 처리. **msw 도입** (devDeps msw@^2.14.5 + `src/test/server.ts` + setup.ts lifecycle hook = beforeAll listen + afterEach resetHandlers + afterAll close). **`client.integration.test.ts` 6 tests** (msw 기반): (1) request 인터셉터 Authorization 자동 부착 / (2) 401 → /auth/refresh → 신규 토큰 store login → 원 요청 retry (새 Authorization) / (3) refresh fail → logout + window.location.href=/login (stub) / (4) 비-2xx → ApiError (status+parsed envelope) / (5) 204 → undefined / (6) skipAuthRefresh → refresh 우회. **vitest.config.ts**: include 15 → 16 (client.ts 추가). client.ts 측정 = 97.61/83.33/100/97.61 (uncovered = `if (axios.isAxiosError(error))` else). **검증**: `npm run test` = **113 passed / 20 files / 12.11s** (107 → 113). coverage All 98.32/91.76/87.80/98.32 (perFile 통과). build 16.54s / lint 0. **G10 누계**: 285 backend + 113 frontend = **398 신규 / 407 passed**. **본 세션 G10-frontend Phase 1~8 = 8 PR (#255~#261 + 본 PR 예정)**. 잔여 = (b) page-level — msw 인프라 정착으로 후속 PR 기존 재사용. | 2026-05-10 | (b) page-level (TanStack Query + MemoryRouter + msw 재사용) / 순차 #2 G10-deep-2 |
| 109 | G10-frontend Phase 7 — Header 통합 smoke + 6 신규 / 107 누계 (15 모듈 화이트리스트) | 품질/CI | **2026-05-10 (후속¹³)** — G10-frontend 트랙 가장 큰 컴포넌트 통합. **Header 통합 smoke** (`src/components/layout/header.test.tsx`, 6 tests): sub-component vi.mock 패턴 (react-i18next + @/i18n 모듈 + useUpdateSettings TanStack mutation + LogoutButton stub + ThemeToggle stub). 검증 = (1) brand + nav 5 href / (2) 로그아웃 분기 = Login·Signup / (3) 로그인 분기 = MyPage·LogoutButton stub / (4) mobile menu max-h class 토글 / (5) 언어 dropdown 클릭 → changeLanguage 호출 (로그아웃 시 settings X) / (6) 로그인 + 언어 변경 → useUpdateSettings.mutate. **vitest.config.ts**: include 화이트리스트 14→15 (header.tsx 추가) + branches floor 85→75 (NavLink isActive·tier separator 직접 호출 어려움). **검증**: `npm run test` = **107 passed / 19 files / 11.19s** (101 → 107). coverage All 98.39/93.19/86.48/98.39 (perFile 통과). build 16.51s / lint 0. **G10 누계**: 285 backend + 107 frontend = **392 신규 / 401 passed**. **본 세션 G10-frontend 누적 PR #255~#260 + 본 PR (예정)** = Phase 1~7 정착. | 2026-05-10 | (b) page-level (TanStack Query + MemoryRouter) / (d) msw + 인터셉터 / 순차 #2 G10-deep-2 |
| 108 | G10-frontend Phase 6 — coverage threshold 점진 도입 (perFile + 14 모듈) | 품질/CI | **2026-05-10 (후속¹²)** — G10-frontend 트랙 인프라 측 마무리 단계. 후속⁹ defer (e) 처리. **vitest.config.ts coverage 강화**: (1) `include` 화이트리스트 14 모듈 명시 (lib + utils + hooks 2 + api 2 + components/blocks 7 + components/layout/footer). 광범위 미커버 영역 (`src/category/*` ~200 파일) 의도적 제외 = 측정 시그널 명료화. 신규 모듈 cover 시 본 리스트에 명시적 추가. (2) perFile thresholds = stmts 90 / branches 85 / funcs 60 / lines 90 (현재 측정값 stmts 99.07 / branches 99.05 / funcs 90 / lines 99.07 → 회귀 방지 floor). 보수적 floor 근거: pagination_bar 60% funcs (pointer-events-none disabled click 콜백 미커버) / footer 66.66% funcs (Dialog onOpenChange) / pagination_bar 91.3% stmts·lines. 점진 상향 가능. **검증**: `npm run test:coverage` thresholds 통과 / `npm run test` 101 passed / build 16.96s / lint 0. **G10 누계**: 285 backend + 101 frontend = **386 신규 / 395 passed** (test 수 동일, 인프라 보강만). | 2026-05-10 | (a-2) Header 통합 / (b) page-level (TanStack Query + MemoryRouter) / (d) msw + 인터셉터 |
| 107 | G10-frontend Phase 5 — Footer 통합 smoke + 6 신규 / 101 누계 (100 이정표 돌파) | 품질/CI | **2026-05-10 (후속¹¹)** — G10-frontend 트랙 sub-step. **Footer 통합 smoke** (`src/components/layout/footer.test.tsx`, 6 tests): react-i18next vi.mock + MemoryRouter wrapper. brand name+description / mailto link / quick links 4건 href / support section legal 4건 href / copyright vi.useFakeTimers 로 currentYear 2026 시뮬 / 인증 Dialog (Radix) 클릭 → role=dialog + 이미지 alt 매핑. **Header 는 별도 PR** (auth store + useUpdateSettings TanStack mutation + ThemeToggle + LogoutButton — provider 세트 부담 큼). **검증**: `npm run test` = **101 passed / 18 files / 10.09s** (95 → 101, 100 이정표 돌파). build 16.88s 클린 / lint 0. **G10 누계**: 285 backend + 101 frontend = **386 신규 / 395 passed**. | 2026-05-10 | (a-2) Header 통합 / (b) page-level (TanStack Query + MemoryRouter) / (d) msw + 인터셉터 / (e) coverage threshold (perFile) |
| 106 | G10-frontend Phase 4 — api/client refactor (모듈 분리) + 18 신규 / 95 누계 | 품질/CI | **2026-05-10 (후속¹⁰)** — G10-frontend 트랙 sub-step 자동 진행. 후속⁹ defer (c) 처리. **refactor**: `api/client.ts` 의 `parseErrorMessage` (40 lines) + `applyAuthorizationHeader` (15 lines) module-internal 이라 단위 test 불가했음 → 별도 파일 추출 (`src/api/parse_error_message.ts` + `src/api/apply_authorization_header.ts`). client.ts 179 → 117 lines 슬림화. `applyAuthorizationHeader` 의 시그니처 = axios `AxiosRequestConfig["headers"]` 정렬로 호환 보장. **신규 18 tests**: (a) parse_error_message 13 = falsy data 3 + string data 5 (envelope error.message / 평탄 message / 비-JSON raw / parsed but no message → raw / 빈 문자열 fallback) + object data 5 (envelope / 평탄 / 미일치 fallback / 비-string ignore / 빈 ignore), (b) apply_authorization_header 5 = undefined / null / `set()` 함수형 in-place + 동일 참조 / plain object 머지 (사본) / 기존 Authorization 덮어쓰기. **검증**: `npm run test` = **95 passed / 17 files / 9.81s** (77 → 95). build 17.23s 클린 / lint 0. **G10 누계**: 285 backend + 95 frontend = **380 신규 / 389 passed**. | 2026-05-10 | (a) Header/Footer 통합 / (b) page-level (TanStack Query mock + MemoryRouter) / (d) msw + 인터셉터 / (e) coverage threshold (perFile) 점진 도입 |
| 105 | G10-frontend Phase 3 — block component smoke 5 = 24 신규 / 77 누계 | 품질/CI | **2026-05-10 (후속⁹)** — G10-frontend 트랙 (1-2일) sub-step 자동 진행. **5 신규 파일 / 24 tests**: (a) `stat_card.test.tsx` 5 (numeric `toLocaleString` / string as-is / undefined → "-" / loading skeleton / zero), (b) `skeleton_grid.test.tsx` 6 (count=N children / count=0 / study-card vs video·content-card 모양 / columns=2·3·4), (c) `section_container.test.tsx` 5 (default `<section>` / `as` prop swap / size=sm·md·lg / container=narrow → `max-w-3xl`), (d) `cover_card.test.tsx` 4 (title+subtitle+action+img / `loading="lazy"` / button 시멘틱 / onClick), (e) `list_stats_bar.test.tsx` 4 (react-i18next vi.mock — totalLabel+page / isFetching loading / className). **인프라 보강**: `eslint.config.js` `globalIgnores` 에 `coverage` 추가 (`npm run test:coverage` 산출물 ESLint 오탐 방지). **검증**: `npm run test` = **77 passed / 15 files / 8.45s** (53 → 77). build 16.56s 클린 / lint 0 problems. **G10 누계**: 285 backend + 77 frontend = **362 신규 / 371 passed**. | 2026-05-10 | (a) Header / Footer 통합 (auth + i18n + router) / (b) page-level (TanStack Query mock + MemoryRouter) / (c) parseErrorMessage 모듈 분리 / (d) msw + 인터셉터 통합 / (e) coverage threshold (perFile) 점진 도입 |
| 104 | G10-frontend Phase 2 — hook + api + component smoke = 25 신규 / 53 누계 | 품질/CI | **2026-05-10 (후속⁸)** — 사용자 결정 = "🟢 1. 즉시 작업 가능 순차" (G10-frontend 후속). **5 신규 파일 / 25 tests**: (a) `hooks/use_auth_store.test.ts` 5 (initial / login user_id+token / access undefined null fallback / logout reset / logout `auth-storage` clear), (b) `hooks/use_language_sync.test.ts` 4 (useUserSettings + i18n vi.mock — 로그아웃 미호출 / 로그인+settings 1회 / appliedRef 가드 rerender / 사이클 리셋), (c) `api/client.test.ts` 4 (`ApiError` class — Error 상속 / status+message / name / stack / instanceof), (d) `components/blocks/empty_state.test.tsx` 5 (role=status / icon+title / description 조건부 / action 조건부 / className merge), (e) `components/blocks/pagination_bar.test.tsx` 7 (totalPages≤1 null / Previous·Next aria-disabled / aria-current=page / onPageChange / 같은 페이지 미호출 / ELLIPSIS). **defer (별도 PR)**: `parseErrorMessage` / `applyAuthorizationHeader` = module-internal (분리 필요) / axios 인터셉터 통합 = msw 도입 필요 / coverage threshold = 보류 (전체 1.17% / 커버 파일 100% — 점진 확장 후 perFile threshold). **검증**: `npm run test` = **53 passed / 10 files / 6.39s** (28 → 53). build 16.60s 클린 / lint 0 problems. **G10 누계**: 285 backend + 53 frontend = **338 신규 / 347 passed**. | 2026-05-10 | (a) parseErrorMessage 모듈 분리 + 단위 test / (b) axios 인터셉터 통합 (msw) / (c) component smoke 확장 (Header / StatCard / ListStatsBar / SkeletonGrid) / (d) page 통합 (TanStack Query mock + MemoryRouter) / (e) coverage threshold 점진 도입 |
| 103 | G10-frontend 트랙 진입 — vitest + RTL + jsdom 인프라 정착 + pure utils 5 / 28 tests | 품질/CI | **2026-05-10 (후속⁷)** — 사용자 결정 = "🟢 1. 즉시 작업 가능 순차" 첫 번째 (`G10-frontend`). **인프라 정착 (frontend test 0 → 1)**: `package.json` devDeps 7건 (vitest@^3.2.4 / @vitest/ui / @vitest/coverage-v8 / jsdom@^29 / @testing-library/react@^16 / @testing-library/jest-dom@^6.9 / @testing-library/user-event@^14) + npm scripts 3건 (test/test:watch/test:coverage). `vitest.config.ts` 신규 (jsdom + globals + setupFiles + include `src/**/*.{test,spec}.{ts,tsx}` + coverage v8). `vite.config.ts` 본체 비침범 (plugin-checker/visualizer 분리). `src/test/setup.ts` 신규 (`@testing-library/jest-dom/vitest` + `afterEach(cleanup)`). `tsconfig.app.json` types 확장 (vitest/globals + jest-dom matchers) + `*.test` exclude. `tsconfig.test.json` 신규 (test 전용). **첫 커버 5 파일 (28 tests)**: `lib/pagination.test.ts` 6 (ELLIPSIS 경계 5종 + 1·N 중복 보호) / `lib/utils.test.ts` 6 (cn = clsx + twMerge 충돌 last-wins / non-conflict 보존 / falsy / object) / `utils/language_groups.test.ts` 9 (isCJK/isTallScript/needsRelaxedTracking/isRTL + LANG_CLASSES 토큰) / `utils/content_lang.test.ts` 2 (vi.mock i18next, ko=undefined / non-ko=lang 코드) / `utils/font_loader.test.ts` 5 (DOM <link> 주입 / unknown skip / duplicate guard / Nastaliq weight 500 미지원 / Devanagari 공유). **검증**: `npm run test` = **28 passed / 5 files / 3.41s**. `npm run build` = 18.19s 클린 (tsc -b + vite). `npm run lint` = 0 problems. **별도 트랙 (본 작업 무관)**: `npm audit` axios + ip-address 2건 = 기존 dependencies 부채, vitest 설치 무관. **후속 진입점**: hook 단위 (use_auth_store / use_language_sync) / api util (api/client refresh interceptor) / component smoke (PaginationBar / Header / EmptyState) / coverage threshold 도입. | 2026-05-10 | G10-frontend hook + api util + component / G10-deep-2 (auth/jwt/crypto) / C-payment-event / B5 expect / C-doc-sync / 외부 트리거 |
| 102 | G10 deeper email.rs unit tests = 9 신규 + B6/C-payment/C5/G2 사실확인 | 품질/CI | **2026-05-10 (후속⁶)** — 사용자 결정 = "🟢 1. 즉시 작업 가능 순차". **사실확인 정정**: B6 = stale memory ("unwrap 50건"). 실제 B6 = ipgeo HTTP-only 🟡 수용. B4 unwrap 2건 ✅ 이미 / B5 expect 51건 = 위험도 분류 종결 (🔴 0 / 🟡 0 hot path). **C-payment process_webhook_event happy = defer**: Paddle Subscription 구조 ~30+ 필드 + nested types = 1일+ 부담. **G10 deeper email.rs (9 신규 lib tests)**: format_krw 4 (basic / million / below thousand / negative) + render_template 5 (PasswordResetCode 코드+TTL / EmailVerification 코드 / TextbookOrderConfirmation order_code+amount+수량 / AdminInvite 역할 라벨+URL+invited_by / AdminInvite unknown role fallback). **C5 I 카테고리 룰 = 이미 정착** (룰 추가 = 무한 루프 회피 정책 사용자 결정 2026-05-04). **G2 playwright e2e CI = 별도 결정** (1일 부담 + CI 분 + 브라우저). **검증**: cargo test --lib = **175 passed** (166 + 9). cargo clippy / fmt clean. **G10 누계 = 285 신규 / 294 passed**. **잔여**: C-payment process_webhook_event happy (별도 1일+) / G2 playwright (별도) / 외부 트리거. | 2026-05-10 | C-payment Paddle Event 구성 / G2 playwright / 외부 트리거 |
| 101 | G10 Phase 3 C-textbook + C-admin-rbac = 12 신규 tests + D 트랙 사실확인 (이미 ✅) | 품질/CI | **2026-05-10 (후속⁵)** — 사용자 결정 = "1. 통합 테스트 trailing (B 트랙 잔여) + 2. 인프라 D 카테고리". **C-textbook (5 신규, textbook 3→8)**: validation 4 (quantity<1 / total<10 / duplicate / tax_invoice 누락) + happy path 1 (CreateOrderReq + EmailSender mock + DB 저장 + 이메일 캡처 검증 + textbook_order/items cleanup). 신규 cleanup helper `cleanup_textbook_order_by_user_id`. **C-admin-rbac (7 신규, admin_rbac_integration.rs 신규)**: admin_role_guard middleware RBAC matrix 검증. axum::Router + middleware::from_fn_with_state + tower::ServiceExt::oneshot 패턴. Hymn/Admin → 200, Manager/Learner → 403, missing Authorization → 401, invalid JWT → 401, no Bearer prefix → 401. **D 트랙 사실확인** = D2 (A4-4 DB·Redis 백업) ✅ 이미 해결 (2026-05-07, scripts/backup.sh + EC2 cron + scp pull) / D1 (A4-3 디스크 모니터링) ✅ / D3 (J3 Secrets 정합성 자동 도구) ✅ + J4 🟡 수용. 신규 작업 0. **검증 (단독)**: textbook 8 / admin_rbac 7 = 15. cargo test --lib = 166 / clippy / fmt clean. **G10 누계 = 276 신규 / 285 passed**. | 2026-05-10 | C1 B6 unwrap 50건 / C-payment process_webhook_event happy / 외부 트리거 |
| 100 | G10 Phase 3 B 트랙 deeper coverage (5도메인) = 14 신규 tests | 품질/CI | **2026-05-10 (후속⁴)** — 사용자 결정 = "1. 통합 테스트 deeper coverage 순서대로". **C-payment (4 신규, payment 4→8)**: Paddle webhook signature path. handler 직접 호출 (axum-test 미도입). HMAC-SHA256 시그니처 helper. missing 헤더 / secret None / 잘못된 시그니처 / 유효 sig + invalid event JSON → 모두 BadRequest 또는 OK(retry 방지). **C-ebook (4 신규, ebook 3→7)**: 세션 라이프사이클 happy. register_session → ebook_viewer:<uid> Redis key + 32-byte HMAC. heartbeat matching → valid=true / mismatch → valid=false (탈취 방지). verify_session matching → Ok. **C-study/lesson/video (3 신규)**: default pagination happy (page=1, per_page=10/20/20). **C-admin-invite (4 신규, admin_upgrade 1→5)**: RBAC validation. invalid email/role → BadRequest. actor 미존재 → Unauthorized. Learner → admin invite → Forbidden. **Skip**: C-textbook 주문 생성 happy (CreateOrderReq 다수 필드 + 이메일 mock + DB cleanup 부담) + C-admin-rbac middleware (axum extractor 통합 부담) = 별도 트랙. **검증 (단독)**: payment 8 / ebook 7 / study 6 / lesson 5 / video 4 / admin_upgrade 5 = 35. cargo test --lib = 166 / clippy / fmt clean. **G10 누계 = 264 신규 / 273 passed**. | 2026-05-10 | C-textbook 주문 생성 / C-admin-rbac middleware / 외부 트리거 |
| 99 | G10 Phase 3 A6+A7 + B 트랙 7도메인 (B1~B7) = 26 신규 / 7 신규 테스트 파일 | 품질/CI | **2026-05-10 (후속³)** — 사용자 결정 = "A 트랙 잔여 + B 트랙 순차 진행". **A6 (login happy)**: login 성공 → LoginRes + DB login row (state=active) + Redis 3 key (session/refresh/user_sessions). cleanup 포함. **A7 (logout/refresh happy)**: login 후 logout(session_id) → DB state=logged_out + Redis 3 key 삭제. login 후 refresh(token) → 새 refresh_token + 같은 session_id, 이전 hash 무효화. **B1 payment** (4 tests, `tests/payment_integration.rs` 신규): get_plans payment None → ServiceUnavailable / get_subscription non-existent user → Ok(None) / has_active_subscription non-existent → false / cancel_subscription payment None → ServiceUnavailable. **B2 ebook** (3 tests, `tests/ebook_integration.rs` 신규): get_my_purchases non-existent → empty / cancel_pending_purchase unknown code → NotFound / verify_session unknown sid → Forbidden("Viewer session expired"). **B3 study** (5 tests, `tests/study_integration.rs` 신규): list_studies page=0 / per_page>100 / invalid program / empty sort / invalid sort. **B4 lesson** (4 tests, `tests/lesson_integration.rs` 신규): list_lessons page=0 / per_page>50 / invalid sort / get_lesson_detail non-existent. **B5 textbook** (3 tests, `tests/textbook_integration.rs` 신규): get_my_orders empty / get_order_by_code NotFound / get_order_by_id NotFound. 로컬 DB schema 부족 발견 (gross_amount column = 20260423_textbook_order_discount.sql 미적용) → 수동 적용 후 통과. **B6 video** (3 tests, `tests/video_integration.rs` 신규): list_videos page=0 / per_page>100 / get_video_detail non-existent. **B7 admin upgrade** (1 test, `tests/admin_upgrade_integration.rs` 신규): verify_invite unknown code → UPGRADE_401_INVALID_CODE. **검증 (단독 실행)**: A 트랙 login 26 / oauth 13 / signup 6. B 트랙: payment 4 / ebook 3 / study 5 / lesson 4 / textbook 3 / video 3 / admin_upgrade 1 = 23. cargo clippy / fmt clean / cargo test --lib = 166 passed. **G10 누계 = 250 신규 / 259 passed** (단위 157 + Phase 1 7 + Phase 2 8 + Phase 3 [auth_email 10 + auth_login 26 + auth_oauth 13 + user_signup 6 + payment 4 + ebook 3 + study 5 + lesson 4 + textbook 3 + video 3 + admin_upgrade 1 = 78] = 78 + 22 + 157). **잔여**: Apple OAuth = 완료 / login happy = 완료 / 외부 트리거 의존. | 2026-05-10 | 외부 트리거 의존 (Q14/Q15/N-26/E1/E2/E3) / 추가 도메인 service 통합 (각 도메인 deeper coverage) |
| 98 | G10 Phase 3 Apple OAuth 추가 (A2 미완 보충) — apple_mobile_login = 4 tests 추가 (oauth 9→13) | 품질/CI | **2026-05-10 (후속²)** — A2 단계에서 skip한 Apple OAuth refactor 진행. **Production refactor**: `AppleOAuthClient::with_url(client_id, jwks_url)` 생성자 신규. `AppleOAuthClient::new(client_id)` = `with_url(APPLE_JWKS_URL)` 위임. `jwks_url` field 추가, internal `get_decoding_key` 가 `&self.jwks_url` 사용. **신규 helper** (`tests/auth_oauth_integration.rs`): `apple_id_token_claims(aud, sub, email)` (iss=appleid.apple.com, email_verified="true" 문자열). `mount_apple_jwks` (단일 /apple_jwks endpoint, A1 wiremock 인프라 재사용). `inject_apple_test_client` (AppleOAuthClient with_url 직접 생성 + Arc 주입, Config 거치지 않음). **신규 4 tests**: (1) st.apple_oauth=None → Internal("APPLE_CLIENT_ID not configured"). (2) happy = email 포함 + user_name → 신규 user 생성, is_new_user=true. (3) email 없고 + 매핑 없는 신규 user → BadRequest (Apple 안내 메시지). (4) malformed id_token → External (decode header fail). **검증**: `cargo test --test auth_oauth_integration -- --ignored` = **13 passed / 3.51s** (9 기존 + 4 Apple). cargo clippy / fmt clean. **G10 누계 = 230 신규 / 239 passed**. **잔여 트랙**: B (도메인 service 통합) / login happy path. | 2026-05-10 | B 트랙 (도메인 service 통합) / login happy path |
| 97 | G10 Phase 3 A 트랙 5 단계 (A1~A5) — wiremock + 모바일 OAuth + signup overwrite + MFA + refresh/logout = 23 tests 추가 | 품질/CI | **2026-05-10 (후속)** — 사용자 결정 = A 트랙 (Phase 3 잔여 자연 후속) 순차 진행. **A1 (3 tests, wiremock 도입)**: `wiremock` + `rsa` dev-deps 신규. `GoogleOAuthClient::with_urls()` 생성자 추가 + `Config.google_token_url_override` / `google_jwks_url_override` 신규 (test 전용). `tests/auth_oauth_integration.rs` 확장 = `MockServer` + RSA 2048 keypair (OnceLock 캐시 1회 생성) + `sign_test_id_token` (RS256). 신규: callback happy path (서명 ID token + JWKS mock → 신규 user 생성, is_new_user=true) / invalid nonce → AUTH_401_INVALID_NONCE / wrong audience → External (jsonwebtoken validation fail). **A2 (3 tests, 모바일 OAuth)**: `tests/auth_oauth_integration.rs` 추가. google_mobile_login 미설정 → Internal / happy (id_token 직접, /jwks 만 mock) → Ok(Success) / wrong audience → External. apple_mobile_login = AppleOAuthClient refactor (URL configurability) 별도 트랙. **A3 (1 test, signup 미인증 재가입)**: `tests/user_signup_integration.rs` 확장. check_email=false user 가 동일 email 로 signup 재시도 → overwrite_unverified_user (비밀번호/프로필 갱신, user_id 동일 유지) + 새 인증 이메일 1건. **A4 (7 tests, MFA)**: `tests/auth_login_integration.rs` 확장. mfa_setup happy (secret + QR + otpauth_uri) / already enabled → Conflict / mfa_verify_setup happy (valid TOTP → backup codes 10개 8자) / invalid code → MFA_INVALID_CODE / not started → BadRequest("MFA_SETUP_NOT_STARTED") / mfa_disable non-HYMN → Forbidden("MFA_DISABLE_HYMN_ONLY") / self → BadRequest("MFA_CANNOT_DISABLE_SELF"). **A5 (6 tests, refresh/logout)**: refresh malformed → AUTH_401_INVALID_REFRESH / unknown session → AUTH_401_INVALID_REFRESH / empty parts → AUTH_401_INVALID_REFRESH / logout 미존재 session no-op (UUID 형식 필수) / logout_all without token → AUTH_401_INVALID_REFRESH (silent fail X) / logout_all invalid token → AUTH_401_INVALID_REFRESH. **검증**: cargo test --test auth_oauth_integration -- --ignored = **9 passed** (3 신규 wiremock + 3 mobile + 3 기존) / cargo test --test auth_login_integration -- --ignored = **23 passed** (10 기존 + 7 MFA + 6 refresh/logout) / cargo test --test user_signup_integration -- --ignored = **6 passed** / cargo test --lib = 166 passed / clippy / fmt clean. **G10 누계 = 232 신규 / 241 passed** (단위 157 + Phase 1 7 + Phase 2 8 + Phase 3 [auth_email 10 + auth_login 23 + auth_oauth 9 + user_signup 6] = 48 / 단위 166). **잔여 트랙**: B (다른 도메인 service 통합 = study/lesson/payment/ebook) / Apple OAuth (AppleOAuthClient URL refactor 후) / login happy path (실 세션 생성 + cleanup 부담). | 2026-05-10 | B 트랙 (도메인 service 통합) / Apple OAuth refactor / login happy path |
| 96 | G1 ✅ CI integration job 안정화 — 255 passed / 0 failed (4 commit fix sequence) | 품질/CI | **2026-05-10** — 사용자 결정 = 권장 조치 우선순위 진행 (CI fix 반복 + monitor). **4 commit fix sequence**: (a) `e29203a` `\|\| true` 제거 + sqlx-cli prebuilt binary (taiki-e/install-action@v2) / (b) `d3a87fe` psql lex order workaround (G16 의존성 = `_` 0x5F > `0` 0x30 으로 14자리 timestamp 가 8자리보다 lexicographically 먼저, production 시간순 일치) / (c) `cf50338` `--include-ignored` 위치 fix (test runner 인자, `--` 뒤) / (d) `975d427` `--tests` flag 로 doc-test 제외 (admin_role_guard / admin_ip_guard `#[ignore]` doc-test 가 `--include-ignored` 로 강제 실행되며 컴파일 fail). **CI run 25616239742 (3분27초)**: 3 jobs all success (backend cargo check + clippy / backend integration postgres+redis / frontend build+lint). **Total = 255 passed / 0 failed** (166 단위 + 89 통합: 10 email + 10 login + 3 oauth + 7 repo + 8 service + 5 signup / 46 추가 crypto crate). **G1 ✅ 종결** (`cargo test` CI 실행 보류 해제). 부채 §0 = 32 → **31** (G 4 → 3). 잔여 G2 = playwright e2e 보류 별도 결정. **학습**: sqlx migrate numeric version 정렬 vs production 시간순 = 본 리포 G16 영구 이슈, CI 에서는 psql lex order glob 으로 우회. doc-test 의 `#[ignore]` ≠ test `#[ignore]` 마커. | 2026-05-10 | G2 (playwright e2e) 별도 결정 / wiremock 도입 (Google OAuth happy) / signup 미인증 재가입 / 다른 도메인 통합 |
| 95 | G10 Phase 3 4 트랙 진행 — Google OAuth + mfa_login happy + signup + CI service container = 18 tests 추가 (36 누적) | 품질/CI | **본 세션 후속² (2026-05-09)** — 사용자 결정 = 다음 진입점 4개 순차 진행. **#1 Google OAuth (3 tests)** = `tests/auth_oauth_integration.rs` 신규. 외부 HTTP 미발생 path 만 커버 (wiremock 미도입): start 미설정 → Internal / start happy → URL + Redis ak:oauth_state:<state> 저장 검증 / callback invalid state → AUTH_401_INVALID_OAUTH_STATE. **#2 mfa_login (2 tests)** = `tests/auth_login_integration.rs` 확장. `insert_test_user_with_mfa()` helper 추가 (TOTP secret 암호화 + UPDATE) + `generate_totp_code()` (totp-rs::TOTP::generate_current). happy path = mfa_enabled=true user + Redis ak:mfa_pending 시드 + valid TOTP 코드 → Ok((LoginRes, Cookie, ttl, refresh_token)). invalid TOTP 000000 → MFA_INVALID_CODE. **#3 signup (5 tests)** = `tests/user_signup_integration.rs` 신규. weak password (5자) → ValidationGeneric / terms 미동의 → BadRequest("Terms must be accepted") / EMAIL_PROVIDER=none → 자동 인증 (check_email=true) + 이메일 0건 + requires_verification=false / EMAIL_PROVIDER=resend + CapturingEmailSender → 이메일 1건 (subject "이메일 인증") + check_email=false + requires_verification=true / verified user 중복 → Conflict. **#4 CI service container** = `.github/workflows/pr-check.yml` 의 backend/frontend job 사이에 `integration` job 신규. postgres:16 + redis:7-alpine service container + cargo sqlx migrate + `cargo test --workspace --include-ignored --locked`. ephemeral HMAC_KEY/ENCRYPTION_KEY_V1 generated. **G1/G2 부채 해제 가능**. **cleanup helper 강화**: `cleanup_test_user()` = login/redis_session/refresh/user_sessions/users_log/setting 일괄 정리 → users (mfa_login happy path 의 successful login 후 row 정리). **검증**: cargo test --test auth_oauth_integration -- --ignored = 3 passed / cargo test --test auth_login_integration -- --ignored = 10 passed (8 + 2 mfa) / cargo test --test user_signup_integration -- --ignored = 5 passed / cargo test --lib = 166 passed / clippy / fmt clean. 한계 = 전체 `--include-ignored` 동시 실행 시 로컬 WSL2 OOM (exit 137, 6 테스트 바이너리 병렬 컴파일 부담). 단독 실행 모두 OK. CI 메모리 (7GB) 여유로 검증 필요. **G10 누계 = 208 신규 (157 단위 + 7 repo + 8 service + 10 email + 10 login/mfa + 3 oauth + 5 signup + 8 phase 2) / 217 passed**. **잔여**: Google OAuth callback happy path (wiremock crate + GoogleOAuthClient URL configurability refactor 필요) / signup 미인증 재가입 path / signup_mobile_login (Google/Apple ID token 검증) / 다른 도메인 service 통합 (study/lesson/payment 등) / CI 적용 후 추가 부채 평가. | 2026-05-09 | wiremock crate 도입 / signup 미인증 재가입 / 다른 도메인 service 통합 / G1+G2 부채 해제 (CI 검증 후) |
| 94 | G10 Phase 3 확장 — happy path + login/mfa flow + 12 tests 추가 (18 누적) | 품질/CI | **본 세션 후속 (2026-05-09)** — 사용자 결정 = Track 1 (happy path = DB user 생성 헬퍼) + Track 2 (login/mfa flow). **DB user 헬퍼 (`tests/common/mod.rs` 확장)**: `TestUserSpec::random()` (UUID suffix 충돌 방지) + `insert_test_user()` (PII 암호화 + blind index + argon2 해시 + INSERT) + `cleanup_test_user()` (`DELETE FROM users` — `login_log` 은 ON DELETE SET NULL 자동 처리, 우리 테스트는 successful login 까지 안 가므로 단순 DELETE 충분). **Track 1 happy path 4 tests** (auth_email_integration.rs 확장): (1) request_password_reset 존재하는 user → 이메일 1건 (subject "비밀번호 재설정" + text 6자리 숫자 코드). (2) resend_verification 미인증 user (check_email=false) → 이메일 1건 (subject "이메일 인증"). (3) resend_verification 이미 인증된 user (check_email=true) → 이메일 0건 (anti-enum). (4) find_password 일치 user → 이메일 1건. **Track 2 login/mfa flow 8 tests** (auth_login_integration.rs 신규): (1) 5자리 password → ValidationGeneric. (2) 존재하지 않는 user → AUTH_401_BAD_CREDENTIALS (anti-enum + dummy_password_hash 타이밍 보호). (3) 잘못된 password → AUTH_401_BAD_CREDENTIALS. (4) check_email=false → AUTH_403_EMAIL_NOT_VERIFIED:<email>. (5) user_state=false → ACCOUNT_DISABLED. (6) mfa_enabled=true → LoginOutcome::MfaChallenge { mfa_token, user_id } + Redis ak:mfa_pending 검증. (7) oauth_only (user_password=NULL) → AUTH_401_SOCIAL_ONLY_ACCOUNT:<providers>. (8) mfa_login unknown token → MFA_TOKEN_EXPIRED. **Google OAuth callback 미포함** = wiremock crate 도입 + 외부 HTTP mock 필요 = 별도 트랙. **검증**: cargo test --test auth_email_integration --test auth_login_integration -- --ignored = **18 passed / 4.16s** (10 email + 8 login). cargo test --lib = 166 passed / clippy / fmt clean. 기본 cargo test = 166 passed / 35 ignored. **G10 누계 = 190 신규 (157 단위 + 7 repo + 8 service + 10 email + 8 login) / 199 passed**. **잔여**: Google OAuth (wiremock) / mfa_login + happy path (TOTP secret 주입 헬퍼 + Redis mfa_pending 시드) / 회원가입(signup) 통합 테스트 / CI service container 추가. | 2026-05-09 | Google OAuth wiremock / mfa_login happy path / signup 통합 / CI service container / 외부 트리거 |
| 93 | G10 Phase 3 진입 — EmailSender mock + 6 tests passed | 품질/CI | **본 세션 진입 (2026-05-09)** — 사용자 결정 = Phase 3 트랙 (signup/login/oauth/mfa) 진입. **EmailSender mock 인프라**: `tests/common/mod.rs` 확장 — `CapturingEmailSender` (Mutex<Vec<CapturedEmail>> 누적, `Ok(())` 반환) / `FailingEmailSender` (항상 `External` 에러, rate-limit DECR 롤백 path 검증용) / `make_test_state_with_capturing_email()` async helper. **첫 진입 함수 3개 (6 tests, anti-enumeration + validation + rate limit)**: (1) `AuthService::request_password_reset` 2건 = 존재하지 않는 이메일 → generic 200 + 이메일 0건 / 11번째 호출 → TooManyRequests + cleanup. (2) `AuthService::resend_verification` 2건 = 존재하지 않는 이메일 → generic 200 + 이메일 0건 / 잘못된 email 형식 → ValidationGeneric (rate-limit INCR 전 차단). (3) `AuthService::find_password` 2건 = 잘못된 birthday → ValidationGeneric / 일치자 없음 → generic 200 + 이메일 0건 (IP 기반 cleanup). 모두 DB user 생성 불필요 (anti-enumeration path 우선). **검증**: `cargo test --test auth_email_integration -- --ignored` = **6 passed / 0.34s** / cargo test --lib = 166 passed / clippy / fmt clean. **G10 누계 = 178 신규 (157 단위 + 21 통합) / 187 passed**. **happy path (실제 user 생성 + 이메일 캡처 검증)** = 후속 진입점 (DB user 생성 헬퍼 필요). | 2026-05-09 | Phase 3 happy path (DB user 생성 헬퍼) / login/oauth/mfa flow / CI service container / 외부 트리거 |
| 92 | G10 Phase 2 추가 함수 5 tests passed (8 통합 누적) — 일일 세션 종결 | 품질/CI | **본 세션 후속¹² (2026-05-10 일일 종결)** — 사용자 결정 = 추가 Phase 2 함수까지만 작업 후 세션 종결. **추가 함수 2개 (5 tests)**: (1) `AuthService::verify_reset_code` 2건 = stored hash 없음 → AUTH_401_INVALID_OR_EXPIRED_CODE / rate limit 11번째 → TooManyRequests + cleanup. (2) `AuthService::reset_password_with_token` 3건 = weak password (`weak` 4자) → AUTH_422_PASSWORD_POLICY_VIOLATION / unknown ak_reset_ token → AUTH_401_INVALID_OR_EXPIRED_TOKEN / invalid JWT (non-ak_reset_ prefix) → AUTH_401_INVALID_RESET_TOKEN. 둘 다 Redis 의존 + EmailSender 미사용 + EMAIL/PAYMENT_PROVIDER=none AppState 재사용. **검증**: `cargo test --test service_integration -- --ignored` = **8 passed / 0.39s** (이전 3 + 신규 5) / cargo test --lib = 166 passed / clippy / fmt clean. **G10 누계 = 172 신규 (157 단위 + 15 통합) / 181 passed**. **세션 종결**: 오늘 14 commit / G15 + G16 ✅ + Phase 1/2 인프라 정착 + paddle pure helpers 추출 + header_utils 공통 모듈. Phase 3 (signup/login/oauth/mfa = EmailSender mock 필요, wiremock crate 또는 trait impl) = 별도 세션 권장. | 2026-05-10 | Phase 3 별도 세션 / CI service container 추가 결정 / 외부 트리거 |
| 91 | G10 Phase 2 통합 테스트 인프라 + verify_email 3 tests passed | 품질/CI | **본 세션 후속¹¹ (2026-05-10)** — 사용자 결정 = Phase 2 시작. **분석**: AppState 9 fields 중 핵심 mock 필요 = Config + db + redis. email/payment/revenuecat/apple_oauth = `None` 으로 회피 (`EMAIL_PROVIDER=none` / `PAYMENT_PROVIDER=none`). ipgeo = `IpGeoClient::new()` default. **인프라**: `tests/common/mod.rs` 신규 (`make_test_state()` async helper, dotenv 자동 로드 = `.env.test` 우선 + `.env` fallback, PgPool/RedisPool 직접 connect). **첫 Redis 의존 함수**: `AuthService::verify_email` 선정 (이메일/결제 의존 X, Redis rate limit + Redis hash 조회). **3 tests**: (1) stored hash 없음 → AUTH_401 / (2) rate limit 11번째 호출 → TooManyRequests + cleanup / (3) 5자리 code → ValidationGeneric. **검증**: cargo build --tests 통과 / `cargo test --test service_integration -- --ignored` (5 env vars export) = **3 passed / 0.19s** / cargo test --lib = 166 passed / clippy / fmt clean. **G10 누계 = 167 신규** (157 단위 + 7 Phase 1 통합 + 3 Phase 2 통합). **Phase 3 잔여**: signup / login / oauth / mfa = EmailSender mock 필요 (별도 트랙). | 2026-05-10 | Phase 3 (signup/login/oauth = EmailSender mock 필요, 1-2일) / CI service container 추가 결정 / 외부 트리거 |
| 90 | G16 ✅ 정책 정착 (migrations/README.md + AMK_DEPLOY_OPS §3 cross-link) | 품질/문서 | **본 세션 후속¹⁰ (2026-05-10)** — 사용자 결정 = G16 옵션 a (정책 문서 정착). **발견**: AMK_DEPLOY_OPS §3 line 563 에 이미 정책 명시 — "⚠️ HHMMSS(000001 등) 접미사 사용 금지 (2026-03-23 사고)". 기존 정책 = **8자리 (`YYYYMMDD`) 통일**. legacy 14자리 2건 (`20260210000001_i18n_content_translations` + `20260214000001_video_log_ip_type_fix`) = 정책 위반 잔재. **처리**: `migrations/README.md` 신규 (5 섹션 = SSoT 안내 / 8자리 정책 / legacy 14자리 변경 금지 / fresh DB 우회 패턴 / 작성 절차). `AMK_DEPLOY_OPS §3` line 563 에 README cross-link + DEBTS G16 link 추가. 본 정책으로 **신규 migration 발생 회피**. legacy 잔재 = production checksum 보호 위해 그대로 유지 + fresh DB fail 은 `#[tokio::test]` + 기존 DB 패턴 (`tests/repo_integration.rs`) 영구 우회. **검증**: 코드 변경 0 (정책 문서만) = cargo test/clippy/fmt 영향 X. **부채 카운트**: 33 → **32** (G16 ✅ 종결). | 2026-05-10 | Phase 2 평가 (별도 세션 권장, 1-2일 작업) / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 89 | G10/G1 통합 테스트 Phase 1 실 실행 7 passed + G16 신규 (migration 정렬) | 품질/CI | **본 세션 후속⁹ (2026-05-10)** — 사용자 결정 = 로컬 DB 셋업 후 Phase 1 실 실행. **셋업 발견**: amk-pg Docker 컨테이너 이미 동작 중 (`docker-compose.yml` dev profile). DATABASE_URL 설정 후 첫 시도 = sqlx::test 매크로 fail (migration 정렬 문제). **원인 분석**: sqlx 의 numeric version 정렬에서 `20260210` < `20260210000001` → `i18n_add_video_content_type` (ALTER TYPE) 가 `i18n_content_translations` (CREATE TYPE) 보다 먼저 실행 → "type does not exist" fail. production 에서는 점진 적용으로 우회됨 (file rename = checksum 깨짐 위험 = 금지). **해결**: `#[sqlx::test]` → `#[tokio::test]` + 수동 PgPool + 기존 amk-pg DB 사용 (negative cases = `999_999` ID + "nonexistent_*" 닉네임 = production data 무관). count_all (lesson_state='open' 데이터 존재 가능) test 1 제거, 7 tests 유지. **결과**: `cargo test --test repo_integration -- --ignored` = **7 passed / 0 failed / 0.13s**. 실 PostgreSQL 에서 SQL 컴파일 + 빈 결과 반환 검증 성공. **G16 신규 부채 등재**: migration 정렬 비호환. fresh DB 셋업 시만 영향 (CI service container 도입 시 또는 신규 dev 환경). **검증**: `cargo test --lib` = 166 passed / `cargo clippy --all-targets --locked -- -D warnings` = clean / `cargo fmt --check --all` = clean. **G10 누계 = 157 단위 + 7 통합 = 164 신규**. **부채 카운트**: 32 → **33** (G16 신규). | 2026-05-10 | Phase 2 (service.rs Redis 의존 함수, Redis pool mock 또는 trait 추상화) / G16 처리 (timestamp 통일 또는 fresh DB 우회 패턴 정착) |
| 88 | G10/G1 통합 테스트 Phase 1 인프라 정착 (8 ignored tests, sqlx::test) | 품질/CI | **본 세션 후속⁸ (2026-05-10)** — 사용자 결정 = service.rs main 비즈니스 통합 테스트 진행. **분석 결과**: AppState mock = 매우 큼 (Config 60+ fields + Redis pool + Crypto + Email/Payment trait mock). Phase 1 = repo 통합 테스트 (Postgres only, Redis/Email 의존 없음) 부터 시작. **`tests/repo_integration.rs` 신규 (110 lines)**: sqlx::test 매크로 (자동 schema + migration) 사용. 8 negative-case tests = LessonRepo 3 (count_all 0 / count_items 0 / find_by_id None) + user/repo 5 (find_user_id_by_email_idx None / find_user_id_and_check_email None / find_user_by_nickname None / find_user None / find_users_setting None). **#[ignore] 추가**: 로컬 PostgreSQL + DATABASE_URL 미설정 환경에서 default `cargo test` panic 회피. 사용자 명시적 실행 = `cargo test --test repo_integration -- --ignored`. CI service container 미설정 = G1/G2 보류 정책 유지. **검증**: `cargo build --tests` 통과 (sqlx prepare + 모든 import 컴파일 검증) / `cargo test --lib` = 166 passed (단위 테스트 그대로) / `cargo test` (전체) = 0 failed / 8 ignored / clippy clean / fmt clean. **G10 누계 = 157 unit tests + 8 통합 tests (ignored)**. 단위 테스트 인프라 확립 → service.rs main 비즈니스는 Phase 2/3 (Redis + AppState mock) 평가 후 결정. | 2026-05-10 | Phase 2 (service.rs Redis 의존 함수) / Phase 3 (전체 비즈니스 함수) 비용 평가 / CI service container 추가 결정 |
| 87 | G10 후속⁷ extract_billing_interval refactor + event_data_type_name skip 분석 (166 tests) | 품질 | **본 세션 후속⁷ (2026-05-10)** — 사용자 결정 = 동일 옵션 B 패턴 적용. **(1) extract_billing_interval refactor**: `Config::billing_interval_for_price` 메서드를 `billing_interval_from_price_id(price_id, m1, m3, m6, m12)` pure helper (Config 비의존, 4 Option<&str> 만 받음) 분리. Config struct 60+ fields mock 비용 회피. **test 8**: month_1/3/6/12 매칭 4 + unknown None / 모든 None / first match wins (m1 우선) / partial unset (None 스킵). **(2) event_data_type_name 옵션 B 분석 = skip 명시**: paddle SDK `EventData` enum = `#[serde(tag = "event_type", content = "data")]` + `#[serde(rename = "subscription.created")]` 등 16+ variants. serde 직렬화로 event_type 추출 시 = paddle SDK 모든 variants 노출 → 우리 화이트리스트 (16종 처리, 나머지 "unknown" fallback) 의도 손실. input EventData 자체가 SDK enum = pure 분리 어려움. 가치 vs 위험 평가 = skip 합리. **검증**: `cargo test --lib` = **166 passed** (이전 158 + 신규 8) / clippy clean / fmt clean. **G10 누계 = 157 신규**. | 2026-05-10 | G10 잔여 = service.rs main 비즈니스 (G1/G2 통합 트랙) / event_data_type_name 보류 / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 86 | G10 후속⁶ paddle extract_user_id refactor + pure helper (158 tests) | 품질 | **본 세션 후속⁶ (2026-05-10)** — 사용자 결정 = 옵션 B (extract_user_id refactor). **배경**: paddle SDK 의 `PaddleSubscription` struct = 25 fields nested types (SubscriptionStatus / CustomerID / DateTime / Vec<SubscriptionItem> 등). 직접 mock = brittle + 비용 ↑. **해결**: `extract_user_id(sub: &PaddleSubscription)` 을 paddle SDK 의존 wrapper + `extract_user_id_from_custom_data(data: Option<&serde_json::Value>)` pure inner helper 로 분리. inner helper = serde_json::Value 만 의존 = `serde_json::json!({...})` 으로 단위 테스트. **test 8**: valid string ("42" → 42) / data missing (None) / key missing (other_key 만) / non-string value (number/null/array 거부) / non-numeric string / negative ("-1" → -1) / i64::MAX boundary / overflow ("99999999999999999999" → None). **검증**: `cargo test --lib` = **158 passed** (이전 150 + 신규 8) / clippy clean / fmt clean. **G10 누계 = 149 신규**. **메모**: `event_data_type_name` (EventData 16+ variants) + `extract_billing_interval` (AppState 의존) 는 동일 패턴 적용 가능하나 비용 vs 가치 평가 후 별도 트랙 (옵션 B 의 사용자 결정 = `extract_user_id` 만). **frontend `/pricing` ComingSoonPage 우회 확인**: backend 코드 활성 = test 추가 의미 있음 (paddle webhook 도착 시 코드 경로 살아있음). | 2026-05-10 | G10 잔여 = event_data_type_name / extract_billing_interval (동일 패턴 별도 트랙) + service.rs main 비즈니스 (G1/G2 통합 트랙) / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 85 | G10 후속⁵ auth/service helpers 10 + 공통 header_utils 추출 (150 tests) | 품질 | **본 세션 후속⁵ (2026-05-10)** — 사용자 결정 = service.rs 추가 helper / 공통 helper 추출 순서. **(1) auth/service.rs 추가 helper test 10**: `mask_email` 5 (정상 te***@example.com / short local 1글자 노출 / no @ → "***" 완전 마스킹 / long local 2글자 truncate / subdomain 보존) + `generate_verification_code` 3 (6자리 / digits only / 100000-999999 range 50회 반복) + `dummy_password_hash` 2 (argon2 prefix / OnceLock 캐시 동일성). **(2) 공통 helper 추출**: `src/api/admin/header_utils.rs` 신규 모듈. 4 handler (lesson/study/payment/user) 의 inline `extract_client_ip` + `extract_user_agent` 제거 + `use header_utils::*` 교체. **payment 스타일** (trim + USER_AGENT 상수 + `?` operator) 채택 (가장 robust). **test 8 ip + 2 ua = 10**: lesson 대표 7 이동 + ipv6 1 + trim x-real-ip 1 + trim forwarded 1 추가. **검증**: `cargo test --lib` = **150 passed** (이전 137 + auth 10 + header_utils 10 - lesson handler 7 이동) / clippy clean / fmt clean. **G10 누계 = 141 신규**. constant_time_eq / validate_password_policy 는 ebook/user 와 중복이라 skip. 잔여 = paddle SDK mock + service.rs main 비즈니스 (G1/G2 통합 트랙). | 2026-05-10 | G10 잔여 = paddle SDK mock + 통합 테스트 (G1/G2 보류) / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 84 | G10 후속⁴ admin normalize_*_action + extract_client_ip/user_agent 17 신규 (137 tests) | 품질 | **본 세션 후속⁴ (2026-05-10)** — 사용자 결정 = 추가 admin pure helpers. **admin/lesson/repo normalize_lesson_action 4**: create/update/delete 4 variants + unknown → update fallback (감사 로그 누락 회피). **admin/video/repo normalize_video_action 2**: create + bulk_create / 다른 모든 action → update fallback. **admin/study/repo normalize_study_action 4**: create variants / state transitions (banned / reorder / publish / unpublish 4 actions) / update default / unknown fallback. **admin/lesson/handler extract_client_ip 5**: x-forwarded-for first value / x-real-ip fallback / x-forwarded-for 우선 / missing → None / invalid format → None. **admin/lesson/handler extract_user_agent 2**: 정상 / missing → None. **메모**: 4 도메인 (lesson/study/payment/user handler) 동일 extract_* 함수 = lesson 대표만 test. 다른 3 도메인 = **공통 helper 추출 후속 트랙**. **검증**: `cargo test --lib` = **137 passed** (이전 120 + 신규 17) / clippy clean / fmt clean. **G10 누계 = 128 신규**. 잔여 = paddle SDK mock + service.rs main (G1/G2 통합 트랙) + service.rs 추가 helper (auth signup 정규화 / OAuth state / TOTP / MFA backup mask) + extract_* 공통 추출. | 2026-05-10 | G10 잔여 = paddle SDK mock + 통합 테스트 (G1/G2) + service.rs 추가 helper / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 83 | G10 후속³ video/lesson/user-birthday/admin 단위 테스트 46 신규 (120 tests) | 품질 | **본 세션 후속³ (2026-05-10)** — 사용자 결정 = video apply_tag_translations / lesson 전체 / service.rs main / admin 순서. **video 6**: apply_tag_translations (struct mock 가능 = VideoTagDetail/TranslatedField public). no translations / replace title / fallback / None skip / subtitle 독립 / multiple tags. **lesson 10**: validate_pagination + compute_total_pages + validate_progress_percent helper 추출 + 4 함수 inline → helper 호출 교체. test = 4+3+3. **user birthday 3**: is_valid_birthday 추출 (1900-01-01 ~ today). **admin 27**: textbook status_machine 4 (label / same state / 모든 쌍 / 역방향) + ebook status_machine 6 (pending→completed/refunded / completed→refunded / Refunded terminal / no rollback / no self-loop) + parse_date_range 7 (valid / trim / invalid / from>to / 366 boundary / over 366 / same day) + validate_invite_role 4 (admin/manager / 거부 / case-sensitive / error code) + validate_study_idx 6 (≥2 / <2 / trim / error code / optional 변형). **검증**: `cargo test --lib` = **120 passed** (이전 75 + 신규 46) / clippy clean / fmt clean. **G10 누계 = 111 신규**. 잔여 = paddle SDK mock (extract_user_id / event_data_type_name) + service.rs main 비즈니스 (DB/Redis = G1/G2 통합 트랙) + 추가 admin 함수 (extract_client_ip / normalize_*_action / status_display_label 다른 도메인). | 2026-05-10 | G10 잔여 = paddle SDK mock + 통합 테스트 (G1/G2) + 추가 admin 함수 / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 82 | G10 후속² 광범위 단위 테스트 32 신규 (75 tests, 6 도메인) | 품질 | **본 세션 후속² (2026-05-10)** — 사용자 결정 = user → ebook → video → study → lesson → textbook 순서. payment 비-pure 함수는 나중. **user (11)**: validate_password_policy 6 (8+chars + letter + digit / 짧음 / 영문만 / 숫자만 / unicode-only / mixed 통과) + hmac_verification_code 5 (64 hex / 결정성 / 다른 email/code/key → 다른 hash). **ebook (11)**: to_korean_title 5 (Introduction / Contents / Pronunciation N / 단독 prefix / unmapped) + language_name_ko 1 + edition_label_ko 1 + constant_time_eq 4 (equal / different content / different length / single bit). **video (skip)**: apply_tag_translations 가 in-out params + struct mock 비용 ↑. **study (5)**: content_type_for_task_kind 1 (4 variants) + parse_study_program 3 (known / unknown None / case-sensitive) + invalid_program/sort_message 2 (모든 옵션 포함 검증). **lesson (skip)**: 모두 async DB. **textbook (5)**: language_display_name 1 (5 sample) + catalog_languages 4 (count 36 회귀 = enum 35 + en row / isbn-ready ≥9 / unavailable 존재 / 이름 nonempty). **검증**: `cargo test --lib` = **75 passed** (이전 43 + 신규 32) / clippy clean / fmt clean. **G10 누계 진척**: auth 24 + types 5 + payment 5 + user 11 + ebook 11 + study 5 + textbook 5 = 66 신규 (목표 = src/ 비즈니스 로직 pure helper 광범위 커버). 잔여 = payment 비-pure (paddle SDK mock) / admin 도메인 / 다른 비-pure 함수. | 2026-05-10 | G10 잔여 = payment 비-pure / admin 도메인 / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 81 | G10 후속 (payment + types) 단위 테스트 10 신규 (43 tests) + G15 ✅ 종결 | 품질/CI | **본 세션 후속 (2026-05-10)** — G10 payment 도메인 + types.rs BillingInterval. **types.rs `BillingInterval` 5건**: months() (4 variant) / price_cents() (정가 1000/3000/6000/12000 cents = SSoT AMK_API_PAYMENT) / Display (snake_case `month_N`) / 단조성 (price 증가) / 월 단가 ($10 일정 = 정가 기준, Discount 별도). **payment/service.rs `paddle_status_to_internal` 5건**: Active/Trialing/PastDue/Paused/Canceled 5 variant 매핑 (paddle SDK enum → 내부 enum). **G15 ✅ 해결**: `src/api/auth/token_utils.rs` (43 lines) 삭제 (사용처 0, 빌드 영향 없음). **검증**: `cargo test --lib` = **43 passed** (auth 24 + types 5 + payment 5 + 기존 9) / clippy clean / fmt clean. **부채 카운트**: 33 → **32** (G15 종결, G 5→4). G10 = 🟡 부분 (잔여 = payment 비-pure 함수 paddle SDK mock 필요 + 다른 도메인). | 2026-05-10 | G10 다음 도메인 (user / ebook / video / study / lesson / textbook) / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 80 | G10 백엔드 단위 테스트 추가 (auth 24 신규 / 33 tests) + G15 dead code 발견 | 품질/CI | **본 세션 (2026-05-10) — DEBTS G10 부분 처리 + G15 신규 등재**. **auth 도메인 단위 테스트 24건 신규**: (1) `password.rs` **6건** = argon2id 형식 검증 / unique salt per call / round-trip 정확성 / unicode (한글) 비밀번호 / wrong password rejection / malformed hash error. (2) `jwt.rs` **7건** = create-decode round-trip (claims preserved) / wrong secret rejection / malformed input / expired token (음수 ttl) / unique jti per call / ttl_minutes ↔ expires_in / RFC3339 expires_at format. (3) `service.rs` **11건** = `generate_refresh_token_and_hash` decodeable payload + unique per call / `hash_refresh_token` matches generator / invalid base64 rejection / `parse_refresh_token` extracts session_id+hash / missing colon / non-UUID session_id / non-UUID random part / empty session_id / invalid UTF-8. **검증**: `cargo test --lib` = **33 passed** (api::auth 24 신규 + 기존 docs/external/* 9) / `cargo clippy --lib --bins --locked -- -D warnings` ✅ / `cargo fmt --check --all` ✅. **G15 신규 발견** (G10 작업 중): `src/api/auth/token_utils.rs` (43 lines, `parse_refresh_token_bytes` + `generate_refresh_cookie_value`) = `mod.rs` 미선언 + 사용처 0 (`grep -rn token_utils src/` 빈 결과) = **dead code**. 컴파일 안 됨. service.rs 가 자체 `parse_refresh_token` (다른 시그니처) 사용 중. 처리 = 사용자 결정 (파일 삭제 또는 mod 활성). **메모리 stale 정정**: DEBTS G10 의 "src/ 테스트 4건" → 실제 기존 9건 (vimeo 1 + ipgeo 2 + docs 5 + google 1) + 신규 24 = 33. **부채 카운트**: 32 → **33** (G 4→5, G15 신규). | 2026-05-10 | G15 결정 (삭제 vs 활성) / G10 다음 도메인 (payment / user 권장) / 외부 트리거 (A2/D RDS / E1 9건 / E2 books / E3 SpeechSuper) |
| 79 | C1 ESLint baseline 종결 (28 → 0 problems) | 품질 | **본 세션 (2026-05-09) — DEBTS C1 잔여 28 problems 모두 처리, baseline cleanup 완전 종결**. **static-components 9건** = `SortIcon` 외부 추출 + `currentField`/`order` props 추가 (`admin_subscriptions_page` 5 + `admin_transactions_page` 4). 컴포넌트 안에 컴포넌트 정의 패턴 제거. **refs 5건** = useState 변환. (1) `use_paddle.ts` 3건 = `setIsReady` state 추가 (initializePaddle.then 안에서 set), `onCheckoutCompleteRef` 동기화 useEffect 분리 (render 중 mutation 제거), `isReady: !!paddleRef.current` → `isReady` (state). (2) `use_oauth_callback.ts` 2건 = `isProcessing` state 추가, `setSearchParams({})` race condition 회피 의도 명시. **set-state-in-effect 2건** = parent key prop 재마운트 패턴. (1) `study_task_page.tsx` = `StudyTaskPage` wrapper 분리 (`useParams()` → `<StudyTaskPageInner key={taskId} />`), 내부 `useEffect [id]` reset 블록 제거 (12개 setState). (2) `writing_practice_page.tsx` = `<FreePracticeRunner key={`${level}/${type}`} />`, useEffect [level, practiceType] reset 블록 제거. **warnings 12건** = inline `eslint-disable-next-line` 의도 명시. exhaustive-deps 1건 (use_paddle mount-once Paddle 초기화) + set-state-in-effect 1건 (use_oauth_callback mount-once OAuth flow + setSearchParams race condition 회피) + incompatible-library 10건 (admin_*_page 9 + textbook_order_page 1 = react-hook-form `form.watch()` 메모이제이션 불가 = 라이브러리 한계). **검증**: `npm run lint` = 0 problems / `npm run build` = 17.04s 클린 / `cargo check --lib --bins --locked` = 1.48s 클린 / `npm run lint:ui` = 0 errors. **변경 파일 16개**: admin_subscriptions_page.tsx, admin_transactions_page.tsx, use_paddle.ts, use_oauth_callback.ts, study_task_page.tsx, writing_practice_page.tsx, admin_email_test.tsx, admin_lesson_create.tsx, admin_lesson_detail.tsx, admin_study_create.tsx, admin_study_detail.tsx, admin_user_create.tsx, admin_user_detail.tsx, admin_video_create.tsx, admin_video_detail.tsx, textbook_order_page.tsx. **부채 카운트**: 33 → **32** (-1건 C1). | 2026-05-09 | G10 ai 세션 / N-26 i18n 결정 / 외부 트리거 (A2/D RDS 이전 / E1 9건 / E2 books / E3 SpeechSuper) |
| 78 | Paddle Live 인프라 종결 + 부채 9건 처리 + M-010 사고 학습 + C1 부분 cleanup | 보안/인프라/품질/사고학습 | **본 세션 (2026-05-08, 12 commit) — A1 카테고리 모두 ✅ + Q7 + F 4건 + G8 + C2 + C1 🟡**. **묶음 1 (A1 KYB stale 정정 9곳, commit `9da0179`)**: 사용자 어제 의문 ("이미 신청 다 완료된 상태인데??") 응답 위해 docs/메모리 전수 grep → KYB 이미 완료 (2026-02-21~25 추정 승인) 사실 발견 + AMK_DEBTS A1 표 (~~A1-2~~ ✅ Webhook Secret 확보 + ~~A1-3~~ ✅ KYB) + A1-5 신규 (은행 USD 계좌 HIGH) + AMK_STATUS Q7/검증된 리스크 표 + AMK_DEPLOY_OPS §7.6/§8.5 + 메모리 다수. **묶음 2 (A1-5 ✅, commit `b4e993b` + `055367b`)**: 사용자 하나은행 USD 통장 개설 완료 통보 + 사진 확인 = 법인 명의 `HYMN CO.,LTD.` (Multi-Foreign Currency, KEB Hana Bank Sejong Jungang, SWIFT KOEXKRSE, 개설일 2026.03.16). **묶음 3 (M-010 사고 정정, commit `5e4d8c5`)**: A1-1 진행 권고 후 검증 시점에 Live 활성 사실 발견 (sandbox=false / Live IDs / GitHub Secrets 13개 등록 2026-03-18). 사용자 책임 전가성 답변 사고 → **M-010 신규 등재** (권고 전 외부 검증 5초 투자 누락 + 부분 정정 패턴). 회피 룰 정착 = 카테고리 stale 발견 시 모든 항목 즉시 동일 검증. **묶음 4 (§7.6 SPF 가이드 정정, commit `b6b7b9c`)**: 어제 작성 시 호스트명 외부 검증 누락 → `_spf.resend.com` (NXDOMAIN, 잘못) → `send.resend.com` (실 DNS lookup 검증, AWS SES chain). Paddle SPF = 불필요 (`@paddle.com` 자체 발송). **묶음 5 (A1-4 ✅, commit `9904830`)**: 사용자 Cloudflare DNS 작업 + Google DNS polling propagation 검증 = `v=spf1 include:send.resend.com include:_spf.mx.cloudflare.net ~all`. SPF chain + lookup 카운트 검증 통과. **묶음 6 (Paddle Payout 검증, commit `8af5b40`)**: 사용자 Paddle Dashboard 스크린샷 2장 → Wire transfer / USD / Bank `KEB Hana Bank` 본점 / Account Holder `HYMN CO.,LTD.` / SWIFT `KOEXKRSE` / Account Number `91591001863238`. **묶음 7 (E2E #1-3 ✅ + Q7 종결, commit `7756b1e`)**: Claude curl 검증 (Health 200 + Plans sandbox:false + Catalog Live IDs). E2E #4-11 = 실제 학습 콘텐츠 시딩 완료 후 진행 결정 (사용자, 빈 시스템 실 결제 비용 회피). Q7 종결. **묶음 8 (F 카테고리 stale 정정 + F4 ✅, commit `638473f`)**: mobile 리포 cross-check → F1 (flutter_rust_bridge 핀닝, M1b 2026-04-07) + F2 (E-book 뷰어 OOM, M6 2026-04-06 LRU + cacheWidth) + F3 (iOS isSecureTextEntry, M7 2026-04-06 no_screenshot 1.1.0) = 모두 처리됨 사실 발견. F4 = `EBOOK_SESSION_TTL_SEC 90→300` 본 리포 적용 (모바일 표준). **묶음 9 (G8 ✅, commit `01758c5`)**: 사용자 GitHub UI 적용 (main = PR 강제 + linear history + force/deletion 차단 / KKRYOUN = force push 허용 + deletion 차단). `gh api` 검증 통과. **묶음 10 (C2 ✅, commit `88e912e`)**: 신규 토큰 6개 (`highlight` + `level-1/2/3/4/5`) 정착. 9곳 위반 → 의미별 토큰 매핑 (status-success / highlight / destructive / level-N). lint:ui 0 errors + build 19.02s. **묶음 11 (C1 🟡 부분, commit `6cd3d06`)**: 12/40 cleanup (자동 fix 2 + react-refresh 7 eslint-disable 의도 + no-empty/no-unused-vars/use-memo 단순 fix 3). 잔여 28 = 코드 구조 변경 (static-components 9 + refs 6 + set-state 2 + warnings 12) = 새 세션. **부채 카운트**: 42 → **33** (-9건). A1 5 + Q7 + F 4 + G8 + C2 = 종결. C1 부분 (미해결 유지). **Paddle Live 인프라 측 = 100% 활성** (KYB + Secrets + 배포 + 통장 + SPF + Payout + E2E #1-3). E2E #4-11 = 시딩 트리거. **M-010 사고 학습 핵심**: (1) 카테고리 stale 발견 시 모든 항목 즉시 동일 검증 (KYB stale = A1 전체 의심) / (2) 권고 전 외부 상태 확인 5초 투자 (`/payment/plans` / `gh secret list` / `dig` / `dns.google`) / (3) 사용자 인지도 stale 가능성 단정 회피. 본 세션 = 같은 패턴 반복 (textbook:442 emerald 누락 / lint:ui 9→10 / F1/F2/F3 stale 미인식) = 룰 정착 + 행동 변화 진행 중. | 2026-05-08 | C1 잔여 28 새 세션 / G10 ai 세션 / N-26 i18n 결정 / 외부 트리거 (A2/D RDS 이전 / E1 9건 / E2 books / E3 SpeechSuper) |
| 77 | B5 hot path expect 제거 + 부채 묶음 정리 (A4-4 ✅ + A1-4/G8 가이드 + B6/E2 결정 + §7.6 SSL 정정) | 보안/인프라/품질 | **본 세션 (2026-05-07 심야~새벽) 두 묶음 처리**. **묶음 1 (B5 hot path, commit `64f0baf`)**: `src/api/auth/service.rs:447` `user_info.expect("checked above")` 를 `let-else` 패턴으로 리팩터. `if user_info.is_none() || !password_ok` 분기를 `let Some(user_info) = user_info else { return 401 }` + `if !password_ok { 실패 로그 + 401 }` 2단 분리. invariant 가 코드로 표현됨. anti-enumeration / timing attack 보호 / 실패 로그 보존 (변경 없음). backend expect 카운트 52→51 / B5 회색 7→6 / hot path 회색 1→0 = production 운영 중 unexpected panic 위험 expect 완전 제거. 검증: `cargo check/fmt/clippy --lib --bins -D warnings` ✅. **묶음 2 (운영 부채 5건, commit `2c31eb1`)**: **A4-4 ✅ 백업 자동화** — `scripts/backup.sh` 신규 (87 라인, `pg_dump --exclude-table=_sqlx_migrations` (gzip) + Redis BGSAVE/LASTSAVE polling 60s + tar.gz 통합 archive + `BACKUP_RETENTION_DAYS=7` 자동 회전 + 로그). 옵션 A 수동 정기 결정 = EC2 일 1회 KST 03:00 cron + 사용자 PC 주 1-2회 수동 scp pull (Windows + WSL → `D:\amk-backup` 14일+4주 회전). 한계 명시 (사용자 PC 의존 + 수동 pull + 암호화 미적용). RDS 이전 시 (A2 트리거) AWS 관리형 자동 전환. `AMK_DEPLOY_OPS §6` cron 등록 + scp 가이드 정착. **A1-4 🟡 SPF 가이드** — `AMK_DEPLOY_OPS §7.6` 신규 섹션 "이메일 발송 SPF 정책". 병합 레코드 = `v=spf1 include:_spf.resend.com include:_spf.paddle.com ~all` (Cloudflare DNS 1개 TXT). 검증 절차 (`dig +short TXT` + 외부 SPF 검증 도구) + 함정 회피 (TXT 분리 무효 / include 한도 10 / `~all` vs `-all`). DNS 작업 = 사용자 (KYB 후, 5-10m). **G8 🟡 branch protection 가이드** — main = PR 강제 (review 0 허용) + linear history + force push/deletion 차단 / KKRYOUN = direct push 자유 + force push 허용 + deletion 차단. `gh api PUT` = security 권한 차단 → 사용자 GitHub 웹 UI 직접 적용. **B6 🟡 ipgeo 결정 정착** — 수익 발생 후 유료 전환 (ip-api $13/월~) 또는 MaxMind GeoLite2 별도 트랙. 사유: GeoIP 영향 작음 (인증/결제 영향 X) + 평문 노출 정보 작음 (IP + 대략적 지역). **E2 🟡 콘텐츠 시딩 트리거 정착** — `E-FUTURE-1` = books 리포 분류/수정 완료 후 본 리포 진입. 본 리포 능동 작업 0 (외부 트리거). **§7.6 SSL/TLS stale 정정** — Phase B 완료 (2026-05-07 저녁 #76) 결과를 `사용 영역` 표 + `비상 시 절차` 표에 반영 (Flexible 모드 → **Full (Strict) 모드**, origin Let's Encrypt cert 활성). **부채 카운트**: `AMK_DEBTS §0 = 42건 그대로` (A4-4 는 §0 카운트 외 / B6 결정 정착도 HTTP-only 자체는 미해결 = 카운트 영향 X). **`.gitignore` 보강** — `scripts/backup.sh / dev_preflight.sh / mk-support-bundle.sh` 화이트리스트 명시 (기존 tracked 보장). | 2026-05-07 | **사용자 결정 대기 5건**: A1-3 Paddle KYB 현재 상태 / F 의도 (F1/F2 외부 vs F4 본 리포 처리) / F4 EBOOK_SESSION_TTL_SEC (옵션 C 300초 권장) / G10 백엔드 테스트 시작 도메인 (auth 권장) / N-26 i18n 방향 (ai 측 4월 14일 stale 발견, 재가동 vs 본 리포 직접 vs 영어 fallback) |
| 76 | HTTPS end-to-end + Cloudflare Full Strict 정착 (인프라 묶음 Phase A+B) | 보안/인프라 | **배경**: 2026-02-10 운영 시작 이후 Cloudflare ↔ origin 사이 = HTTP 평문 (Flexible 모드). 사용자 ↔ Cloudflare 만 HTTPS = "Cloudflare 인프라 신뢰 가정"에 의존하는 보안 갭. 결제/개인정보 다루는 글로벌 서비스 보안 모범 사례 미달. **Phase A (코드/docs/compose 정비, 2026-05-07 오후)**: `nginx/nginx.conf` HTTPS 블록 정교화 (TLS 1.2+1.3 / Mozilla Intermediate cipher / OCSP stapling / HSTS / SSL session cache / Referrer-Policy / X-Frame / rate limit). 주석 유지 = Phase B 활성 시 안전 default. `docker-compose.prod.yml` certbot entrypoint = `--quiet --deploy-hook` 추가. `AMK_DEPLOY_OPS §3` Phase B 단계별 절차 정착 (인증서 발급 / Cloudflare DNS grey-cloud 임시 / SSL 모드 Flexible → Full Strict 전환 / 검증 / 롤백 시나리오 3건). §9 Cloudflare 정책 정정 (Flexible 보안 갭 명시 + Full Strict 권장). **Phase B (사용자 EC2 + Cloudflare 작업, 2026-05-07 저녁)**: B-1~10 단계별 실행. (1) Cloudflare DNS api A 레코드 grey-cloud 임시. (2) `certbot --dry-run` + 실제 발급 (Let's Encrypt cert, 만료 2026-08-05). (3) DNS orange-cloud 복귀. (4) `nginx-https-enabled.conf` 별도 파일 commit + cp + nginx -t 검증. (5) docker bind mount stale 발견 → `docker restart amk-nginx` (mount 새로 attach + nginx fresh start). (6) Cloudflare SSL 모드 → **Full (Strict)** 전환 = end-to-end 암호화 + valid CA cert 검증. (7) host crontab 매일 03:00 nginx reload (Amazon Linux 2023 cronie 별도 설치) + `renew --dry-run` 통과. **검증**: `curl https://api.amazingkorean.net/health` 200 / TLSv1.3 handshake / Server certificate Let's Encrypt R10 / SSL Labs B 등급 (Cloudflare edge default 영향, origin 자체 A+ 수준). **부채 해결**: `AMK_DEBTS` A4-1 + A4-2 = ✅ / `AMK_AUDIT` N-13 = ✅ (~~3건~~ → **1건** 잔여 = N-26 i18n). **신규 부채 등재**: B8 SSL Labs B → A+ 강화 (Cloudflare SSL/TLS Edge Certificates 설정, 사용자 결정, 우선순위 낮음). **운영 학습 정착**: docker bind mount stale 패턴 (host cp 후 컨테이너 안 옛 config = restart 또는 md5 비교 검증) / Amazon Linux 2023 cron 미설치 (cronie + crond enable 필요) / cron editor (vim) 우회 (`echo > /tmp/mycron && crontab /tmp/mycron`) | 2026-05-07 | SSL Labs A+ (B8, 사용자 결정) |
| 75 | 관리자 대리 주문 생성 (POST /admin/textbook/orders) | 기능/풀스택 | **배경**: 영수증 발급(#73) 과 페어. 외부(전화·이메일·오프라인) 주문을 시스템에 입력하거나 통계 관리용으로 관리자가 직접 생성. 대리 주문 → status=paid 즉시 세팅 → 영수증 발급 흐름. **백엔드**: `textbook/repo.rs InsertOrderParams.user_id: i64 → Option<i64>` (DB 컬럼 원래 nullable), `admin/textbook/dto.rs AdminCreateOrderReq` 신규 (`user_id`/`initial_status`/`enforce_min_quantity` + CreateOrderReq 동일 필드), `admin/textbook/service.rs::create_order` (검증 → insert → update_status 로 paid_at 자동 세팅 → admin_textbook_log Create 기록 → confirmation 이메일), POST /admin/textbook/orders 라우트 등록. `UNIT_PRICE`/`MIN_TOTAL_QUANTITY`/`catalog_languages` pub(crate) 승격. **프론트**: `admin/textbook/page/admin_textbook_order_create.tsx` 신규(~450줄, 관리자 옵션+신청자/배송/품목/세금계산서/비고), `use_admin_textbook::useAdminCreateTextbookOrder` 훅, 주문 목록 헤더 "+ 새 주문 생성" 버튼, `/admin/textbook/orders/new` 라우트 등록. i18n ko/en: `admin.textbook.create.*` 40여 키. **재사용**: 기존 `TextbookRepo` 함수 전부(신규 SQL 0건) + `TextbookService::get_order_by_id`. **검증**: `cargo check` 클린 + `npm run build` 8.95s 클린 ✅ | 2026-04-19 | 사용자 검색 UI (user_id 자동완성), 잠복 버그 후속 PR(study_task_explain Rust 참조) |
| 73 | 교재 주문 간이 영수증 발급 기능 | 기능/UI | **배경**: 실 주문 1건에서 영수증 발급 요청. 기존 견적서/주문확인서 인쇄 페이지에 `?type=receipt` 3번째 타입 추가로 시스템화. **공급자 상수** `frontend/src/category/textbook/supplier_info.ts` 신규 (HYMN 법인 정보 `(주) 힘 HYMN Co., Ltd.` / 사업자등록번호 `505-88-03252` / 대표자 `김경륜 (Kyoung Ryun KIM)` / 주소 `세종시 한누리대로 350 6층 SB3호`). **인쇄 페이지** (사용자 `textbook_order_print.tsx` + 관리자 `admin_textbook_order_print.tsx`): isReceipt 분기 — 공급자 정보 박스 + `paid_at` 기준 발행일 + 공급가액/VAT/합계 3단 분리 (`total_amount / 1.1` 역산) + 서명란 + 입금 계좌 비노출 + `paid_at=null` 시 발급 거부. **버튼**: 사용자 주문 상태 페이지 + 관리자 주문 상세, `order.paid_at != null` 조건부 노출. **i18n**: `textbook.print` 22개 locale 영수 키 10개 추가 (ko/en 완전 번역, 나머지 20개 영어 fallback) + `admin.textbook.print` ko/en 양쪽 + `admin.textbook.printReceipt` 버튼 레이블. **검증**: `cd frontend && npm run build` 9.31s 클린 ✅ | 2026-04-19 | 법인 인감 이미지 업로드, 영수증 고유번호 체계, 다른 locale 개별 번역 |
| 72 | 교재 500문장 시딩 선행 마이그레이션 M01~M04 (단일 PR) | 콘텐츠/스키마 | **목적**: 500문장 × 36언어 번역 시딩(M05~M08) 전 스키마 정리. 총 8개 마이그레이션 중 선행 4건(스키마·레거시 정리) 을 단일 PR 로 일괄 반영. **M01 `20260418_study_task_idx.sql`**: `study_task` 에 `study_task_idx varchar(100) NOT NULL UNIQUE` 컬럼 추가. 해설집 문장 참조 안정 키 + 재시딩 멱등성 + `content_translations.content_id` 논리 연결 안정성 3가지 요구를 단일 컬럼으로 해결. **M02 `20260419_reset_test_studies.sql`**: `seeds/20260208_AMK_V1_SEED.sql` 의 test-1~test-9 더미 + 연관 로그 전량 삭제 + IDENTITY 시퀀스 1 부터 재시작. **M03 `20260420_rename_basic_900.sql`**: `ALTER TYPE study_program_enum RENAME VALUE 'basic_900' TO 'basic_500'` + Rust (`types.rs`/`study/service.rs`/`handler.rs`/`admin/study/stats/*`) + Frontend (`admin/study/types.ts`) + **i18n locale 22개** (`programBasic900`→`programBasic500` + 레이블 "900"→"500" 다국어 숫자 포함) 동시 변경. **M04 `20260421_expand_supported_languages.sql`**: `supported_language_enum` 에 13개 언어 추가 (`tl`/`tr`/`bn`/`ar`/`ur`/`fa`/`lo`/`ky`/`it`/`sw`/`uk`/`am`/`pl`). sentences.json 36개 번역 중 기존 enum(22) 미지원 13개 커버. `pt_pt`→`pt` 병합. Rust `SupportedLanguage` 13 variant 추가. **문서**: `AMK_API_MASTER §4.8` + `AMK_SCHEMA_PATCHED §2.4.2/§2.4.6` + `AMK_API_FUTURE` + `AMK_CODE_PATTERNS` + `AMK_CHANGELOG` 동기화. `study_explain` → `study_task_explain` 스키마 문서 불일치 정정(실 DB 기준). **검증**: `cargo check --all-targets` ✅, `cargo clippy --all-targets -- -D warnings` 0 warnings ✅, `cargo sqlx prepare` ✅, 로컬 DB M01~M04 전체 적용 (`study_task_idx` NOT NULL UNIQUE, enum 35개, study/study_task 0 rows seq=1) ✅, `cd frontend && npm run build` ✅ | 2026-04-18 | 후속: M05~M08 시딩 본체 (sentences.json → SQL 생성 스크립트 amazing-korean-books one-off 선행 필요) + HYMN system 계정 프로덕션 선제 생성 |
| 71 | INC-001 재발 방지 외부 모니터링 구축 | 관측/인프라 | **UptimeRobot Free 플랜 HTTP 모니터** 세팅 — `https://api.amazingkorean.net/health`, 5분 간격, 이메일 알림(`amazingkoreancenter@gmail.com`). 발사 테스트 완료 (DOWN 알림 1~2분 내 수신 확인). GitHub Actions deploy "success" false positive 와 독립. **부가**: `nginx/nginx.conf` 에 `gzip_min_length 1024;` + `location = /health { gzip off; ... }` 추가 (origin 평문 유지, 범용 튜닝). `.github/workflows/deploy.yml` scp source 에 `nginx/nginx.conf` 추가 + Deploy step 에 `nginx -t && nginx -s reload` (config 반영 자동화). `docs/AMK_DEPLOY_OPS.md §7.5` 신규 — 구성/테스트/업그레이드 옵션. **학습 (Keyword 모니터 포기 경위)**: CF Free 플랜 edge 가 Accept-Encoding: gzip 요청에 자체 재압축 → UptimeRobot raw 바이트 keyword 검색 실패. nginx gzip off + `Cache-Control: no-transform` 시도했으나 (a) CF 가 origin 헤더를 strip 하거나 (b) 무시, curl 실측으로 확인. CF Custom Request Headers 는 유료라 `Accept-Encoding: identity` 회피 불가. grey-cloud DNS+자체 SSL 은 과잉. INC-001 감지에는 HTTP 상태코드 모니터만으로 충분 (컨테이너 crash → CF 521 → 200 아님). | 2026-04-17 | Cloudflare Pro 구독 시 CF Health Checks 로 upgrade 가능, grey-cloud 서브도메인으로 Keyword 복원 가능 |
| 70 | JsonRejection → AppError 통합 | 코드 품질 | `src/extract.rs` 신규 — `AppJson<T>` 커스텀 extractor (axum `FromRequest` 구현). JsonRejection 을 AppError 로 매핑: JsonDataError → Unprocessable(422), JsonSyntaxError/MissingJsonContentType/BytesRejection → BadRequest(400). `src/lib.rs` `pub mod extract;` 추가. 19개 핸들러 파일에서 요청 바디 추출기 `Json(x): Json<T>` → `AppJson(x): AppJson<T>` 치환 (80 call sites, perl 정규식 일괄 + import 추가). 응답 직렬화 `Json(res)` 는 실패 가능성 없어 그대로 유지. `AMK_CODE_PATTERNS` §1.3(핵심 포인트+규칙) / §1.4(handler 예제) 갱신. **효과**: #69 trace_id 검증 시 발견한 갭 (Axum `Json<T>` extractor 실패 시 `text/plain` 응답으로 표준 envelope + trace_id body 필드 우회) 제거. 전 엔드포인트 에러 응답 통일. **검증**: `cargo check` + `cargo clippy --lib --bins` 0 warnings. **런타임 4/4** (로컬): (1) 필드 누락 `{}` → 422 JSON `UNPROCESSABLE_ENTITY` "Invalid JSON data: missing field \`email\`" + trace_id, (2) 깨진 JSON `{invalid}` → 400 `BAD_REQUEST` "Malformed JSON", (3) Content-Type 누락 → 400 `BAD_REQUEST` "Expected Content-Type: application/json", (4) 정상 JSON + 잘못된 자격 (회귀 확인) → 401 기존 동작 유지 | 2026-04-16 | — |
| 69 | 요청 trace_id 구현 | 관측 | `src/trace_id.rs` 신규 — `tokio::task_local! REQUEST_ID` + `middleware` (UUID v7 생성 / 유효한 업스트림 `x-request-id` 승계, 형식 검증 ASCII alphanumeric·`-`·`_` ≤128자). `TraceId` extension 주입, 응답 헤더 `x-request-id` 에코백. `src/error.rs:210` 하드코딩 `"req-TODO"` → `crate::trace_id::current()` 동기 조회. `src/main.rs` 최외곽 layer 로 바인딩 (보안 헤더·CORS 위) + CORS `expose_headers("x-request-id")` + `allow_headers` 에 `x-request-id` 추가. `Cargo.toml` uuid feature `v7` 활성화. `AMK_CODE_PATTERNS.md:319` 스테일 PLACEHOLDER 주석 교체 + 에러 처리 섹션 trace_id 문단 추가. **효과**: 에러 응답 trace_id ↔ 서버 로그 ↔ Cloudflare 요청 ID 상관추적 가능. INC-001 진단 속도 저하 원인 중 하나 제거. 병행 작업 🅱️ 일정 앞당겨 처리 (D+4~5 예정 → D+0 착수). **정적 검증**: `cargo check` 31.65s + `cargo clippy --lib --bins` 28.37s 0 warnings. **런타임 검증** (로컬 127.0.0.1:3100 + `curl -i` 6/6): (1) 헤더 누락 → UUID v7 신규 (version=7·variant=9 확인), (2) 유효 업스트림 승계, (3) 악성값 거부·재생성, (4) >128자 거부·재생성, (5) body `trace_id` = 응답 헤더 `x-request-id` (MATCH — `task_local!` scope 동작 증명), (6) 핸들러 경유 401 AppError 에서도 정상 반영 | 2026-04-16 | 핸들러에서 `Extension<TraceId>` 로 추출해 감사 로그·외부 API 호출 헤더에 전달 (후속) · **알려진 갭**: Axum `Json<T>` extractor 실패 시 `text/plain` 응답이 AppError envelope 우회 (§8.2 보류 항목 추가) |
| 68 | Gemini 리뷰 백로그 전건 반영 (PR #157~#163 누적 18건) | 보안/성능/문서/i18n | **HIGH 2 + MEDIUM 16 = 18건 D+0 한 세션 처리**. **HIGH**: (1) `src/api/auth/repo.rs::find_login_refresh_hashes_by_session_ids` + `update_login_states_by_sessions` 의 `WHERE login_session_id::text = ANY($1)` → `ANY($1::uuid[])` 로 UNIQUE 인덱스 사용 복원 (풀스캔 방지). **MEDIUM A/i18n 4건**: `kk.json:744` `мұрақаты`(archive)→`нөмірі`(number), `hi.json:722,727` `टू-फैक्टर`→`टू-फ़ैक्टर`(nukta), `mn.json:804` `Дараа нь`→`Дараах`(Next). **MEDIUM B+D/문서 3건 + NIT 2건**: `CHANGELOG` `BingBot`→`Bingbot`, `CODE_PATTERNS:319` `req_id`→`"PLACEHOLDER"` + 주석, `SCHEMA:560` `Paddle/payment`→`Paddle/transactions`. NIT 2건(메모리 외부 참조)은 1인 CEO + 메모리 시스템 의도적 분리 + 교훈 인라인 요약 포함으로 처리 스킵. **MEDIUM C/코드 2건**: `src/api/mod.rs::robots_txt` 하드코딩 문자열→`const CRAWLERS: &[&str]` 배열 + `format!`/`join`, `src/main.rs::security_headers` 5개 `.parse().unwrap()`→`HeaderName::from_static` + `HeaderValue::from_static` (런타임 panic 방지). **MEDIUM E/auth 성능 5건**: `enforce_session_limit` 유령 탐지 2단계 `exists`→`redis::pipe()` (2N→2), 유령 정리 SREM 배치 (N→1), eviction DEL+SREM 배치 (3N→2), `logout_all` DEL+SREM 배치 (3N→2), `apple.rs::get_decoding_key` write-lock 후 Double-Checked Locking 재확인. **검증**: `cargo check` 7.79s + `cargo clippy --lib --bins` 0 warnings 12.04s. **워크플로우 교훈**: 초기에 feature 브랜치 6개 + PR #164~#169 분할 제출했다가 사용자의 "분리 PR" 용어 의미(브랜치가 아닌 커밋 분리)와 달라 close + KKRYOUN 단일 PR 재구성. `feedback_work_rules.md` 에 규칙 저장 | 2026-04-16 | `project_gemini_review_backlog.md` 메모리 삭제 (PR 머지 후) |
| 67 | E-book session_id 필수화 — Phase 2~5 전환 완료 | 보안 | **Phase 1 관측 (2026-04-16~2026-04-23, D+0~D+7) 결과 0건** — `docker logs amk-api --since 168h \| grep EBOOK_SESSION_AUDIT \| wc -l` = 0 (2026-04-23 사용자 실측). 구버전 클라이언트 / 어뷰즈 / 캐시 모두 부재 확인. **Phase 2 전환** (2026-04-23, D+7 조건 충족 → D+8 일정 하루 앞당김): Backend `verify_session(st, user_id, session_id: &str)` — Option 제거, warn! 블록 제거, 내부 Option 언래핑 제거, 저장된 session_id ↔ 요청 session_id 엄격 비교. Handler 2곳 (`get_page_image`/`get_page_tile`) 헤더 파싱 `ok_or_else(|| Forbidden("Missing session header"))` 즉시 거부. **Phase 3 Web frontend**: `fetchPageImage`/`fetchPageTile` `sessionId?: string` → `sessionId: string`, 삼항 분기 제거 직접 `"X-Ebook-Session": sessionId` 대입, `hmacSecret && sessionId` → `hmacSecret` 만 가드. `usePageImage`/`usePageTiles` default `""` — meta 미로드 시 `enabled: false` 로 fetch 차단. **Phase 4 Desktop**: 웹과 동일 3 파일 (별도 PR `amazing-korean-desktop`). **Phase 5 Mobile**: `required String sessionId` 로 이미 준수 — 작업 없음. **검증**: `cargo check` 14.66s + `cargo clippy --lib --bins` 0 warnings + `npm run build` 9.77s 클린 (로컬). 배포 후 모니터: `docker logs amk-api --since 24h \| grep "Missing session header"` → 0 or 극소 (의도된 거부만). **플랜 SSoT**: `~/.claude/plans/ebook-session-required-phase25.md`. **관측 모드 선택 근거**: INC-001 (2026-04-15 프로덕션 2h33m 다운) 경험으로 "fail-closed 게이트 추가는 코드 분석만 신뢰 말고 프로덕션 로그로 선확인" 방침 확립. D+7 실측 통과로 방침 성공 실증 | 2026-04-23 (Phase 2~5 완료, D+8 예정 대비 1일 앞당김) | **롤백**: Backend revert PR (handler 2곳 + service.rs 원복). 프론트는 sessionId 전송 상태라 optional 복귀 후 무해. 트리거: 24h 내 `"Missing session header"` 403 > 예상값 10배 or 사용자 이슈 리포트 |
| 66 | 속도 개선 Phase S5 — Swiper CSS 분리 + Pretendard 비동기 + 추가 lazy 5개 | 성능 | **S5-1**: `index.css` Swiper CSS import 4줄 → `seal_list.tsx` 로 이동 + 카탈로그 2페이지(Textbook/Ebook) eager → lazy. Swiper CSS 가 비-Book 페이지에서 완전 제외. `vite-env.d.ts` 신규 (sub-path CSS 타입 선언). **S5-2~3 SKIP**: 이미지 preload (book-hub) — 이미 eager/high priority + URL 동적 (i18n). cover srcset — 이미 40KB webp. FCP 가 우선 병목. **S5-4**: Pretendard CSS `media="print" onload` 패턴으로 비동기 로딩 (render-blocking 제거). **S5-5 SKIP**: Critical CSS inline — SPA 에서 비실용적 (HTML body=`<div id="root">`). SSR/prerender 필요. **S5-6**: book-hub FCP 8s 비정상값 발견 → `BookHubPage`/`BookLandingPage`/`AboutPage`/`LoginPage`/`SignupPage` 5개 eager → lazy (HomePage 만 eager). **결과**: 메인 번들 **277KB → 199KB (-28%)**. 로컬 Lighthouse: home **64→87** (FCP 5785→2612ms), faq **72→88**, book-hub **61→76** (LCP 9287→4375ms, -53%). 90+ 부분 달성. 잔여 갭은 SPA 구조적 한계 (CSS 66KB + JS 199KB) — SSR 도입 시점으로 이연 | 2026-04-16 | 90+ 도달 위해 SSR (Cloudflare Workers / vite-plugin-ssg) 도입 시점 검토 |
| 65 | SEO hardening — Cloudflare Managed robots.txt 우회 | SEO/보안 | **배경**: #64 배포 후 외부 검증에서 Cloudflare 가 zone 레벨로 `# BEGIN Cloudflare Managed content` 블록을 robots.txt 본문 앞에 자동 주입 발견. 주입 블록의 `User-agent: *` + `Allow: /` 가 우리 Disallow 와 tie-breaking 에서 Allow 승리로 무력화. **Fix A**: `src/api/mod.rs::robots_txt` 에 명시적 크롤러 10종(Googlebot, Googlebot-Image/News/Video, AdsBot-Google, Bingbot, DuckDuckBot, Yeti, NaverBot, Daum) 각각 `Disallow: /` + `*` 폴백. Google 의 "더 구체적인 user-agent 그룹 우선" 규칙으로 Cloudflare `*` 우회. **Fix B-2**: `src/main.rs::security_headers` 미들웨어에 `x-robots-tag: noindex, nofollow` 추가. 전역 Axum `.layer()` 이므로 **api 전체 응답에 자동 주입**. HTTP 헤더는 Cloudflare 가 수정 불가 → robots.txt 와 독립 작동하는 2중 방어. **Fix C**: Cloudflare Zone 설정 확인 결과 AI Crawl Control "관리형 robots.txt" 토글 ON, zone-wide 단일 토글(호스트/경로 제외 미지원). 프론트엔드 AI 봇 자동 차단(15건/일) 유지 위해 **ON 유지 결정** — 저희 코드 수정이 Cloudflare 와 독립 작동하도록 설계돼 전략적으로 충분. **검증**: PR #161 재배포 후 외부 curl 검증 — `curl -I /`, `/health`, `/auth/google` 모두 `x-robots-tag: noindex, nofollow` 확인, 쿼리스트링 캐시 우회 시 확장된 robots.txt 본문 확인, 기존 엔드포인트 회귀 없음. Cloudflare 엣지 캐시 `cf-cache-status: HIT` + `max-age=14400` (4h TTL) 관찰 — 일반 요청은 4시간 이전 버전 서빙 중이나 X-Robots-Tag 가 이미 결정적 차단이라 대기 선택. **교훈 메모리**: `feedback_seo_api_subdomain.md` 신규 | 2026-04-15 | 향후 공개 색인 엔드포인트 추가 시 x-robots-tag override 로직 필요 |
| 64 | SEO 3건 수정 (Google Search Console) | SEO | **배경**: Search Console "페이지 색인 안 되는 이유" 대시보드에서 Soft 404(1) + 404(1) + 리디렉션 실패(3) 발견. 리디렉션 3건은 `_redirects` 301 정상 작동 중(무해). 실제 수정 대상 2건. **Fix 1 /courses Soft 404**: React Router catch-all 이 200 + NotFoundPage 반환해 Google 이 Soft 404 로 분류. `frontend/public/_redirects` 에 `/courses /book 301` 추가. 의미상 `/pricing`(ComingSoon) 이 맞지만 실콘텐츠 `/book` 으로 보내 재발 방지. **Fix 2 api.amazingkorean.net/ 404**: Google 은 (1) `GET /auth/google` JSON 의 auth_url 에 redirect_uri 평문 노출, (2) Google Cloud Console OAuth Client 승인 URI, (3) sc-domain 속성의 서브도메인 자동 감지 3경로로 알게 됨 (차단 불가). 대응: `src/api/mod.rs` 에 `GET /` 200 JSON 서비스 메타 + `GET /robots.txt` `User-agent: *\nDisallow: /\n` 핸들러 2개 추가. Google 봇이 이후 api 서브도메인 전체 크롤링 금지. **검증**: `cargo check` + `cargo clippy --lib` 0 warnings + `npm run build` 클린, `dist/_redirects` 복사 확인 | 2026-04-15 | `/pricing` 실페이지 전환 시 `/courses` 목적지 재검토 |
| 63 | 🚨 INC-001 프로덕션 백엔드 2h33m 다운 + 복구 | 인시던트 | **원인**: Gemini 리뷰 H1 IAP fail-closed 커밋(`017e8c1`) 의 `src/config.rs:441` production panic gate (`APP_ENV=production + REVENUECAT_API_KEY.is_none() → panic!`) 가 `.github/workflows/deploy.yml` 의 `.env.prod` heredoc 동기화 누락과 충돌. 배포 후 Rust 부팅 panic (exit 101) → `restart: always` crash loop → Cloudflare 502 Bad Gateway. GitHub Actions 는 `docker compose up -d` exit 0 으로 "success" 표기해 탐지 실패. **다운타임**: 2026-04-15 00:32~03:05 UTC (2h33m). **발견**: SEO 작업 중 Search Console `api.amazingkorean.net` 404 기록을 `curl` 로 조사하다 502 확인. **긴급 복구**: EC2 SSH → `.env.prod` 에 `REVENUECAT_API_KEY=placeholder_not_configured_yet` 주입 → `docker compose up -d api` recreate → 정상 부팅 (`✅ Server listening on http://0.0.0.0:3000`). **영구 수정**: 커밋 `d528e04` / PR #159 로 `deploy.yml` 에 `REVENUECAT_API_KEY` + `REVENUECAT_WEBHOOK_AUTH_TOKEN` 2줄 추가. GitHub Secrets 2건 등록(placeholder 값). **현재 보안**: IAP 경로 runtime fail-closed 유지 (placeholder 키로 RevenueCat 호출 실패 → 결제 우회 재발 없음). 실 키 준비 시 Secret 값만 교체하면 됨. **검증**: 재배포 후 `curl /health` 3회 200, `/auth/login` 422. **교훈**: `feedback_deploy_env_sync.md` — production panic gate 추가 시 deploy.yml + Secrets + DEPLOY_OPS §4 표 4건 동시 반영 필수 | 2026-04-15 | ~~배포 후 헬스체크 자동화~~ ✅ deploy.yml Health check step (#66 후속) + ~~Cloudflare 502 알림~~ ✅ UptimeRobot HTTP 모니터 (#71, 2026-04-17) |
| 62 | 문서·메모리 전수 조사 정리 | 문서/메모리 | **Plan mode 5 Phase 워크플로**로 누적 문서·메모리 정리, 커버리지 97% 달성. **메모리**: 완료 플랜 5개 삭제(`project_writing_practice_plan`/`strictmode_audit_plan`/`gemini_review_backlog`/`design_md_plan`/`risk_analysis`) + 축약 2개(`perf_plan` S5만/`figma_plan` breadcrumb만) + `project_status.md`·`MEMORY.md` 갱신. 20→14개 (-30%). **문서 고아/스테일 삭제**: `AMK_SCHEMA_PATCHED.sql`(0건 참조 확인) + `AMK_PIPELINE.md`·`AMK_MACMINI_SETUP.md`(amazing-korean-ai 이관 완료 껍데기) + `AMK_DESIGN_MD_ANALYSIS.md`(v4.2 통합 전 분석) + `frontend/DESIGN.md`(Stitch 미사용) + `.claude/plans/EXTERNAL_API_INTEGRATION_PLAN.md`(Stripe→Paddle 폐기). **참조 재매핑**: PIPELINE/MACMINI 참조 9곳을 `amazing-korean-ai/docs/AMK_AI_*.md`로 일괄 수정. **§8.2 완료 10행 strikethrough 제거** (§8.1 중복). **spec-to-code 갭 해소**: `AMK_API_PAYMENT.md`의 `⚠️ 미구현` 마커 8건 제거 + 실제 코드 필드(`course_count`, `expire_at`)로 통일. **스테일 메타 갱신**: `AMK_CODE_PATTERNS.md` 타임스탬프 5곳 2025-01-22→2026-04-08, `line 319` `"req-TODO"` 플레이스홀더 교체, `AMK_SCHEMA_PATCHED.md:561` "추후 pay 테이블 연동" 주석 수정. **크로스 프로젝트 인계**: mobile M5.5 IAP + desktop CORS stale 식별 (각 프로젝트 세션에서 반영 예정) | 2026-04-15 | — |
| 61 | Gemini 리뷰 전수 반영 (PR #152~#155 세션 A~D 일괄) | 보안/결제/성능/UX | Gemini 리뷰 13건 중 미처리 12건(HIGH 5 + MED 7) 을 1개 묶음 PR 로 처리. **A/H1** IAP fail-closed(`config.rs` 프로덕션 RevenueCat 게이트). **A/M1** RevenueCat RFC3339 DateTime 파싱. **C/H2+H3+M4+M5** `enforce_session_limit` 전면 리팩터: `find_login_refresh_hashes_by_session_ids` + `update_login_states_by_sessions` 배치 repo 함수 2개 신규, `find_active_sessions_oldest` 반환을 `Vec<(sid, refresh_hash)>` 로 확장, 유령 세션 정리/eviction 루프 N+1 제거, Redis 호출 fail-closed(`?` 전파), DB 빈 결과 시 random eviction 금지(명시적 에러). **D/H4+M7** K6 thresholds: 커스텀 `Trend` 4개 삭제 → http 호출부 `tags:{endpoint:...}` 직접 부여(기존엔 태그 필터가 no-op 였음), `get_task_status` 를 `endpoint:progress` 로 태깅해 progress 시나리오 신설. **D/M2+M3** Apple OAuth 싱글톤(`AppState.apple_oauth: Arc<AppleOAuthClient>`) + JWKS `Arc<RwLock<HashMap>>` 캐시로 매 로그인 JWKS fetch + reqwest 재생성 제거. **D/M6** `index.html` noscript fallback 으로 Noto Color Emoji 폰트 JS off 환경 대응. **B/H5** `WritingPracticeInput` 한글 IME 조합 버그 3종: (1) 마지막 글자 `isComposing` 시 `pending` 유지, (2) 다음 자모 하이라이트가 조합 중 음절에서 `decomposeSyllable` 기반으로 다음 기대 자모 계산, (3) `isComposing` 의존성 배열 추가. **보너스 4건**: 커밋 전 감사 중 발견한 동일 안티패턴을 `reset_password` / `logout` / `logout_all` / `reset_password_with_token` 에도 일괄 반영(N+1 제거 + fail-closed 전파). Rate-limit/verification/OAuth-state/MFA-token 의 `unwrap_or(())` 는 TTL 안전망 영역이라 의도적으로 유지. **검증**: `cargo check` + `cargo clippy --lib` (0 warnings) + `npm run build` 클린. IME 수동 브라우저 검증은 사용자 측. 남은 Gemini 백로그: **0건** | 2026-04-14 | IME 수동 검증 권장 |

> **암호화 참고**: 대상 PII — `user_email`, `user_name`, `user_birthday`, `user_phone`, `oauth_email`, `oauth_subject`, `login_ip`, `admin_action_log.ip_address`
> **키 관리**: `ENCRYPTION_KEY_V{n}` (AES-256, 다중 버전) + `HMAC_KEY` (blind index), KeyRing 로드
> **다국어 참고**: 21개 언어 (아랍어 RTL 제외), Fallback: 사용자 언어 → en → ko, 공개 조건: `status = 'approved'`

### 8.2 진행 예정 항목

> **실행 순서 SSoT**: [`AMK_APP_ROADMAP.md §4`](./AMK_APP_ROADMAP.md) — 의존성 그래프, 크리티컬 패스, 병행 가능 구간 포함.

#### 핵심 (실행 순서)

| 순서 | 항목 | 카테고리 | 예상 | 내역 | 조건 |
|:----:|------|---------|:----:|------|------|
| 1 | **Paddle Live 전환** | 결제 | 1일 | GitHub Secrets 교체 + 배포 + E2E 검증 | 언제든 가능 |
| 2 | **교재 번역 + PDF 생성** | 콘텐츠 | 병행 | 22→34언어 확장 완료. 번역 33언어 검증 완료. **남은: 13언어 PDF 재생성 + 22언어 PDF 갱신** (`amazing-korean-books` 프로젝트) | 전 기간 병행 |
| 3 | **학습 콘텐츠 시딩** | 콘텐츠 | 2-3일 | 교재 JSON → DB 시딩, 실 콘텐츠 투입. **진행 중** — 선행 마이그레이션 3개 PR 분할 (PR #1 M01+M04 ✅ 2026-04-18, PR #2 M02 예정, PR #3 M03 예정). 시딩 본체(M05~M08)는 amazing-korean-books 에서 SQL 생성 스크립트 작성 후 착수 | 해설용 출판본 완성 후 (교재 본체는 즉시 가능) |
| 4 | **모바일 앱** | 앱 | ~21-23일 | **Flutter** (`amazing-korean-mobile` 별도 리포). 상세: [`AMK_APP_ROADMAP.md §2`](./AMK_APP_ROADMAP.md) | **지금 착수** |
| 5 | **데스크탑 앱** | 앱 | ~7.5일 | **Tauri 2.x** (`amazing-korean-desktop` 별도 리포). 상세: [`AMK_APP_ROADMAP.md §3`](./AMK_APP_ROADMAP.md) | **지금 착수** |
| 6 | **RDS/ElastiCache 이전** | 인프라 | 3-5일 | EC2 단일 DB → AWS RDS + ElastiCache | 앱 개발 이후 |
| 10 | 다중 서버 구성 (HA) | 인프라 | — | ①nginx 복제 → ②ALB+EC2 → ③ECS Fargate | RDS 완료 후 |
| 11 | 시스템 모니터링 | 인프라 | — | DB/Redis 상태, Admin 대시보드 | 필요 시 |
| 12 | K6 성능 테스트 | 테스트 | `k6/` 디렉터리 세팅 완료 (smoke + load 시나리오) | 인증/조회/진도저장 부하 테스트, CI 연계 | 테스트 계정 생성 후 실행 |

**K6 성능 목표치 (엔드포인트별)**:

| 엔드포인트 | 목표 RPS | P95 응답시간 |
|----------|---------|-------------|
| 인증 (login/refresh) | 100 | < 200ms |
| 목록 조회 (videos/studies) | 200 | < 100ms |
| 상세 조회 | 300 | < 50ms |
| 진도 저장 (progress) | 100 | < 150ms |

**대표 시나리오**: 회원가입 → 로그인 → 비디오 조회 → 시청 → 진도 저장 → 학습 문제 풀이
| 7 | 마케팅/데이터 분석 | 기능 | 사용자 세그먼트, 리텐션 분석, 마케팅 자동화 | 데이터 기반 의사결정 | 사용자 확보 후 |

#### 이 리포(amazing-korean-api) 독립 착수 가능 큐 (2026-04-21 정리)

> 외부 의존 없이 이 리포 내에서 단독 진행 가능한 작업. 우선순위순 정렬.
> M05~M08 시딩 본체 작업은 `amazing-korean-books` `scripts/textbook/gen_seed_sql.js` 선행 필요해 이 목록에서 제외.

> **📌 Q13 — 쉬운 설명** (2026-04-28)
>
> **무엇**: 사이트 메뉴/버튼 번역을 22 → 36 언어로 확장. 학습 콘텐츠는 35 언어 번역 보유하나 UI(메뉴/버튼/에러 메시지) 는 22 언어만 — 그 갭 해소.
>
> **4단계**:
> 1. **DB 정리** ✅ (커밋 `731d4d1`) — `es-ES`(유럽 스페인어), `pt-PT`(유럽 포르투갈어) 언어 코드 추가. 2026-04-21 "유럽 변형은 일반 es/pt 로 합쳐도 돼" 결정 번복.
> 2. **UI 번역 15 언어 추가** — **맥미니가 함**. 메뉴·버튼 1,384개를 15 언어로 번역. 맥미니 `amazing-korean-ai` 리포에 검증된 자동 번역 시스템 (Wave 1) 보유 (이전 20 언어 4,330개 번역 실적). 13 lang 즉시 가능, es-ES/pt-PT 는 DB 정리 머지/배포 후.
> 3. **메타 표시** (1-2시간) — about/faq 페이지에 "본 콘텐츠는 36 언어로 번역됨" 표시.
> 4. **books 작업** — `amazing-korean-books` 리포 `gen_seed_sql.py` 의 es_es/pt_pt skip 로직 제거.
>
> **헷갈리지 말 것**: `content_translations` (DB) = **학습 콘텐츠** 번역 (강의/문제/해설). `i18n/locales/*.json` (frontend) = **UI 텍스트** 번역. 본 Q13 은 후자. 둘은 별개 도메인.

| # | 우선 | 작업 | 공수 | 근거 |
|:-:|:----:|------|:----:|------|
| ~~Q1a~~ | ✅ 완료 | ~~field_name 잠복 버그 fix~~ — Consumer service 4곳 (course/lesson/study/video) 짧은 이름 → 긴 이름 치환 + admin `find_content_records`/`find_source_fields` 에 Course·StudyTaskWriting 매핑 추가. Video 에 `video_title`/`video_subtitle` 필드 노출(source_text=None). 마이그레이션 불필요. **2026-04-21 완료** (KKRYOUN 브랜치, cargo check + clippy 클린). | 0.5일 | 2026-04-21 정합 조사. #76 동종 패턴. 실 `lesson_title='approved'` 1건 정상 반환 복원 |
| ~~Q1b~~ | ✅ 완료 | ~~`?lang=` 미구현분 구현~~ — `GET /videos/{id}` + `GET /studies/tasks/{id}` + `GET /studies/tasks/{id}/explain` 에 `?lang=` 파라미터 추가. task payload(choice/typing/voice/writing) 내부 + study_explain 본문 번역 주입. VideoDetailRes 에 title/subtitle 필드 신규. **2026-04-21 완료** (KKRYOUN 브랜치, cargo check + clippy 클린). video_tag 번역은 Q1c 로 이연 (response 에 video_tag_id 노출 필요 — 스키마 설계 결정 범위). | 0.5~1일 | Q1a 선행 완료. [AMK_API_LEARNING.md §9-841](./AMK_API_LEARNING.md) 참조 |
| ~~Q1c~~ | ✅ 완료 | ~~응답 스키마 최종 정렬~~ — (A) `TranslationMeta` + `TranslationCoverage` enum 신규, 10 Consumer 엔드포인트 루트에 `translation_meta` 필드 주입 / (B) video 테이블 `video_title`/`video_subtitle` 물리 컬럼 추가 + 백필 (migrations/20260422) + admin Create/Update 확장 (backward-compat) / (C) `VideoTagDetail.id` 추가 + service 에서 video_tag 번역 주입. **2026-04-21 완료** (cargo check/clippy/sqlx prepare/frontend build 전부 클린). | ~1.2일 | Q1a/Q1b 완료 후. 프론트 소비 0건이라 회귀 리스크 없음 |
| Q2 | 중간 | 법인 인감 이미지 업로드 — 영수증 서명란의 `(인)` 텍스트를 PNG/SVG 이미지로 대체. `supplier_info.ts` 에 `sealImageUrl` 추가, `ReceiptSignature` 에 `<img>` 렌더. | 반나절 | #73 영수증 "다음 단계" (AMK_CHANGELOG 2026-04-19) |
| Q3 | 중간 | 영수증 고유번호 체계 — 현재 `order_code` 재사용. 별도 `receipt_no` 필드(`R-YYYYMMDD-NNNN`) 추가 or 조합 규칙 정의. | 반나절 | 동일 |
| Q4 | 중간 | 기타 locale 영수증 번역 개별 추가 — 현재 20개 locale 은 영어 fallback. 일본어/중국어 우선 번역 권장. | 반나절 | 동일 |
| ~~Q5~~ | ✅ 완료 | ~~사용자 검색 UI~~ — `admin_textbook_order_create.tsx` 의 user_id 수동 입력을 검색 콤보박스로 대체. 신규 `UserSearchCombobox` 컴포넌트 (Input + 300ms debounce + 드롭다운). 백엔드 `useAdminUsers({q})` 재사용 (이메일 blind index exact match / 닉네임 LIKE). 수동 입력 토글 폴백 유지. **2026-04-22 완료** (프론트만, `npm run build` 9.09s 성공). | 반나절 | #75 "후속 작업" (AMK_CHANGELOG 2026-04-19) |
| ~~Q6~~ | ✅ 완료 | ~~`admin_textbook_log` Create 액션 조회 UI~~ — 신규 엔드포인트 `GET /admin/textbook/logs` + action/order_id/admin_user_id 필터 + 페이지네이션. 프론트: `/admin/textbook/logs` 페이지 + orders 페이지 상단 "감사 로그" 버튼. 관리자 이메일은 서비스 레이어에서 `CryptoService.decrypt` 복호화. **2026-04-22 완료** (cargo check + clippy 클린, frontend build 8.04s 성공). | 반나절 | 동일 |
| ~~Q7~~ | ✅ 종결 | ~~**Paddle Live 전환**~~ — 인프라 측 모두 ✅ 활성 (sandbox=false, Live IDs, SPF + DKIM + Payout 정착) | — | §8.2 #1 + §8.5. KYB ✅ (2026-02) + GitHub Secrets ✅ (2026-03-18 Live) + 배포 ✅ + 통장 ✅ (2026-05-08) + SPF ✅ (2026-05-08) + Paddle Dashboard Payout ✅ (2026-05-08) + **E2E #1-3 ✅ (Claude 검증)**. **E2E #4-11 (실 transaction / Webhook / 환불 / Retain / 프론트 UX) = 실제 상품 완성 후 진행 결정 (사용자 2026-05-08)** = 별도 트랙, 시딩 (#3) 트리거 후 |
| Q8 | 낮음 | **K6 성능 테스트 실행** — `k6/` 디렉터리 세팅은 완료. 테스트 계정 생성 후 smoke + load 시나리오 실행 | 0.5일 | §8.2 #12. **블록: 테스트 계정 생성 필요** |
| Q9 | 낮음 | **E-book 로컬 파일시스템 의존 해소** — `ebook/service.rs` 9곳 `fs::read` 를 S3/CDN 으로 전환. RDS 이전 선행 작업. | 3~5일 | §8.2 #6 검증된 리스크 CRITICAL. 앱 개발 이후 공식 로드맵에 있음 |
| ~~Q10~~ | ✅ 완료 | ~~QA run 2026-04-22 프론트 수정 3건 묶음~~ — 2.1 ebook subtitle 공백 + 2.2 textbook subtitle 공백 + 2.4 `/book` 캐러셀 dot `aria-label`. `<br className="hidden sm:block" />` 패턴 **전수 6곳** (ebook/textbook 카탈로그 + coming_soon + error 3종) 모두 `<>{" "}<br.../></>` 로 교체 (모바일 공백 보존). 캐러셀 dot 은 전수 **3곳** (book_hub + ebook_detail_modal + textbook_detail_modal) 에 `aria-label={t("common.goToSlide", { n })}` + `aria-current` 추가. i18n 키 `common.goToSlide` 신규 (ko/en). **2026-04-22 완료** (`npm run build` 9.74s 성공). | 30분 | `docs/QA_결과.md` 2.1/2.2/2.4 |
| ~~Q11~~ | ✅ 답변 완료 (코드 수정 없음) | ~~QA 2.3 pt 데스크톱 footer 텍스트 오버랩~~ — **시나리오 B (QA prompt 보강) 채택** (2026-04-23). 근거: Gemma reason 절반이 hallucination (프롬프트 첫 문장을 페이지 텍스트로 오인) + 1440px 스크린샷상 overlap 시각적 미확인 + 잠재 문제 구간 (768-1023px) 은 QA 뷰포트 매트릭스 밖. 실 버그 증거 없음. 전역 footer 수정 리스크 > 가치 판단. API 팀 코드 변경 없음. QA 쪽에서 `text_overflow.md` prompt 에 "footer copyright+legal 조밀 배치는 정상, 명확히 가려지거나 잘린 경우만 flag" 가드 문구 추가. 후속 2-3 full run 관찰 후 효과 검증. | 15분 (답변) | `docs/QA_결과.md §6` (API 팀 답변 2026-04-23) |
| ~~Q12~~ | ✅ 완료 | ~~QA 3.1 JWT TTL QA 전용 연장 설정 답변~~ — 현재 `JWT_ACCESS_TTL_MIN=15` (분, env override 지원). **옵션 A 권장**: QA `.env.qa` 에서 `JWT_ACCESS_TTL_MIN=360` (6h) 주입. 프로덕션 무영향, API 코드 변경 불필요. 옵션 B (refresh 플로우) 참고정보 함께 문서화 (웹은 `ak_refresh` HttpOnly 쿠키, 모바일은 JSON body). **답변은 `docs/QA_결과.md §6.1`** 에 문서화. | 15분 | `docs/QA_결과.md` §6.1 |
| Q13 | **중간** | **`supported_language_enum` 확장 + UI locale 보강 (es_es / pt_pt + 13 누락)** — 2026-04-21 "pt_pt → pt 병합 정책" 번복 (사용자 결정 2026-04-27). books `sentences.json` 35 언어 전부 DB enum 으로 수용 + frontend UI locale 22 → 35 확장. ✅ **Phase 1 완료 (2026-04-28, 커밋 `731d4d1`)** — `migrations/20260428_add_es_pt_variants.sql` (ALTER TYPE +es_es/pt_pt), `src/types.rs` `SupportedLanguage` enum +EsEs/PtPt (sqlx 'es_es'/'pt_pt', serde 'es-ES'/'pt-PT'), 문서 4개 정정. **🚨 INC-004 발생 후 hotfix `a5cd2ea` 로 20260421 주석 수정 되돌림** — 이미 적용된 마이그레이션은 sqlx checksum 불일치로 crash. 정책 번복 노트는 신규 마이그 20260428 주석 + CHANGELOG 에서 충분. cargo check + clippy --lib --bins -D warnings 클린. ✅ **Phase 2 인프라 확정 (2026-04-28)** — amazing-korean-ai 측 Wave 1 인프라 SSoT 채택 (Mac Mini Ollama gemma4:26b + 검증 4종 E1/M01/Q-prefix/orthography + 외부 LLM 합의). plan §2 정정 (옵션 A/B 폐기, gemma 단일 채널), api 측 reference 메모리 + AMK_API_LEARNING cross-link. ai 측 핸드오프 본문 = `amazing-korean-ai/docs/AMK_AI_TRANSLATION_HANDOFF.md`. 작업 분기: 그룹 1 (13 lang gemma 자동) + 그룹 2 (es-ES/pt-PT 수동 inline diff). → **Phase 2 실행** (ai 측 세션, api 측은 ai PR 머지 후 `SUPPORTED_LANGUAGES` 확장 + RTL/폰트 후속). ✅ **사전 조사 완료 (2026-04-30)** — RTL 707곳 마이그, 폰트 6 신규 (ar/fa/ur/bn/am/lo), 데스크탑 동일 작업, 모바일 영향 없음. PR-A (logical 마이그 1일) / PR-B (RTL 인프라 + 폰트 + SUPPORTED_LANGUAGES 반나절) / PR-C (시각 검증 반나절~1일) / PR-D (데스크탑 별도 리포 반나절~1일) 4분할. 총 2.5-3.5일. plan §2.4~§2.4.6 SSoT. ✅ **세션 단위 실행 계획 수립 (2026-04-30)** — INC-001~004 학습 적용. 1 세션 = 1 PR 의 1-2 커밋 단위. **S1** (frontend 자동 sed 1-2h) / **S2** (LTR 보호 + PR-A 푸시 2-3h) / **S3** (PR-B-pre dormant 2-3h) / **S4** (데스크탑 마이그 3-4h) / S5~S7 ai 결과 대기. 자가 점검 체크리스트 + 이슈 hotfix 패턴 (INC-004 절차) 명시. plan §2.9 SSoT. ✅ **S1+S2 = PR #190 머지 완료 (2026-04-30 밤, `ddf89a9`)** — frontend src/ Tailwind logical sed 707곳 (PR-A 1/4) + input type LTR 보호 45곳 (2/4) + `<pre>`/`<code>` 12곳 (3/4) + WritingTask 4 main + receipt 6 span (4/4) + docs 동기화 (5/5). ~131 파일 / +677 -649 (5 커밋). 자가 점검 8/8 통과 (S1/S2 각각). `npm run build` 8.67s + `npx tsc --noEmit` 0 error. **deploy success × 3 (PR push + docs + 머지) + /health 200 ✅**. INC 0 건. plan §2.9.5 갱신: 옵션 C (자동 sed + 수동 분할) 채택, 가격 보호 범위 좁힘 (영수증 핵심만, 나머지 30+ 곳 toLocaleString 은 PR-C 또는 helper component `<Price>` 도입 검토로 미룸). ✅ **S3 = PR-B-pre 인프라 PR #191 머지 (2026-04-30 밤, `0f803fa`, plan §2.9.6)** — font_loader 6 lang 신규 (ar/fa/ur/bn/am/lo) + language_groups RTL Set + isRTL + lang-rtl class + i18n applyLangClasses RTL dir 토글. SUPPORTED_LANGUAGES 변경 X = dormant (PR-B / S5 까지 ai 측 RTL 번역 PR 도착 대기). 3 파일 / +37, build 9.57s 클린, deploy success. ✅ **S4 = PR-D 데스크탑 (2026-04-30 밤, `amazing-korean-desktop` 커밋 `e63e20e`, plan §2.9.7)** — api S1+S2+S3 동기화 한 묶음. 93 파일 / +624 -587 (S1 sed 540 + S2 LTR 47 + S3 RTL 인프라 37). desktop = no remote = main 직접 커밋. WritingTask/receipt 미포함 (수동 작업 0). build 8.20s 클린 + tsc 0 error. ✅ **ai 측 Phase 2 결과 도착 + 검증 패치 5건 머지 (2026-05-01 ~ 2026-05-03)** — PR #194 (13 신규 lang 번역 + Wave 1 stale cleanup + common.goToSlide) / #195 (gemma 실패 65 keys 재번역 6 lang) / #196 (Option C Phase A cross-lang 5건) / #197 (Phase B major/high 52건 합의 패치) / #198 (Phase C minor 22건). 모두 `i18n/phase2-*` feature 브랜치 (ai 측 흐름, KKRYOUN 정책 외 별도 세션). **단 SUPPORTED_LANGUAGES 활성은 아직 미수행 — RTL 여전히 dormant**. PR #199 (Phase D-1 패턴 정정 13건) OPEN. 누계 94건 패치. ✅ **PR #193 폐기 + 새 PR (2026-05-03)** — 2026-04-30 작성 PR #193 이 머지 안 된 채 #194~#198 진행으로 docs stale → 옵션 B 채택 (사용자 결정), PR #193 close + KKRYOUN reset --hard origin/main + 새 PR 작성. 잔여 가치 = font_loader.ts Nastaliq weight 500 미지원 정정 1줄 보존 (`wght@400;500;700` → `wght@400;700`). docs 갱신 (CHANGELOG + STATUS Q13 본 행 + 메모리). 검증 build 11.19s + tsc 0. → **S5** (`SUPPORTED_LANGUAGES` 13 신규 활성, ai 측 PR #199 머지 + Phase D 종결 후) → **S6** (PR-C, RTL 시각 검증 + 가격 helper component `<Price>` + Gemini 9건 도록 패치) → **S7** (desktop `SUPPORTED_LANGUAGES` 활성, ai 데스크탑 PR 후) → **Phase 3** (메타 표시, 1-2시간) → **Phase 4** (books `gen_seed_sql.py` 의 es_es/pt_pt skip 제거, books 측 handoff). ✅ **2026-05-03 후속 작업 (PR #201 묶음)** — (a) **stale key cleanup**: `admin.textbook.detail` leaf 13 신규 lang 에서 제거 (i18next dict-vs-leaf 충돌 해소, 12 sub-key fallback 정상화). (b) **books-api-bridge §3 Stage 1 #1 — TextbookLanguage enum 14 추가**: migration `20260503_textbook_language_expand.sql` + types.rs 21→35 + textbook/ebook service catalog/display 14 매핑. 신규 14 (am/ar/bn/es_es/fa/it/ky/lo/pl/pt_pt/sw/tr/uk/ur) 모두 `available=false` (출판본 미준비). (c) **번역 갭 신규 발견**: 22 base lang × 518 keys 누락 (62.6% 완성도, Q1~Q12 시기 추가 키들 미반영) — Mac Mini Wave 1 Phase 1c (가칭) 트랙으로 큐잉. **TextbookLanguage enum 21 확장 = 본 PR 로 해결**. | Phase 1+2 인프라 완료, 실행은 ai 세션 위임 | **plan SSoT**: `~/.claude/plans/supported-language-es-pt-variants-expansion.md` |

**QA 에 QA 쪽 조치로 회송** (이 리포 작업 아님, 외부 협조):
- **QA 3.2 OpenAPI drift 1328 cell** → QA 가 warn 격하 (Security 위반만 fail). `reference_qa_automation.md` 기록.

**큐 외 대기** (이 리포 작업이지만 일정 대기):
- ~~**#67 D+7 관측 로그 체크**~~ — 2026-04-23 ✅ **완료** (0건 확인)
- ~~**#67 Phase 2~5 일괄 전환**~~ — 2026-04-23 ✅ **완료** (D+8 예정 대비 1일 앞당김. 데스크탑은 별도 PR 진행 중)
- **Q14 — e-book 페이지 이미지 EC2 업로드** (2026-05-03 결정, 사용자 트리거 대기): books-api-bridge plan §3 Stage 2 #3-B. books 측 `dist/ebook_pages/` (8,928 페이지 / 693MB / 36 lang × 2 edition) → EC2 `${EBOOK_PAGE_IMAGES_DIR}` 동기화. 정책 = RDS 이전 전까지 EC2 local fs (RDS 이전 시 S3 + Q9 ebook fs::read 9곳 전환과 함께 통합 전환). 의존: 사용자 → EC2 디스크 여유 확인 (`df -h`) + books 측 동기화 스크립트 작성 (rsync 또는 `aws s3 sync`). 시간 추정: 인프라 결정 + 스크립트 반나절 + 업로드 30분. 정책 본문: [`AMK_API_EBOOK.md` "페이지 이미지 저장 위치 정책"](./AMK_API_EBOOK.md). 운영 모니터링: [`AMK_DEPLOY_OPS.md §6`](./AMK_DEPLOY_OPS.md) + 본 문서 §8.4 #8.
- **Q15 — E-book 보안 옵션 A (행동 기반 봇 탐지)** (2026-05-03 권고, 사용자 트리거 대기): 네이버웹툰 TOONRADER 2025~2026 고도화 벤치마크 분석 결과 우리 시스템에서 가장 명확한 격차 = **자동화 탐지 부재**. 작업 = (1) 다중 IP/UA 동시 접속 감지 (Redis 세션 메타 확장) → 강제 종료 + 알림, (2) 페이지 요청 간격 통계 (<1s 빈발 → 봇 의심 카운터), (3) 헤드리스 시그널 (`navigator.webdriver` + WebGL/canvas 핑거프린트). 시간 추정 1~2일, 기존 Rate Limit/세션 인프라 재사용. 옵션 B (외부 모니터링) 는 1인 운영 한계로 보류, 옵션 C (AI 매칭) 는 콘텐츠 규모 대비 과투자로 비권장. 본문 SSoT: [`AMK_EBOOK_SECURITY.md` §2.5 + §4 Phase 1-6](./AMK_EBOOK_SECURITY.md).
- **Q16 — Frontend lint + lint:ui baseline cleanup** (2026-05-04 발견 + 확장, 사용자 트리거 대기): PR-check workflow (`pr-check.yml`) 도입 시 main baseline 의 누적 위반 36건 검출. **합계 = ESLint 27 errors + lint:ui 9 errors**. (a) **ESLint 27건**: 대부분 `react-refresh/only-export-components` (shadcn/ui 컴포넌트 + variants 같은 파일 export 패턴이 새 plugin 룰과 충돌). 일부 `react-hooks/set-state-in-effect`, `react-hooks/incompatible-library`, `prefer-const`. 작업 = shadcn 컴포넌트 파일 분할 (예: `button.tsx` → `button.tsx` + `button-variants.ts`) + react-hooks 룰 위반 useEffect 리팩터 + prefer-const 1건. (b) **lint:ui 9건 (하드코딩 색상)**: `textbook_order_page.tsx` 4곳 / `receipt_parts.tsx` 1곳 / `book_hub_page.tsx` 4곳 / `HangulKeyboardKey.tsx` 1곳. 색상 = emerald/amber/rose/teal/red. 작업 = 디자인 토큰 결정 (예: 결제수단·할인·학습단계·destructive 별 시맨틱 토큰) + 9곳 코드 교체. 시간 추정 1-2일 (디자인 결정 0.5일 + 전수 수정 0.5-1일 + 회귀 테스트). 임시 조치 (PR #205) = pr-check.yml 의 `npm run lint` + `npm run lint:ui` 둘 다 `continue-on-error: true`. 본 작업 완료 후 둘 다 제거.
- **Q17 — Backend cargo test + Frontend playwright e2e CI 도입 검토** (2026-05-04 식별, 보류): 두 검사 모두 **도입됐으나 자동 강제 안 되는 dormant 정책** (lint/lint:ui 와 같은 패턴). cargo test = 테스트 코드 일부 존재 (`cargo test --no-run` 통과 확인), 실행은 PostgreSQL 의존 → CI 환경에 postgres service container 셋업 필요. playwright e2e (`npm run test:e2e`) = 한글 자판 P10-C 시점 도입 (`ae1d624`), 시나리오/대상 미확인, 브라우저 런타임 + CI 분 사용 큼. 두 항목 모두 baseline 미검증 + 셋업 비용 큼 → 별도 트랙. 우선순위 = Q16 완료 후 검토. 도입 시 pr-check.yml 별도 job 으로 추가.

#### 검증된 리스크 (2026-03-31 코드베이스 팩트체크 완료)

| 작업 | 리스크 | 심각도 | 근거 |
|------|--------|:------:|------|
| ~~Paddle Live~~ | ~~12개 PADDLE_* Secret 일괄 교체~~ | — | ✅ **해결 (2026-03-18 추정)**. `gh secret list` 13개 등록 + `curl /payment/plans` `sandbox=false` + Live IDs 검증 (2026-05-08 M-010 사고 정정) |
| ~~Paddle Live~~ | ~~Webhook Secret 1회성 (재확인 불가)~~ | — | ✅ **해결 (2026-02 추정)**. `AMK_STATUS §8.5 #7` Secret Key 확보 완료 → A1-1 의 `PADDLE_WEBHOOK_SECRET` 항목으로 업데이트 시 재사용 |
| ~~Paddle Live~~ | ~~KYB/Onfido 인증 지연 가능~~ | — | ✅ **해결 (2026-02-21~25 추정 승인)**. `AMK_STATUS §8.5 #1 = ✅` |
| ~~Paddle Live~~ | ~~SPF 레코드 병합~~ | — | ✅ **해결 (2026-05-08, 사용자 Cloudflare DNS 작업 + propagation 검증)**. 적용 = `v=spf1 include:send.resend.com include:_spf.mx.cloudflare.net ~all` |
| ~~Paddle Live~~ | ~~하나은행 USD 계좌 영문 예금주명 등록 (Payout)~~ | — | ✅ **해결 (2026-05-08, 사용자 통보)** 통장 개설 완료. 잔여 = Paddle Dashboard Payout Settings 입력 (A1-1 GitHub Secrets 업데이트와 같은 시점) |
| RDS 이전 | E-book 로컬 파일시스템 의존 (9곳 fs read) | CRITICAL | ebook/service.rs:63,381,627,641,650,731,746,755 + watermark.rs:13 (HEAD 2026-05-04) |
| RDS 이전 | SSL 연결 필수 (현재 미사용) | HIGH | config.rs:109-110 (DATABASE_URL localhost 기본값) |
| RDS 이전 | ElastiCache AUTH 토큰 필요 (현재 인증 없음) | HIGH | config.rs:113 (redis://127.0.0.1:6379) |
| ~~동시 세션~~ | ~~제한 로직 미구현~~ ✅ 구현 완료 | — | enforce_session_limit() — SCARD + 유령 정리 + 역할별 정책 |
| ~~모바일 인증~~ | ~~login-mobile/refresh-mobile~~ ✅ 구현 완료 | — | auth/router.rs, handler.rs |
| ~~모바일 인증~~ | ~~X-Platform 헤더 검증~~ ✅ refresh-mobile에 적용 | — | auth/handler.rs:refresh_mobile |
| ~~Rust 크레이트~~ | ~~amazing-korean-crypto~~ ✅ 추출 완료 | — | crates/crypto/, Cargo.toml 워크스페이스 |
| ~~Flutter~~ | ~~flutter_rust_bridge 버전 핀닝 필수~~ | — | ✅ **해결 (2026-04-07, mobile M1b)**. `pubspec.yaml` `=2.12.0` 핀닝 + Rust edition 2021 |
| ~~Flutter~~ | ~~E-book 뷰어 메모리 OOM (14MB/페이지)~~ | — | ✅ **해결 (2026-04-06, mobile M6)**. LRU 10페이지 + cacheWidth/cacheHeight 화면 해상도 디코딩 |
| ~~Flutter~~ | ~~IAP receipt 검증 엔드포인트~~ ✅ 구현 완료 | — | POST /ebook/purchase/iap + POST /payment/webhook/revenuecat |
| ~~Flutter~~ | ~~iOS isSecureTextEntry 비공식 API~~ | — | ✅ **해결 (2026-04-06, mobile M7)**. `no_screenshot 1.1.0` + Android FLAG_SECURE + iOS isSecureTextEntry + UIScreen.isCaptured fallback |
| ~~Flutter~~ | ~~앱 백그라운드 시 세션 만료 (TTL 90초)~~ | — | ✅ **해결 (2026-05-08, EBOOK_SESSION_TTL_SEC 90→300)**. config.rs:91, 376 + .env.example:125 + AMK_API_EBOOK.md:493. mobile 30s heartbeat (M6) + 300s TTL = 백그라운드 4분 30초 grace |
| Tauri | macOS 캡처 방지 불가 (Apple 정책) | MEDIUM | AMK_APP_ROADMAP.md R5 (수용) |

> **팩트체크 방법**: 코드베이스 전수 grep + 파일별 라인 검증. 총 32개 주장 중 31개 확인, 1개 수정 (Secret 13→12개).

#### 보류/조건부

| # | 항목 | 카테고리 | 내역 | 예상 결과 | 조건/시점 |
|:-:|------|---------|------|----------|----------|
| 8 | ~~Apple OAuth~~ | 외부 API | Apple Sign In 구현 → **§8.2 핵심 순서 4a로 이동** | iOS 사용자 편의성 | 스펙 완료 (AMK_API_AUTH.md §5.3-17) |
| 9 | GeoIP 전환 | 인프라 | ip-api.com → MaxMind GeoLite2 로컬 DB | HTTPS, 무제한 쿼리 | 트래픽 증가 시 |
| 10 | step-up MFA | 보안 | 민감 작업 시 추가 인증 요구 | 결제/비밀번호 변경 시 보안 강화 | 필요 시 |
| 11 | ~~이메일 수신~~ | ~~외부 API~~ | ~~`support@amazingkorean.net` 수신~~ | ~~사용자 문의 처리~~ | ✅ Cloudflare Email Routing 설정 완료 (→ Gmail 포워딩) |
| 12 | 토큰 Redis 캐싱 | 보안 | 재발급 시 DB 조회 → Redis 캐시 | 동시 접속 성능 개선 | 동시접속 10K+ |
| ~~13~~ | ~~enum sqlx::Type 전환~~ | ~~코드 품질~~ | ~~수동 match → `#[sqlx(type_name)]` derive~~ | ~~보일러플레이트 감소~~ | ✅ **2026-05-04 검증 결과 이미 전환 완료** — `src/types.rs` 에 `#[sqlx(type_name = ...)]` 36건 적용. AMK_DEBTS C5 와 함께 해결 등재 |
| 14 | Keyset 페이징 | 기능 | page/size → keyset pagination | 대용량 테이블 성능 개선 | 데이터 1만 건+ |
| 15 | Lesson 통계 | 기능 | `/admin/lessons/stats` 구현 | 수업별 진행도 분석 | 필요 시 |
| 16 | 학습 문제 동적 생성 | 기능 | 커리큘럼 기반 문제 자동 생성/전달 | 학습 콘텐츠 확장 | 커리큘럼 완비 후 |
| 17 | 통계 비동기/배치 분리 | 인프라 | 집계 로직 비동기 처리 | API 응답 속도 개선 | 집계 복잡화 시 |
| 18 | OAuth 중복 통합 | 코드 품질 | auth repo/service 리팩토링 | 코드 중복 제거 | 세 번째 OAuth 추가 시 |
| 19 | manager 역할 구현 | 기능 | class 기반 접근 권한 부여 | 담당 학습자 범위 내 관리 | class 테이블 구현 후 |
| ~~20~~ | ~~JsonRejection → AppError 통합~~ | ~~코드 품질~~ | ~~`Json<T>` extractor 실패 시 Axum 기본 `text/plain` 응답을 커스텀 추출기로 가로채 AppError 로 매핑~~ | ~~에러 응답 envelope + `trace_id` 전 경로 통일~~ | ✅ #70 완료 (2026-04-16) |

#### 다국어 UI 참고 (21 → 36 언어 확장 진행 중, RTL 인프라 dormant)

> **2026-04-30 정정**: PR #191 (`0f803fa`) 로 RTL 인프라 정착 (font_loader 6 lang ar/fa/ur/bn/am/lo + RTL Set + isRTL + lang-rtl class + i18n applyLangClasses dir 토글). 단 `SUPPORTED_LANGUAGES` 활성은 ai 측 Phase 2 결과 머지 후 (S5 단계). 현재 dormant.

| 항목 | 설명 |
|------|------|
| **폰트** | Noto Sans 패밀리 동적 로딩 (Latin/Cyrillic/CJK/Thai/Myanmar/Khmer/Sinhala/Devanagari) |
| **텍스트 길이** | 독일어 등 60%+ 길어질 수 있음 → 고정 폭 금지, flex/grid 사용, `text-overflow: ellipsis` |
| **줄 높이** | Thai/Myanmar/Khmer/Sinhala 결합 문자 → `line-height: 1.6~1.8` |

### 8.3 세부 검토 사항 — 한국어 발음 교정 AI (Pronunciation Coaching AI)

**현재 상태**: 조사 완료, 단계별 전략 확정 (2026-03-03)

**문제 정의**: 한국어 학습자의 발음 교정은 1:1 원어민 교사 없이는 사실상 불가능하다. **한 글자 단위 발음 교정이 초급 학습자에게 가장 중요**하며, 기존 서비스는 이를 지원하지 않는다.

**핵심 원칙**: 한국어는 비성조 언어 → 음높이 변화 거의 없음 → 한 글자씩 끊어서 발음 연습이 핵심.

#### 기존 API 비교 (조사 완료)

| API | 한 글자 | 음소별 점수 | 한국어 | 가격 | 평가 |
|-----|:-------:|:---------:|:------:|------|------|
| **SpeechSuper** | **지원** | **있음** (오발음 피드백 포함) | 4단계 (글자/단어/문장/단락) | $0.004/건 (단어) | **최적** |
| ETRI e-PreTX | 미지원 | 없음 (1~5점 단일 점수) | 문장만 | 무료 (1K/일) | 제한적 |
| Azure Speech | 미설계 | 있으나 음소명 미제공 (ko-KR) | 있음 | ~$1/시간 | 부적합 |

- **SpeechSuper**: 유일하게 한글 1자 음소별 평가 지원, REST + WebSocket, Rust SDK 제공
- **ETRI**: 2025.07 aiopen → e-PreTX 이전, PCM 전용, 자유 발화 시 전문가와 상관관계 없음 (Kim & Ko, 2022)

#### 3단계 전략 (확정)

**Phase 1** (현재): 따라하기 안내만 (녹음/판별 없음)

**Phase 2** (콘텐츠 완성 후): SpeechSuper API 프로토타이핑
- 한 글자 + 단어/문장 발음 평가, 음소별 점수 + 오발음 피드백
- Rust 백엔드 통합, 사용자 반응/데이터 수집
- 비용: $0.004/건 → 1,000명 활동 시 ~$1,680/월 (Growth $0.0028)

**Phase 3** (기술 검증 후): 커스텀 모델 개발
- **왜 한국어가 유리한가**: 음절 = 초성(19) + 중성(21) + 종성(28) 고정 구조, ~40개 음소 소규모 분류 문제, 성조 없음
- **베이스 모델**: `kresnik/wav2vec2-large-xlsr-korean` (Apache 2.0, WER 4.74%, CER 1.78%)
- **학습 데이터**: AIHub 71469 (1,030시간, 영어모국어 한국어 음성, 음소 시간 정렬 + 오류 태그) — HYMN 법인으로 접근 가능
- **아키텍처**: wav2vec2 파인튜닝 → 초성/중성/종성 3-way 분류 헤드 + GOP 점수화 (0~100)
- **학술 검증**: PER 10.25%, 전문가 일치 90% (2024, 1.56h 데이터), L1별 39개 오류 패턴 분류 (ICPhS 2023)
- **개발 비용**: GPU $200~$1,000, 추론 CPU 가능 (<100ms/음절, $50~200/월), 기간 2~3개월
- **장점**: API 비용 제거 + 데이터 주권 + L1별(20개 언어) 맞춤 피드백

#### L2 학습자 공통 오류 패턴 (ICPhS 2023)

1. 격음/경음 → 평음 대치 (ㅋ/ㄲ→ㄱ, ㅌ/ㄸ→ㄷ, ㅍ/ㅃ→ㅂ, ㅊ/ㅉ→ㅈ, ㅆ→ㅅ)
2. 종성(받침) 탈락
3. 이중모음 → 단모음 대치
4. L1별 고유 패턴 (베트남어: /l/→/n/ 종성, 일본어: /ŋ/ 삽입 등)

#### 조음 애니메이션 (Phase 15 연동)

- **15~17개 다이어그램**으로 한국어 전체 음소 커버 (자음 7 조음 위치 + 성문 1 + 모음 7~9)
- 평음/경음/격음은 같은 입 위치 → 성문 상태도로 차이 표현
- **Wikimedia CC0 SVG** 활용 (IPA 조음 단면도, 퍼블릭 도메인) → 자체 제작 최소화
- 기술: Figma → SVG → GSAP MorphSVG (무료) + React
- **한국어 전용 조음 애니메이션 도구 부재 → 차별화 기회** (경쟁사 분석 완료)

#### 맞춤형 학습 AI 확장 로드맵

```
데이터 플라이휠:
사용자 발음 녹음 → 모델 평가 → 피드백/재시도 → 데이터 축적 → 모델 재학습 → 정확도 향상 → 사용자 증가 → ...
```

1. **발음 평가 AI** (Phase 14) — 음소별 정확도 판정
2. **학습자 프로파일링** — L1 기반 취약 음소 자동 파악 + 개인별 반복 오류 패턴 추적
3. **맞춤형 학습 경로** — 취약 부분 자동 반복 출제, 조음 영상 재안내
4. **20개 언어별 특화 모델** — L1 간섭 오류 집중

**핵심 차별점**: 교재(콘텐츠) + AI(개인화) 조합 → 데이터가 쌓일수록 격차 벌어짐 → 진입 장벽

#### 사용 가능한 데이터셋

| 데이터셋 | 규모 | 음소 라벨 | 오류 태그 | 접근 |
|----------|------|----------|----------|------|
| **AIHub 교육용 영어모국어 한국어 음성 (71469)** | **1,030시간** | **있음 (시간축 정렬)** | **있음 (TextGrid)** | 한국 국적 |
| AIHub 외국인 한국어 발화 (505) | 4,302시간 | 없음 (단어 단위) | 없음 | 한국 국적 |
| KsponSpeech (한국어 자연발화) | 1,000시간 | 없음 | 없음 | 한국 국적 |
| Zeroth-Korean | ~50시간 | 없음 | 없음 | **오픈** |

#### 오픈소스 선례

| 프로젝트 | 아키텍처 | 특징 |
|----------|---------|------|
| DevTae/SpeechFeedback | Deep Speech 2 (CNN+BiGRU+CTC) | Docker+FastAPI, 한글→44 IPA 변환, AIHub 60만 샘플 |
| kresnik/wav2vec2-large-xlsr-korean | wav2vec2 XLSR Large (0.3B) | WER 4.74%, CER 1.78%, Apache 2.0 |
| ai-pronunciation-trainer | Whisper + Epitran + 규칙기반 | 영어/독일어만, 한국어 적용 시 커스텀 필요 |

#### 참고 링크

- SpeechSuper 한국어 데모: speechsuper.com/demo/korean/
- SpeechSuper 가격: speechsuper.com/pricing.html
- SpeechSuper API 샘플 (Rust 포함): github.com/speechsuper/SpeechSuper-API-Samples
- AIHub 교육용 한국어 음성: aihub.or.kr/aihubdata/data/view.do?dataSetSn=71469
- ETRI e-PreTX: epretx.etri.re.kr
- 2025 논문 (Whisper 파인튜닝): eksss.org/archive/view_article?pid=pss-17-1-51
- 2024 논문 (아동 발음장애): arxiv.org/html/2403.08187v1
- ICPhS 2023 (L2 오류): arxiv.org/abs/2306.10821

### 8.4 상시 모니터링 항목

프로젝트 전반에 걸쳐 지속적으로 수행해야 하는 조사·분석·모니터링 활동.

| # | 항목 | 분류 | 내역 | 주기 | 참고 |
|:-:|------|------|------|:----:|------|
| 1 | 한국어 교육 시장 조사 | 시장 | 경쟁사 동향 (신규 앱, 가격 변동, 기능 출시), TOPIK 응시자/학습자 통계, 시장 규모 업데이트 | 월 1회 | [`AMK_MARKET_ANALYSIS.md`](./AMK_MARKET_ANALYSIS.md) |
| 2 | 교육 앱 UX/UI 트렌드 | 시장 | 주요 교육 앱 UI 변화, 온보딩 플로우, 게이미피케이션 패턴, 접근성 트렌드 | 월 1회 | — |
| 3 | 결제/수익 모델 동향 | 시장 | Apple/Google IAP 정책 변경, 수수료율 변동, 지역별 가격 전략, 프로모션 사례 | 분기 1회 | [`AMK_MARKET_ANALYSIS.md §4`](./AMK_MARKET_ANALYSIS.md#4-모바일-앱-결제-전략) |
| 4 | AI/ML 기술 동향 | 기술 | LLM 경량화 (BitNet 후속), 음성인식 (Whisper 후속), 온디바이스 AI SDK, 발음 평가 API | 월 1회 | `amazing-korean-ai/docs/AMK_AI_PIPELINE.md §11` (이관됨) |
| 5 | 모바일 프레임워크 동향 | 기술 | Flutter / Tauri / Kotlin Multiplatform 변화, flutter_rust_bridge 업데이트, 크로스플랫폼 보안 사례 | 분기 1회 | [`AMK_APP_ROADMAP.md`](./AMK_APP_ROADMAP.md) |
| 6 | 인프라/보안 동향 | 기술 | AWS 신규 서비스, 컨테이너 오케스트레이션, 인증 표준 (Passkey 등), OWASP 업데이트 | 분기 1회 | [`AMK_DEPLOY_OPS.md`](./AMK_DEPLOY_OPS.md) |
| 7 | 규제/법률 동향 | 사업 | 교육 앱 개인정보보호 (COPPA, GDPR-K), DMA/DSA 후속 조치, 각국 앱스토어 규제 | 분기 1회 | — |
| 8 | EC2 디스크 여유 + e-book 페이지 이미지 크기 | 인프라 | `${EBOOK_PAGE_IMAGES_DIR}` 점유량 (baseline 693MB, 임계값 2GB), 전체 디스크 여유 (< 30% 시 점검). 2026-05-03 정책 = RDS 이전 전까지 EC2 local fs | 주 1회 | [`AMK_DEPLOY_OPS.md §6`](./AMK_DEPLOY_OPS.md) |

[⬆️ 목차로 돌아가기](#-목차-table-of-contents)

### 8.5 Paddle Live 전환 체크리스트

Sandbox → Live(프로덕션) 전환. 코드 구현 완료, Dashboard 설정 대부분 완료.

> **참고 문서**: [Go-live checklist (Paddle Developer)](https://developer.paddle.com/build/onboarding/go-live-checklist)

#### 완료 항목

| # | 작업 | 상태 |
|:-:|------|:----:|
| 1 | 계정 인증 (KYB + Onfido) | ✅ |
| 2 | 도메인 인증 (`amazingkorean.net`) | ✅ |
| 3 | Products 생성 (구독 + E-book) | ✅ |
| 4 | Prices 생성 (1/3/6/12개월 정가 + E-book) | ✅ |
| 5 | API Key 발급 (`pdl_apikey_live_...`) | ✅ |
| 6 | Client Token 발급 (`live_...`) | ✅ |
| 7 | Webhook Destination (11개 이벤트, Secret Key 확보) | ✅ |
| 8 | Payment Methods (Card + PayPal/Apple Pay/Google Pay) | ✅ |
| 9 | Balance Currency (USD) + Sales Tax Settings | ✅ |
| 10 | Default Payment Link (`https://amazingkorean.net/pricing`) | ✅ |
| 11 | Retain (Payment Recovery, 이메일 브랜딩, Postmark 인증) | ✅ |
| 12 | pwCustomer 코드 (`use_paddle.ts` + 3개 호출 사이트) | ✅ |
| 13 | Retain 홈페이지 Paddle.js 초기화 (`home_page.tsx`) | ✅ |
| 14 | Cloudflare Email Routing (`support@amazingkorean.net` → Gmail) | ✅ |
| 15 | 환경변수 정리 (`PADDLE_PRODUCT_ID` 제거, `PADDLE_PRICE_EBOOK` 통일) | ✅ |
| 16 | Paddle Discount 3개 생성 (3/6/12개월 flat $5/$10/$20 off) | ✅ |
| 17 | Discount 코드 적용 (`config.rs`, `dto.rs`, `service.rs`, `use_paddle.ts`, `pricing_page.tsx`) | ✅ |
| 18 | GitHub Secrets Discount ID 3개 등록 | ✅ |

#### 남은 작업

##### ~~Step 1: Paddle Discount 생성 — 유저 (Dashboard)~~ ✅

##### ~~Step 2: 코드 수정 — Claude~~ ✅

Paddle Dashboard → **Catalog → Discounts** 에서 3개 생성:

| Discount | Type | Amount | Recur | Restrict To | 최종가 |
|----------|------|--------|-------|-------------|--------|
| 3개월 할인 | Flat | $5 off | Yes (영구) | 3개월 Price ID만 | $30 → $25 |
| 6개월 할인 | Flat | $10 off | Yes (영구) | 6개월 Price ID만 | $60 → $50 |
| 12개월 할인 | Flat | $20 off | Yes (영구) | 12개월 Price ID만 | $120 → $100 |

- Code: 비워두기 (코드 없이 생성 → `discountId`로 자동 적용)
- 생성 후 Discount ID (`dsc_...`) 3개 확보

##### Step 2: 코드 수정 — Claude

| 파일 | 변경 내용 |
|------|----------|
| `src/types.rs` | `price_cents()` 정가로 수정: 3개월 $30(3000), 6개월 $60(6000), 12개월 $120(12000) |
| `src/config.rs` | Discount ID 환경변수 3개 추가 (`PADDLE_DISCOUNT_MONTH_3/6/12`) |
| `docker-compose.prod.yml` | Discount ID 환경변수 3개 추가 |
| `.github/workflows/deploy.yml` | Discount ID Secrets 3개 추가 |
| `.env.example` | Discount ID 환경변수 3개 추가 |
| `src/api/payment/service.rs` | Plans API 응답에 `discount_id` 포함 |
| `frontend/src/category/payment/` | checkout 시 `discountId` 자동 적용 |

##### ~~Step 3: GitHub Secrets 업데이트 — 유저~~ ✅ **2026-03-18 추정 완료 (2026-05-08 M-010 사고 정정)**

> 검증: `gh secret list` 결과 13개 Secret 모두 등록 (2026-02-18 ~ 03-19) + `curl https://api.amazingkorean.net/payment/plans` 응답 = `sandbox: false` + `client_token: live_*` + Live Price IDs (`pri_01k...`) + Live Discount IDs (`dsc_01km2c...`).

| Secret | 값 |
|--------|-----|
| `PADDLE_SANDBOX` | `false` |
| `PADDLE_API_KEY` | Live API Key (`pdl_apikey_live_...`) |
| `PADDLE_CLIENT_TOKEN` | Live Client Token (`live_...`) |
| `PADDLE_WEBHOOK_SECRET` | Live Webhook Secret (`pdl_ntfset_...`) |
| `PADDLE_PRICE_MONTH_1` | Live Price ID ($10) |
| `PADDLE_PRICE_MONTH_3` | Live Price ID ($30, 정가) |
| `PADDLE_PRICE_MONTH_6` | Live Price ID ($60, 정가) |
| `PADDLE_PRICE_MONTH_12` | Live Price ID ($120, 정가) |
| `PADDLE_PRICE_EBOOK` | Live Price ID ($10) |
| `PADDLE_DISCOUNT_MONTH_3` | Discount ID (`dsc_...`) |
| `PADDLE_DISCOUNT_MONTH_6` | Discount ID (`dsc_...`) |
| `PADDLE_DISCOUNT_MONTH_12` | Discount ID (`dsc_...`) |
| `PAYMENT_PROVIDER` | `paddle` |

##### ~~Step 4: 배포 — 유저~~ ✅ **2026-03-18 추정 완료 (Step 3 완료와 동일 시점)**

> 검증: production API `/payment/plans` Live 모드 응답 + `AMK_CHANGELOG 2026-03-18` "Paddle Live 전환 + E-book Paddle Checkout 연동" 명시.

1. ~~커밋 → push → GitHub Actions 자동 배포~~ ✅
2. ~~서버 로그 확인: `💳 Payment provider enabled: Paddle Billing (Production)`~~ ✅

##### Step 5: E2E 검증 — 유저 (#1-3 ✅ Claude / #4-11 = 실제 상품 완성 후 사용자 진행 결정 2026-05-08)

| # | 검증 항목 | 방법 | 기대 결과 | 상태 |
|:-:|----------|------|----------|:--:|
| 1 | API Health | `curl https://api.amazingkorean.net/health` | 200 OK + `status: live` | ✅ **2026-05-08 Claude 검증** |
| 2 | Plans API | `curl https://api.amazingkorean.net/payment/plans` | `sandbox: false`, Live Price ID | ✅ **2026-05-08 Claude 검증** |
| 3 | E-book Catalog | `curl https://api.amazingkorean.net/ebook/catalog` | `sandbox: false`, Live Price ID | ✅ **2026-05-08 Claude 검증** |
| 4 | Webhook Simulator | Dashboard → Notifications → Webhook Simulator | 200 OK | 🟡 **실제 상품 완성 후** |
| 5 | 구독 실결제 | `/pricing` → 1개월 $10 → 카드 결제 | DB subscription + transaction 생성 | 🟡 동일 |
| 6 | 구독 Discount | `/pricing` → 3개월 → 체크아웃에서 ~~$30~~ $25 표시 | Discount 자동 적용 | 🟡 동일 |
| 7 | 구독 환불 | Dashboard에서 환불 | `adjustment.created` → transaction `refunded` | 🟡 동일 |
| 8 | E-book 실결제 | `/ebook` → Paddle overlay | `ebook_purchase` 상태 `completed` | 🟡 동일 |
| 9 | E-book 환불 | Dashboard에서 환불 | `ebook_purchase` 상태 `refunded` | 🟡 동일 |
| 10 | Retain URL 검증 | Paddle Retain → Check URL `https://amazingkorean.net` | Paddle.js 감지 | 🟡 동일 |
| 11 | 프론트 UX | `/pricing`, `/ebook`, `/ebook/my` 페이지 | 정상 로딩 + checkout 동작 | 🟡 동일 |

> **E2E #4-11 트리거 결정 (2026-05-08, 사용자)**: 실제 학습 콘텐츠 시딩 완료 (`AMK_STATUS §8.2 #3` 학습 콘텐츠 시딩) + 실 상품 가치 정착 후 진행. 사유: 빈 시스템에서 실 카드 결제 + 환불 처리 비용 회피 + 실제 운영 환경에서 검증 가치 ↑.
> ⚠️ Live 모드 = 실제 결제. 테스트 후 반드시 Dashboard에서 환불 처리.

##### ~~Step 6: 은행 — 유저~~ ✅ **2026-05-08 완료**

- ~~하나은행 세종중앙금융센터(044-867-1111)에 USD 계좌 영문 예금주명 등록 요청~~ ✅ **2026-05-08 (통장 개설)**
  - 계좌 종류 = **Multi-Foreign Currency Savings Account** (USD 포함 다중 외화 입금 가능)
  - 예금주명 = **`HYMN CO.,LTD.`** (법인 명의, 대문자 + 콤마 + 마침표)
  - 개설일 = 2026.03.16
  - 지점 = KEB Hana Bank Sejong Jungang Banking Center (275, Hannuri-daero, Sejong, 30127, South Korea)
  - SWIFT/BIC = `KOEXKRSE`
- ~~Paddle Dashboard → Payout Settings 입력~~ ✅ **2026-05-08 완료 (스크린샷 확인)**
  - **Country**: South Korea
  - **Business Details**: Account Type = Corporation / Legal Name = `HYMN Co., Ltd.` / VAT = Optional 비움 / Address = 350 Hannuri-daero, 30121 Sejong-si
  - **Company Representative**: `Kyoung Ryun Kim` / DOB
  - **Payment Method**: Wire transfer / Minimum Threshold = $100
  - **Wire transfer details**:
    - Bank Country = South Korea / Transfer Currency = USD
    - Bank Name = `KEB Hana Bank`
    - Bank Address = `35 Eulji ro Jung gu Seoul South Korea` (KEB Hana Bank 본점 주소 = wire transfer 표준)
    - **Account Holder Name = `HYMN CO.,LTD.`** (통장 표기와 정확히 일치)
    - BIC/SWIFT = `KOEXKRSE`
    - Account Number = `91591001863238` (통장의 `915-910018-63238` 하이픈 제거)

> **Bank Address 정합 노트**: 통장 표기 (`275 Hannuri-daero Sejong`, 지점 주소) ≠ Paddle 입력 (`35 Eulji-ro Jung-gu Seoul`, 본점 주소). Wire transfer 표준 = SWIFT 코드 보유 본점 주소 사용 → Paddle 입력이 정확.
>
> **Legal Name vs Account Holder Name 차이 노트**: Legal Name = `HYMN Co., Ltd.` (사업자등록증 표기) / Account Holder Name = `HYMN CO.,LTD.` (통장 표기 정확 일치). 두 필드 의미 다름, 일치 불필요.

#### 가격 구조

| 구간 | 정가 (Paddle Price) | Discount | 최종가 (고객 결제) |
|------|--------------------|---------|--------------------|
| 1개월 | $10 | — | $10 |
| 3개월 | $30 | $5 off | $25 |
| 6개월 | $60 | $10 off | $50 |
| 12개월 | $120 | $20 off | $100 |
| E-book | $10 | — | $10 |

#### 결제수단 / 세금 참고

**결제수단**: Paddle 기본 수수료(5% + $0.50/건)에 **모든 결제수단 포함** (추가 비용 없음)
- 카드(Visa/MC/Amex), PayPal, Apple Pay, Google Pay, KakaoPay, NaverPay, Samsung Pay, Alipay, UnionPay, iDEAL 등
- 고객 위치/기기/통화에 따라 Paddle이 자동으로 적절한 결제수단 표시

**세금**: Paddle MoR(Merchant of Record)가 100개국+ **자동 처리**
- VAT (EU/UK), GST (호주/인도), Sales Tax (미국 주별) — 자동 계산 + 징수 + 납부
- 세금 관련 법적 책임도 Paddle에 귀속
- 설정: "Prices include tax" OFF 권장 (표시 가격 + 세금 별도)

[⬆️ 목차로 돌아가기](#-목차-table-of-contents)

---

### 8.6 학습 콘텐츠 보안 전략 (Content Protection)

> 자체 플랫폼(웹/앱)의 학습 콘텐츠와 외부 유통 EPUB3에 대한 7중 보안 아키텍처.

#### 유통 채널별 전략

| 채널 | 형식 | 보안 방식 |
|------|------|----------|
| **자체 웹/앱** | 인터랙티브 학습 플랫폼 (DB → API → 렌더링) | 구조적 보안 + 포렌식 마킹 + 플랫폼 보안 |
| **외부 플랫폼** (Amazon/Apple/Google/Kobo) | EPUB3 | 플랫폼 DRM 자동 적용 |

- 자체 사이트에서는 EPUB3 파일을 제공하지 않음 — **다운로드 가능한 파일 자체가 존재하지 않음**
- 교재 콘텐츠(JSON 데이터)를 서버에서 인터랙티브 학습 콘텐츠로 동적 렌더링
- EPUB3는 외부 플랫폼 유통 전용으로만 생성

#### Layer 1 — 구조적 보안 (콘텐츠 제공 방식 자체가 방어)

| 방어 요소 | 설명 |
|----------|------|
| 파일 없음 | 다운로드할 파일이 존재하지 않음 (EPUB3/PDF 미제공) |
| 서버 렌더링 | 콘텐츠가 DB → API → 조각 단위 전송, 전체 데이터 한 번에 노출 안 됨 |
| 페이지/단원 단위 접근 | 요청한 부분만 내려줌, 전체 책을 한 번에 수집 불가 |
| 인증 + 구독 확인 | 로그인 + 활성 구독 상태에서만 접근 가능 |
| 데이터 분해 | 어휘/문장/활용/조사가 개별 API 응답으로 분리 — 크롤링해도 원본 교재 형태로 재조립 극히 어려움 |

#### Layer 2 — 포렌식 워터마킹 (유출 시 추적, 실시간 동적 생성)

API 응답마다 구매자별 고유 마킹 패턴을 **실시간 동적 생성** — 세션마다 패턴이 바뀌어 패턴 분석 자체가 불가능.

| 층 | 기법 | 추적 대상 | 제거 난이도 | 비고 |
|:--:|------|----------|:---------:|------|
| 1 | **ZWC (Zero-Width Character) 삽입** | 텍스트 복사 | 쉬움 | 유니코드 영폭 문자로 구매자 ID 인코딩 |
| 2 | **동형 문자(Homoglyph) 치환** | ZWC 제거 후에도 생존 | 중간 | 육안 구분 불가 유니코드 문자 교체 |
| 3 | **이미지 LSB 삽입** | 이미지 캡처 | 어려움 | 픽셀 최하위 비트에 데이터 삽입 |
| 4 | **CSS 미세 변형** | 화면 캡처 | 어려움 | letter-spacing/color 0.01px/0.01% 단위 차이 |

- 인터랙티브 플랫폼 장점: EPUB3는 구매 시 1회 마킹이지만, 플랫폼에서는 **매 API 요청마다 패턴 변경 가능**
- 마킹 정보: 구매자 ID + 타임스탬프 + IP + 기기 + 세션 ID
- 4개 레이어 중 **하나만 살아남아도 법적 추적 근거 확보**
- "마킹이 되어있다"는 사실 자체가 심리적 유출 억제 효과

#### Layer 3 — 플랫폼 보안 (기존 인프라 활용)

| 환경 | 보안 조치 |
|------|----------|
| **웹** | 우클릭 차단, 텍스트 선택 차단 (`user-select: none`), 개발자도구 감지, 인쇄 차단 |
| **앱 (iOS/Android)** | 스크린샷/화면 녹화 차단 API, 암호화 캐시, 오프라인 읽기 시 암호화 유지 |
| **외부 플랫폼** | Amazon/Apple/Google/Kobo 자체 DRM 자동 적용 (별도 설정 불필요) |

#### 보안 한계 인식

- **어떤 DRM도 100% 완벽하지 않음** — 넷플릭스도 뚫림
- 핵심은 "완벽한 차단"이 아니라 **"비용 대비 효과"** — 대다수에게 충분히 어렵게 만드는 것
- 3중 방어 구조에서 콘텐츠를 완전히 탈취하려면: 인증 돌파 → 조각 데이터 전수 크롤링 → 원본 재조립 → 4중 마킹 전부 제거 필요
- ₩25,000 교재를 이 수준으로 뚫으려는 동기 대비 방어 비용이 충분히 합리적

#### 미해결 이슈

| # | 위치 | 내용 | 심각도 |
|:-:|------|------|:------:|
| ~~1~~ | ~~`src/api/admin/user/repo.rs:453`~~ | ~~`admin_get_user_logs()` 복호화 미적용~~ — **확인 결과 service.rs:822-826에서 복호화 구현 완료됨** (2026-03-30 검증). DTO `Option<String>`으로 LEFT JOIN NULL도 안전 처리 | ~~Medium~~ **해결됨** |

> DB 암호화 Phase 2 계획(Bug 1~8) — **전체 완료** 확인 (2026-03-30 재검증). 8개 Bug + Sub-Phase 2B~2D 모두 구현 완료.

#### 학습 콘텐츠 개선 방안

> 교재(JSON) 데이터를 웹 학습 콘텐츠로 변환하는 설계. 교재 페이지 순서 = 학습 순서.

##### 핵심 원칙

- 교재(JSON)의 내용이 곧 웹 학습 콘텐츠의 원본 데이터
- 교재 페이지 순서 = 학습 순서 (`page_manifest.json` 기준)
- 동영상/음성은 현재 미보유 → 추후 제작
- 기존 course/lesson/study DB 데이터는 테스트 데이터 → 삭제 가능

##### 교재 JSON 데이터 → DB 매핑

**데이터 소스** (`scripts/textbook/data/`)

| JSON 파일 | 내용 | 항목 수 | Study Program |
|-----------|------|---------|---------------|
| vocabulary.json | 어휘 카드 (한국어 + 20개 언어 번역) | 280+ | basic_word |
| sentences.json | 문법 예문 (한국어 + 번역) | 496+ | basic_500 |
| pronunciation.json | 한글 조합표 (자음×모음) | 테이블 7+ | basic_pronunciation |
| pronunciation_test.json | 발음 테스트 | 연습 문제 | basic_pronunciation |
| particles.json | 조사 활용표 | 테이블 | basic_500 |
| conjugation.json | 동사/형용사 활용 (현재/과거/미래) | 테이블 | basic_500 |
| structure.json | 문장 구조 (의문사 패턴) | 테이블 | basic_500 |
| appendix.json | 숫자, 문법 연습 | 테이블 | basic_500 |

**DB 계층 구조**

```
Course "놀라운 한국어 기초"
├── Part I. 발음 (Pronunciation)
│   ├── Lesson 1~7: pronunciation.json + pronunciation_test.json + vocabulary.json(발음)
├── Part II. 문법 기초 (Grammar Basics)
│   ├── Lesson 8~10: particles.json + structure.json
├── Part III~IV. 서술어/부사어 문법
│   ├── Lesson 11~30: sentences.json (섹션별 1 Lesson)
├── Part V. 동사 활용
│   ├── Lesson 31~33: conjugation.json
└── Part VI. 부록
    ├── Lesson 34~35: appendix.json
```

##### Lesson 구조 (설명 + 영상 + 문제)

```
Lesson = [설명 콘텐츠] + [Video (있으면)] + [Study Tasks]

- 설명: lesson_description에 해당 단원의 개념 설명
- Video: 강사 영상 (있을 경우 lesson_item kind=video)
- Study: lesson_item kind=task로 연결된 문제들
- Explain: study_task_explain에 문법 테이블/해설 저장
```

**Lesson 예시 (모음 1)**

```
Lesson "모음 1 (Vowel 1)"
├── [설명] "한국어에는 10개의 기본 모음이 있습니다..."
├── [Video] 입모양 발음 시범 영상 (추후)
├── [Study: choice × 5] ㄱ+ㅏ=? → [가,나,다,라]
├── [Study: typing × 3] 직접 타이핑 또는 클릭 순서 배열
└── [Study explain] 자음+모음 조합표 (pronunciation.json)
```

##### 문제 유형 (Study Task Kinds)

현재 DB 지원: choice, typing, voice

| Kind | 방식 | 교재 데이터 활용 |
|------|------|----------------|
| **choice** (4지선다) | 보기 중 정답 선택 | vocab → 뜻 맞추기, sentence → 번역 맞추기, pronunciation → 조합 맞추기 |
| **typing** (타이핑) | 직접 입력 | vocab → 한국어 쓰기, conjugation → 활용형 쓰기 |
| **typing** (클릭배열) | 형태소/블록 순서 배열 | sentence → 어순 배열, particles → 조사 선택 배열 |

**클릭 순서 배열형** (typing 확장 또는 신규 kind 'ordering')
- 문제: "나는 행복합니다"를 올바른 어순으로 배열하세요
- 보기: [행복합니다] [나는] ← 셔플된 블록
- 정답: [나는] [행복합니다]
- 기존 typing kind를 확장하거나 ordering kind 신규 추가 검토

**오답 생성 전략**
- **vocab choice**: 같은 카테고리(같은 페이지/챕터) 내 다른 어휘에서 랜덤 추출
- **sentence choice**: 같은 문법 섹션 내 다른 예문 번역에서 추출
- **pronunciation choice**: 유사 자모 조합에서 추출 (가/나/다/라)
- **particles choice**: 다른 조사를 오답으로 (는/를/에/로)

##### 영상 제작 전략 (2026-03-03 확정)

**핵심 방향: AI 기술 적극 도입**
- 강사 직접 촬영이 아닌 **AI 음성 + 애니메이션** 중심
- 목표: 일관된 퀄리티, 대량 생산 가능, 정확한 발음 전달

**한국어 발음 교육의 핵심 원칙**
- **성조 없음**: 음높이 변화 거의 없음 → 음율적이지 않게 한 글자씩 발음
- **한 글자씩**: 학습 시 반드시 한 글자 단위로 끊어서 발음 연습
- **남성 + 여성 음성**: 모든 발음을 남녀 AI 음성 2가지로 제공
- **입모양/혀 위치**: 실사 촬영 대신 **애니메이션**으로 시각화 (더 명확)
- **두 글자 이상**: 목차별 순차적 듣기 + 따라하기 연습

**파트별 영상 구성**

Part I. 발음 (1순위 — 텍스트 대체 불가)

| 요소 | 내용 |
|------|------|
| **AI 음성** | 한 글자씩 남성/여성 TTS, 성조 없이 평탄하게 |
| **애니메이션** | 입모양 + 혀 위치 다이어그램 (자음/모음별) |
| **자막** | 한글 + 발음기호 + 학습자 모국어 (20개 언어) |
| **흐름** | 글자 표시 → 애니메이션 → 남성 발음 → 여성 발음 → 따라하기 |
| **조합 연습** | 자음+모음 조합표 한 줄씩 순차 재생 + 듣기 |

Part II. 문법 기초

| 요소 | 내용 |
|------|------|
| **시제** | 핵심 동사 기준 → 현재/과거/미래 × 기본형/의문형, AI 발음 시범 |
| **조사** | 핵심 명사 기준 → 각 조사별 발음 시범 |
| **문장구조/의문사** | 동일 패턴: 텍스트 + AI 발음 |

Part III~VI. 문장/서술어/부사어/기타 문법

| 요소 | 내용 |
|------|------|
| **공통** | 각 목차의 문법/형태를 AI 발음으로 보여줌 |
| **예문** | 10개 예문 순차 재생 (남/여 음성) + 모국어 자막 |

**학습 흐름: 영상 → Study 복습 (확정)**

```
[영상] 전체 흐름을 한 번 봄 (수동적, 개요 파악)
  ↓
[Study] 자기 페이스로 연습 + 복습 (능동적, 반복)
  ↓
[이후 복습] Study만 반복 (영상 재시청은 선택적)
```

- 영상 = 설명/시범, Study = 연습/평가 — 역할 분리
- Study에서 audio_url로 개별 음성 재생 가능 → 인터랙티브 장점 흡수
- 인터랙티브 웹 콘텐츠 별도 구현 불필요 (기존 video + study 구조 유지)

**발음 평가 (음성 인식) — 단계적 도입**

| Phase | 범위 | 접근 방식 | 시기 |
|-------|------|----------|------|
| **Phase 1** | 따라하기 안내만 (녹음/판별 없음) | 없음 | 지금 |
| **Phase 2** | **한 글자 + 단어/문장** 발음 평가 | SpeechSuper API ($0.004/건) | 콘텐츠 완성 후 |
| **Phase 3** | 커스텀 AI로 전환 | wav2vec2 파인튜닝 + 초성/중성/종성 분류 | 기술 검증 후 |

- **한 글자 발음 교정이 초급 학습자에게 가장 중요** (핵심 원칙)
- Phase 2: SpeechSuper가 유일하게 한글 1자 음소별 평가 지원 → 프로토타이핑 + 사용자 검증
- Phase 3: AIHub 71469 데이터(1,030h, 음소 라벨+오류 태그) + wav2vec2-xlsr-korean 파인튜닝
- 커스텀 모델 장점: API 비용 제거, 데이터 주권, L1별 맞춤 피드백 (20개 언어)
- **상세 조사**: [pronunciation_ai_research.md](pronunciation_ai_research.md) (메모리 토픽 파일)

**입모양/혀 애니메이션 제작 방안 (조사 완료)**
- **15~17개 다이어그램**으로 전체 커버 (자음 7위치 + 성문 1 + 모음 7~9)
- 평음/경음/격음은 같은 입 위치 → 성문 상태도로 차이 표현
- **Wikimedia CC0 SVG** 활용 (IPA 조음 단면도, 퍼블릭 도메인) → 자체 제작 최소화
- 자체 제작 필요: ㅈ/ㅊ/ㅉ 치경구개음 SVG만
- **기술**: Figma → SVG path → GSAP MorphSVG (무료) + React
- **기간**: 3~6주 (Level 2: SVG 모핑)
- **한국어 전용 조음 애니메이션 도구 부재 → 차별화 기회**
- **상세 조사**: `amazing-korean-ai/docs/AMK_AI_PIPELINE.md §12.1 조음 애니메이션 조사` (이관됨)

**AI 기술 스택**
- **TTS**: 한국어 고품질 AI 음성 (남/여) — Google Cloud TTS, CLOVA, OpenAI TTS 등 (조사 필요)
- **발음 평가**: Phase 2 SpeechSuper API → Phase 3 커스텀 wav2vec2 모델 (조사 완료, [상세](pronunciation_ai_research.md))
- **입모양 애니메이션**: CC0 SVG + Figma → GSAP MorphSVG + React (조사 완료, 상세: `amazing-korean-ai/docs/AMK_AI_PIPELINE.md §12.1` 이관됨)
- **영상 자동 생성**: JSON 데이터 → 스크립트 → 영상 자동 렌더링 파이프라인
- **자막**: 교재 번역 데이터(20개 언어)에서 SRT/VTT 자동 생성

**다국어 자막 전략**
- 음성: 한국어 AI TTS (남/여)
- 자막: 교재 JSON translations에서 자동 추출 → 20개 언어
- 결과: 영상 템플릿 1개 → 자막만 교체로 20개 언어 커버

##### 구현 접근법: Seed Script

```
scripts/textbook/JSON → seed_script → DB (study + study_task + lesson + course)
```

1. JSON 파일 읽기 (vocabulary, sentences, pronunciation, ...)
2. Study 세트 생성 (program별: basic_pronunciation, basic_word, basic_500)
3. StudyTask 생성 (choice/typing 문제 자동 생성 + explain 해설)
4. Lesson 생성 + lesson_description에 단원 설명 + LessonItem에 task 연결
5. Course 생성 + course_lesson으로 Lesson 연결

[⬆️ AMK_API_MASTER.md로 돌아가기](./AMK_API_MASTER.md)
